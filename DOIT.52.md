# DOIT.52 - Push-driven filesystem and session-file refresh

Goal: reduce browser API polling without losing freshness. Finish the current Preferences/cache work first, then add a server push channel. If native filesystem watchers are unavailable, the server may poll locally and push only changed-state notifications to browser clients.

- [x] P1 - Finish current polling controls and cache work. DONE 2026-06-09 (dev2): added Preferences-backed `performance.settings_refresh_ms`, `performance.activity_summary_refresh_ms`, and `file_explorer.session_files_refresh_seconds`; changed `/api/session-files` and `/api/transcripts` to serve fresh/stale cache with background refresh; explicit user refreshes use `force=1`. Targeted backend checks passed: `py_compile`, `test_settings.py`, `test_server_query.py`, and cache tests in `test_app.py`.
- [x] P2 - Add server-side push plumbing. DONE 2026-06-09 (dev2): added `ClientEventBroker`, app-level `publish_client_event()`, authenticated `/api/client-events` SSE stream, keepalive pings, and focused broker/routing tests.
- [x] P3 - Add client push subscriber. DONE 2026-06-09 (dev2): boot installs one `/api/client-events` `EventSource`, handles `settings_changed`, `fs_changed`, `session_files_changed`, and `transcripts_changed`, invalidates browser caches before forced refreshes, and leaves runtime polling installed as fallback.
- [x] P4 - Add backend watch/poll invalidation. DONE 2026-06-09 (dev2): added dependency-free server-side polling fallback, `/api/watch/roots` root registration, settings/transcript/filesystem signatures, cache invalidation, and push events only after signature changes. Native OS watcher adapters are not required for this pass; the poll fallback satisfies the no-extra-dependency path and can be swapped behind the same event contract later.
- [x] P5 - Add regression tests. DONE 2026-06-09 (dev2): added tests for Preferences exposure, `/api/session-files` and `/api/transcripts` stale-cache refresh, SSE broker/routing, client source wiring, `/api/watch/roots`, and fallback watcher signature-change behavior.
- [x] P6 - Rebuild and validate. DONE 2026-06-09 (dev2): rebuilt static assets and locale catalogs, ran static/build checks, JS syntax/layout checks, full pytest, and restarted dev2 on port 8002. Live verification: `https://localhost:8002/api/ping` returned HTTP 401 in 0.044s, expected for unauthenticated access.
- [x] P7 - Fix post-push forced refresh churn from live debug evidence. DONE 2026-06-10 (dev2): latest JS Debug showed frequent expensive `/api/session-files?...force=1` calls. Push-driven session-file refreshes now coalesce through the non-forced cached path, `fs_changed` no longer also fetches session-files, connected SSE clients skip expensive metadata/settings/filesystem polling while renewing watch roots, transcript parent directories stay on the transcript watcher instead of the filesystem watcher, and filesystem changes no longer evict the server session-files cache. Validation passed: static build check, JS syntax check, layout/source test, focused backend tests, full `python3 -m pytest tests -n 4 -q` (`475 passed`), and `git diff --check`.
- [x] P8 - Record final live debug result before CPS. DONE 2026-06-10 (dev2): user-provided JS Debug at 10:39:36 showed `events=70`, `api=69`, and one script-error entry. The important improvement held: `/api/session-files` dropped to 6 calls with avg 347.1ms, only one `force=1` call for session 6 during an explicit fresh-load/session-switch path, and follow-up non-forced cached calls were 58.7ms, 187.3ms, 68.3ms, and 59.3ms. Remaining slow calls are separate work: `/api/activity-summary?force=1` max 3899.0ms, `/api/context-items` max 1449.6ms, one startup `/api/transcripts` at 586.4ms, and one `/api/auto-approve` outlier at 935.3ms.
