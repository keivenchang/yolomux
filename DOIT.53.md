# DOIT.53 - tmux window bar: move to Info Bar, number-prefix labels, make clicks switch windows

Goal: the per-window buttons (currently `bash` `claude` `codex` in the top pane-tab container) must live in the Info Bar / detail row, show as `index:name` (e.g. `0:bash 1:claude 2:codex`), actually switch tmux windows on click, and collapse with the details on/off toggle. Screenshots: `20260610-013.png` (details shown), `20260610-014.png` (details hidden).

## Audit 2026-06-10 21:1x (read this first)

- The whole backend half of this feature is UNCOMMITTED working-tree code and NOT RUNNING anywhere: `/api/tmux-window` route (`server.py:622`), `#{window_name}` capture (`sessions.py:52`, `common.py:260`), and the Codex working-detection fix (`prompt_detector.py:414-424` `_CODEX_INPUT_HINT_RE` / `_CODEX_MODEL_STATUS_LINE_RE`) exist only on disk. Backend files were written 21:02; the 7777 python started 20:52 (0.2.81) and 8002 started 18:49 (0.2.80) — both predate them. `git log -S` confirms no commit contains them.
- Consequence: button CLICKS currently POST `/api/tmux-window` to servers that don't have the route -> error path (`tmux window failed: …` status) after an optimistic label flash. This — not the click wiring — is why clicks "do nothing" right now. The frontend wiring is already correct in source: dockview header routes `data-window-index` (`75_dockview_layout.js` `handleDockviewHeaderActionClick`) -> `handleWindowStepButtonClick` (`80_panes_preferences.js:1382`) -> `tmuxWindow(session, {windowIndex})` POST (`99_terminal_boot.js:1350-1364`), plus the in-panel delegate (`99_terminal_boot.js:779`).
- Same staleness hides the P6/YO-spin fix: the Codex chrome matrix now classifies `working` against source (verified: working line + input hint + `gpt-5.5 xhigh ~/path` status all = `working`), but live servers still run the old classifier.
- LABEL REGRESSION RISK: `tmuxWindowDisplayName` (`80_panes_preferences.js:1284`) prefers raw `window_name` FIRST. Today's screenshot shows `codex` only because the stale backend sends no window_name and the code falls back to `process_label`. After restart, that window's tmux name is `node` (status line `2:node`) so the button would flip `codex` -> `node` — the WRONG direction per the request (wanted: `bash`, `claude`, `codex(1)`…). Prefer the process-aware agent label when one exists; use `window_name` as the fallback.
- Bar placement: NOT yet moved — still rendered into the pane header via `tmuxWindowsHtml` (`99_terminal_boot.js:67`); the detail row (`99_terminal_boot.js:109-116`) has no bar and no pending diff moves it. P1 still open. The details on/off toggle itself works (verified in `20260610-014.png`: the row collapses; the buttons stay visible only because they live in the header).
- Ship checklist for whoever lands this: commit the working tree (backend + JS together), restart BOTH servers, then live-verify clicks. A backend route in source does nothing until the python process restarts; the static bundle alone reloading is what made the buttons VISIBLE while the feature stayed dead.

- [x] P1 - DONE 2026-06-10 (dev2): moved the shared `tmuxWindowBarHtml()` call out of the top pane header and into `.panel-detail-row` after `#panel-agent-${session}` and before `.panel-detail-close`, so the bar hides with collapsed details and lives under the in-panel click delegate. Regression pins the source call-site order, absence from `panelControlsHtml()`, and the existing detail-row collapse CSS. Verification: `python3 tools/static_build.py`, `node --check static/yolomux.js`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, `python3 -m py_compile yolomux.py yolomux_lib/*.py`, and `git diff --check`.
- [x] P2 - DONE 2026-06-10 (dev2): `tmuxWindowDisplayName()` now prefers `process_label` over raw `window_name`, `tmuxWindowRecords()` exposes `indexedNameLabel`, and `tmuxWindowBarHtml()` renders `index:name` in names mode while preserving duplicate-name suffixes and number-only overflow. Regression covers `1:bash`, `2:codex(2)`, process-label preference over raw `node`, and numeric fallback. Verification same as P1.
- [x] P3 - DONE 2026-06-10 (dev2 source): the window bar now renders inside the panel, so button clicks use the existing `delegate(panel, 'click', '[data-window-dir], [data-window-index]', ...)` path to `handleWindowStepButtonClick()` and `/api/tmux-window?session=...&window=...`. Regression pins the delegated source path and the select-window POST. NOTE: no commit/restart was performed in this pass because the user requested DOIT work, not a commit; live verification should happen after the next requested restart/CPS. Verification same as P1.
- [x] P4 - DONE 2026-06-10 (dev2): `fetchDirectory()` now clears path-keyed list errors on benign push-depth/suppressed-background nulls and cache hits, real failures are recorded through `setFileExplorerListError(path, error, status)`, and `openFileExplorerAt()`/manual path commit only paint invalid when `currentFileExplorerListError(root)` belongs to the same root. Regression covers stale path A not tainting path B, benign push-refresh null clearing same-path stale error, and real 403 recording a path-keyed error. Verification same as P1.
