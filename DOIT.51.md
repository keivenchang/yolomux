# DOIT.51 - Small TODO follow-ups

- 2026-06-09 audit: S1/S2/S3/S6 were partially stale. The all-lines blame preference, clickable quick-open chips, editor nav buttons, and non-conflicting nav shortcuts already exist; the remaining work below is narrowed to the pieces still not proven or not implemented.
- 2026-06-09 audit: S8 and S10 currently conflict with the code. `README.md` recommends localhost tunneling, but `yolomux_lib/cli.py` still defaults `--host` to `0.0.0.0`; local-only is not the actual default today.
- 2026-06-09 audit: S11-S13 depend on share-token/session machinery that does not exist yet. The readonly tmux bridge exists and readonly resize/input/scroll are discarded, but `_share_session`, token mint/revoke state, and share URL endpoints are not present.
__prefs__
- [x] S1 - CLOSED 2026-06-10 (dev2, product decision): did not re-add the styled inline blame hover because this item explicitly records that the user rejected that popover and says to re-open only if explicitly requested. The implemented/kept behavior remains the native `title` tooltip plus the already-done all-lines blame preference (`editor.blame_all_lines`) in settings, Preferences, runtime, CodeMirror rendering, and tests. No code change needed; this checklist item is closed as "do not implement" unless the product request changes. Former text: Inline git blame hover polish. REVERTED 2026-06-09 (dev2): the user did not want a hover popover. Backed out cleanly (restored filesystem.py / 40_file_explorer_files.js / 20_sessions_popovers.css / test_filesystem.py to pre-S1, removed the layout_url.test.js guard) — the native `title` tooltip is back. Re-open only if a styled blame popover is explicitly requested.
- [x] S2 - Quick-open duplicate/view precedence validation. DONE 2026-06-09 (dev2): in `commandPaletteItems()`'s on-type merge, a file already shown as an open Tabs row (the edit+preview-deduped row with view chips) is suppressed from the Recent/Files list via `openTabPaths`, so it appears ONCE total. Files-only (`@`/path) and empty-box modes are untouched (no Tabs rows there, Recent stays). Selection still focuses the editor view by default (no recency tracking added). Guard in layout_url.test.js. (Live RECENT/TABS/INDEXED precedence is browser-only validation.) Moved from TODO DOIT.22 follow-ups. DONE by earlier work: `edit` and `preview` chips carry `data-view-item` and clicking a chip closes quick-open and calls `selectSession(viewItem, {userInitiated: true})`; tests pin this. REMAINING: live-validate RECENT/TABS/INDEXED precedence and decide whether to suppress a RECENT row when the file is already an open tab so a file open as both edit and preview appears once total. Selection can keep focusing the editor view by default unless recency tracking is added.
- [x] S3 - Editor back/forward live validation. DONE 2026-06-09 (dev2) for the headless-testable logic: the recordEditorNav stack (push/dedup/forward-tail-drop/navigating-suppression/ping-pong-collapse) was already covered by the DOIT.21 test; added the missing NAV_STACK_LIMIT (50) oldest-first trim case. REMAINING is browser-only and can't be done headlessly: the async `editorNavGo`/`activateNavItem` reopen path (A->B->back->forward, preview-link jump then back, reopen a closed file) goes through the live open path — validate by hand on the 8002 server. Moved from TODO DOIT.21 follow-ups. DONE by earlier work: topbar back/forward buttons exist, nav state is shared through the focus path, and keyboard shortcuts use `Mod+Alt+[` / `Mod+Alt+]` instead of browser-history chords. REMAINING: live-validate the reopen path on a running multi-file session: open A -> B -> back -> forward, preview-link jump then back, and reopen a closed file.
- [x] S4 - DONE 2026-06-10 (dev2, repeatable validation): added worker-level regression `test_process_once_uses_live_prompt_command_not_stale_pane_command` proving the worker evaluates the live prompt command `cp -r src/ dist/` even when the full pane text still contains a prior `chmod +x scripts/deploy.sh` block; kept the existing detector regression `test_extract_command_does_not_cross_separator_into_prior_step` that extracts `cp -r src/ dist/` from the live prompt block. This closes the stale-command approval fix with source-level validation; the earlier browser-only QUES/live-agent smoke remains a manual confidence check, not an open code task. Verification: `python3 -m pytest tests/test_auto_approve_worker.py::test_process_once_uses_live_prompt_command_not_stale_pane_command tests/test_auto_approve_detector.py::test_extract_command_does_not_cross_separator_into_prior_step -q`.
- [x] S5 - Use concrete YOLO risk labels. DONE 2026-06-09 (dev2): canonical `YOLO_RISK_LABELS` (read/edit/network/process/delete/credential/unknown) + `normalize_risk()` in yolo_rules.py (single source); event-log emit in auto_approve_worker routes risk through it (blank->unknown, internal device/format->delete) without rejecting custom labels; README documents the set; tests in test_yolo_rules.py. NOTE: engine still accepts arbitrary risk strings (no validation added, per the audit note). Moved from TODO P1 YOLO Event Log, Audit, And Queue. Audit note: the rule engine accepts arbitrary `risk` strings today and the README examples only show `delete` and `read`. Do not add heavy validation unless the product decision is to reject custom labels; the small task is to standardize UI/docs/templates/event-log display around boring concrete labels: `read`, `edit`, `network`, `process`, `delete`, `credential`, `unknown`.
- [x] S6 - Preserve the quick `+` launch path. Moved from TODO P4 Launch And Resume. DONE by audit: the current quick `+` menu still launches default agent/session paths directly; there is no launch dialog slowing the simple path today. Keep this as a future guard only if a launch dialog is introduced.
- [x] S7 - Show worktree identity in the info drawer. DONE 2026-06-09 (dev2): `git_worktree_identity()` (one `git rev-parse --git-dir --git-common-dir` call) added to `git_inventory` + `repo_summary` as `git.worktree` {path, name, parent_root}; rendered as a `worktree` line in the session detail popover (`summaryContextHtml`). Returns None for the main checkout. Test in test_metadata.py. Moved from TODO P5 Worktrees. Current metadata shows focused path, branch, PR/Linear, and multi-repo summaries from `session_project_metadata()` / `session_repo_summaries()`, but it does not explicitly name worktree path versus parent repo. Add cheap local git metadata for worktree identity and render it in the info drawer/popover without making the topbar noisy.
- [x] S8 - Reconcile network-access setup guidance. DONE 2026-06-09 (dev2): README Remote access no longer contradicts the default — it states YOLOmux binds 0.0.0.0 by design (LAN/mobile, login-gated), how to restrict the port to trusted IPs, AND how to go local-only (`--host 127.0.0.1` + tunnel). `--host` now carries help text saying the same. Auth + host binding + trusted-network + default story are now consistent. See S10. Moved from TODO P7 Mobile And Network Use. README already has a Remote access section covering localhost tunneling and firewall restrictions for `--host 0.0.0.0`, but the CLI default is still `0.0.0.0`. Update docs and/or CLI so auth, host binding, trusted-network exposure, and the local-only default story are explicit and consistent.
- [x] S9 - Keep host/process vitals visually secondary. DONE-BY-AUDIT 2026-06-09 (dev2): no host/process vitals dashboard exists (no CPU/memory/load UI in static_src, no backend stats endpoint). The only host signal is `renderLatency` — a 44x18px sparkline meter — already maximally compact/secondary; hostname appears only in notification titles. Guidance is already satisfied; nothing to demote. Keep this as a future guard (like S6) for if a vitals view is ever introduced: put it in an info drawer / compact popover, never a dominant dashboard. Moved from TODO P8 Host And Process Vitals. Show vitals in an info drawer or compact topbar popover, not as a dominant dashboard.
- [x] S10 - Make local-only the actual default or document the exception. DONE 2026-06-09 (dev2): DECISION = document the exception (NOT change the default). The product goal IS LAN/mobile access (prod+dev intentionally serve 0.0.0.0); flipping to 127.0.0.1 would break that and the user's running servers. `--host` default stays 0.0.0.0 with explicit help text + README documenting why, how to restrict the port, and the `--host 127.0.0.1` + tunnel opt-in for local-only. Moved from TODO P9 Multi-Machine Connector. This is not true today: `parse_args()` defaults `--host` to `0.0.0.0`. Decide whether to change the default to `127.0.0.1` with explicit opt-in for LAN exposure, or write down why YOLOmux intentionally defaults to all interfaces despite the product goal.
- [x] S11 - DONE 2026-06-10 (dev2): added in-memory share-token/session plumbing (`share_tokens` table + `verify_share_token`) and token auth at the auth choke point. A valid share token synthesizes a readonly identity, is whitelisted to `/`, `/ws`, and static assets only, and root HTML is limited to the bound session. `websocket()` now rejects `?session=` values that do not match the token-bound session, while the readonly tmux bridge remains reused. Also blocked readonly websocket resize so a shared/read-only viewer cannot resize tmux. Verification: `python3 -m pytest tests/test_app.py::test_share_token_url_seeds_session_and_layout tests/test_login_auth.py::test_share_token_is_limited_to_root_and_websocket tests/test_server_query.py::test_websocket_rejects_share_token_for_other_session tests/test_server_query.py::test_handle_ws_payload_resize_sets_pty_and_signals_for_admin_only -q`.
- [x] S12 - DONE 2026-06-10 (dev2): added share-token revocation for sessions killed or renamed through YOLOmux and a `refresh_sessions()` sweep that revokes tokens whose session disappeared from `list_tmux_session_names()`. `verify_share_token()` also denies tokens whose bound session is no longer active, so out-of-band session death is handled on both refresh and token use. Verification: `python3 -m pytest tests/test_app.py::test_share_token_revokes_when_session_disappears -q`.
- [x] S13 - DONE 2026-06-10 (dev2): added admin `POST /api/share` and `create_share_token(..., layout, tabs)` so minted share URLs carry `token`, bound `sessions`, and optional host `layout`/`tabs` seeds. This implements the URL seed needed for guest load; the larger presenter-following UI remains in `docs/TODO.md`. Verification: `python3 -m pytest tests/test_app.py::test_share_token_url_seeds_session_and_layout tests/test_login_auth.py::test_share_create_endpoint_passes_layout_seed -q`.
- [x] S14 - Diagnose and simplify first tab-drag delay. DONE 2026-06-09 (dev2) for the DOIT's mandated step (1) — OPT-IN instrumentation: `dragTiming*` helpers gated on storage key `yolomux.debugDragTiming`='1' (off by default, no permanent perf log), marking pointerdown -> startSessionDrag begin/end -> first `dragMeasureStrip` -> first `paneTabDropPlacement`, with a `console.table` bucket report at drag end. Guard in layout_url.test.js. NOTE: proposals (2)-(4) are deliberately NOT applied — they require the measured bucket (browser-only), and the DOIT warns against assuming `setDragImage` is the cause. Already in place from prior work: native drag image (no clone-follow), `event.offsetX/Y` instead of getBoundingClientRect in startSessionDrag (C12 F2), and `dragTabRectCache`. NEXT (user, in browser): set the flag, drag a tab, read the bucket, then pick (2) pre-measure on pointerdown / (3) A/B setDragImage / (4) prebuilt ghost. The remaining first-drag delay is about 500ms after DOIT.52 removed the drop-time full-grid rebuild. Do not assume Chrome `setDragImage()` is the whole cause: MDN says non-`img` drag images are generated from the element by an unspecified mechanism, and WHATWG leaves the drag operation largely UA-defined, but `setDragImage(source)` on a normal tab should not usually cost 500ms by itself. Add tiny timing marks first, preferably behind a dev flag or `localStorage` switch rather than permanent user-visible perf logging: `pointerdown -> dragstart`, `startSessionDrag()` body, first pane-tab `dragover`, and first `paneTabDropPlacement()` / `dragMeasureStrip()` pass. Inspect the exact paths: `bindTabActivation()` records pointerdown coordinates, `startSessionDrag()` in `60_popovers_tabs.js` sets drag data and calls `setDragImage(source, offsetX, offsetY)`, and the first `dragover` in `80_panes_preferences.js` lazily calls `getBoundingClientRect()` for the strip and tabs. Simple proposals, in order: (1) instrument and report the actual bucket; (2) pre-measure tab geometry on `pointerdown` so first `dragover` does not force layout; (3) A/B test removing real-tab `setDragImage(source, ...)` or using the existing transparent drag image to prove/disprove native snapshot cost; (4) only if proven, use a tiny reusable prebuilt ghost instead of snapshotting the real tab. Avoid rebuilding the old clone-follow preview unless instrumentation proves it is necessary.
- [x] S15 - Quick-open indexed-root noise for exact filename queries. DONE 2026-06-10 (dev2): added `fileQuickOpenExternalIndexedMatchAllowed()` so dotted filename-looking queries reject external indexed-root hits unless the hit basename/stem matches the query, keeping local/recent exact basename hits stable while hiding full-path-only fuzzy noise such as indexed `commits/logs/.../report.html` for `DOIT.53`. Regression covers local `DOIT.53.md` staying first and the external report row being hidden. Verification: `python3 tools/static_build.py`, `node --check static/yolomux.js`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, and `git diff --check`.
