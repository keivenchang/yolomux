# v0.7.7 specification and implementation discrepancy guide

> **GUIDANCE ONLY.** This file is the 0.7.7 audit handoff for later workqueue design. It is not an active DOIT queue, is not yet part of `STATUS-REPORT.md`, and does not authorize implementation, deployment, or live-server mutation.

Audit baseline: `89bf6be952813ec0105741c6830cd7b9c2e07bf4` on 2026-08-15 PT. The audit covered all 13 pre-existing files under `docs/specs/`, their internal links and test references, the current route catalog and named source owners, the 48-row recovery traceability matrix, and the active queues cited by those contracts under `queues/backlog/`.

## Audit disposition

| Specification | Audit result |
| --- | --- |
| `AGENT_PROMPTS_AND_COMMUNICATION.md` | Current detector, prompt-corpus, and transport-owner symbols resolve; no documentary correction required. |
| `API_RESPONSE_CONTRACT.md` | Corrected stale implementation status, durable browser-observation ownership, and the absent diagnostic route. Open contract gaps remain below. |
| `ASYNC_INTERACTION.md` | Normative accepted-operation contract remains current; implementation is partial and the concrete gaps remain below. |
| `BACKEND_ARCHITECTURE.md` | Six-service topology matches current source. Corrected descriptor-authorization and native-watch fallback overstatements. |
| `BACKEND_TEST_CONTRACT.md` | Verification rules remain current. Clarified that `BackendCohortManifest` is not yet an executable shared type. |
| `EDITOR-CODEMIRROR.md` | Corrected a historical reference to removed textarea markup; current CodeMirror ownership matches source. |
| `FS_INTERACTIVITY.md` | 0.7.3 shipped status and breadth-first lifecycle remain current. Corrected the claim of complete descriptor-generation authorization. |
| `GUI.md` | Existing 0.7.7 corrections and the current GUI test map match source, including rename completion-generation and tmux-window authority invariants. |
| `RECOVERY.md` | Added the counted traceability boundary: 17 covered, 22 partial, and 9 uncovered rows. The remaining matrix is not release evidence. |
| `SEARCH_INDEXER.md` | Corrected the stale final-gate-pending status and recorded the indexed-metadata authorization caveat. |
| `SELF_UPDATE_AND_RESTART.md` | Replaced obsolete line references and manual restart/check-runner instructions with current symbol and workflow owners. |
| `STATS_API.md` | Schema 7, wire protocol 2, cost-report schema 3, resolutions 1/10/60/300, and the 600-bucket bound match current source. |
| `TMUX.md` | Corrected the snapshot route and moved volatile source references to current module/symbol owners. |

## Discrepancies for later workqueue planning

| Contract/spec | Current implementation evidence | Discrepancy | User/risk impact | Likely workqueue owner | Required verification |
| --- | --- | --- | --- | --- | --- |
| API-wide response envelope and 250 ms immediate answer in `API_RESPONSE_CONTRACT.md` | `QueuedDeliveryLedger`, operation status/ack routes, and terminal SSE replay exist, but `http_routes.py` still exposes route-specific JSON shapes and forced session metadata still performs synchronous work. | The shared accepted-operation parent is real, but the ready/queued/failed envelope, trace propagation, typed failures, and timing bound are not enforced across every JSON route. | A slow or failed route can still block a handler or return a shape the browser cannot diagnose uniformly. | New 0.7.7 API-envelope migration queue; activity summary remains separately owned by [`DOIT.p1.e5.activity-summary-async-replacement.md`](../../queues/backlog/DOIT.p1.e5.activity-summary-async-replacement.md). | Derive the route inventory from `http_routes.py`; prove each applicable route's ready, queued, failed, timeout, disconnect, and producer-death paths against one envelope and a fixed handler deadline. |
| Aggregated authenticated failure query in `API_RESPONSE_CONTRACT.md` | Qualified browser observations upload through `POST /api/stats-observations`; no `/api/diagnostics/failures` route exists in the catalog. | Durable ingestion exists, but one bounded server/browser failure query grouped by code and origin does not. | Operators and agents still need multiple views or logs to correlate a browser failure with its server cause. | New 0.7.7 observability-query queue extending the current browser-observation storage owner. | Add an authenticated bounded query with retention, grouping, redaction, pagination/limit tests, browser/server correlation fixtures, and production-safe response-size evidence. |
| Universal accepted-operation lifecycle in `ASYNC_INTERACTION.md` | Session-files, filesystem/watch-diff, and jobd product paths use durable receipts, status, acknowledgment, and `operation_terminal`; activity summary remains disabled. | Conversion and presentation are incomplete, and the journal can be mis-scoped across sibling ports. | An accepted operation can lack visible progress, use a blocking legacy path, or be falsely abandoned by another server. | Existing [`DOIT.p1.e3.operation-journal-port-scope.md`](../../queues/backlog/DOIT.p1.e3.operation-journal-port-scope.md), [`DOIT.p1.e3.queued-operation-pending-indicator.md`](../../queues/backlog/DOIT.p1.e3.queued-operation-pending-indicator.md), [`DOIT.p1.e3.filesystem-delete-lane-split.md`](../../queues/backlog/DOIT.p1.e3.filesystem-delete-lane-split.md), [`DOIT.p2.e2.jobd-bulk-fairness.md`](../../queues/backlog/DOIT.p2.e2.jobd-bulk-fairness.md), and activity-summary queue. | Reproduce each queue's current failure first, then prove one receipt identity from acceptance through terminal/replay/ack/UI clearing, including sibling-port, reconnect, supersession, timeout, and cancellation cases. |
| Executable cohort ownership in `BACKEND_TEST_CONTRACT.md` | No production or test symbol named `BackendCohortManifest` or shared `cohort_id` parent exists. Tests encode isolation through fixture-specific roots, sockets, ports, processes, and browser profiles. | The review manifest is not one typed object that can reject overlapping mutable resources before parallel tests run. | Parallel-only ownership defects can remain scattered across fixtures and be found only after collision. | New 0.7.7 backend-test-infrastructure queue. | Add one immutable manifest type and overlap validator, migrate representative process/browser cohorts, inject every resource-class collision, and prove fixture cleanup removes every declared owner under parallel execution. |
| Recovery release decision in `RECOVERY.md` | The current traceability table contains 17 Covered, 22 Partial, and 9 Uncovered rows across E01-E48. | The implementation has substantial recovery behavior, but 31 rows lack complete automated evidence and the required real-client/reboot/release journey is not closed. | Session loss, resume, conflict, auth, accessibility, failure recovery, and multi-pane edge cases can regress without a release-blocking proof. | New 0.7.7 recovery-acceptance queue derived directly from the 31 incomplete rows; do not duplicate rows already fully covered. | Revalidate every cited test on the implementation HEAD, implement exact missing transitions, run isolated two-server/private-socket browser journeys, real Claude/Codex resume smokes, the operator reboot gate, and the canonical gate on one frozen identity. |
| Descriptor-generation authorization claimed across filesystem specs | The active P0 records known generation gaps in listing, recursive ZIP, Git diff, indexed-search metadata, and namespace mutation consumers. Current path policy and partial descriptor protections do not retain one authorized object generation through every use. | Several specs previously described the descriptor policy as complete when the consumer matrix remains open. | A namespace replacement can expose blocked bytes or metadata after a path was authorized. | Existing [`DOIT.p0.e5.filesystem-descriptor-authorization.md`](../../queues/backlog/DOIT.p0.e5.filesystem-descriptor-authorization.md). | Reproduce every current race with deterministic barriers, migrate every consumer through one descriptor-lifetime parent, run the property-derived matrix, and repeat zero-leak probes against the restarted implementation server. |
| Per-root native-watch safety on network filesystems | `watchd` now bounds shallow native registrations and falls back to polling after native backend failure/unavailability, but it does not classify each watched root by its actual mount before native admission. A launcher shim is still used on the affected NFS host. | Whole-configuration fallback is not the specified mixed local/native plus network/polled partition. | Registering an NFS root can block the watch daemon and make the local server unresponsive; globally disabling native watch also delays safe local roots. | Existing [`DOIT.p1.e4.native-watch-network-fs.md`](../../queues/backlog/DOIT.p1.e4.native-watch-network-fs.md). | Prove mount classification and mixed-root routing with synthetic tests, then run the required 15-minute real NFS-host acceptance before removing the workaround. |
| Bounded daemon memory and readiness contract across backend/stats/search specs | Current specs describe logical storage and watch topology, while the active memory queue records whole-history stats startup cost, missing process-memory readiness budgets, and unclosed release evidence for shallow/capped watch registration. | The spec set does not yet define or prove one numeric `/livez` versus `/readyz` memory/watch/readiness contract for every daemon. | A process may answer a listener probe while still consuming excessive memory, registering too many watches, or lacking a usable serving generation. | Existing [`DOIT.p1.e5.memory-hog.md`](../../queues/backlog/DOIT.p1.e5.memory-hog.md). | Freeze production-shaped stats/watch fixtures, derive numeric budgets, prove bounded cold/steady behavior and recovery, document the final serving/readiness owners, and certify Linux/Darwin plus an authenticated soak on one frozen identity. |

## Workqueue conversion rule

When this guide is promoted later, preserve each row exactly once. Reuse the named active queue where one already owns the defect. Create a new DOIT only for rows explicitly marked as a new 0.7.7 owner, and split a row further only when the resulting source owners and release evidence are independent. Update `STATUS-REPORT.md` only after those queue files exist and their checkbox denominator is frozen.
