# DONE

Archive of completed YOLOmux work, newest first. Concise by design — the full implementation detail (file/symbol, fix, tests) lives in the git commit history on `main`.

Unless an entry says otherwise, every item shipped with the standard check gate green (`tools/check.py`: py_compile, `static_build.py --check`, both `node --check`, `node tests/layout_url.test.js`, full pytest, `git diff --check`). Entries call out only verification that goes BEYOND that gate (live user confirmation, focused browser repros, notable test counts).

## 2026-06-25

### Refactor simplify/reuse audit

- Completed and removed `DOIT.refactor_simplify_reuse.md`. The final pass moved the transparent native drag-image hidden geometry into `.transparent-drag-image`, removed the matching inline style writes from `transparentNativeDragImage()`, and routed the clipboard textarea fallback through `OFFSCREEN_POSITION_PX`. Earlier passes in the same queue routed the audited environment defaults, theme/light-mode drifts, state keys, metadata shapes, event signatures, settings fallbacks, session-files types, query validation, button/hover CSS owners, and CSS/JS timing split through their shared owners. Verification included `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node --check static/yolomux.js`, `node tests/editor_preview.test.js`, `node tests/tabber.test.js`, and source invariants for the final drag-image/off-screen owner.

### DOIT queue consolidation

- Moved the remaining root `DOIT*.md` queue files from `~/yolomux.dev8002` into `~/yolomux.dev8003` and studied the consolidated queue set. Closed the completed license-rewrite and single-owner background indexing queue files: their source work, tests, docs, current-tree migration, and isolated live verification were already done; the remaining force-push and live-fleet checks now live in `docs/TODO.md` because they require explicit authorization or a deliberate multi-port restart. Updated the Claude fixture queue so stale checkboxes match the shipped cursor-follow/navigation work, leaving only the real-capture adoption decision open. Kept the refactor audit as an active backlog.

### Claude prompt fixture cursors

- Completed and removed `DOIT.claude-fixture-cursor.md`. The missing real-capture files named by the old queue are no longer present, so the durable adoption decision is to keep the current synthetic fixture coverage and add explicit synthesized cursor metadata for the remaining cursor-less Claude fixtures. The root and promoted-captures inventories now have no Claude fixture with `cursor=missing`, and `tests/test_mock_agents.py` includes a regression for that contract. Verification: direct inventory scan reported `all claude fixtures have cursor metadata`, `python3 -m pytest tests/test_mock_agents.py::test_all_claude_prompt_corpus_fixtures_have_cursor_metadata -q` passed, and full `python3 -m pytest tests/test_mock_agents.py -q` passed (`152 passed`).

## 2026-06-24

### Background indexing owner visibility

- YO!info now shows whether the current browser is connected to the `search-index` owner (`indexing server`) or to a follower (`read server`) and names the connected server plus the index owner. The client loads `/api/background/status` once at startup, refreshes it on manual refresh and EventSource reconnect, and applies `background_owner_changed` pushes directly. Runtime owner takeovers now notify the app through a background-owner acquire callback, so a follower that takes over after a dead indexing owner publishes `background_owner_changed` instead of silently changing only the on-disk owner record. `docs/DEVELOPMENT.md` now spells out the `YOLOMUX_STATE_DIR` layout: `background-owner/`, `search_index/`, `watch-index.json`, and separate per-target auto-approve locks under `locks/`.

### Filesystem watch diff/keyframe frames

- Implemented MPEG-style filesystem watch frames. The server now records a bounded shared filesystem snapshot history, sends full frames for the first/keyframe/manual paths, sends compact `fs_changed` invalidations for ordinary watch changes, and answers stateless client `GET /api/fs/watch-diff?since=<token>` requests with changed-root diffs or a full stale-token fallback. Browser clients keep their own token, request `full=1` from the top-right refresh path, invalidate removed roots, and no per-client server state is needed for multiple clients per server. Verification: focused filesystem watch and route tests, `node tests/layout_async.test.js`, and full `python3 tools/check.py`.

### Visual animation verification rule

- Refactored the agent-status ball debugging lesson into one readable source of truth. `docs/specs/GUI.md` owns the detailed visual contract and status-ball acceptance criteria; `docs/DEVELOPMENT.md` now keeps only the workflow checklist; `AGENTS.md` points agents back to those docs instead of repeating the full rule. The root lesson is still the same: prove the exact reported element changes in the browser over time before saying a visual animation fix is done.

### Differ rootless-empty session-files flicker

- Fixed the Differ panel flashing between `~/yolomux.dev8002` and an empty result for Tab `8002`. The frontend accepted a transient `session_files_ready` payload with `repos: []` and `files: []` for the selected session, so a weak session-discovery snapshot could blank an already-loaded rooted Differ payload until the next rooted payload arrived. Background fetches and SSE pushes now preserve the current same-session Differ payload when the incoming update is rootless and empty; a real clean result still applies because it carries the live repo root with `count: 0`. Added regression coverage for the exact `8002` / `~/yolomux.dev8002` shape and rebuilt `static/yolomux.js`. Focused verification: `node tests/share_theme.test.js` and `python3 -m pytest tests/test_node_suite.py`.

### Tmux signal patch key refactor

- Completed and removed `DOIT.refactor_tmux_signal_keys.md`. Tmux signal window patching now routes record keys through one backend helper (`window_record_key()`, backed by `window_key()`) and one frontend helper (`tmuxSignalWindowKey()`), instead of rebuilding `session:window_index` in `app.py`, patch merge code, pane lookup, and global activity counters. Added regression coverage for fallback session/window records with no explicit `key`.

### Slow API/SSE payload optimization

- Completed and removed `DOIT.optimize_slow_api_calls.md`. Auto-approve roster status now has a single cached stale-while-refresh owner with single-flight refreshes; session-scoped auto-approve calls bypass the full roster cache; timer `auto_approve_changed` pushes compact `{refresh: true, signature}` invalidations instead of the all-session roster; auto-approve window rows skip path/git inventory and reuse selected-pane screen classification; Debug shows auto-approve phase timings. Client-event payloads are smaller: `fs_changed` sends compact refresh invalidations, watch-state transcript pushes send signature-only refreshes, unchanged session-files payloads do not republish across repeated `fs_changed` triggers, watch-root registration is debounced, and tmux signal pushes use changed-window patches with full-payload fallback. Startup control-socket `BrokenPipeError` is treated as a benign disconnect. Verification: focused `python3 -m pytest` slices for auto-approve/control/client-event paths, `python3 -m pytest tests/test_app.py -k 'auto_approve_payload_includes_agent_window_statuses' -q`, `python3 tools/static_build.py --check`, and `node tests/layout_async.test.js`.

### YO!agent oversized conversation recovery

- Completed and removed `DOIT.yoagent_chat_413_request_entity_too_large.md`. YO!agent now caps persisted auxiliary/tool stream data through one shared sanitizer for conversation JSONL and live stream state, so megabyte `rg`/tool output lines cannot wedge future turns. Codex app-server sends detect HTTP 413 / request-too-large failures, drop the poisoned resume thread, start a fresh Codex thread, and retry once before surfacing an actionable error. The YO!agent chat UI now shows a specific “conversation too large to resume” message instead of raw `chat failed: Request Entity Too Large`, and the YO!agent prompt now tells the model to keep shell output bounded with `rg -M 2000 --max-columns 2000`, targeted paths, and summaries. Verification: `python3 -m pytest tests/test_yoagent_stream_state.py tests/test_yoagent_transports.py tests/test_activity_summary.py -q`, `python3 -m pytest tests/test_app.py -k 'reset_yoagent_chat_clears_cli_sessions or yoagent_codex_backend_falls_back_to_exec_when_app_server_fails' -q`, `python3 -m pytest tests/test_app.py -k 'yoagent_model_chat_appends_history_and_skips_activity_for_simple_followup' -q`, `python3 tools/static_build.py`, and `node tests/layout_async.test.js`.

### DOIT queue audit

- Audited the remaining `/tmp/DOIT*` queue against `main` at `56ed4dd8`. Cleared `/tmp/DOIT.balls_not_pulsating_research.md`: current code restores status-ball pulse cadence through the shared high-specificity `.status-indicator.heartbeat-pulse` parent, keeps static Claude/Codex symbols with separate glowing balls, pins reduced-motion parity with `test_status_balls_keep_ask_pill_pulse_cadence_under_reduced_motion`, and documents the contract in `docs/specs/GUI.md`. Kept `/tmp/DOIT.yoagent_chat_413_request_entity_too_large.md`, `/tmp/DOIT.slow-fresh-reload-client-event-flood.md`, and `/tmp/DOIT.optimize_slow_api_calls.md` with audit notes because they still contain real unchecked follow-up work.

### Agent status ball reduced-motion pulse

- Completed and removed `/tmp/DOIT.balls_not_pulsating_research.md`. The reduced-motion freeze was a CSS specificity split: status balls kept `animation-name` on a high-specificity dot selector but inherited timing from the low-specificity `.heartbeat-pulse`, so `prefers-reduced-motion` reset the ball duration to `0s` while the `ASK?` pillbox kept pulsing. The pulse cadence now lives on the shared high-specificity `.status-indicator.heartbeat-pulse` parent, generated `static/yolomux.css` is rebuilt, and `test_status_balls_keep_ask_pill_pulse_cadence_under_reduced_motion` proves working/attention balls keep the same nonzero `attention-ring-fade` duration, delay, timing, and running/pending animation state as the `ASK?` pillbox under reduced motion. `docs/specs/GUI.md` already records the contract: status balls remain separate from the static agent symbol and must keep the same cadence as `ASK?` under `prefers-reduced-motion: reduce`.

### Correctness bug audit

- Completed and removed `DOIT.bug-audit.md`. Verified the current tree covers all 13 audited backend/frontend correctness bugs: share viewer snapshots, hidden-tab terminal resize recovery, PTY/share cleanup, bounded tmux option calls, owned share-reader fds, vanished auto-approve session cleanup, incremental UTF-8 stream decoding, tmux replacement decoding, SIGKILL wait, bounded share sends, late websocket-frame guards, terminal fit callback teardown, and non-ASCII websocket-key rejection. Final focused verification for the remaining items: `python3 -m pytest tests/test_server_query.py::test_share_viewer_send_frame_restores_bounded_timeout tests/test_server_query.py::test_accept_websocket_rejects_non_ascii_key_cleanly -q` and `node tests/editor_preview.test.js`.

### Slow fresh reload and activity refresh

- Completed and removed `DOIT.yolomux-slow-reload-fix.md`. Fresh reload slowness had two separate causes: large versioned static startup assets were served raw with `Cache-Control: no-store`, and all-session activity summary repeatedly scanned the 48 MB event log once per session. Static GET/HEAD now negotiates gzip for compressible assets, sends `Vary: Accept-Encoding`, and gives only `?v=` URLs immutable cache headers while HTML/JSON remain `no-store`; live 8003 gzip checks shrank `yolomux.js` 2,088,364->479,661 bytes, `yolomux.css` 416,024->75,068 bytes, Dockview 794,201->136,607 bytes, and xterm 392,419->91,653 bytes. `EventLog.tail()` now scans backward and `tail_many()` lets activity summary collect recent events for all sessions in one pass; live 8003 timings after restart were activity summary 2.297s, forced activity summary 4.195s, cached transcripts 0.004s, and forced transcripts 0.753s. Frontend boot now paints the saved shell before awaiting transcript metadata, with `static/yolomux.js` rebuilt from `static_src`. Verification: `python3 -m pytest tests/test_server_static.py -q`, `python3 -m pytest tests/test_observability.py -q`, `python3 -m pytest tests/test_app.py -k activity_summary_payload -q`, `python3 -m pytest tests/test_static_build.py::test_generated_static_assets_are_current -q`, `python3 tools/static_build.py --check`, `node tests/share_theme.test.js`, `python3 -m py_compile yolomux.py tmux_wall.py auto_approve_tmux.py yolomux_lib/*.py`, authenticated live 8003 timing/header probe, and 8003 restart/ping from `/home/keivenc/yolomux.dev8003`.
- Follow-up queue replay added explicit timing regression coverage for the reload fix: `test_event_log_tail_timing_regression_stops_before_old_history` makes old log history artificially slow so a full forward scan fails, and `test_activity_summary_payload_batches_recent_events_for_multiple_sessions` proves activity summary batches recent events through one `tail_many()` call. Re-measured live 8003 after restart: activity summary 5.036s, forced activity summary 4.615s, cached transcripts 0.009s, forced transcripts 1.644s; static gzip remained correct (`yolomux.js` 2,088,568->479,713 bytes, `yolomux.css` 416,024->75,068 bytes, Dockview 794,201->136,607 bytes, xterm 392,419->91,653 bytes). `cProfile` showed the old event-log scan is no longer the hot path; remaining activity-summary time is metadata/session-files/background work, now routed to the active single-owner background-indexing queue.
- Drained and removed `DOIT.slow-fresh-reload-timing-regression.md` after moving it from `/tmp`; its only remaining unchecked follow-up was already represented by `DOIT.single_owner_background_indexing.md`.

## 2026-06-23

### Working agent indicator: static symbol + glowing green ball, and stop rotating the YO marker

- Final design (after iterating through a symbol opacity-pulse and a symbol<->ball alternate, both of which the user rejected): every Claude/Codex status indicator shows its agent symbol STATIC with a SEPARATE glowing ball beside it — `[symbol][ball]`, side by side — on dock pane tabs, the tmux sub-window bar, Tabber rows, and Info Pane buttons. Working uses a quieter green ball, attention uses a stronger red ball, and cooldown uses a stronger yellow ball; all three glow via the existing `attention-ring-fade` status-dot animation. `--active` keeps the `agent-symbol-glow-cadence` symbol glow because it is current-window state, not a status ball. Focused Selenium verifies static symbol opacity/animation, a working-tone dot animating `attention-ring-fade`, wrapper `display:flex`, and no YO rotation.
- Implementation (`static_src/css/yolomux/20_sessions_popovers.css`, `static_src/js/yolomux/45_agent_window_activity.js`): `agentWindowStatusDotHtml` now renders the dot for the working state (not just attention/cooldown); working symbols no longer get `heartbeat-pulse` (stay static); the old grid/alternate rules and keyframes are gone so status indicators keep base inline-flex. The activity ball is sized from `--agent-window-icon-size` (not the surrounding font) so it is the same size on every surface — before, the dock-tab ball (11.4px) and Tabber ball (10.4px) came out different sizes.
- Stopped the YO marker rotating to signal working (the glowing ball does that now): removed the `yolo-marker-rotate` animation from `.session-yolo-marker.working` and from `.about-brand-yo` (About modal); the sign-in `brand-yolo` already had no rotation. Loading/thinking spinners (`changes-loading-yolo`, `yoagent-waiting-spinner`, `yoagent-chat-spinner`) keep spinning via their own explicit rotation rule — they indicate transient loading, not session-working.
- Also from this investigation, kept: the shared working predicate so the indicator renders whenever `sessionYoloIsWorking` (incl. the `screen.key==='working'` proxy, the original "Tab 7 YO'ing but nothing shows" case) — `sessionWorkingAgentWindowForTab` in `60_popovers_tabs.js`, used by dock tab/Tabber/window bar.
- Tests (`tests/test_browser_dockview.py`, `tests/test_browser_layout.py`, `tests/tabber.test.js`, `tests/editor_preview.test.js`, `tests/share_theme.test.js`): renamed the working-state tests to the static-symbol + glowing-ball contract, added split-layout coverage for working/attention/cooldown across dock tabs / Tabber / Info Pane buttons, added `test_tabber_live_rows_use_custom_hover_without_native_titles`, and kept `test_platform_controls_use_pc_glyphs` for static YO. Spec updated in `docs/specs/GUI.md`. Work is on 8001 only this round — NOT landed on main/prod. Full `tools/check.py` still fails in the known live-tmux `test_app.py` area (now 7 failures with unexpected `%16`) and two parallel browser Dockview wait timeouts that pass serially; focused Selenium/Node tests pass.

### Agent activity status refactor

- Completed and removed `DOIT.refactor_agent_activity_status.md`. Agent activity UI now routes pulse cadence, status dot/glyph animation, agent icon sizing, popover host behavior, and per-window activity rendering through shared parents instead of duplicated Tabber/Info Bar/popover selectors. A real Claude Code 2.1.186 capture (`working_empty_prompt_below_counter__claude-code-2.1.186_20260623.yaml`) now proves a visible working counter remains `RUN` even when a bare empty composer prompt sits below it. Verification: `python3 -m pytest tests/test_agent_tui.py -q` (`96 passed`), `node tests/editor_preview.test.js` (`100 passed`), `node tests/tabber.test.js` (`38 passed`), focused parity/browser pytest, `python3 tools/static_build.py --check`, full `python3 tools/check.py` (`CHECK PASSED in 56.13s`), and dev8001 restart/ping (`pid 2126896`, unauthenticated `/api/ping` returned 401).

## 2026-06-22

### Wrapped ASK? question highlight

- Completed and removed `DOIT.question-highlight-wrap.md`. ASK? question highlighting now matches the triggering question sentence across wrapped terminal rows, paints one overlay segment per visual row, stops before unrelated text such as the explanatory parenthetical after `Want me ... chat?`, and uses a slightly stronger red fill/ring without adding a heavy left-edge block. Regression coverage now includes the existing single-line prompt, an explicit wrapped `Want me to draft...?` payload, a generic fallback wrapped prompt, and nearby non-question rows. Verification: `node tests/editor_preview.test.js` (`96 passed`), `python3 tools/static_build.py --check`, and full `python3 tools/check.py` (`CHECK PASSED in 54.94s`).

### Browser reload on client/server mismatch

- Completed and removed `DOIT.client_server_version_reload_prompt.md`. The existing server-version reload path now treats enabled semantic differences as client/server mismatches in either direction, asks "Do you want to reload the browser?", and shows the existing Reload plus Keep controls while preserving the same metadata poll, safe auto-reload gate, idempotence guard, and self-update suppression. Verification: `node tests/layout_async.test.js`, `node tests/editor_preview.test.js`, `node tests/layout_url.test.js`, and full `python3 tools/check.py` (`CHECK PASSED in 99.34s`).

### Backend-owned per-window agent view model

- Completed and removed `DOIT.per_window_viewmodel_backend_source.md`. `agent_window_status_payloads` now owns each Claude/Codex tmux sub-window record (`pid`, `active`, state, touched repo roots, path entries, and git facts), `/api/activity?hours=...` serves those records from the cached activity/session-files path, and the frontend `sessionAgentWindowStatusPayloads` / `windowViewModel` accessors feed popover, Tabber, Info Bar, and tmux sub-window bar rendering. The old frontend per-sub-window path resolvers (`sessionWindowMetadataRows`, `tabberRepoEntriesForWindow` session-files parse, selected-pane path use for agent metadata) were retired. Verification: `node tests/tabber.test.js`, `node tests/editor_preview.test.js`, `node tests/layout_async.test.js`, `node tests/layout_url.test.js`, `node tests/share_theme.test.js`, focused backend/browser-share pytest, and full `python3 tools/check.py` (`CHECK PASSED in 96.19s`).

## 2026-06-21

### YO!agent Claude thinking progress

- Completed and removed `DOIT.claude_thinking_share_claudepy_parent.md`. Real Claude Code 2.1.185 stream-json was reproduced with hidden thinking (`thinking_tokens = 9954`) but no readable `thinking_delta` text; the supported signal is `system/thinking_tokens` token progress plus an empty signed thinking snapshot. YO!agent now consumes the cumulative token progress, ignores duplicate empty deltas and empty final snapshots, preserves the final token heartbeat through turn-done, and renders token-only progress as `thinking (~N tokens)…` instead of `thinking (2 words)…` / `thinking: thinking…`. Verification: real capture replay `/tmp/yolomux-claude-thinking-real-20260621-223855.jsonl` ended with `thinking... (~9900 tokens)`, `has_duplicate_prefix False`, and `has_token_progress True`; focused tests passed (`python3 -m pytest tests/test_yoagent_stream_events.py tests/test_yoagent_stream_state.py -q` -> 19 passed, `node tests/layout_async.test.js` -> 3 passed); full `python3 tools/check.py` passed (`CHECK PASSED in 97.50s`); 8001 restart/ping returned 401.
- Additional real Claude Code 2.1.185 transport verification found the other live shape: readable `thinking_delta` words interleaved with `thinking_tokens` heartbeats. YO!agent now treats token heartbeats as progress only until real words arrive, then preserves and appends the word stream instead of replacing it with later token-count heartbeats. Verification: real `ClaudeStreamJsonTransport` -> normalizer -> YO!agent callback emitted 23 thinking updates, `word_seen_live True`, `token_heartbeat_replaced_words_after_word_seen False`, final stream items `thinking` then `assistant`; focused stream tests passed (`python3 -m pytest tests/test_yoagent_stream_state.py tests/test_yoagent_stream_events.py tests/test_yoagent_transports.py -q` -> 50 passed); full `python3 tools/check.py` passed (`CHECK PASSED in 95.58s`); 8001 restart/ping returned 401.

### Queue drain — Tabber, popovers, recency, and YO!agent

#### Tabber status, chrome, sort, popover parity, and per-window paths
- Completed and removed `DOIT.tabber_status_color_system.md`, `DOIT.tabber_row_icon_and_context_menu.md`, `DOIT.tabber_session_tab_equal_width.md`, `DOIT.tabber_sort_control.md`, `DOIT.tabber_tab_popover_parity.md`, `DOIT.per_window_path_one_source.md`, and `DOIT.popover_id_label_and_copy.md`. First-level Tabber session rows now reuse the real pane-tab chrome/popover at the full available width, keep disclosure/row tree ownership, expose the shared A-Z/Z-A/recent/oldest sort select, use the shared tab context menu on session/window/tab rows and the file context menu on repo rows, show PIDs/session IDs with shared copy buttons, and use the same per-window touched-repo resolver as the popover path rows.
- Status text/dots now share the common tone system: ASK? is a filled red attention dot/text pulse, working is green, cooldown/settled are static, `active` and sub-minute plain recency use the max-contrast token, and static/localized bundles carry `branch.current=active` plus `popover.sessionId`.

#### Recency and Claude working detection
- Completed and removed `DOIT.tabber_recency_stuck_under_15sec.md` and `DOIT.claude_working_shows_black_dot.md`. Terminal protocol/report-only input no longer pegs activity recency at `<15 sec ago`; genuine terminal input still records just-active state. Claude/Codex visible duration/token-counter working screens are covered by detector tests/captures, and the activity-dot state machine can return from settled/cooldown to green when working is detected again.

#### YO!agent stream and orchestration tests
- Completed and removed `DOIT.yoagent_claude_thinking_words_recurring.md` and `DOIT.yoagent_orchestration_tests.md`. YO!agent now preserves ordered assistant/thinking/tool stream items, keeps real Claude thinking through heartbeat and turn-done updates instead of replacing it with the `thinking` placeholder, counts words from the real auxiliary text, renders expanded auxiliary details without an inner scroll box, and adds repeatable orchestration coverage for server-verified sends, sequential dependent sends, wait-then-send, and prompt-answer selector routing.
- Verification: `python3 tools/check.py` passed (`CHECK PASSED in 118.35s`) after rebuilt static assets and the new/updated node, browser, backend, YO!agent, and detector regressions were in place. `DOIT.00_index.md` was removed after all queue files were drained.

### UI, Tabber, Editor, and Tmux sub-windows

#### Active window parity and ASK? red-dot pulse
- Completed and removed `DOIT.popover_active_window_wrong.md`, `DOIT.popover_tabber_infoline_agree.md`, and `DOIT.ask_red_dot_glow_animation.md`. Popover active-window state now uses one shared active-index helper that prefers the optimistic override, then tmux `window_active`, with selected pane only as fallback; the focused tmux sub-window shows `active` while non-focused windows keep their own working/idle/ASK state. Info Bar now joins the same per-window metadata source as popover/Tabber when touched path/git metadata exists, so active AI windows show the touched repo root and matching git facts (`0 dirty · N ahead`) instead of raw cwd subdirs; transcript-derived repo metadata is preserved when no shared per-window metadata exists. Red ASK?/attention dots now have the same-strength pulse selector as working dots plus a real 1px border channel, making the window-bar, popover, and Tabber dot visibly pulse in lockstep with the ASK? badge. Verification: runtime Chrome computed-style repro before/after, `python3 -m pytest tests/test_browser_layout.py::test_attention_agent_dots_share_ask_badge_pulse_computed_style -q`, `node tests/editor_preview.test.js`, `node tests/tabber.test.js`, `node tests/layout_url.test.js`, rebuilt static assets, and full `python3 tools/check.py` (`CHECK PASSED in 92.32s`).

#### Tabber AI icon before window name
- Completed and removed `DOIT.tabber_ai_symbol_before_name.md`. Tabber window labels now render the Claude/Codex agent icon before the canonical `index:name` text while leaving the disclosure triangle in the tree-icon column and keeping the activity dot, PID, and right-side status after the name. Bash/non-AI rows do not gain a leading icon. Verification: `python3 tools/static_build.py`, `node tests/tabber.test.js` (`38 passed`), and full `python3 tools/check.py` (`CHECK PASSED in 97.02s`). The full gate also exposed a YO!agent preference-test isolation flake; `tests/test_yoagent_preferences.py` now uses the existing isolated YO!agent conversation fixture and the focused test passes.

#### ASK? status recency and active-dot brightness pulse
- Completed and removed `DOIT.ask_status_time_ago.md` and `DOIT.circle_pulse_brightness_visible_on_active.md`. Tabber and popover attention rows now read `ASK? <time> ago` using the same compact recency formatter as idle rows, so the old `ASK? approval` / `ASK? needs input` subtype words are gone while the red attention pulse remains. Working rows still show `working for <duration>`.
- Status dots now pulse their own brightness through the shared `attention-ring-fade` animation in addition to the glow: dot users opt into `--attention-pulse-brightness-rest: 0.82` and `--attention-pulse-brightness-peak: 1.34`, while non-dot users keep neutral filter defaults. The active tmux-window button contrast halo remains in place, so the pulse is visible on bright active-accent backgrounds.
- Verification: `python3 tools/static_build.py`, `node tests/editor_preview.test.js` (`92 passed`), `node tests/tabber.test.js` (`38 passed`), full `python3 tools/check.py` (`CHECK PASSED in 101.10s`), 8001 restart/ping (`pid 1066151`, unauthenticated `/api/ping` returned 401), and live Chrome against the served 8001 app confirmed `ASK? <15 sec ago` / `ASK? <1 min ago` with no subtype words plus active-dot `attention-ring-fade`, brightness `0.82 -> 1.34`, and the active-control text-shadow halo.

#### Shared working-dot glow waveform
- Completed and removed `DOIT.circle_pulse_glow_waveform_match_ask.md`. Working/cooldown/ASK? status dots now use the same parameterized `attention-ring-fade` glow waveform as ASK? badges: the dot stays fully opaque, the halo pulses weak-to-strong, and state color is supplied by `--attention-ring-rgb` (green working, yellow cooldown, red attention).
- Verification: `node tests/editor_preview.test.js` (`91 passed`), `python3 -m pytest tests/test_browser_dockview.py::test_dockview_window_bar_working_status_dot_uses_shared_pulse -q`, `python3 tools/static_build.py`, full `python3 tools/check.py` (`CHECK PASSED in 81.89s`), 8001 restart/ping (`pid 2254306`, unauthenticated `/api/ping` returned 401), and live 8001 computed-style verification reported `animationName: attention-ring-fade`, `animationDuration: 1.55s`, `opacity: 1`, and green `--attention-ring-rgb: 82 210 115`.

#### Tabber PIDs and transcript copy rows
- Completed and removed `DOIT.tabber_agent_windows_missing_pid.md` and `DOIT.hover_transcript_id_and_location_copy.md`. Tabber window rows now render `index:agent (pid=N)` and `index:bash (pid=N)` from `record.pid` data instead of parsing the label string; session popover agent-window blocks now show per-AI-window Transcript ID and Transcript location rows with copy buttons.
- Backend `agent_window_status_payloads` now exposes `transcript`, `transcript_id`, and `agent_session_id` per Claude/Codex tmux sub-window, with the frontend falling back to the transcript basename when no explicit ID exists. The copy buttons reuse the existing delegated `[data-copy-path]` handler.
- Verification: `python3 tools/static_build.py`, `node tests/tabber.test.js` (`38 passed`), `node tests/editor_preview.test.js` (`91 passed`), `python3 -m pytest tests/test_app.py::test_auto_approve_payload_includes_agent_window_statuses -q`, full `python3 tools/check.py` (`CHECK PASSED in 85.55s`), 8001 restart/ping (`pid 3984578`, unauthenticated `/api/ping` returned 401), and live Selenium against `https://localhost:8001` confirmed Tabber PID labels plus Transcript ID/path copy clicks.

#### Tabber status column clipping
- Completed and removed `DOIT.tabber_status_column_clips_working_ask.md`. Tabber long status rows (`working for…` and `ASK?…`) now get a `tabber-status-long` class so the status cell sizes to content and truncates at the end when needed; normal ago rows keep the compact fixed recency column. Date/status text now uses a row-scale font one step below the tree label instead of the tiny 3xs size.
- Verification: `python3 tools/static_build.py`, `node tests/tabber.test.js` (`38 passed`), full `python3 tools/check.py` (`CHECK PASSED in 83.12s`), 8001 restart/ping (`pid 390948`, unauthenticated `/api/ping` returned 401), and live Selenium rendered `working for 2m 4s`, `ASK? needs input`, and `<1 min ago` rows from the served bundle to confirm no leading-character clipping and larger date/status text.

#### Window activity, Tabber chrome, recency, popovers, editor theme, and YO rotation
- Completed and removed `DOIT.agent_activity_circle_states.md`, `DOIT.recency_red_bold_under_1min.md`, `DOIT.tabber_session_row_reuse_tab_chrome.md`, `DOIT.popover_merge_window_rows_no_repeat.md`, `DOIT.editor_theme_follows_global_theme.md`, `DOIT.window_button_redundant_index_label.md`, `DOIT.active_window_circle_contrast.md`, and `DOIT.yo_rotates_on_any_window_active.md`.
- Tmux/Tabber activity now uses one per-sub-window state source: ASK? persists as a red pulsing dot and propagates to the tab, just-finished tmux sub-windows cool down yellow for 60 seconds, active tmux sub-window dots get a theme-aware contrast halo, and the YO/running count spins when any hidden or shown Claude/Codex tmux sub-window is working. Duplicate indexed labels now render as `n:name` instead of `n:name(n)`, while name-only labels keep `name(n)` for disambiguation.
- Recency styling makes <15s dates red/bold/pulsing and <1m dates red/bold in Ago and Date modes; Tabber session rows reuse the real pane-tab chrome with the YO button and ASK? badge; session popovers show each tmux sub-window once with metadata beneath; CodeMirror follows the Global theme when the editor theme inherits it.
- Verification: `python3 tools/static_build.py`, `node tests/editor_preview.test.js` (`91 passed`), `node tests/tabber.test.js` (`38 passed`), full `python3 tools/check.py` (`CHECK PASSED in 89.91s`), 8001 restart/ping (`pid 3295417`, unauthenticated `/api/ping` returned 401), and live Selenium against `https://localhost:8001` confirmed duplicate `bash` labels, active-dot contrast CSS, and hidden-window YO working aggregation.

#### Current-window popovers, ASK? pulse text, sub-minute Ago labels, and theme contrast
- Completed and removed `DOIT.popover_per_window_fixes.md`, `DOIT.ask_status_text_red_pulse.md`, `DOIT.recency_under_15sec_ago_label.md`, and `DOIT.light_dark_font_contrast.md`. Current/focused agent windows no longer display false `idle` text in popovers, tmux-window header rows use the pane-tab shaded token, and all popover copy buttons route through the shared `data-copy-path` handler.
- ASK?/needs-input status text now uses the same red attention label, pulse class, and animation-delay clock as the ASK? badge and red dot; non-ASK? working/idle rows keep their prior styling. Ago text now shows `<15 sec ago` inside the same 15-second pulse window, `<1 min ago` from 15-60 seconds, and idle AI windows use the same `<time> ago` wording instead of `idle …`.
- Light-mode contrast now keeps blue/green/link/code text dark enough against light surfaces, active-accent text contrasts per accent, and Preferences labels read `Global appearance` with `System` / `Dark mode` / `Light Mode` plus `Theme color`.
- Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/editor_preview.test.js` (`92 passed`), `node tests/share_theme.test.js` (`1 passed`), `node tests/tabber.test.js` (`38 passed`), full `python3 tools/check.py` (`CHECK PASSED in 117.31s`), and final 8001 restart/ping (`pid 2654734`, unauthenticated `/api/ping` returned 401). User-side visual checks remain the final eyeball for the live pulse/copy/light-dark rendering.

### YO!agent

#### Claude/Codex goal-active prompt detection
- Completed and removed `DOIT.goal_active_extraction_claude_and_codex.md`. Claude `/goal active (<duration>)` now shares the same goal-duration parser path as Codex `Pursuing goal (<duration>)`, feeding `goal_elapsed_seconds` and `display_elapsed_seconds` so displayed working time can prefer the cumulative goal timer instead of only the current spinner step.
- Verification: real prompt-corpus captures `goal_active__claude-code-2.1.185_20260621.yaml` and `goal_active__codex-cli-0.141.0_20260621.yaml` are registered together, `test_real_goal_active_captures_prefer_goal_elapsed_for_display` covers both, and the focused detector run covering goal-active, ASK?, interrupted, and corpus classification passed (`44 passed`).

#### YO!agent send orchestration and prompt answers
- Completed and removed `DOIT.yoagent_send_orchestration.md`. YO!agent now treats the tmux session number/label as the handle, routes explicit send/ask/tell requests through server-verified tmux sends instead of model refusals, answers visible approval/question prompts through the shared selector path without pasting free text into menus, and decomposes same-session dependent asks into send -> wait -> derive -> follow-up send.
- Verification: focused parser/action/skill/app tests passed, `python3 tools/static_build.py` passed, full `python3 tools/check.py` passed during implementation and again after live proof (`CHECK PASSED in 83.75s`), 8001 restart/ping returned the expected 401, and live 8001 API checks against disposable mock tmux sessions proved ready send, sequential first-send decomposition, and selector answer (`/api/yoagent/actions/preview-send` + `/execute-send` selected option 2 in `so10ask`, and the pane showed `● You picked: Roger Federer`).

#### Stream tool-call coalescing
- Completed and removed `DOIT.yoagent_combine_consecutive_tool_calls.md`. Consecutive YO!agent `tool` stream items now coalesce into one Tool call details block while assistant/thinking rows still split separate tool runs; thinking summaries show the full word count for the coalesced thinking block.
- Verification: `python3 tools/static_build.py`, `python3 tools/check.py`, 8001 restart/ping, and a live 8001 Selenium verifier rendered three adjacent tool items through `yoagentMessageStreamItemsHtml` and found one `.yoagent-toolcall-details` containing every command/output line.

#### Claude backend availability and stream-json CLI flags
- Completed and removed `DOIT.yoagent_claude_stale_availability_deterministic.md`. YO!agent no longer treats transient Claude auth probe failures as sticky logged-out state, refreshes frontend agent availability via `/api/agent-auth`, keeps explicit Claude/Codex selections explicit when the CLI is installed, and surfaces provider-specific fallback reasons instead of silently downgrading to deterministic.
- Fixed the live restart verification failure exposed by Claude Code 2.1.185: the shared Claude stream-json argv no longer passes the stale `--show-thinking` flag, which the installed CLI rejects. The transport still uses `--include-partial-messages` with `--output-format stream-json`.
- Verification: `python3 -m pytest tests/test_yoagent_transports.py::test_claude_stream_json_argv_includes_partials_and_optional_tools -q` passed, final `python3 tools/check.py` passed, 8001 was restarted with `tools/yolomux-restart-dev1.sh`, unauthenticated `/api/ping` returned the expected 401, and an authenticated live `/api/yoagent/chat` request with `yoagent.backend=claude` returned `backend_used: "claude"` / `fallback: false` / `answer: "YOAGENT_BACKEND_CHECK"`.

### Build, Refactor, Docs, and Tests

#### Refactor audit implementation pass
- Completed and removed `DOIT.refactor_audit_20260620.md`. YO!agent stream state moved out of `TmuxWebtermApp` into `yoagent/streaming.py`, `YoagentController.yoagent_chat` now runs through explicit intent handlers, shared tree interaction split into selection/expansion/click/keyboard helpers, file-tree rows use a row-state builder, editor rendering is dispatched by state/kind/mode, request body parsing routes through one helper, scattered UI timer literals moved under the timing owner, repeated component CSS literals moved to tokens with a static-build lint, and YO!agent stream-state tests moved into `tests/test_yoagent_stream_state.py`.
- Verification: focused pytest/node checks for each owner passed, including `python3 -m pytest tests/test_yoagent_stream_state.py -q` (`9 passed`), the combined stream-state rerun (`12 passed`), `python3 -m pytest tests/test_static_build.py -q` (`22 passed`), `node tests/layout_url.test.js` (`179 passed`), `node tests/editor_preview.test.js` (`92 passed`), `node tests/share_theme.test.js` (`1 passed`), `node tests/tabber.test.js` (`38 passed`), `python3 tools/static_build.py --check`, final `python3 tools/check.py` (`CHECK PASSED in 117.31s`), and final 8001 restart/ping (`pid 2654734`, unauthenticated `/api/ping` returned 401).

## 2026-06-20

### YO!agent

#### YO!agent thinking stream, heartbeat states, and per-window metadata
- Completed and removed `DOIT.yoagent_always_show_thinking_claude_codex.md`, `DOIT.yoagent_thinking_preview_last_50_words.md`, `DOIT.shared_pulse_heartbeat_parent.md`, `DOIT.agent_circle_working_stopped_states.md`, `DOIT.popover_per_window_path_branch_git.md`, and `DOIT.docs_update_per_window_model.md`.
- YO!agent now asks Claude for readable stream-json partials, asks Codex app-server for reasoning summaries, preserves ordered assistant/thinking/tool stream rows, stores `streamItems`, and keeps expanded thinking/tool details untruncated while collapsed thinking previews show the last ~50 words.
- ASK?, recency pulses, metadata pulses, and Claude/Codex activity dots now share one heartbeat timing parent; working windows pulse green, windows that just stopped pulse red for the shared 15-second window, observed stopped windows settle to a neutral static dot, and cold-idle windows keep the hollow idle marker.
- Popovers now use per-AI-window metadata from `window_metadata[]`, so each Claude/Codex tmux sub-window carries its own path/branch/git/HEAD/worktree/state and shared-repo cases collapse to one block; docs now describe 0+ AI windows per session instead of one flat session agent.
- Verification: focused browser regression `python3 -m pytest tests/test_browser_layout.py::test_yoagent_busy_chat_uses_one_vertical_scroll_owner -q` passed, final `python3 tools/check.py` passed, and 8001 restart/ping passed (`/api/ping` returned expected unauthenticated `401`, served version `0.4.19`).

#### Tabber working duration and resumed YO!agent context
- Completed and removed `DOIT.working_elapsed_uses_pursuing_goal_duration.md`, `DOIT.tabber_show_working_duration.md`, and `DOIT.yoagent_summarize_no_context_on_resume.md`.
- Codex `Pursuing goal (<duration>)` is now parsed as the display elapsed time while the per-step working counter remains the liveness signal, so popovers and Tabber show the cumulative goal duration rather than the short spinner duration.
- Tabber AI tmux sub-window rows now reuse the same shared state text as the popover and show `working for <duration>` for working Claude/Codex tmux sub-windows while idle/non-AI rows keep their existing text.
- YO!agent now re-injects activity context when a resumed backend thread cannot be trusted to retain it, always includes context for Claude turns that need it, forces fresh activity for summarize/list-session asks, and fixes the activity-context matcher that was returning false for non-empty questions.
- Verification: `python3 -m pytest tests/test_auto_approve_detector.py -q` (`121 passed`), `python3 -m pytest tests/test_app.py -q` (`196 passed`), `python3 -m pytest tests/test_yoagent_preferences.py -q` (`29 passed`), `node tests/tabber.test.js` (`38 passed`), `node tests/editor_preview.test.js` (`89 passed`), exact browser-editor rerun passed after one transient full-gate `Script error.`, and final `python3 tools/check.py` passed (`CHECK PASSED in 47.67s`).

#### YO!agent Claude stream wrappers and ask serialization
- Fixed the current Claude CLI stream-json wrapper shape for YO!agent: `stream_event` partials are now unwrapped before normalization, wrapped text deltas can supply the final answer when no final assistant block exists, and duplicate tool-use snapshots no longer create a second tool-start row while the later tool output still appears in the Tool call block.
- YO!agent Ask queue is now enforced on both sides of the request: the browser already queues while busy or while pending waits exist, and the server now serializes the actual Claude/Codex backend turn so overlapping `/api/yoagent/chat` requests cannot run two model calls at once. Verification: real installed-Claude stream captures under `/tmp`, focused stream/transport/backend queue tests, final `python3 tools/check.py`, and 8001 restart/ping.

#### YO!agent persisted-job watch predicates
- Shipped the first batch of the persisted-job watch roadmap: `notify_session_needs_input` fires only on real `needs-input`/question states, `notify_session_blocked` fires on approval/error/disconnect blockers, `notify_session_done_after_working` fires only after that job first observed `working` and later observed an accepting/done/idle state, and `cancel_session_jobs` cancels queued or pending-confirmation jobs for one session while leaving fired/failed/timed-out and other-session jobs alone. YO!agent chat now understands phrases such as `notify me when session 1 asks a question`, `tell me when agent 1 is blocked`, `notify me when session 1 is done after it was working`, and `cancel pending jobs for session 1`; the direct API also exposes `POST /api/yoagent/jobs/cancel-session`.
- Verification: `python3 -m py_compile yolomux_lib/yoagent/actions.py yolomux_lib/yoagent/controller.py yolomux_lib/http_routes.py yolomux_lib/app.py` passed, `python3 -m pytest tests/test_yoagent_actions.py tests/test_app.py tests/test_server_query.py -q` passed (`283 passed`), and the added done-after-working focused selection passed (`8 passed`).

#### YO!agent work-next ranking
- Completed the roadmap item for expanding `what should I work on?` beyond prompt context. The deterministic YO!agent fallback now ranks needs-input, blockers/errors, failing tests, failing CI, review feedback, user-local priority hints, dirty repos, changed files, recently active work, and stale sessions; local priority hints can come from structured activity payload fields or explicit YO!agent context lines such as `work-next priority: session 4 - ...`. Default answers still show only the top three recommendations, while `full inventory` returns the full ranked list.
- Verification: `python3 -m pytest tests/test_activity_summary.py -q` passed (`12 passed`) with coverage for the default top-three cutoff and full ordering across blockers, test failures, review feedback, local priorities, dirty work, active work, and stale work.

#### YO!agent deterministic intent fixtures
- Added deterministic parser fixtures for the common Preference aliases in the YO!agent spec: white/black background, tab width/bigger tabs, smaller UI, bigger terminal font, quiet/no notifications, light/dark terminal, and language endonyms. Fixed the parser gaps those fixtures exposed: non-ASCII aliases such as `日本語` no longer collapse to the wrong language, terminal sizing no longer routes through UI font size, and natural phrases like `tab width 220` / `light terminal` are treated as setting writes.
- Added routing fixtures for wait-then-send, send-and-show-result, opt-out send-only, and two-session handoff/fact-check language. Fixed the routing parser so `wait for session 4, send it date...` queues a wait job even without the word `then`, `do not wait` no longer triggers the wait branch, and handoff prompts strip target-session routing text before sending the second prompt. Verification beyond the standard gate: `python3 -m pytest tests/test_settings.py -q` passed (`31 passed`).

#### YO!agent defaults, activity summaries, tmux labels, Tabber sync, recency pulse, and About/Preferences polish
- Completed and removed `DOIT.yoagent_reasoning_tc_rootcause.md`, `DOIT.yoagent_default_effort_low.md`, `DOIT.yoagent_defaults_backend_auto_claude_haiku.md`, `DOIT.yoagent_background_summaries_first_launch_only.md`, `DOIT.yoagent_activity_summary_print_once.md`, `DOIT.preferences_group_yolo_yoinfo_yoagent.md`, `DOIT.ctrlb_np_unclick_and_remove_window_colors.md`, `DOIT.tab_popover_recent_active_label.md`, `DOIT.window_label_working_idle_icons.md`, `DOIT.popover_window_label_uses_agent_kind.md`, `DOIT.tabber_current_window_update_lag.md`, `DOIT.recency_just_updated_pulse_not_visible.md`, `DOIT.yoagent_invocation_preferences_accuracy.md`, and `DOIT.about_dialog_links_and_license.md`.
- YO!agent now defaults backend to Auto, Claude model to Haiku, and Claude/Codex effort to Low; Codex app-server requests reasoning summaries; Claude thinking gets truthful heartbeat auxiliary lines; YO!agent activity context prints once, background transcript summaries run only on first YO!agent launch, and Preferences keeps YOLO next to YO!agent while accurately describing Codex app-server vs Claude stream-json transport.
- Tmux AI sub-window labels now share one canonical `index:agent` helper across popovers, tmux sub-window bar, and Tabber; tmux sub-window bar and Tabber append shared `●` working / `○` idle icons only for Claude/Codex tmux sub-windows; Tabber active sub-window highlighting repaints immediately on click/keyboard overrides and uses session-scoped `/api/tmux-signals`; file/date recency pulse now has a visible border target and covers Finder/Differ/Tabber Ago and Date modes. Help -> About now renders `Keiven Chang - YOLOmux GitHub` and the PolyForm Noncommercial License link.
- Verification: `python3 tools/static_build.py`, `node tests/editor_preview.test.js` (`83 passed`), `node tests/tabber.test.js` (`36 passed`), `node tests/share_theme.test.js`, focused backend pytest set (`327 passed`, one existing fake-process thread warning), and final `python3 tools/check.py` passed (`CHECK PASSED in 49.79s`). Live visual verification follows after the required 8001 restart.

#### YO!agent panel polish, live tools, model catalog, activity recency, and tmux sub-window sync
- Completed and removed `DOIT.yoagent_panel_polish.md`, `DOIT.yoagent_response_latency_in_header.md`, `DOIT.yoagent_claude_thinking_missing.md`, `DOIT.yoagent_toolcall_label_color_truncation.md`, `DOIT.yoagent_chat_context_and_tools.md`, `DOIT.yoagent_codex_model_catalog.md`, `DOIT.activity_recency_one_source_no_redundant_calls.md`, `DOIT.tab_popover_show_window_number.md`, and `DOIT.window_button_stale_readback_and_ctrlb_np.md`. YO!agent assistant headers now persist and show `(N.N seconds to respond)` from the same response timing used by Details; diagnostic-only Details collapse to `details…` without leaking usage JSON, real thinking/tool gist previews remain, the composer uses the YOU color token, and light-mode YO!agent/YOU text uses readable theme-aware colors.
- Claude stream-json now passes `--include-partial-messages` and surfaces encrypted/empty Claude thinking as token heartbeat auxiliary lines while keeping raw `<think>` out of answer text; tool-call details are localized as `tool call`, color executed commands, and keep expanded auxiliary output at the larger 5000-line bound. YO!agent live external-data prompts now require backend tools when available, retain the current-work/Recent Agents activity context across follow-up questions, and keep missing-tool diagnostics only for genuinely unavailable tools.
- Codex model metadata now has one catalog owner for GPT-5.5, GPT-5.4, GPT-5.4-Mini, and GPT-5.3-Codex-Spark with display names, default effort, and effort options; no nano model was confirmed in the local Codex CLI catalog. Activity/Tabber/popover recency now uses backend-derived `active_recency_ts`, agent popovers show `window_label` such as `0:codex`/`1:claude` with live working elapsed time, and tmux sub-window buttons keep optimistic explicit targets until confirmed while repeated `n/p` navigation is mirrored during tmux repeat-time.
- Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `python3 tools/static_build.py --lint-light` (`0 light-mode pairing issue(s)`), focused YO!agent backend tests (`4 passed`), `node tests/editor_preview.test.js` (`83 passed, 0 failed`), and final `python3 tools/check.py` passed all lanes (`CHECK PASSED in 47.56s`).

#### YO!agent chat retention, warm prompts, and auxiliary visibility
- Completed and removed `DOIT.yoagent_conversation_retention.md`, `DOIT.yoagent_simple_question_latency.md`, `DOIT.yoagent_tools_and_aux_visibility.md`, and `DOIT.yoagent_transcript_copy_button.md`. YO!agent now treats the persisted JSONL transcript as the authoritative history source, ignores partial conversation payloads that lack `messages` instead of clearing the visible chat, appends back-to-back asks and answers, keeps simple follow-up prompts bounded by skipping all-session activity context unless the question needs YOLOmux state, and pins first-after-prewarm Codex app-server reuse so the first ask after server-start prewarm does not spawn a second app-server.
- Auxiliary details now have an explicit disclosure triangle, real reasoning/tool-call stream lines remain split into quiet thinking and `TC` details, text-less reasoning deltas show `reasoning...` instead of a fake permanent `thinking`, and live external-data questions such as weather return a clear no-web/search/weather answer instead of hanging inside a backend that has no such tool. All path-copy buttons now route through one delegated `[data-copy-path]` handler, so the YO!agent transcript copy icon copies the transcript path and reports success.
- Verification: focused YO!agent persistence/latency/no-web/warm-prewarm tests passed, stream-event regression passed, source/DOM copy-handler ownership passed, `node tests/layout_async.test.js` passed (`layout suite: 3 passed, 0 failed`), `node tests/editor_preview.test.js` passed (`layout suite: 82 passed, 0 failed`), the transient browser diff failure from the first full check passed when rerun directly, and the final `python3 tools/check.py` passed (`CHECK PASSED in 56.29s`).

#### YO!agent backend comms and TTFT
- Completed and removed `DOIT.yoagent_backend_comms_and_ttft.md`. Claude/Codex structured backend communication now lives in `yolomux_lib/agent_comms/`: shared JSON-RPC helpers, Codex app-server protocol/session with warm reuse timing, Claude stream-json process helpers, interruption handling, and one normalized stream-event schema.
- YO!agent and the text clients now consume the shared primitives; duplicate YO!agent app-server read/write helpers were removed, `yoagent/stream_events.py` is a compatibility import, and `tools/codex.py`/`tools/claude.py` use the shared normalizers while staying responsible for REPL rendering.
- Verification: focused shared-client/transport/persistent-reuse/prewarm tests passed, real Codex and Claude exact-answer plus Bash-tool smokes passed with metrics, post-restart live 8001 YO!agent browser verification passed for Codex and Claude with screenshots under `/tmp/yolomux-live-yoagent-8001`, dev8001 was restarted and auth-gated `/api/ping` returned 401, and final `python3 tools/check.py` passed (`CHECK PASSED in 50.99s`).

#### YO!agent active request UX
- Completed and removed `DOIT.yoagent_active_request_ux.md`. YO!agent chat now creates an immediate local thinking stream before the backend answers, keeps the composer usable while one request is active, queues additional asks FIFO, shows queued asks with per-item cancel, exposes a stop button for the active request, interrupts Codex app-server and Claude stream-json transports through a request-scoped cancel route, and renders canceled partial answers with a visible stopped state.
- Thinking and tool-call auxiliary output now stay split: thinking previews show the last two live lines and collapse to the final line when done, while tool calls render under their own `TC` expander with a one-line gist and full output on expand. Verification: focused Node/backend/Selenium checks passed, full `python3 tools/check.py` passed (`CHECK PASSED in 47.72s` after rerun), dev8001 restarted with `tools/yolomux-restart-dev1.sh`, and `https://localhost:8001/api/ping` returned the expected login-gated 401 from the restarted process.

#### YO!agent composer backend/model/effort selectors
- Completed and removed `DOIT.yoagent_composer_selectors_and_queue.md`. The YO!agent composer now shows compact backend -> model -> effort selectors in one row, filters backend choices to installed/logged-in Codex or Claude agents, disables chat when no usable backend exists, and persists selections through the same YO!agent settings paths used by Preferences.
- Verification: focused composer/preference/layout tests passed, `python3 tools/static_build.py --check` passed, focused YO!agent browser regressions passed (`2 passed`), and full `python3 tools/check.py` passed (`CHECK PASSED in 51.23s`).

#### YO!agent chat wheel over message bubbles
- Completed and removed `DOIT.yoagent_chat_wheel_over_messages.md`. YO!agent message bubbles and message bodies are explicitly not vertical scroll containers, so wheel/trackpad input over `YO!agent` or `YOU` message text scrolls the `.yoagent-chat-history` owner just like empty gaps; auxiliary previews clip without becoming hidden scroll boxes, while code/detail blocks keep horizontal scrolling and normal vertical scroll chaining.
- Verification: `python3 tools/static_build.py`, `python3 -m pytest tests/test_browser_layout.py::test_yoagent_busy_chat_uses_one_vertical_scroll_owner -q`, `node --test tests/editor_preview.test.js`, and full `python3 tools/check.py` passed (`CHECK PASSED in 49.99s`).

#### Central Claude/Codex TUI API and YO!agent auxiliary streams
- Completed and removed `DOIT.cli_api_claude_codex.md` and `DOIT.yoagent_stream_thinking_toolcalls.md`. Visible Claude/Codex pane control is centralized in `agent_tui.py`, with cursor/composer capture, shape-based prompt/approval/question/activity classification, send/clear verification, auto-approve action fields, shared tmux-wall status, and real CLI fixture harvesting that treats Claude as a frequently changing TUI rather than a stable string API.
- YO!agent chat now uses shared Claude/Codex event normalization for assistant deltas, reasoning/thinking, tool starts/output/done, approval requests, usage, errors, and turn completion; the UI renders auxiliary thinking/tool-call lines in a subdued collapsible details panel while keeping normal assistant text separate.
- Verification: focused detector/agent_tui/auto-approve tests passed (`176 passed`), focused YO!agent stream/send tests passed (`37 passed, 162 deselected`), isolated real-agent browser smoke passed, live 8001 YO!agent browser verifier passed for Claude and Codex with screenshots under `/tmp`, and full `python3 tools/check.py` passed.

### Agents and Automation

#### ASK? no-selector and interrupted prompts
- Completed and removed `DOIT.ask_detection_gaps.md` and the now-empty `DOIT.00_index.md`. Claude AskUserQuestion menus now classify as `needs-input`/ASK? from the visible question, numbered options, and footer even when no selector is highlighted; accessible screen-reader menu rows such as `menu: 1. Pane capture — ...` are parsed without treating descriptions as labels. Claude interrupted prompts such as `Interrupted · What should Claude do instead?` use the same ASK? path and never enter the approval/auto-answer path.
- Verification: real Claude Code 2.1.185 captures for no-selector accessible AskUserQuestion, highlighted AskUserQuestion, and interrupted state are registered in the prompt corpus; detector tests prove `agent_screen_state`/`visible_choice_prompt_text` return `needs-input` with the prompt text while `detect_prompt` and `approval_prompt_state` stay non-approval; live 8001 `/api/auto-approve` against `yoreal-no-selector-ax`, `yoreal-no-selector-proof`, and `yoreal-interrupt-proof` reported `screen.key == "needs-input"` and `prompt.visible == false`; final `python3 tools/check.py` passed (`CHECK PASSED in 118.75s`).

#### Summary provider settings and availability gate
- Added a real `summary.*` settings group and backend catalog entries for AI summary provider, Codex model, effort, service tier, lookback, and timeout. `/api/summary-stream` now reads those settings for metadata, `codex exec` argv, and timeout, and refuses to build a prompt or launch Codex when the provider is disabled, Codex is missing, or Codex is installed but not logged in.
- Documentation now names `summary.*` as the owner of summary defaults; the old `YOLOMUX_SUMMARY_*` env vars only seed defaults when they match valid catalog values. Verification beyond the standard gate: `python3 -m pytest tests/test_settings.py tests/test_server_query.py -q` passed (`100 passed`).

#### Mock agents moved to tools and corpus replay
- Completed and removed `DOIT.mock_agents.md`. Mock Claude/Codex now live under `tools/`, declare Claude Code 2.1.183 and Codex CLI 0.141.0, render current startup chrome/status counters/approval labels, and expose `mockcase list` plus `mockcase <case>` to replay every real and synthetic prompt-corpus family from `tests/fixtures/prompt_corpus/` inside tmux.
- Verification: `python3 -m pytest tests/test_mock_agents.py tests/test_auto_approve_detector.py tests/test_agent_tui.py tests/test_e2e_auto_approve.py tests/test_browser_layout.py::test_mock_agent_prompt_payload_renders_ask_attention_in_live_browser -q` passed (`250 passed`), and full `python3 tools/check.py` passed (`CHECK PASSED in 49.44s`).

#### Activity detection from live status counters
- Completed and removed `DOIT.activity_elapsed_counter_detection.md`. Visible Claude/Codex status-counter rows now count as live activity when their elapsed/token counters advance, even with arbitrary status words and trailing tip/composer chrome, so pane tabs, YO rings/markers, Tabber, activity APIs, YO!agent status, wait-then-send, and auto-approve refusal all share the same `visible-counter` evidence.
- Verification: focused detector/app tests passed (`44 passed`), full `python3 tools/check.py` passed, and live 8001 verification against Claude `2.1.183 (Claude Code)` on pane `%20` saw advancing rows such as `✽ Hashing… (3s · ↓ 26 tokens)` through `/api/auto-approve` and `agent_screen_state(..., pane_target='%20')` as `working` with `activity_source='visible-counter'`.

#### Claude/Codex text-client slash-command parity
- Completed and removed `DOIT.cli_add.md`. `tools/claude.py` and `tools/codex.py` now derive slash-command names, aliases, help rows, completion, and compatibility notes from the shared `tools/text_client_common.py` registry; Codex gained Claude-style permission aliases, reasoning/thinking controls, `/context`, real `/usage`, and conversation-clearing `/clear`; Claude gained `/reasoning`, and both clients use `/cls` for terminal clearing.
- Verification: `python3 -m pytest tests/test_text_client_common_metadata.py` passed (`13 passed`) and covers registry/export/help parity, compatibility notes, thinking/reasoning aliases, `/clear` vs `/cls`, and Codex `/usage`.

### Tmux, Terminal, YO!share, and Tabber

#### Tmux popover hierarchy labels and shared working-dot pulse
- Completed and removed `DOIT.window_working_circle_pulsate.md` and `DOIT.popover_label_tmux_session_window.md`.
- Claude/Codex working dots now inherit a shared `status-indicator status-indicator--dot status-indicator--working` parent behavior: the green `●` pulses continuously with the existing `command-palette-thinking` animation, idle `○` remains static, and reduced-motion disables the pulse.
- Session popovers now label hierarchy explicitly with localized `popover.tmuxSession` and `popover.tmuxWindow` strings, so headers read `tmux session 8001 · …` and agent rows read `tmux sub-window 0:codex — working …` while preserving the canonical agent-kind label and avoiding duplicate prefixes.
- Verification: `node tests/editor_preview.test.js`, focused browser tests for the rendered working-dot animation and popover title, `python3 tools/static_build.py`, final `python3 tools/check.py`, and 8001 restart/ping all passed.

#### Backend tmux resize and runner cleanup
- Completed the Refactor Audit Backlog rows for backend resize helper, attach command builder, and fire-and-forget tmux calls. `resize_pty_and_signal_process` now owns PTY resize plus live-process `SIGWINCH` for both `ShareTerminalUpstream.update_dimensions` and WebSocket resize; `tmux_attach_command(readonly=...)` already covers readonly/admin attach paths; server list-client, refresh-client, and has-session checks now use the shared `tmux()` runner instead of direct `subprocess.run(tmux_command(...))` calls.
- Reconciled backend-poll default fallbacks through the settings default snapshot: app-side server poll, background file poll, directory poll, Tabber activity refresh, and auto-approve interval methods now read fallback values from `DEFAULT_PERFORMANCE_SETTINGS`, while existing settings migrations and catalog metadata remain the single source for stale saved defaults and UI descriptions.
- Verification: focused backend regression subset passed (`python3 -m pytest tests/test_server_query.py -k 'resize or tmux_attach or routing_ws_readonly or configure_session_tmux_options'`: 9 passed), full backend query tests passed (`python3 -m pytest tests/test_server_query.py`: 68 passed), focused app/settings poll checks passed (`python3 -m pytest tests/test_app.py tests/test_settings.py`: 218 passed, one existing fake Codex app-server thread warning), exact browser-editor rerun passed after one transient full-gate `Script error.`, and final `python3 tools/check.py` passed (`CHECK PASSED in 50.54s`).

#### Tmux sub-window stale-signal bounce and roadmap cleanup
- Fixed the direct tmux-window selection bounce where clicking `1:claude` could briefly repaint `0:codex` from a stale `/api/tmux-signals` readback before settling back to `1:claude`. Explicit window targets now also normalize the cached signal snapshot while the override is active, so the window bar, Tabber active row, active path, and Finder sync source do not contradict the clicked target during stale readbacks.
- Pruned shipped roadmap rows from `docs/TODO.md`: visible YO!agent job list, editor save hygiene, reload-from-disk, editor status counts, Search & Runs full-text search, compact run history, Finder/Differ/Tabber shared-tree row rendering, HTTP share parity, and share replay-health diagnostics; those features are already covered by current code, README/DEVELOPMENT/spec docs, DONE entries, and tests.
- Verification: `node tests/editor_preview.test.js`, `node tests/tabber.test.js`, `python3 tools/check.py`, ignored-queue scan (`rg --files -uu -g 'DOIT*.md'`), TODO link check, `git diff --check`, and 8001 restart/ping (`pid 2556156`, `/api/ping` returned expected unauthenticated `401`).

#### Tmux sub-window button immediate highlight
- Completed and removed `DOIT.window_button_immediate_highlight.md`. Direct tmux sub-window clicks and `Ctrl-b <num>` now set the active window button and `aria-pressed` synchronously before the backend/network round trip, keep that explicit target through interim renders to avoid flicker, and still run the authoritative tmux POST/read-back path afterward. Relative `Ctrl-b n/p` remains read-back-driven and does not guess a local next/previous window; its read-back uses the fast transcript metadata path without auto/activity refresh fan-out.
- Verification: `node tests/editor_preview.test.js` covers synchronous direct-click and numeric-prefix highlighting before unresolved network calls, no local metadata prediction, and relative `n/p` landing on backend `window_active`; `python3 tools/static_build.py` rebuilt the bundle and the final `python3 tools/check.py` passed (`CHECK PASSED in 56.29s`).

#### Tab popover agent working and idle time
- Completed and removed `DOIT.tab_popover_agent_working_time.md`. The tab/session popover now lists every Claude/Codex agent window near the top, sorted with working agents first, showing live status-counter elapsed time for working panes and activity-ledger idle time for idle panes; tabs with no agent windows say so explicitly.
- Verification: backend payload regression passed, Node layout suite passed (`layout suite: 166 passed, 0 failed`), full `python3 tools/check.py` passed (`CHECK PASSED in 51.56s`), dev8001 was restarted with `tools/yolomux-restart-dev1.sh`, auth-gated `/api/ping` returned 401, and a live authenticated browser render against 8001 showed session `8001` with `codex — working 18m 23s` above `claude — idle 3h 00m`.

#### Tmux sub-window switch read-back
- Completed and removed `DOIT.tmux_window_keys_nav_sync.md`. Tmux sub-window switches no longer preview a local relative-index guess: direct buttons still POST `/api/tmux-window`, raw `Ctrl-b n/p/<num>` bytes still pass through unchanged, and both paths wait for forced transcript metadata read-back so the highlighted button comes from backend `window_active`/`window_name`.
- Verification: live `tmux list-windows -t 8001 -F '#{window_index} #{window_name} #{window_active}'` showed three windows, `python3 tools/static_build.py`, `node --test tests/editor_preview.test.js`, `python3 -m pytest tests/test_browser_dockview.py::test_dockview_window_bar_buttons_select_tmux_windows -q`, focused rerun of the one flaky browser diff test passed, and full `python3 tools/check.py` passed (`CHECK PASSED in 47.35s`).

#### Tabber compact home paths
- Completed and removed `DOIT.tabber_home_path_tilde.md`. Tabber row rendering now runs human-visible labels, descriptions, and titles through the shared `compactHomePath()` helper, so repo paths under the configured home display as `~/...` and the bare home displays as `~`, while non-home paths, already-tilde paths, absolute repo metadata, and synthetic `/s_.../w_...` ids are unchanged.
- Verification: `node tests/tabber.test.js` passed (`35 passed`, with existing disabled-fetch fixture warnings only) and full `python3 tools/check.py` passed (`CHECK PASSED in 47.48s`).

### Editor, Finder, Differ, and Files

#### Differ zero-change repo filtering
- Completed and removed `DOIT.differ_zero_change_repos_and_count.md`. Differ repo summaries now count and render only Differ-visible rows (`status != "T"`), so zero-change candidate repos, including sibling worktrees such as `~/yolomux.dev8002`, stay out of the rendered repo group list while transcript-only rows remain available in the raw file rows. The header count and rendered repo list now come from the same filtered set.
- Verification: `python3 -m pytest tests/test_session_files.py -q` passed with the new zero-change sibling/focused-anchor regression, including repeated samples and count/render agreement; after restart, direct backend samples for sessions `1` and `8001` were stable across three reads and did not include `~/yolomux.dev8002`; the final `python3 tools/check.py` passed (`CHECK PASSED in 56.29s`).

#### Editor preview capability cleanup
- Completed and removed `DOIT.preview_drop_when_identical_to_editor.md`. The preview registry now distinguishes renderer identity from preview availability: generic text/code uses `previewable: false`, so `.txt` and code files open editor-only with no Preview/split/popout affordance, while distinct renderers such as markdown, images/media, structured JSON/YAML/TOML/env, and CSV/TSV remain previewable.
- Verification: `python3 tools/static_build.py`, `node --test tests/layout_restore.test.js`, `node --test tests/editor_preview.test.js`, `python3 -m pytest tests/test_browser_editor.py::test_editor_preview_direct_media_formats_use_shared_dispatch -q`, and full `python3 tools/check.py` passed (`CHECK PASSED in 49.98s`).

#### Differ repo-set cutoff stability
- Completed and removed `DOIT.differ_repo_flicker_ai_config.md`. Session-files repo selection now applies a short shared cutoff grace around the lookback boundary, so a secondary repo touched exactly near the 24h cutoff does not appear on one poll and disappear on the next; the fix is generic and does not special-case `ai-config` or add a frontend debounce.
- Verification: current live 8001 no longer reproduced the original `~/ai-config` blink, five authenticated forced `/api/session-files?session=1&hours=24&force=1` samples all returned only `~/yolomux.dev8001`, the boundary regression passed, full `python3 tools/check.py` passed (`CHECK PASSED in 43.15s`), and 8001 restarted from `/home/keivenc/yolomux.dev8001`.

### UI, Theme, Preferences, and Localization

#### i18n untranslated-value lint and Chinese backfill
- Completed and removed `DOIT.i18n_untranslated_values_lint_and_backfill.md`.
- Static builds now report locale values that still equal `en.json`, with allowlists for intentionally identical brand/token strings and baseline regression errors so existing low-coverage locales warn without blocking while new regressions fail.
- `zh-Hans` and `zh-Hant` now have zero non-allowlisted untranslated entries; the cited `pref.uploads.custom_actions.label` and `.help` strings are translated in source and generated catalogs with `{path}`, `{qpath}`, `{paths}`, `{qpaths}`, `{name}`, `{count}`, and `{category}` preserved.
- Documentation now requires new locale keys to be translated rather than English-seeded, and `docs/TODO.md` records the locale-by-locale backfill path for low-coverage catalogs.
- Verification: `python3 -m pytest tests/test_static_build.py -q`, `python3 tools/static_build.py --check`, `python3 tools/check.py`, live 8001-served zh-Hant locale fetch, generated Preferences rendering under zh-Hant, and 8001 restart/ping all passed.

#### CSS theme token cleanup
- Completed the remaining Refactor Audit Backlog row for repeated raw CSS hex colors. Inactive agent/YO markers now use shared text/fill/border tokens, YO!agent inactive backend dots share the same marker fill with their own border token, transcript role accents now use transcript-specific tokens, repeated light muted detail text uses `--text-muted-soft`, and the light submenu background uses its own surface token instead of a raw duplicate.
- Verification: targeted non-token CSS scan for `#f8fafc|#050608|#cfd8e3|#d9e2ee|#60a5fa|#4ade80|#f472b6|#667085` returned no matches, `python3 tools/static_build.py --check`, `node tests/editor_preview.test.js`, `node tests/tabber.test.js`, `git diff --check`, and final `python3 tools/check.py` passed (`CHECK PASSED in 48.52s`).

### Mixed and Cross-Cutting

#### UI tree and CLI follow-ups
- Completed and removed `DOIT.ui_tree_cli_followups.md`. Refresh now resizes visible tmux tabs, Finder/Differ/Tabber timestamp recency styling covers Ago and Date modes, Tabber shell/process rows use neutral process affordances instead of checkbox-looking squares, and Claude/Codex text-client startup flags plus slash-command help are pinned to docs and versioned real-client fixture lists.
- Verification: focused frontend/browser tests passed for refresh, Tabber row shape, recency styling, and process-row affordances; `python3 -m pytest tests/test_text_client_common_metadata.py` passed (`15 passed`) for CLI docs/fixture parity; full `python3 tools/check.py` passed (`CHECK PASSED in 45.44s`).

## 2026-06-19

### YO!agent

#### YO!agent suggestion prompts, waits, and editor image paste
- Completed and removed `DOIT.refix.md`. YO!agent now treats Claude NBSP and Codex ANSI-dim bottom-composer suggestions as idle placeholder UI, detects real typed drafts separately, clears real drafts through the verified target-send path when sending, sends to suggestion-only Claude/Codex panes without clearing prompt text, records transcript/edited-file evidence before visible-pane fallback, always clears pending wait rows on success/partial/no-output timeout, and exposes a Clear control backed by the existing action wait store. Markdown editor image paste now uploads beside the edited Markdown file and inserts relative `.uploads/...` links while preserving terminal paste behavior and preview resolution.
- Verification: raw live Claude/Codex suggestion captures were added as tests; focused YO!agent/upload route tests passed (`38 passed` in `tests/test_app.py`, `3 passed` in `tests/test_server_query.py`), `node tests/layout_async.test.js`, `node tests/editor_preview.test.js`, and final `python3 tools/check.py` passed (`CHECK PASSED in 49.16s`). Live smoke sent marker/date prompts to session `1:0` Claude and `1:1` Codex, both returned marker-bearing YO!agent result messages with `pending_waits: []`; user completed the 8001 manual visual gate after restart on version `0.4.19`.

#### YO!agent central command
- Completed and removed `DOIT.yoagent_central_command.md`. Claude `Try ...` placeholders no longer block sends; explicit target sends default to result capture with opt-outs; server-owned sends revalidate target pane, prompt state, drafts, and submission; pending waits, multiple sends, wait-then-send, handoffs, visible jobs, and work-next ranking all use the shared YO!agent/all-session path. Verification: parser/app/layout/job tests, mock Claude/Codex real-tmux E2E, real `7777` smoke, rebuilt static assets, and final `python3 tools/check.py` passed all 7 lanes (`CHECK PASSED in 44.88s`).

#### YO!agent chat scrollbar ownership
- Completed and removed `DOIT.yoagent_chat_scrollbars.md`. YO!agent thinking/activity now has one normal vertical scroll owner: `.yoagent-chat-history`; the outer YO!agent list keeps vertical overflow hidden, the chat history owns stable vertical scrolling, active-pane hover keeps the rail neutral, and direct scrollbar hover/drag still gets the active thumb. Streaming/busy refreshes respect manual scrollback and keep the composer separated from the history.
- Verification: `tests/test_browser_layout.py::test_yoagent_busy_chat_uses_one_vertical_scroll_owner` builds a busy + streaming YO!agent state at desktop and narrow widths, captures screenshots, asserts only history overflows vertically, and verifies scrollback is not yanked to bottom; `node tests/layout_url.test.js` passed 154/154; final `python3 tools/check.py` passed all 7 lanes (`CHECK PASSED in 42.84s`).

### Agents and Automation

#### Agent prompt ASK? attention
- Completed and removed `DOIT.agent_prompt_attention_ask.md`. Claude/Codex approvals and questions now raise a clearable red `ASK?` cue in session tabs, pane chrome, and global activity; prompt detection is backed by a fixture inventory, positive/negative corpus, capture harness, mock-agent E2E, and real-agent smoke. Verification: detector corpus tests, browser ASK? tests, mock Claude/Codex tmux tests, real Claude/Codex smoke, rebuilt static assets, and final `python3 tools/check.py` passed all 7 lanes (`CHECK PASSED in 44.88s`). The inventory still records bell-only live-signal coverage as an explicit uncaptured gap, not a captured fixture claim.

### Tmux, Terminal, YO!share, and Tabber

#### Tmux sub-window keys, Info Bar rename, and per-session info drawer
- Completed and removed `DOIT.tmux_window_keys_and_infobar.md`. Terminal `Ctrl-b n`/`p`/numeric tmux sub-window switches now mirror YOLOmux active-window/Header/Info Bar state without swallowing input, user-visible pane metadata copy says `Info Bar`, and each Info row has a lazy per-session drawer sourced from the existing YO!info/activity-summary metadata path with full path, git state, PR/CI/Linear metadata, latest summary, and recent events. Verification: backend activity-summary shape tests, frontend drawer/cache/source-shape tests, focused previously failing tests (`3 passed in 1.67s`), rebuilt `static/yolomux.js`/`static/yolomux.css`, and final `python3 tools/check.py` passed all 7 lanes (`CHECK PASSED in 45.42s`).

#### Shared Tabber and YO!info lookback controls
- Completed and removed `DOIT.lookback_slider_tabber_and_info.md`. Tabber and YO!info now share the 30-minute-through-14-day lookback options, backend normalization accepts the full range, YO!info can request all visible tmux sessions with the selected touched-path lookback, and Tabber has its own persisted lookback control that invalidates/reloads touched-path state and sends the selected `hours` through both batch and single-session session-files requests. Verification: backend all-session/lookback pytest coverage, node URL/control/cache-invalidation coverage, rebuilt `static/yolomux.js`, and final `python3 tools/check.py` passed all 7 lanes (`CHECK PASSED in 43.83s`).

#### Tabber session rows styled like pane tabs
- Completed and removed `DOIT.tabber_session_tab_styling.md`. First-level Tabber tmux session rows now render their label/description inside an inner `.tabber-session-tab` using the shared pane-tab tokens for height, width, border, radius, inactive/hover/active colors, font, and focus ring; window/repo/loading/non-tmux rows keep the normal tree-row rendering. The row itself remains the treeitem, disclosure, click target, and ARIA owner, so expand/collapse, selected/current state, indentation, keyboard behavior, and row delegation stay on the shared tree path. Long descriptions truncate inside the tab label while the numeric session name and date/ago column stay visible; current sessions read as active tabs via `.tabber-active-session`/`aria-current` without changing active-window styling.
- Verification: `python3 -m pytest tests/test_browser_layout.py -k 'tabber_session_rows_use_pane_tab_shape_and_keep_columns' -q` passed dark/narrow and light/wide geometry plus screenshot checks; `node --check static/yolomux.js`, a generated-source Tabber guard, `python3 tools/static_build.py --check`, and `python3 tools/check.py` were run. The final gate passed all lanes except `node-layout`, which is blocked by unrelated search-history expectations (`Search & Runs` in the File menu and `__search_history__` in virtual tabs).

### Editor, Finder, Differ, and Files

#### Shared Finder/Differ/Tabber tree controller
- Completed and removed `DOIT.shared_tree_finder_differ_tabber.md`. Finder, Differ, and Tabber now register through one shared tree interaction controller for row discovery, lead/selection state, range/select-all, disclosure/expand-collapse, activation, current-row sync, aria, and scroll reveal. Finder keeps Finder-only commands for Return rename, Cmd-open, Space preview, typeahead, and enclosing-folder navigation while cursor movement and expansion use the shared parent; Tabber has Finder-style keyboard navigation plus active session/window sync; Differ has the same keyboard and mouse selection path while diff-ref inputs keep priority. Source guards now prevent bespoke Tabber row builders and Differ selected/current style forks. Verification: `node tests/layout_url.test.js` passed `162 passed, 0 failed`; final `python3 tools/check.py` passed all 7 lanes (`CHECK PASSED in 45.73s`).

#### Finder Ago recency brightness and pulse
- Completed and removed `DOIT.finder_ago_recency_brightness.md`. Finder rows in Ago mode now color only the date cell by one shared mtime-to-recency helper, use dark/light recency tokens, keep old rows muted, and gently pulse files modified within about a minute for 10 seconds without restarting on same-mtime refreshes. Date/None modes and Differ rows keep their previous styling.
- Verification: node regression `Finder Ago recency brightness and pulse are scoped to relative Finder rows` covers bucket mapping, Date/None clearing, pulse start/expiry/restart, and Differ scoping; full `python3 tools/check.py` passed all 7 lanes (`CHECK PASSED in 40.25s`).

#### Editor save hygiene, reload, and status counts
- Completed and removed `DOIT.editor_polish.md`. The editor now has opt-in save hygiene settings for trailing-whitespace trim and final newline, a reload-from-disk action that preserves the dirty-buffer confirmation path, and a status count segment showing live line/word/character counts alongside cursor/selection status.
- Verification: node tests cover helper/default behavior, save-path behavior, dirty reload cancel/confirm, and live count updates; settings tests cover defaults/sanitization; full `python3 tools/check.py` passed all 7 lanes (`CHECK PASSED in 40.25s`).

### UI, Theme, Preferences, and Localization

#### Search and run history foundation
- Completed and removed `DOIT.search_and_run_history.md`. YOLOmux now has scan-on-query full-text search across captured events and summaries, `/api/search`, compact persisted run history rows with prompt/cwd/agent/timestamps/final state/PR/latest summary, `/api/run-history`, and a Search & Runs UI tab/menu entry that renders search results plus compact run rows without scraping terminal text. Verification: observability pytest covers search/result shape/run rows, node layout tests cover frontend fetch/rendering, static assets are rebuilt, and final `python3 tools/check.py` passed all 7 lanes (`CHECK PASSED in 43.83s`).

### Build, Refactor, Docs, and Tests

#### Refactor split large files and structural guards
- Completed and removed `DOIT.refactor_split_large_files.md`. HTTP routes now use a grouped route registry; YO!agent flow moved into controller/backend/session-summary owners; share/replay/drop, editor/preview/popout, Info/YO!agent/preferences/debug, DOM action, and timing code have separate source owners; filesystem moved into a package; browser tests and JS layout tests were split; repeated CSS colors and z-index values moved to shared tokens. Verification: recursive py-compile guard now includes nested split packages, static assets were rebuilt, and final `python3 tools/check.py` passed all 7 lanes (`CHECK PASSED in 44.88s`).

#### DOIT index cleanup
- Removed `DOIT.00_index.md` after all descriptive DOIT queue files were archived; it was only the stale queue map and had no standalone product change.

#### Archived verified done queues
- Completed and removed `DOIT.done_differ_codex_transcript.md`: multi-Codex missing-transcript data is warning-only when valid repo/file data exists; the original live state no longer reproduces, and the conditional transcript-discovery follow-up is not needed unless a future warning blocks valid data again.
- Completed and removed `DOIT.done_yoagent_no_backend.md`: YO!agent backend diagnostics are precondition-specific and CLI-backed chat already exists; exposing managed SDK transports to non-managed visible targets remains a product decision, not active queue work.
- Completed and removed `DOIT.done_autoapprove_mock_agents.md`: mock Claude/Codex yes/no approval handling is verified through detector and real-tmux mock E2E coverage; the file had no active unchecked boxes, only non-blocking cursor-placement polish noted as `[~]`.
- Verification: the latest `python3 tools/check.py` passed all 7 lanes (`CHECK PASSED in 42.84s`); the archived files' audits name the existing focused tests for warning demotion, backend diagnostics, and mock-agent approval.

#### Self-update restart and browser reload UX
- Completed and removed `DOIT.self_update_restart_and_ux.md`. Self-update now records a restart context for the running checkout, resolves script/module launchers, preserves argv/env needed for the active server, relaunches from `PROJECT_ROOT` with a detached helper that kills only its own PID, and documents the launcher-agnostic contract. The browser side dismisses the update toast immediately, hides the badge, starts self-update-specific ping polling after `restarting: true`, reloads when safe, suppresses the generic reload banner for the owned target, and defers with a clear Software Update toast when dirty editors or active typing would be interrupted.
- Verification: pytest covers relative script, absolute script, module launcher, stripped-env/nohup-style env, current-PID-only kill, helper detachment/stdio/log behavior, and no systemd/pkill; node tests cover immediate toast removal, reload polling, banner suppression, dirty-editor deferral, and active-typing deferral; full `python3 tools/check.py` passed all 7 lanes (`CHECK PASSED in 40.25s`).

## 2026-06-18

### Tmux, Terminal, YO!share, and Tabber

#### DOIT.82 light window buttons, share theme sync, System repaint, and blank terminals
- Completed and removed `DOIT.82.md`. Light-mode active tmux sub-window buttons now keep the shared pressed-fill token even when `data-window-agent` is present; replay-shell YO!share viewers apply live `appearance` and `viewport` frames so host light/dark/system changes flip the mirrored viewer and terminal theme; System/follow-app terminal repaint is covered from both Dark and Light OS-resolved paths; and tmux attaches request immediate plus delayed `refresh-client` passes so a newly opened terminal paints the current PTY screen without manual input.
- Reopened stale prior DONE claims for terminal-theme repaint, share-theme sync, and active tmux sub-window buttons. Live verification after rebuild/restart: active light window button and active pane tab both `rgb(79,158,58)`; real `/share/<id>` viewer flipped light/dark/system with xterm background following `follow-app`; blank terminal crop advanced from one background color to rendered rows without external refresh. Verification: focused pytest/Selenium, `node tests/layout_url.test.js`, and full `python3 tools/check.py` (`CHECK PASSED in 39.64s`).

### Editor, Finder, Differ, and Files

#### DOIT.83 editor toolbar, Tabber recency, and selected-window repo metadata
- Completed and removed `DOIT.83.md`. The editor Info Panel front controls now render as `#`, the wrap icon, then `Differ`; the old right-side wrap icon is gone, and compact editor FROM/TO controls stay hittable without overlapping the right mode controls.
- Tabber now keeps the `<time> ago` recency column visible at narrow widths and lets the tree/path label truncate first. Session-file rows carry `agent_windows` attribution, so Tabber touched repo rows and YO!agent Recent Agents attach paths only to the matching tmux agent window.
- The terminal Info Pane path/repo metadata now follows the selected tmux sub-window. A Codex window can show its transcript-touched repo while a separate bash window in the same tmux session shows its own cwd and does not inherit that repo.
- Verification beyond the standard gate: focused `test_editor_diff_ref_reset_is_visible_and_hittable`, focused session-files/recent-agent pytest checks, `node tests/layout_url.test.js`, and full `python3 tools/check.py` (`CHECK PASSED in 36.75s`).

## 2026-06-17

### Tmux, Terminal, YO!share, and Tabber

#### Real repo inferred from agent-edited dirs in session metadata
- A Claude/Codex launched from `$HOME` (or any other non-repo cwd) no longer shows "no git checkout detected" while it is actually editing files in a real repo. `candidate_session_cwds` now also feeds `session_touched_dirs(info)` — the directories each agent has EDITED files in, from the same transcript edit-tool scan the Modified-files/Tabber panes use (edits, not reads) — so `session_git_inventory` and the YO!info repo list surface the real repo and branch even when the live pane cwd has no git checkout. The touched-dir signal outranks the session-number default; final primary selection across all candidates still happens by git activity in `repo_summary`.
- Added a `(path, mtime, size)` memoization cache to `scan_claude_transcript` (matching the existing codex scanner) since `candidate_session_cwds` now scans transcripts on the hot metadata-refresh path and an unchanged transcript should read from disk once.
- Tests: `test_candidate_session_cwds_surfaces_transcript_touched_repo` (home pane editing a real repo -> repo detected), `test_session_touched_dirs_collects_edited_dirs` (dir dedup, reads excluded), `test_scan_claude_transcript_refreshes_cache_on_change` (no stale cache hit after append). Reproduced live against the real session 8003 transcript (home pane -> `~/yolomux.dev8003` branch surfaced). Commit `a874f13`.

#### Full-speed wheel scroll for alt-screen panes (claude/codex/vim)
- The terminal wheel was unconditionally routed into tmux copy-mode, which scrolls nothing on alt-screen TUIs (they own the mouse and keep no scrollback, `history_size` stays 0), so the app never received the wheel. Gated the copy-mode route on `sessionPaneIsAlternateScreen`; alt-screen panes forward the wheel to the app, driven by the same `terminalWheelSignedLines` count the copy-mode path uses (plus a per-session fractional remainder for touchpads) so alt-screen scroll matches scrollback-pane speed. Layout-suite regression `t@6318` + a `docs/specs/TMUX.md` "wheel routing by screen mode" note. Commit `ddf3bc7`.

#### tmux / share / auto-approve lifecycle hardening
- A batch of verified backend/frontend lifecycle and race fixes: share `viewer_ids` iterated without the lock (now a deep-copy snapshot), PTY resize dropped while a tab is hidden and never re-sent on return, openpty fd / viewer-registration leak when attach setup raises before the try, untimed tmux subprocess calls under the share upstream lock, `reader_loop` master_fd use-after-close race, auto-approve worker leak when the watched session dies outside YOLOmux, codex summary per-chunk UTF-8 split and strict capture-pane decode, SIGKILL with no wait leaving a transient zombie, share send with no socket timeout, write-after-dispose terminal race, uncancelled fit timers on teardown, and a non-ASCII WebSocket key returning 400 instead of 500. Restored the diff-overview no-temporary-rail invariant and rebuilt its browser test on a frozen single-chunk fixture instead of the moving `docs/TODO.md`. Guards: `t@7185` + ~12 python tests (viewer_ids snapshot, hidden-tab pending resize, bounded timeouts, sigkill wait, openpty cleanup, codex UTF-8 across chunks, non-ASCII WS key, ...). Commit `1a2dfe7`.

#### tmux resize authority bound to the active browser
- Fixed the "pane too wide / expands to the widest screen" bug. Two browser surfaces on one session attach as two differently-sized tmux clients, and plain `window-size largest` let a wider background browser stretch the foreground pane. Now every YOLOmux attach starts with tmux `ignore-size`; when a Chrome/Safari page becomes active it scans its visible pane tabs, sends foreground resize claims for each visible xterm, clears `ignore-size` for those clients, and sets `ignore-size` on the other YOLOmux-managed clients for that session; re-entering an already-authoritative browser at the same dims is a tmux/PTY no-op. Guards: `test_configure_session_tmux_options_uses_active_surface_authority`, `test_claim_tmux_resize_authority_ignores_other_yolomux_clients`, plus node source-guards; `docs/specs/TMUX.md` and `GUI.md` document the contract. Commit `6e425c3`.

### Editor, Finder, Differ, and Files

#### Diff/Differ view: wrapped continuation rows of long added/changed lines no longer render blank (DOIT.2)
- In the `@codemirror/merge` diff view with word-wrap on, a long inserted/changed line that soft-wrapped showed only its first visual row — the continuation rows rendered blank (text was in the DOM but painted over). Root cause: the full-bleed green/red change band (`box-shadow: -100vw 0 0 …` + `clip-path: inset(0 -100vw)`) was applied to the INLINE `cm-insertedLine`/`cm-deletedLine` marks, not only the block lines; on a soft-wrapped inline mark that clip buries every continuation row under the parent `.cm-changedLine` block's band. Fix in `static_src/css/yolomux/60_editor_file_panels.css`: keep the band trick on block-level elements only (`.cm-changedLine`, `.cm-deletedChunk`, `.cm-insertedChunk`, `.cm-inlineChangedLine`) and reset `box-shadow: none; clip-path: none` on the inline marks — the block still paints the full-width fill so the band is unchanged and the buried text returns. No JS change. The "Expand / collapse all unchanged lines" control was never floating (`position: static`, zero overlap); it only looked wrong because the rows were blank.
- Regression: Selenium `tests/test_browser_layout.py::test_diff_wrapped_inserted_line_continuation_rows_show_text` asserts each wrapped continuation row has height > 0, non-empty caret text, and the inserted mark as the topmost painted element (word-wrap on + `collapseUnchanged` active) — verified it FAILS on the old CSS and passes on the fix; plus a `tests/layout_url.test.js` CSS-contract guard. Contract documented in `docs/specs/EDITOR-CODEMIRROR.md` and `docs/specs/GUI.md`. DOIT.2 queued and fully drained.

### Build, Refactor, Docs, and Tests

#### Gate speedup: stop double-running the node suite
- The default `tools/check.py` gate ran the node layout suite twice concurrently — once as the always-on `node-layout` lane and again inside the `pytest` lane via `test_node_suite.py` (marker `node_bridge`), which shells out to the identical `node tests/layout_url.test.js`. It was the single slowest pytest item (~32s under load) and the two node processes thrashed the cores the 32 xdist browser workers needed. Excluded it from the default pytest lane with `-m "not node_bridge"` (node coverage stays via the `node-layout` lane; a bare `python3 -m pytest tests` still runs the bridge) and updated the `test_check_runner.py` guard. Full gate wall-clock dropped from ~47s to ~33s, verified green across 8 consecutive runs. Tuning `-n` did not help — the gate is bound by its single longest test. `docs/DEVELOPMENT.md` and `AGENTS.md` updated. Commit `dbfd981`.

#### Portable self-update restart
- Self-update restart no longer depends on `systemd-run`/`systemctl`/`pkill` (non-portable on macOS/non-systemd Linux, and the server failed to come back). It now spawns a detached `nohup bash -lc` helper that does a best-effort `kill`, waits 2s, a best-effort `kill -9`, then relaunches the same Python argv. Guard `test_self_update_restart_uses_portable_nohup_helper` asserts the portable `nohup bash -lc` shape and that `systemd-run`/`systemctl`/`pkill`/`setsid` are absent. Commit `f9e5758`.

## 2026-06-16

### Tmux, Terminal, YO!share, and Tabber

#### DOIT.80 terminal green selection blocks
- Completed and removed `DOIT.80.md`. Terminal visible-selection handling now has one classifier/cleanup path in `static_src/js/yolomux/10_core_utils.js`: it records xterm selection, browser DOM selection inside the terminal container, recent OSC 52 fallback text, and best-effort tmux pane-mode labels, then clears browser/xterm selection after a terminal copy/open action consumes the selected text.
- Explicit `Copy tmux selection` no longer leaves tmux copy-mode rows painted after the clipboard payload is captured. `yolomux_lib/app.py::tmux_copy_selection()` still uses `copy-selection-no-clear` for the existing fresh-buffer signature check, then sends `send-keys -X cancel` after success, no-copy, or save-buffer failure.
- Regression coverage now includes Node/source assertions for context-menu cleanup routing, backend pytest assertions for tmux cancel calls, and a focused Selenium test proving DOM/xterm visible selection clears in the live runtime fixture. README and `docs/specs/GUI.md` document the preserve-until-consumed, then clear-stale-selection rule.
- Verification: `python3 tools/static_build.py`, `node tests/layout_url.test.js` (`147 passed, 0 failed`), `python3 -m pytest tests/test_session_actions.py -q` (`8 passed`), focused Selenium `python3 -m pytest tests/test_browser_layout.py -k 'terminal_visible_selection_cleanup_clears_browser_and_xterm_state' -q` (`1 passed, 181 deselected`), full `python3 tools/check.py` (`CHECK PASSED in 48.15s` after an unrelated README diff Selenium flake passed in isolated rerun), and dev1 restart/smoke on port `8001` (`ping: 401 0.048482s`).

#### DOIT.77 tmux-signal activity awareness
- Completed and removed `DOIT.77.md`. YOLOmux now builds a server-wide tmux signal snapshot, invalidates it from a read-only control-mode watcher/hooks/subscriptions, counts active/attention windows outside the current tab, gates idle pane captures without skipping pending prompts, and discovers server-wide agent panes for YOLO.
- The YO!agent/activity UI now reflects tmux dead/running/silence/bell/presence/zoom/layout/mode/read-only/synchronized signals, prefers live tmux pane path/command for Finder and agent context, sorts/dims recent-agent rows by tmux recency, and uses tmux recency to prioritize activity payload and rolling-summary refreshes.
- `/api/tmux-snapshot` is scrollback-aware through `history_size`/`history_bytes`, caps capture depth, and returns `unchanged` when history has not grown; client sizing data now includes active client details and an authoritative non-control viewer candidate.
- Verification beyond the standard gate: focused tmux/app/server/auto-approve pytest set (`206 passed`), new B5 recency-priority tests, full `python3 tools/check.py` (`CHECK PASSED in 49.86s`), dev1 restart on port `8001`, and `/api/ping` returning the expected unauthenticated `401`.

#### DOIT.0 test isolation: YO!share Selenium no longer touches live tmux/dev state
- Completed and removed `DOIT.0.md`. Browser/live generated-share tests now use an isolated fixture with tmp config/state, ephemeral HTTP server ports, short private `/tmp` tmux/control sockets, and generated `yt-<pid>-<uuid>-N` tmux sessions instead of live `8001`/`7777` ports or real host sessions.
- Added the `YOLOMUX_TMUX_SOCKET` tmux command hook so app/server tmux calls can target the fixture-owned socket, plus a source guard that rejects live YOLOmux port literals and generated-share tests that bypass the isolated runtime helper.
- YO!share DOM replay now filters terminal placeholder serialization by the active share's authorized session scope, so an unauthorized host terminal cannot appear as a healthy placeholder and then fail `/ws/share-view` with `403`.
- Verification beyond the standard gate: focused generated-share Selenium pair (`2 passed in 29.85s`), focused affected pytest sets (`12 passed`, `128 passed`), dev1 restart on port `8001`, and `/api/ping` returning the expected unauthenticated `401`.

### Editor, Finder, Differ, and Files

#### DOIT.78 deterministic image input into terminal-backed agent panes + test-gate integrity
- Completed and removed `DOIT.78.md`. Image paste/drop into a terminal pane now routes through ONE shared image-payload contract (`dataTransferHasImagePayload` + `dataTransferImageFiles` + `dataUrlToImageFile` in `99_terminal_boot.js`): any image-bearing exposure (File item, plain File list, image MIME type, rich `text/html` `<img>`, data: URL) is CLAIMED (`preventDefault`+`stopPropagation`) so the raw image can never leak to the agent as a rich `[Image #N]` attachment, then every image uploads and inserts only its text path reference. Multi-image is deterministic (N images → N refs). The file-drop path uses a new `hasUploadableDrag` (file OR image rich-data); the single-image `pastedImageFile` was removed in favor of the shared parent.
- Fixed a longstanding FALSE-GREEN in `tests/layout_url.test.js`: the trailing async IIFE silently hung on un-flushed batched `/api/fs/batch` listings (the harness stubbed `setTimeout` as a no-op, so the 8 ms fs-batch flush never fired), leaving `.finally` unreached and the process exiting 0 with NO `layout suite:` summary — masking every later test. Fixed structurally (the `setTimeout` shim now fires only the 8 ms fs-batch flush via real `setImmediate`; 8 ms is unique to it) + a real-node watchdog that fails loud (`SUITE DID NOT SETTLE`, exit 1) if the IIFE ever hangs again, and fixed the unmasked cross-realm `deepStrictEqual` defects (missing `canonical()`). Documented the whole failure mode in `AGENTS.md`.
- Tests: rich-data paste claim contract, multi-image paste→two-refs, a payload-matrix contract over the shared detector/extractor, and a source-grep invariant that paste/drop route through the shared parent. `node tests/layout_url.test.js`: `147 passed, 0 failed`.
- Verification: `python3 tools/static_build.py` rebuilt `static/yolomux.js`; full `python3 tools/check.py` was `909 passed, 1 failed` where the single failure is `test_readme_diff_waits_for_payload_before_building_codemirror`, a flaky CodeMirror merge-view Selenium `'Script error.'` UNRELATED to this change (no CodeMirror code touched) that passes on isolated re-run.

### UI, Theme, Preferences, and Localization

#### DOIT.79 link context menu order and duplicate copy actions
- Completed and removed `DOIT.79.md`. Terminal and markdown link context menus now share one URL-menu helper in `static_src/js/yolomux/10_core_utils.js`, so `Open URL in a new tab` is the first row, URL selections that already equal the href no longer show a redundant generic `Copy`, and differing visible text is labeled explicitly as `Copy selected text`. The same localized labels now ship in every checked-in locale catalog.
- Regression coverage in `tests/layout_url.test.js` now inspects the rendered terminal context-menu rows for both cases from the queue: selected text equals href and selected text differs from href. README now documents the new right-click URL behavior under terminal copying.
- Verification: `python3 tools/static_build.py`, `node tests/layout_url.test.js`, full `python3 tools/check.py` (`CHECK PASSED in 47.79s`), and dev1 restart/smoke on port `8001` (`ping: 401 0.045859s`).

### Build, Refactor, Docs, and Tests

#### DOIT.81 refactor audit follow-ups
- Completed and removed `DOIT.81.md`. Terminal copy now has one action contract for menu, shortcut, DOM copy-event, URL, tmux-selection, and OSC52 paths; labels/status strings are localized and selection cleanup consistently clears the xterm/browser/OSC52 visible-selection state after copy consumption.
- Browser timing defaults now read server-provided settings defaults through the existing `clientSettingsDefaults` parent instead of duplicating refresh/timing literals in bootstrap and settings reload code; the Node and Selenium fixtures now seed production-shaped settings defaults.
- Finder/Differ row derived state now has one builder/applier shared by full render and lightweight status refresh. The regression mutates changed-file and indexed-directory payloads, then proves the refreshed row matches a fresh full render for classes, status/title, agent slot, count slot, and display name.
- Light-mode YO!agent bubbles/details/action/code blocks, vanilla editor swatches, and command-palette/shortcuts dialog surfaces now route through existing panel/line/light editor tokens plus new `--lt-code-block-*` neutral code-block tokens instead of per-component raw hex copies.
- Verification: `node tests/layout_url.test.js`, focused Selenium checks for terminal selection cleanup, menu hover defaults, light-mode surfaces, focused reruns of unrelated intermittent full-gate failures, and final `python3 tools/check.py` for each item, with the final gate passing after R4.

## 2026-06-15

### Tmux, Terminal, YO!share, and Tabber

#### Final ignored DOIT queue drain: YO activity, terminal resize authority, and bare directory counts
- Completed and removed the remaining ignored queue files `DOIT.1.md`, `DOIT.75.md`, and `DOIT.76.md`; these are ignored by git, so normal `git status -s` did not show them even while `rg --files -uu -g 'DOIT*'` did.
- YO activity now captures the selected/preferred agent pane in a multi-agent tmux session before falling back to a deterministic agent pane, so the session-level RUN/YO marker follows the visible working Codex/Claude pane instead of the first discovered idle agent.
- Terminal resize authority now rejects hidden/background browser-tab resize frames, sends explicit `foreground: true` from foreground terminal clients, ignores `foreground: false` server-side, and reuses the last known host terminal dimensions on no-explicit-resize attach instead of letting a default small PTY shrink shared tmux sub-windows.
- Finder/Differ directory and repo diff metadata now keeps `+N -N` in the shared diff spans and renders the changed-file count as a bare number with no `file changed` / `files changed` label.
- Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, focused Selenium replay/share/file-tree set (`4 passed in 34.83s`), focused auto-approve detector/app/server tests (`52 passed`, `5 passed`, `6 passed`), full `python3 tools/check.py` outside the sandbox (`CHECK PASSED in 52.84s`), dev1 restart on port `8001`, and `/api/ping` returning the expected unauthenticated `401`.

#### Ignored DOIT queue cleanup: theme switching and YO!share pointer stalls
- Completed and removed ignored queue files `DOIT.theme-switch-gaps.md` and `DOIT.share-pointer-stall.md` (these were hidden from normal `git status -s` / ignore-respecting scans by `DOIT*.md` ignore rules; use `rg --files -uu -g 'DOIT*'` for queue discovery).
- Theme switching now publishes YO!share appearance frames through the shared `applyGlobalThemeMode` parent, system theme changes carry the shared publish reason, and terminal theme updates force an xterm repaint with texture-atlas clearing so idle terminals visually switch back to the resolved theme.
- REOPENED 2026-06-18 via `DOIT.82.md`: the prior terminal-theme and share-theme claims were not sufficient; System repaint and live share replay-shell appearance handling needed renewed live verification and regression coverage.
- YO!share pointer stalls now avoid redundant full-DOM replay work while the cursor is active: topology replay keyframes wait for a pointer-quiet window, respect the host keyframe floor, and non-topology keyframes cancel stale pending topology keyframes after they capture the same DOM. Server viewer connections keep latest pointer moves in a side slot, preserve click-frame order, send pointer moves before queued DOM keyframes, and keep pointer moves out of the 256 KB high-water queue budget.
- Verification: `python3 tools/static_build.py`, `node tests/layout_url.test.js`, `python3 -m pytest tests/test_server_query.py -q` (`51 passed`), focused generated-share Selenium regression (`1 passed in 40.81s`), focused rerun of two xdist-only browser failures (`2 passed in 40.74s`), and full `python3 tools/check.py` outside the sandbox (`CHECK PASSED in 58.86s`).

#### DOIT.74 terminal fit oscillation
- Completed and removed `DOIT.74.md`. Terminal fitting now uses one shared measurement contract: the pre-render fallback cell probe reads the bundled mono font token instead of a hardcoded fallback stack, local `fitTerminal` calls record a signature over content size, cell metrics, rows/cols, and terminal font settings, duplicate observer echoes skip before `term.resize()`, and Dockview host resize observer work is coalesced to one layout per animation frame.
- The existing bundled-font-ready refit remains the only post-font refit; no second font-ready path was added. The fit signature includes terminal font settings so real font/metric changes still refit, while identical geometry does not churn the xterm grid or remote tmux size.
- Specs and user docs now state that terminal panes fit against the full pane content box and must not self-trigger resize loops. Regression coverage lives in both `tests/layout_url.test.js` and `tests/test_browser_layout.py::test_terminal_fit_ignores_duplicate_resize_observer_echoes`, proving full-width cols, no duplicate resize on unchanged geometry, and resize on real width changes.
- Verification: `python3 tools/static_build.py`, `node tests/layout_url.test.js`, focused Selenium `python3 -m pytest tests/test_browser_layout.py -k 'terminal_fit_ignores_duplicate_resize_observer_echoes' -q`, full `python3 tools/check.py` outside the sandbox (`CHECK PASSED in 49.63s`), dev3 restart on port 8003 with served bundle containing `terminalFitSignature`, `terminalProbeFontFamily`, and `dockviewScheduleLayoutToHost`, and throwaway real-xterm runtime capture on port 8013 with six stable samples at `cols=125 rows=62` and matching pane/container/xterm rects `1131x1258`; `/tmp/yolomux-fit-8013.png` visually confirmed the terminal filled the pane.

#### YO!share terminal buffer reset race
- Completed and removed `DOIT.share-terminal-buffer-reset.md`. Share-view terminals now track whether `/ws/share-view` has delivered bytes, skip `term.reset()` on later host-size fits after bytes have painted, and still resize/refresh to the host rows/cols. This fixes the case where a right-top pane could be DOM `mirrored` but visually blank because a delayed fit or `terminal-host-resize` cleared the xterm buffer after the tmux repaint.
- Replay health/debug now reports terminal stream state separately from DOM mirror state, including stream status, socket ready state, received-byte flag/count, last byte age, last reset age, and skipped reset count per terminal placeholder. The spec and test inventory now state that terminal content remains outside DOM replay and host resize must not clear already-painted viewer bytes.
- Verification: `python3 tools/static_build.py`, `node --check static/yolomux.js`, `node tests/layout_url.test.js` (`24.41s`, passed with expected CodeMirror fallback warnings), focused share terminal/debug Selenium set (`5 passed, 173 deselected in 1.53s`), the single new regression (`1 passed in 2.19s`), full `python3 tools/check.py` (`CHECK PASSED in 52.27s`), and dev1 restart/smoke on port `8001` (`ping: 401 0.046790s`).

### Editor, Finder, Differ, and Files

#### DOIT queue drain: YO!share, Finder, refactor audit, changed-file rows, and Meta+Arrow tab traversal
- Completed and removed `DOIT.1.md`, `DOIT.cmd-arrow-tmux-nav.md`, `DOIT.finder-disappearing.md`, `DOIT.gui-specs-row-date.md`, `DOIT.refactor-audit.md`, `DOIT.share-pointer-sluggish.md`, and `DOIT.share-theme-sync.md`. Finder-width stale `row@22(...)` layouts now reset non-Finder splits to 50%, while share bootstrap/layout paths preserve a host-minimized Finder.
- YO!share fixes: host OS theme flips now publish appearance frames, viewers honor the host-resolved `system` theme, pointer frames are single unsequenced latest-wins frames at a 50 ms cadence, server viewer queues coalesce stale pointer frames, pointer/ripple replay ignore selectors match real classes, and DOM delta byte checks use the single serialization result from `sharePublish`.
- REOPENED 2026-06-18 via `DOIT.82.md`: replay-shell share viewers were swallowing live `appearance` frames, so host light/dark/system changes did not actually flip the mirrored viewer until fixed again.
- Finder and changed-file fixes: `Cmd/Ctrl+B` no longer writes the sticky closed Finder latch, restore works even if the in-memory restore snapshot is gone, the shortcut is suppressed in editors/terminals/inputs, stat-less changed-file rows keep the date column visible with `—`, and missing touched paths render as missing/deleted instead of ordinary modified.
- Refactor audit fixes: min split-pane sizes route through shared helpers, semantic raw color literals now reuse CSS tokens with a build lint to prevent drift, share scroll state uses one target map, repeated session HTTP guards use `@requires_known_session`, and topbar z-index comments reference `--z-topbar`.
- Meta+Arrow tab traversal now walks every visible tab in pane visual order, including Finder, Preferences, file editors, and tmux tabs, with wrap-around and CodeMirror exclusion. Added VM regressions for the mixed traversal order and a live Selenium regression (`test_meta_arrow_walks_visible_pane_tabs_in_live_runtime`) that boots generated JS and dispatches the real browser keydown path.
- Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node --check static/yolomux.js`, `node tests/layout_url.test.js`, focused Selenium `python3 -m pytest tests/test_browser_layout.py::test_meta_arrow_walks_visible_pane_tabs_in_live_runtime -q` (`1 passed in 1.50s`), and full `python3 tools/check.py` outside the sandbox (`CHECK PASSED in 45.74s`, real 45.78s).

#### YO!share Finder toggle replay drift
- Completed and removed `DOIT.share-finder-toggle-replay.md`. DOM replay keyframes/deltas now own a replay-only epoch/sequence, so interleaved semantic frames such as `ui-state`, `layout`, and Safari-cadence `geometry-digest` no longer make the next DOM delta look non-contiguous. Topology/keyframe publication drains pending mutation and terminal-placeholder batches so stale RAF-delayed deltas cannot cross a keyframe boundary.
- DOM delta payloads now carry terminal placeholder metadata and the viewer applies it after child-list mutations, so moved/recreated terminal panes rebind the same xterm/socket instead of going blank or opening duplicate sockets. Replay health/debug copy now includes sanitized `lastReplayError` details with expected and actual epoch/sequence/base fields.
- Strengthened the real generated-link Selenium matrix: it waits for the host terminal before creating the share, opens the real `/share/<id>` debug URL, exercises rapid Finder toggles, paused Finder toggles, and forced 2000 ms geometry-digest cadence, and asserts the viewer stays `mirrored`, dropped/keyframe-request counters do not increase during the loops, terminal placeholders stay connected, and no shared terminal session has duplicate live sockets.
- Verification: `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, focused share regression set (`7 passed in 41.46s`), the strengthened generated-link matrix alone (`1 passed in 30.33s`), and full `python3 tools/check.py` (`CHECK PASSED in 53.94s`).

## 2026-06-14

### YO!agent

#### DOIT.72 YO!agent Preferences and product-state operator
- Completed and removed `DOIT.72.md`. YO!agent can now answer product-aware questions about Preferences, settings values/defaults/choices/limits/config location, current product state, recent work, changed files, PR metadata, session state, YO!skills/context paths, and broad `what can YOLOmux/YO!agent do?` capability questions without forcing users through the Preferences pane.
- Added a backend settings catalog and deterministic YO!agent operator that handles read-only explanations before slow model fallback, parses natural-language setting changes into validated patches, routes writes through the existing settings save path, reports compact before/after tables, carries timing metadata, preserves concurrent settings, and keeps risky or ambiguous requests in clarification mode.
- Enforced role and safety boundaries: readonly users can ask explanatory questions but cannot write; admin writes reject credential-like paths, unsafe config paths, invalid PR refs, duplicate/no-op list additions, hidden/internal choices, broad resets, and higher-risk YO!agent/YOLO config changes without confirmation.
- Added a product capability registry plus built-in `settings-operator` YO!skill guidance, and updated `README.md`, `docs/YOAGENT_SKILLS.md`, and `docs/GUI_SPECS.md` to document Preference parity, product-state answers, settings safety, and user-local skill/context behavior.
- Verification: focused settings/operator suites passed (`python3 -m pytest tests/test_yoagent_preferences.py tests/test_settings.py -q`, 48 tests; then `python3 -m pytest tests/test_yoagent_preferences.py tests/test_settings.py tests/test_yoagent_actions.py tests/test_app.py -q`, 169 tests), the live-GUI Selenium regression passed (`python3 -m pytest tests/test_browser_layout.py::test_yoagent_settings_operator_updates_live_gui_and_denies_readonly -q`), `node tests/layout_url.test.js` passed, `python3 tools/static_build.py` regenerated the bundle, and the full canonical gate `python3 tools/check.py` passed (`CHECK PASSED in 31.14s`).

#### DOIT.67 YO!agent structured transports and central orchestration
- Completed and removed `DOIT.67.md`. YO!agent now has a transport-aware backend model for managed Claude/Codex work instead of treating tmux paste as the only send path: `claude-sdk`, `claude-channels` diagnostics, `claude-stream-json`, `codex-sdk`, `codex-app-server`, `codex-mcp-server`, `codex-exec`, and labeled `tmux-legacy` are represented in the transport registry with explicit capabilities and result-source metadata.
- Codified the durable research conclusion: arbitrary already-open Claude/Codex TUI panes do not expose a supported structured prompt-injection/result API, so robust orchestration must own the session lifecycle, start an opt-in control channel, or use provider SDK/app-server/MCP/exec protocols; visible panes without that protocol remain a terminal-automation fallback and must be labeled as such.
- Added persisted YO!agent job/result metadata for transport debugging, immediate managed-transport result recording, managed-target routing through the registry, structured Codex SDK/app-server/MCP/exec prototypes, a working Claude stream-json fallback for local managed Claude jobs, and lifecycle-based final-result watcher coverage so first assistant text before tool use is not treated as final.
- Documented the UX contract in `README.md`, `docs/DEVELOPMENT.md`, `docs/GUI_SPECS.md`, and `docs/YOAGENT_SKILLS.md`: SDK/app-server/MCP/exec targets are background YO!agent-managed sessions unless a real TUI/PTY exists, visible tmux panes use `tmux-legacy`, and YO!agent remains the central orchestrator for multi-session handoffs unless the user explicitly asks for relay/chaining.
- Verification: focused transport/action/app checks passed (`python3 -m pytest tests/test_yoagent_transports.py tests/test_yoagent_actions.py tests/test_app.py -q`, 134 tests), `node tests/layout_url.test.js` passed, live Codex SDK smoke returned `YOLOMUX_SDK_SMOKE_OK` in a temporary venv, live Claude stream-json smoke returned `YOLOMUX_CLAUDE_STREAM_OK`, the full `python3 tools/check.py` gate passed, and dev2 on port `8002` returned the expected authenticated `401` ping response.

### Tmux, Terminal, YO!share, and Tabber

#### YO!share tab-popover geometry replay
- Fixed a real replay geometry gap in tab hover popovers. The previous test only proved the popup appeared, and the first strengthened version compared the full rect and failed: host and viewer had the same `left/top`, but the viewer recomputed `width` from `36vw` against its own viewport, so the popup wrapped wider/taller. `positionPaneTabPopover` now serializes host-computed inline `left`, `top`, and `width`, and read-only replay keeps viewer-local hover popovers disabled.
- Updated the test inventory/specs and AGENTS.md to state the rule: host-owned popover replay must compare `left/top/width/height`, not just visible text or position. Verification: `python3 tools/static_build.py`, `python3 -m pytest tests/test_browser_layout.py::test_generated_share_link_mirrors_interactive_ui_surface_matrix -q`, `python3 -m pytest tests/test_browser_layout.py::test_share_replay_popups_menus_and_modals_use_normal_dom_replay -q`, `python3 -m pytest tests/test_browser_layout.py::test_dockview_tab_hover_shows_session_detail_popover -q`, `node tests/layout_url.test.js`, `python3 tools/static_build.py --check`, and `git diff --check`.

#### YO!share replay stale-delta and popover regression follow-up
- Diagnosed the cursor jitter and occasional blank viewer as a replay repair loop: stale DOM deltas arriving after a newer keyframe were being classified as gaps, causing repeated `viewer behind` status and keyframe rebuilds. Fixed replay sequence handling so old/already-applied deltas increment `staleFrames` and are ignored, while true non-contiguous future deltas still request repair.
- Fixed the tab-hover info regression in replay clients. The corrected contract is host-owned popup mirroring: tab popover positions are serialized onto the replayed node, and read-only replay clients do not bind local hover controllers that could create client-only popup geometry.
- Added debug detail so future keyframe requests include the actual frame numbers (`epoch`, `sequence`, `baseSequence`, `currentEpoch`, `lastSequence`) instead of only `reason: gap`.
- Reflected the missed coverage in `AGENTS.md`, `docs/DEVELOPMENT.md`, and `docs/specs/SHARE_MIRRORING.md`: tests must cover stale-after-keyframe, true gap, replay listener rebinding, and debug/profiling fields. The old Selenium test missed the bug because it asserted the previous wrong behavior, that a stale epoch should request a keyframe.
- Verification: `python3 tools/static_build.py`, `node tests/layout_url.test.js`, focused Selenium `python3 -m pytest tests/test_browser_layout.py::test_share_replay_shell_applies_only_contiguous_deltas -q`, full `python3 tools/check.py` (`CHECK PASSED in 37.34s`), dev1 restart on port 8001, and HTTPS `/api/ping` returning unauthenticated `401`.

#### DOIT.72 YO!share DOM replay rewrite
- Completed and removed `DOIT.72.md`. YO!share read-only and write viewers now use the replay shell by default: the host serializes sanitized `#appRoot` keyframes and coalesced DOM/scroll/pointer deltas, viewers apply only contiguous epoch/sequence frames into inert DOM, and repair requests a fresh keyframe on join, gap, digest mismatch, unsafe mutation, slow-viewer backpressure, or manual debug. The temporary semantic escape hatch remains for diagnostics through `shareReplay=0`, `shareSemantic=1`, bootstrap `shareReplay: false`, or `localStorage.yolomux.shareReplaySemantic=1`.
- Terminal panes stay on the host-sized tmux byte stream. DOM replay carries terminal placeholders, viewer xterms bind/rebind to those placeholders without recreating the stream, `terminal-host-resize` resets rows/cols before the server requests a bounded `tmux refresh-client`, and terminal bytes keep flowing when replay falls behind.
- Write-mode shares now forward validated input intents instead of mutating local mirrored UI. HTTPS write viewers send terminal input/paste/scroll and allowed UI command intents over `/ws/share-ui`; the server rejects viewer-authored semantic `layout`, `ui-state`, Finder/editor, and `popup-layer` frames, so the host remains the only UI mutator and visual feedback returns through replay.
- Replay security and observability are covered by sanitizer/redaction helpers, keyframe/delta frame allowlists, a bounded delta ring, replay health diagnostics (`domDigest`, epoch/sequence, dropped frames, redaction policy, terminal placeholder health), and a build-time source guard that fails when private surfaces, token-bearing fields, terminal containers, or app-visible popup hosts bypass replay/redaction registration.
- Specs were moved under `docs/specs/`: `GUI.md`, `EDITOR-CODEMIRROR.md`, and `SHARE_MIRRORING.md`. README and `docs/DEVELOPMENT.md` now describe the replay default, terminal placeholders, write input-intent model, share sockets (`/ws/share-host`, `/ws/share-ui`, `/ws/share-view`), replay health, and the semantic escape hatch. During final validation, a banner regression was fixed so normal viewer-banner refreshes no longer overwrite replay `mirrored` status with the legacy `mirror ...` geometry placeholder.
- Verification: focused replay-health browser regression (`test_share_replay_debug_copy_exports_sanitized_health`), replay/source guards in `node tests/layout_url.test.js`, server protocol tests, the full `python3 tools/check.py` gate (`CHECK PASSED in 29.75s`), and dev1 restart/smoke on port 8001 with `/login` returning `200 0.044692`.

### Editor, Finder, Differ, and Files

#### DOIT.72/73 file-tree diff stats and shared watch-root index
- Completed and removed `DOIT.72.md`. Finder/Differ repo-root and changed-directory line counts now use the same right-aligned `.file-tree-diff` slot as files, with separate `changes-diff-add` and `changes-diff-remove` spans; the old inline combined `+N/-N` repo/directory formatter is gone, while branch/sync metadata and gray directory file counts stay separate.
- Completed and removed `DOIT.73.md`. Directory watch roots now persist to the shared `STATE_DIR/watch-index.json` store with read-modify-write `file_lock` + `atomic_write_text` writes, per-server/source/session ownership, TTL expiry, lock-free tolerant reads, fair capped snapshots, and automatic selected-pane/git-root indexing without waiting for `/api/watch/roots`.
- Verification: `python3 tools/static_build.py`, `node --check static/yolomux.js`, `node tests/layout_url.test.js`, `python3 tools/static_build.py --check`, `python3 -m pytest tests/test_browser_layout.py -k 'platform_controls_use_pc_glyphs' -q`, `python3 -m py_compile yolomux_lib/app.py yolomux_lib/common.py`, focused watch-root pytest (`6 passed`), `python3 -m pytest tests/test_app.py -q` (`108 passed`), `python3 -m pytest tests/test_server_query.py -q` (`34 passed`), and dev3 restart/smoke on port `8003` (`ping: 401`). `python3 tools/check.py` was not green only because of the unrelated pre-existing `test_diff_overview_matches_actual_todo_codemirror_rows` offset pin (`485 != 744`); all other lanes passed and full pytest otherwise reported `782 passed`. Prod was not restarted because this run was explicitly scoped to dev3 only.

## 2026-06-13

### YO!agent

#### DOIT.66/67 YO!agent skills, watches, controlled sends, and coordination audit
- Completed and removed `DOIT.66.md` and `DOIT.67.md`. YO!agent now has built-in YOLOmux skills/context plus user-local overlays in `~/.config/yolomux/skills.d/` and `~/.config/yolomux/context.d/`, with admin-only APIs and chat intents to list, read, write, update, disable, and delete user-local skill/context files. `docs/YOAGENT_SKILLS.md` documents the schema, locations, examples, and management commands.
- Added the first persisted YO!agent job layer. Jobs live in YOLOmux state under `yoagent_jobs`, survive server restart, dedupe by hashed idempotency key, support one-shot notify-session-idle, notify-all-idle, and wait-then-send workflows, expose list/create/confirm/cancel APIs, poll from the existing client-event loop, debounce ready states, timeout once, fire toasts/browser notifications, and publish `yoagent_jobs_changed` events.
- Tightened controlled-send safety. Target prompts preserve perspectives (`ask agent 1 to <do ...>` sends only `<do ...>`), multi-session handoffs stay centrally orchestrated by YO!agent, direct relay/chaining requires explicit user wording, sends use the resolved live tmux pane paste-plus-Return path with Claude/Codex prompt acceptance checks, and jobs re-preview/revalidate before sending. Approval prompts, busy agents, non-agent panes, stale target metadata, missing sessions, and high-risk prompt text do not silently receive bytes.
- High-risk text now forces confirmation even when the user did not ask for preview. Risk labels cover secret-like assignments, credential paths, recursive delete, hard reset, broad `pkill`, recursive permission changes, and SSH commands. Public job/action payloads, persisted YO!agent messages, and event previews redact known `token=`, `secret=`, `password=`, and `api_key=` style values; job idempotency keys are hashes instead of raw JSON signatures.
- Archived the DOIT.67 audit into durable docs: tmux capture remains acceptable for observation, but structured Claude hooks, Codex app-server, ACP/MCP-style APIs, and transcript/event feeds are the durable path for approvals, state, and non-visible agent coordination. Follow-ups for a visible job list, richer recommendation ranking, more watch predicates, structured channels, golden-frame fixtures, and YOLOmux-as-control-plane live in `docs/TODO.md`.
- Verification: focused YO!agent checks (`python3 -m pytest tests/test_yoagent_actions.py tests/test_yoagent_skills.py` plus the new action/job tests) passed; after restoring the bundled font assets and updating the TODO diff fixture pins, the full standard gate `python3 tools/check.py` passed (`CHECK PASSED in 29.88s`).

### Tmux, Terminal, YO!share, and Tabber

#### YO!share wrapped text/layout parity
- Fixed the recurring share-view indentation/wrap drift class by making the bundled UI/mono fonts block fallback-font layout, waiting for YOLOmux font metrics before the first app render, and remeasuring layout-sensitive surfaces after fonts settle. Preferences autosized textareas, tab strips, session buttons, terminals, and CodeMirror panels now get a shared post-font refresh instead of keeping stale fallback-font geometry.
- Extended the share geometry digest with a generic `textWraps` bucket for native controls and wrapped text surfaces such as Preferences textareas/help text, menu labels, tabs, Finder/Differ rows, YO!info rows, and Tabber labels. Wrapped text/control drift now reports as `textWraps`, so screenshot mismatches caused by local browser wrapping no longer hide behind `mirror ✓`.
- Follow-up fix: first render now waits for the actual font-ready promise instead of a 2.5s timeout race, so the host should not repaint/re-autosize a few seconds after load. Full UI-state snapshots also carry host wrapped-control metrics, and viewers apply host app-space width/height to native settings textareas before scroll replay; this keeps Preferences controls clipped/expanded exactly like the host instead of letting the viewer choose a different native textarea height.
- `docs/specs/GUI.md` now defines typography/wrapped-control parity as a YO!share contract for future widgets.

#### YO!share Preferences scroll, YO badge, and viewer status cadence
- Fixed the remaining Preferences/share mismatch by adding current host scroll targets to full YO!share UI snapshots and replaying them after semantic pane state renders. Scroll frames also merge into the server share record, so late-joining viewers and geometry resync no longer fall back to a stale/top Preferences position.
- Fixed the topbar `Tabs` badge and tmux menu YO state on share viewers by carrying compact host auto-approval state in the share UI snapshot and publishing a UI-state frame when YO is toggled or refreshed. Read-only viewers still cannot toggle YO, but their chrome now computes from the host state.
- Confirmed editor refresh for share viewers is push-driven: host saves publish `file-version` frames over the share UI WebSocket, not a high-frequency viewer poll. The read-only viewer `/api/share` status fetch is now a 30s backup for banner metadata while the local countdown still updates every second.

#### YO!share modal border polish
- The YO!share modal boundary now uses a normal one-pixel mixed border without the extra green left stripe, and the active-share `Users` list is an inline subsection with row separators instead of a nested framed table. `docs/specs/GUI.md` records that Users must not draw a second card border inside the active share row.
- Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `python3 -m pytest tests/test_browser_layout.py::test_share_modal_users_section_is_inline_not_nested_card -q`, full `python3 tools/check.py` (`CHECK PASSED in 14.74s`), and dev1 restart/smoke on port 8001 (`ping: 401 0.122186s`).

#### YO!share users list and YO!info mirror follow-up
- YO!share active-share rows now show a `Users` section with one row per distinct connected browser: connected duration, IP address, and summarized browser type. Viewer accounting is registered on the share UI socket and remains refcount-compatible with terminal websockets, so one browser tab is still one viewer even when the layout opens multiple mirrored panes.
- YO!info is now part of the host-owned share snapshot and scroll bus. Branch-table sort column/direction publishes with the UI state, `#info-content` horizontal and vertical scroll frames apply on clients, and read-only local scroll restores to the last host-authored position instead of snapping the client to its own top/left.
- `docs/specs/GUI.md` and `README.md` record the new user-list and YO!info mirror contracts. Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, focused backend/share browser tests including `tests/test_browser_layout.py::test_share_readonly_info_sort_and_horizontal_scroll_are_host_owned`, full `python3 tools/check.py` (`CHECK PASSED in 14.54s`), and dev1 restart/smoke on port 8001 (`ping: 401 0.048765s`).

#### YO!share resize repaint and mirrored chrome follow-up
- Fixed the post-layout terminal blanking race from the 2026-06-13 screenshots. When a share viewer reattaches to an existing shared terminal upstream, the server now requests a bounded `tmux refresh-client`; when the host terminal size changes, the server broadcasts `host-resize` before requesting the repaint so clients resize/reset before the fresh terminal bytes arrive instead of clearing the just-repainted buffer.
- Host layout commits now schedule a full UI-state snapshot after the layout frame, and the mirrored state now includes chrome that was previously browser-local: tab metadata visibility and the active YO!info/YO!agent sub-tab. Per-editor diff expand/collapse overrides now publish immediately, so expanding unchanged regions on the host updates read-only share clients without waiting for an unrelated snapshot.
- The no-regression plan is visual, not only source-grep: `tests/test_browser_layout.py::test_share_readonly_diff_scroll_and_popup_mirror_are_host_owned` renders a real CodeMirror diff in share view and asserts host-owned diff expansion, YO!agent sub-tab, tab metadata visibility, popup mirrors, and read-only scroll restoration. `tests/layout_url.test.js` covers the share snapshot/apply contract, and `tests/test_server_query.py` covers viewer reattach refresh plus resize/repaint ordering.

### Editor, Finder, Differ, and Files

#### YO!share topology reset audit and Finder-minimize terminal hardening
- Audited the recurring YO!share failure classes and documented the durable architecture direction in `docs/TODO.md` and `docs/specs/GUI.md`: the current semantic mirror should use complete epoch-keyed host snapshots as the short-term repair path, but the final read-only architecture should be a host-rendered DOM replay stream with terminal panes kept on the host-sized tmux byte stream.
- Fixed the concrete Finder minimize regression in the semantic bridge. Share bootstrap/layout frames now preserve host-missing Finder instead of letting the viewer's local Finder default re-dock it, share Finder state no longer mounts the Finder when the host layout omits it, and Dockview panel show/hide revalidates that xterm is bound to the current connected terminal container before treating it as live.
- Verification: `python3 -m pytest tests/test_browser_layout.py::test_share_viewer_finder_minimize_keeps_dockview_terminal_tabs_live tests/test_browser_layout.py::test_share_geometry_digest_resyncs_slot_drift_in_browser tests/test_browser_layout.py::test_share_viewer_expiry_redirects_to_login_without_fragment_token tests/test_browser_layout.py::test_dockview_hidden_inner_header_keeps_terminal_content_full_height -q` (4 passed), `node tests/layout_url.test.js`, full `python3 tools/check.py`, dev1 restart on 8001, and `/login` returning 200 after restart.

#### DOIT.65 Finder/Differ self-heal and reconnect resync
- Completed and removed the new `DOIT.65.md`. Finder/Differ/Tabber now self-heals if URL parsing, Dockview adoption, reconnect, wake, hidden-tab measurement, or resize work produces a layout that lost the reserved pane without an explicit user close. Intentional close is stored per browser tab, so `Mod+B`, File -> Finder, and the close button keep the pane hidden until the user restores it.
- Dockview hardening now skips adoption while the host has zero measurable area, gives Finder/Differ groups a Dockview min-width floor, re-queues recovery if Dockview removes the Finder group, and clamps serialized snapshot dimensions to functional minimums without forcing visible small hosts to overflow.
- Client-events reconnect now backfills auto-approve state so stale YO markers resync immediately on `ready`. Page wake and browser `online` schedule the shared refresh path, and a narrow auto-status fallback poll runs only while the client-events stream is disconnected.
- Preserved the larger edge-pinned-tab architecture proposal in `docs/TODO.md` as future work instead of keeping an active DOIT file open for a multi-day refactor.
- Verification: `node tests/layout_url.test.js`, `python3 -m pytest tests/test_browser_layout.py::test_client_events_ready_refetches_yolo_marker_after_reconnect tests/test_browser_layout.py::test_dockview_finder_survives_hidden_host_adoption_and_reshow -q` (2 passed), and the final full standard gate `python3 tools/check.py` (`CHECK PASSED in 15.11s`).

#### Mermaid Preview: split-view chopping, dark-on-dark labels, post-open jump, and bright-mode lines fixed
- Four live-reproduced Mermaid preview bugs (each verified against the running `:8003` server with real Dockview in authenticated headless Chrome, not synthetic fixtures, and each pinned by a regression that fails pre-fix):
- CHOPPING: in split view the diagram was chopped on the right at every zoom. Root cause (from dumping live geometry): the preview pane is `position:absolute; inset:0 0 0 50%` (right half), but `installPreviewZoomSurface` adds `.file-editor-preview-zoom-full { width:100%; height:100% }`, and an explicit `width:100%` overrides `right:0`, so the pane rendered full-width offset to `left:50%` and its right half ran off-screen. Fix: `.file-editor-content .file-editor-preview-pane-panel.file-editor-preview-zoom-full { width:auto; height:auto }` so the inset defines the box. Measured pane 930px -> 465px (the right half); diagram fully inside the visible half at Fit/1:1.
- DARK-ON-DARK LABELS: default (dark-fill) Mermaid nodes rendered dark text -> invisible (the "Open file" node and every node in classDef-less diagrams). Root cause: `svgReadableLabelColor` used the nearest ancestor fill (the label's own light fill) instead of the node's background shape, so it chose dark text for a dark node; and `svgStyleValue` failed to parse `fill:#fef3c7 !important` (Mermaid stamps `!important` on classDef fills), so light nodes read as fill-less. Fix: read the node's own background SHAPE fill (`svgNodeBackgroundFill`, skipping label-internal shapes) and strip a trailing `!important` in `svgStyleValue`. Measured: 0 unreadable labels, every node label now >=14:1 contrast vs its actual background.
- POST-OPEN JUMP: the diagram visibly shrank (~417->353px) ~150ms after appearing. Root cause: the editor pane opens at a transient height and Dockview re-lays-it-out (content 644->489px), so the fit-scaled diagram re-fit. Fix: a reveal gate keeps the diagram hidden (stage `visibility:hidden`, viewport still measurable) until its viewport size settles (debounced after the last change), then reveals once at the settled fit. Also coalesced the zoom resize observer to one apply per frame (the corrected half-width pane had surfaced a `ResizeObserver loop ... undelivered notifications` warning).
- BRIGHT-MODE LINES: in Bright/Vanilla preview (white surface) the diagram lines/labels were light-gray and illegible. Root cause: `mermaidPreviewConfig` and the edge/label colors read document `--text`, which follows the app theme (light when the app is dark) rather than the preview display mode. Fix: a `mermaidSurfacePalette()` keyed off `editorPreviewThemeState()` drives the config + edge + label colors, so Bright/Vanilla render dark lines/text on white and Dark renders light on dark; the diagram re-renders when the preview mode changes. Measured edge contrast 14.3:1 (dark) and 16.4:1 (Bright), up from ~1.1:1 illegible.
- Regression (`test_direct_mermaid_sample_real_bundle_keeps_svg_text_labels`, plus the markdown-visual mermaid test): asserts the split pane is ~half the content width and neither pane nor diagram overflows the content's right edge; asserts every node label clears 3:1 contrast against its node background (catches dark-on-dark, which the prior label-presence/font asserts missed); asserts the diagram reveals (not stuck hidden) after settle; toggles the preview to Bright/Vanilla and asserts the rendered edge stroke and node labels clear 3:1 against the white surface (catches the gray-on-white lines). `docs/specs/GUI.md` records the split-pane sizing, label-readability, preview-mode-palette, zoom-surface, and reveal-gate contracts. Full suite `python3 -m pytest tests -q` -> `629 passed`.

#### Previewer: type-aware Preview for images, SVG, PDF, Mermaid, structured data, and media
- Completed and removed `DOIT.previewer.md`, `DOIT.previewer.next.md`, and `DOIT.previewer.robust-inputs.md`. Preview capability and rendering kind route through one shared `PREVIEW_RENDERERS` registry + helper layer (`previewKindForPath` / `previewMediaKindForPath` / `previewMimeForPath` / `defaultFileEditorViewModeForPath`), not ad hoc extension checks. Direct PNG/JPG/JPEG/GIF/WebP/BMP/ICO/AVIF/SVG render as raw-file image previews; SVG goes through `<img src=/api/fs/raw>` and is never trusted as app DOM; PDF uses a sandboxed iframe with open/download fallback; `.mmd`/`.mermaid` default to Preview; audio/video use native controls (no autoplay); JSON/YAML/TOML/CSV/TSV and notebook `.ipynb` use bounded structured previews (notebook never executes code); misleading-extension binaries recover via sniffed MIME from `/api/fs/info`; unsupported binaries show path/MIME/size + open/download.
- Markdown Preview resolves local image URLs relative to the Markdown file's directory through `/api/fs/raw` (not the app URL), keeps safe external images, blocks unsafe sources, shows broken-image fallback UI, and has a local fallback parser when `marked`/highlight.js fail to load. Mermaid is local-vendored at `static/vendor/mermaid.min.js`, lazy-loaded, sanitized before display. Backend `read_raw` MIME handling covers AVIF/PDF/`.mmd`/`.mermaid`. `docs/specs/GUI.md` carries the durable preview contracts; `docs/preview-samples/` holds the sample files.

#### Preview zoom: shared zoom/pan surface + corner-case and refactor passes
- Completed and removed `DOIT.preview-zoom-refactor.md` and `DOIT.preview-zoom-corner-cases.md`. One owner for zoom policy/actions (`previewZoomPolicy`, `previewZoomActions`, `previewZoomStateForAction`) and renderer fit caps (`previewZoomRendererDefaults`, `previewZoomOptionsForKind`); image tabs moved off the legacy `imageMode` path onto the shared `previewZoom` surface; shared image checkerboard CSS. Corner cases fixed: split Preview no longer inherits full-Preview manual zoom (zoom keys scoped by preview/split/popout context), the pop-out zoom toolbar is hydrated rather than inert markup, pop-out CSS keeps the zoom-shell grid/scroll viewport, async pop-out snapshot replacement rehydrates zoom controls, and embedded Markdown Mermaid blocks use scoped zoom keys.

#### YO!share host-owned Finder and YO!info row state
- Fixed the client-only Finder jump where a read-only share viewer could briefly retarget or repaint Finder from local transcript/session-files refreshes after the host snapshot applied. Read-only share clients now treat Finder root, root mode, target session, and expansion as host-owned model state: local active-tab sync, local session-files payloads, active-file reveal, and manual opens no-op outside a host UI-state apply. Same-root host frames refresh in place instead of reopening the tree and briefly clearing expansion.
- Fixed the YO!info screenshot mismatch where a viewer rendered rows from its own scoped `transcriptMeta` instead of the host table. YO!info share snapshots now include sanitized branch-table rows, and read-only viewers render those host rows until a newer host row snapshot arrives.
- Completed the read-only share security audit in tests. Share-token POST auth now has a route matrix proving only `/api/fs/batch` is allowed; mutating POST routes including event, settings, share management, upload, tmux-window, watch roots, drop actions, YO!agent chat, and every fs mutation reject with 403. The allowed fs batch endpoint is pinned to list/info only, and the share UI websocket receive-only gate is asserted before JSON parsing or UI-state handling.
- Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, `python3 -m pytest -q tests/test_browser_layout.py::test_share_readonly_info_sort_and_horizontal_scroll_are_host_owned tests/test_browser_layout.py::test_share_readonly_finder_session_is_host_authoritative`, `python3 -m pytest -q tests/test_server_query.py`, and full `python3 tools/check.py` (`CHECK PASSED in 14.88s`).

#### YO!share YO!info columns, Differ TO ref, and remote cursor parity
- Fixed the YO!info mismatch where the host could resize the Branch/desc columns and horizontally scroll, but a share viewer kept its own default column widths, making the same host scroll offset reveal different columns. YO!info share snapshots now include resizable column widths; clients apply those widths without persisting or echo-publishing them, and the browser fixture asserts the applied CSS variables plus host-owned horizontal scroll restoration.
- Fixed Differ FROM/TO parity for repo-scoped refs. `setRepoDiffRefs()` now schedules a full share UI-state snapshot after updating the repo map, so selecting TO on the host no longer leaves read-only clients stuck on `current`.
- Changed the remote share cursor from a native-pointer-like marker to a colored target/crosshair. The target uses the existing participant color and stays inert above the mirrored app so it is visibly remote and cannot be confused with the local OS cursor.
- Audited adjacent share-state paths and kept coverage on the existing host-owned mirror contract: YO!info sort/scroll/columns, Differ refs, popup/menu/tab hover mirroring, editor diff expansion, viewport/appearance, terminal dimensions, and read-only scroll blocking. Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, `python3 -m pytest tests/test_browser_layout.py::test_share_readonly_info_sort_and_horizontal_scroll_are_host_owned -q`, full `python3 tools/check.py`, and dev1 restart/smoke on port 8001.

#### YO!share editor cursor stability
- Diagnosed the shared-editor cursor jump as stale CodeMirror view-state in full YO!share UI snapshots: typing changed the document and visible cursor, but the cached `editor.modes[].viewState` stayed at an older anchor such as the top of the file until a scroll or pane switch refreshed it. CodeMirror edit and diff dispatch paths now refresh the file-editor view-state cache immediately after document or selection transactions, and normal edit mode publishes selection-only share scroll frames too.
- Reproduced with `tests/test_browser_layout.py::test_share_host_editor_snapshot_tracks_codemirror_cursor_after_typing`, which failed with `cachedAnchor: 0` after a CodeMirror edit in an active share and now asserts the outgoing share snapshot carries the live cursor. Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, focused browser editor/share tests, full `python3 tools/check.py` (`CHECK PASSED in 14.04s`), and dev1 restart/smoke on port 8001 (`ping: 401 0.043477s`).

#### DOIT.70/71 YO!share mirror architecture and Finder-session pinning
- Completed and removed `DOIT.70.md` and `DOIT.71.md`. DOIT.70 was a review/design DOIT, not an implementation ticket; its durable conclusion is now in `docs/specs/GUI.md` and the phased DOM-replay rewrite/backlog is in `docs/TODO.md`: read-only shares should eventually become sanitized host-DOM replayers, terminals stay on the byte stream, write mode can move later to input forwarding, and the digest needs content coverage plus real repair actions.
- Fixed the concrete DOIT.71 bug in the current semantic architecture. Read-only share clients now treat the Finder/Differ session as host-authoritative: local tab/focus/typing interaction paths no-op through `noteFileExplorerChangesSessionInteraction`, and `fileExplorerSessionFilesTargetSession()` resolves only the host-pinned session instead of falling back to a background `/api/session-files` payload or `sessions[0]`. Host-authored `finder` UI-state frames still move the client to the new session.
- Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, `python3 -m pytest tests/test_browser_layout.py::test_share_readonly_finder_session_is_host_authoritative -q`, `python3 -m pytest tests/test_browser_layout.py::test_share_readonly_info_sort_and_horizontal_scroll_are_host_owned -q`, full `python3 tools/check.py` (`CHECK PASSED in 15.85s`), and dev1 restart/smoke on port 8001 (`ping: 401 0.044465s`).

### UI, Theme, Preferences, and Localization

#### DOIT.67 Dockview tab overflow record archived
- Completed and removed `DOIT.67.md`, which was a note-only checklist from the dev2 Dockview tab overflow regression. The checked items confirm wrapped Dockview tabs, auto-height Dockview headers, right-side action reservation, crowded tmux/file-editor browser coverage, generated CSS rebuild, and dev-server restart expectations.
- The durable tab-wrapping contract remains in `docs/specs/GUI.md`: crowded Dockview tabs wrap into additional rows, the header grows, toolbar/content starts below the full wrapped header, and tabs must not scroll or paint under the first-row action cluster.

### Build, Refactor, Docs, and Tests

#### Documentation cleanup pass
- Cleaned the Markdown docs so the README leads with the default all-tmux-session launch path, `AGENTS.md` has a clear documentation map, `docs/DEVELOPMENT.md` uses a portable detached `nohup` restart recipe instead of the old transient-unit notes, `docs/TODO.md` drops stale disabled-layout wording, `docs/GUI_SPECS.md` states that Wall is the separate `tmux_wall.py` companion rather than an app layout mode, and the CodeMirror/vendor notes read more cleanly.
- Verification: `git diff --check -- '*.md'`, a local Markdown link check over all tracked `.md` files, and a stale-phrase grep for the old restart and disabled-layout wording.

### Mixed and Cross-Cutting

#### DOIT.debug1 / DOIT.refresh-state-unknown flaky sync and file-state fixes
- Completed and removed `DOIT.debug1.md` and `DOIT.refresh-state-unknown.md`. Finder Sync now invalidates stale in-flight root opens when the explicit tmux target changes, schedules the new explicit target even when the root path already matches, and refuses to apply an old forced tmux sync after the explicit session moved elsewhere. This fixes the 5 -> 6 -> 5 race where session 6 could finish late and leave Finder rooted at the wrong worktree.
- The high-contention Markdown scroll regression test now waits for the live bundle globals and grid before running its async editor script, so it no longer reports a misleading `ReferenceError: fileEditorItemFor is not defined` when Chrome is still exposing the page under load.
- File inspection status now preserves backend/API error detail through one shared formatter, including `payload.error`, `error`, `message`, and HTTP status before falling back to `Cannot inspect <path>`. A valid existing preview sample lookup is covered so an inspectable file does not get marked `file state unknown`.
- Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, focused Selenium regressions for stale sync, hover sync, existing file inspection, and long Markdown scroll (`4 passed` then `5 passed` after narrowing the tmux-only guard), 10 serial reruns of `test_sync_mode_does_not_follow_hovered_tmux_session` (`10 passed`), full `python3 tools/check.py` outside the sandbox (`CHECK PASSED in 23.87s`), and dev3 restart/smoke on port 8003 (`ping: 401 0.044468s`).

## 2026-06-12

### Tmux, Terminal, YO!share, and Tabber

#### DOIT.69 YO!share host-authoritative terminal grid
- Completed and removed `DOIT.69.md`. Share-view terminals now size only from the host PTY rows/cols; if host dimensions are unavailable, the viewer skips local resizing instead of estimating from its own pane box or viewport. Host dimension changes resize the viewer xterm, call `reset()` on actual row/col changes, and repaint from the host-sized grid instead of preserving smeared rows.
- The share upstream resize path now requests `tmux refresh-client` after `set_pty_size`/SIGWINCH so host resize events produce a clean tmux repaint for viewers. Geometry-drift resync also reapplies terminal dimensions, so a drift repair can re-pin the terminal grid rather than only relaying layout/chrome.
- Read-only share viewers now have a stronger host-owned scroll contract: scroll publishing is impossible from read-only viewers, local scroll events restore directly to the last host-authored value, pending Finder/Differ/Preferences/editor scroll frames replay after pane rebuilds, and read-only mirrored scrollbars/client-only Preferences chrome stay hidden so the client remains visually host-identical.
- Topbar menu restoration republishes the popup layer after rerenders, keeping File/tmux/Tabs/language hover-open menus and option highlights on the same host-owned popup path. The localization sidecar moved additional visible frontend strings, including share mirror status, disabled/admin labels, tmux-window labels, file-drop insertion statuses, editor fallback/error text, Markdown task checkbox labels, and HTML preview title, into locale keys.
- Verification for this archive entry: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, `python3 -m pytest tests/test_server_query.py::test_share_viewers_receive_host_terminal_dimensions -q`, `python3 -m pytest tests/test_browser_layout.py::test_share_readonly_diff_scroll_and_popup_mirror_are_host_owned -q`, full `python3 tools/check.py` outside the sandbox (`CHECK PASSED in 14.77s`), and dev1 restart/smoke on port 8001 (`ping: 401 0.042744s`).

#### DOIT.68 YO!share mirror state and popup parity regressions
- Completed and removed `DOIT.68.md`. Read-only YO!share clients now mirror host-owned editor diff state instead of falling back to plain editor content: diff mode, FROM/TO refs, per-editor unchanged expansion, vertical/horizontal scroll, and selection are carried in share UI/scroll frames and restored before or after the CodeMirror scroller exists.
- Client input over mirrored editors, Finder, and Differ stays passive. Host-authored scroll frames are the only authority for read-only restoration, while client hover, focus, wheel, scroll, keyboard, and pointer events are blocked before local pane/Dockview/Finder/editor handlers can publish or snap back to top.
- Tabs dropdowns, menu option hover/highlight, tab hover popovers, diff-ref dropdowns, and other host popups now mirror through the popup-layer channel as sanitized inert HTML. Popup selectors cover detached and inline tab popovers, app menu commands carry a real `share-mirror-active` class, and owner/sequence handling keeps stale frames from resurrecting closed host UI.
- The scoped localization sweep moved the remaining YO!share fit-toggle and extend-action labels into `share.*` locale keys, regenerated `static/locales/*`, and left risky broader copy cleanup for a separate pass instead of touching unrelated UI.
- Verification for this archive entry: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, `python3 -m pytest tests/test_browser_layout.py::test_share_readonly_diff_scroll_and_popup_mirror_are_host_owned -q`, full `python3 tools/check.py` outside the sandbox (`CHECK PASSED in 16.00s`), and dev1 restart/smoke on port 8001.

#### DOIT.67 YO!share mirror completeness follow-ups
- Completed and removed `DOIT.67.md`. YO!share transient UI is now host-owned end to end: popup-layer open/close frames publish immediately from menu/modal/popover owners, carry owner/sequence metadata, and drop stale frames so a delayed open cannot resurrect a closed host popup on clients.
- Mirrored popups now mount under the scaled app root, use host app-space rectangles, inherit the app font, and reset direct-child positioning/visibility/transform so app menus, tab hover popovers, context menus, and the YO!share modal render once through the root mirror transform. The 024/025 popup drift was the direct-child popup CSS re-anchoring itself from the host rect; the shared mirror-shell reset fixes that instead of patching YO!share only.
- Read-only share viewers now treat host scroll as authoritative for editors, Finder, and Differ: wheel/touch/keyboard/scroll input over mirrored app surfaces is blocked or restored to the last host-authored scroll frame while selection, browser copy, and context-copy stay available.
- Host language changes are carried in share appearance snapshots and live appearance frames, and applying a mirrored language frame re-renders chrome, menus, popovers, Finder/Differ labels, and open pane UI without blanking Differ rows. User-facing language choices show names/endonyms only; raw locale codes stay in data/payload/debug surfaces.
- The 022/023 pane screenshots showed viewer-only/read-only chrome and stale local state as the first visible mismatch, not a pane split-ratio defect. Geometry coverage remains on the shared digest (`viewport`, slots, tab strips, terminal cells, editors, fonts) plus the browser popup geometry check.
- Verification for this archive entry: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, `python3 -m pytest tests/test_app.py::test_share_ui_state_normalizes_viewport_and_appearance -q`, `python3 -m pytest tests/test_browser_layout.py::test_share_mirror_root_transform_and_bundled_fonts_render_in_browser -q`, full `python3 tools/check.py` outside the sandbox (`CHECK PASSED in 13.24s`), and dev1 restart/smoke on port 8001.

#### DOIT.66 YO!share read-only mirror follow-ups
- Completed and removed `DOIT.66.md`. Read-only share viewers now receive live countdown/viewer status, a fixed black mirror stage for cover/contain letterbox and crop bands, inert host-owned popup-layer mirrors for menus, language pickers, context menus, modals, and transient UI, and a read-only blocker that stops activation/mutation while preserving browser text selection, copy, context-copy, and scroll/navigation defaults.
- YO!share management now uses short `/share/<id>#t=<secret>` URLs, a compact topbar share icon, a compact sectioned modal with `New share` and `Active share URLs`, inline Stop controls, stronger modal boundaries, admin-only expiry extension, client and server redaction of mirrored share URLs/tokens, and clean pre-auth login/setup language pickers with no native select popup artifact.
- Editor/diff viewers refresh open clean files on saved file-version frames and mirror the stable CodeMirror scroll/selection subset; unsaved editor buffers remain last-saved-state by design and are documented in `docs/specs/GUI.md`.
- Verification for this archive entry: `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, focused share/auth/browser pytest slices, full `python3 tools/check.py` outside the sandbox passed every lane, and dev1 was restarted on 8001 and verified with `curl -sk -I https://localhost:8001/` returning the login redirect.

#### DOIT.65 YO!share mirror-frame correctness
- Completed and removed `DOIT.65.md`. YO!share viewers now render the whole YOLOmux app inside a host-sized `#appRoot` mirror frame with one cover/contain transform, host-published viewport and appearance inputs, bundled UI/mono fonts, app-viewport breakpoint classes, app-space geometry helpers, pinned host terminal rows/cols, shared-editor file read allow-listing, scroll/selection mirroring, simplified viewport-coordinate cursor mapping, and a geometry digest that shows `mirror ✓` or names persistent drift before resyncing. The old per-widget pointer fallbacks and per-terminal scale path are gone.
- The share UI channel now keeps read-only viewers passive and lets HTTPS write viewers propagate layout, active tabs, menus, Finder/Differ/Tabber state, editor/Preferences state, scroll state, and participant cursors back to the host and other viewers without echo loops. Viewer resize changes only the mirror transform, not host tmux dimensions.
- Documentation was already updated in `README.md` and `docs/specs/GUI.md` to describe the full-layout share contract, mirror frame, cover/contain fit, digest, read/write colors, read-only passivity, write fanout, cursor rules, and terminal resize behavior.
- Verification for this archive entry: `python3 tools/static_build.py`; full `python3 tools/check.py` outside the sandbox passed every lane (`py_compile`, `static_build.py --check`, JS syntax, `node tests/layout_url.test.js`, full pytest, `git diff --check`); dev1 was restarted on 8001; live Selenium acceptance against `https://127.0.0.1:8001` passed read-only and write-mode two-browser coverage with `mirror ✓` for read-only and write viewers, no terminal horizontal chop, editor scroll mirror to 180, read-only tab-click blocking, write tab/menu/cursor fanout, and writer resize preserving host terminal dimensions.

#### DOIT.64 YO!share live sharing
- Completed and removed the YO!share `DOIT.64.md`. File -> `YO!share...` now mints live share URLs with fragment-held secrets, short ids, read-only/http defaults, https-only write mode, viewer caps, expiry/revoke handling, `/share/<id>` boot, same-port plaintext share carve-out, one multiplexed viewer websocket, one shared upstream tmux attach per token/session, bounded viewer queues, layout/tab/focus/Finder/Differ/menu/pointer mirroring, ghost cursor/click ripples, host-sized terminal scaling, and terminal-input-only write mode.
- Finished the concurrent-share pass: the backend allows multiple active tokens, `/api/share` returns `shares`, `/api/share/stop` can stop one token/short id, the host keeps per-token publish sockets, the modal lists active shares while keeping the create form available, the topbar pill aggregates multiple shares, and Preferences exposes live-applied defaults for `share.ttl_seconds`, `share.max_viewers`, `share.read_only`, and `share.scheme`.
- Follow-up correction: YO!share scope is the entire current YOLOmux layout, not one focused tmux pane. Share records now carry every tmux session from the pane/tab layout, `/share/<id>` bootstraps all of them, share websockets allow exactly that session set, and viewer caps count distinct viewer browsers rather than the number of terminal sockets opened for that layout. Host pointer positions are transmitted as pane/viewport percentages and reconstructed against the viewer's geometry so different screen sizes do not use 1:1 pixel mapping.
- Verification for this archive entry: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node --check static/yolomux.js`, `node tests/layout_url.test.js`, `python3 -m pytest tests/test_activity.py tests/test_app.py tests/test_login_auth.py tests/test_server_query.py tests/test_tls_config.py tests/test_settings.py -n auto -q` (144 passed), live dev1 restart on 8001, authenticated live smoke creating two shares (`ro/http` and `rw/https`), active-list check, plaintext `/share/<id>` shell check, scoped cleanup (`stopped=2`), and post-smoke active share count 0. Full `python3 tools/check.py` outside the sandbox passed every lane except the pre-existing unrelated browser assertion `tests/test_browser_layout.py::test_diff_overview_matches_actual_todo_codemirror_rows` (`toB/endB` expected `40750/40749`, actual `40716/40715`; 609 passed).

### Editor, Finder, Differ, and Files

#### YO!share modal and diff-tab mirror parity follow-up
- Fixed the remaining screenshot-pair regressions after DOIT.69. Mirrored YO!share modals no longer force the child modal to `height: 100%`, so clients keep the host-measured modal width/content height without a client-only inner scrollbar or clipped active-share rows. Share-scoped file reads now include every host-visible editor tab identity (`file:`, `filediff:`, `filecopy:`, and `image:`) plus mirrored editor UI-state paths, so read-only clients can fetch the same diff/editor payloads as the host instead of showing `admin access required` and triggering tab-strip/editor drift.
- `docs/specs/GUI.md` records both contracts, `tests/test_app.py` guards the share allowlist for normal, Differ-preview, duplicate-editor, image, and UI-state file paths, and `tests/layout_url.test.js` guards the mirrored modal sizing rule. Verification: `python3 tools/static_build.py`, `node tests/layout_url.test.js`, focused app/login/browser share tests, full `python3 tools/check.py` outside the sandbox (`CHECK PASSED in 15.19s`), and dev1 restart/smoke on port 8001 (`ping: 401 0.044301s`).

#### DOIT.65 Differ tab home and physical-file editor identity
- Completed and removed `DOIT.65.md`. Re-opening an existing file/diff editor now activates the tab in the pane where the user last put it; normal Finder, Differ, Quick Search, and context-menu opens do not silently rehome an existing editor to the active/first/leftmost pane. Explicit file drag/drop still opts into rehoming with `rehomeExisting: true`.
- Editable text editors are now unique by backend physical-file identity, not path string. The backend emits safe `realpath`, `file_id`, and `file_identity` fields from `stat()` through list/read/write/search/path-info payloads; blocked symlink escapes and broken symlinks expose no editable identity. The frontend records display-path-to-identity and identity-to-primary-open-path mappings, folds real-path/symlink/hard-link aliases into the existing editor, preserves dirty buffers, switches to the requested edit/diff mode, and dedupes rapid same-path open races through an in-flight open promise.
- Differ context-menu Open actions are listed first in the requested fixed order: `Open in a Differ`, `Open in a new Differ`, and `Open in a new Editor`. Repeat Differ row clicks now apply the final Diff/Edit decision to the actual opened editor item, so a tab that was briefly or manually in Edit mode returns to Differ view. README and `docs/specs/GUI.md` document the pane-home and physical-identity contracts.
- Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node --check static/yolomux.js`, `node tests/layout_url.test.js`, `python3 -m pytest tests/test_filesystem.py -q` (60 passed), `python3 -m pytest tests/test_browser_layout.py::test_differ_reopen_keeps_dragged_file_tab_home tests/test_browser_layout.py::test_dockview_symlink_alias_focuses_existing_file_editor -q` (2 passed), and unsandboxed `python3 tools/check.py` (`CHECK PASSED in 15.73s`; an earlier full run had one browser xdist timing failure that passed focused and on rerun). Restarted `yolomux-dev2-8002.service`; live 8002 verification confirmed the served bundle/locale changes, a moved `filediff:/repo/app/src/live-main.py` stayed in `slot2` after reopen, and a real path plus symlink alias focused one dirty editor.

#### DOIT.63 terminal hard-wrapped URL links
- Completed `DOIT.63.md`. Terminal URL link stitching now handles ink/TUI-style hard wraps where each visual URL row is emitted as a separate non-`isWrapped` line, including zero-indent continuations at terminal width or width-1. The continuation guard still rejects a following fresh `https://`, `file://`, or `www.` row and still refuses to merge when the previous row did not reach the terminal edge.
- Regression coverage in `tests/layout_url.test.js` now checks hanging-indent links, zero-indent 3-row OAuth-style links from rows 1-3, the fresh-URL false-merge case, the short-previous-row false-merge case, and the xterm link provider's use of the shared wrapped-line stitcher. `docs/specs/GUI.md` records the terminal URL contract.

#### DOIT.64 Differ click and duplicate-editor follow-up
- Differ row clicks now request the Diff editor even for transcript-touched `T` rows when the file can render a real repo diff. If the diff fetch or renderability check fails, the editor falls back to Edit with a visible `diff unavailable: ...` status instead of silently looking like a normal editor open. Differ row opens start with collapsed unchanged context through a per-editor override, so clicking a Differ row no longer changes the global persisted diff expansion setting.
- The Differ row context menu now exposes `Open in a new Diff Editor` first and `Open in a new Editor` second, through locale keys in every shipped catalog. Background file reloads now capture/restore each same-file editor tab's CodeMirror selection and scroll, and push/watch reloads defer replacement while any editor for that path has focus or the file was just saved/returned to clean.
- Verification for this archive entry was focused rather than the full standard gate: `python3 tools/static_build.py`, `node tests/layout_url.test.js`, `python3 tools/static_build.py --check`, `node --check static/yolomux.js`, `python3 -m pytest tests/test_browser_layout.py::test_dockview_file_editor_tabs_stay_above_toolbar tests/test_browser_layout.py::test_dockview_many_tabs_stay_one_row_above_content tests/test_browser_layout.py::test_dockview_header_actions_stay_on_first_row tests/test_browser_layout.py::test_editor_diff_button_waits_for_clean_payload_before_showing_refs -q` (4 passed), and `git diff --check`.

#### DOIT.57 terminal file-action menu completed
- Completed and removed `DOIT.57.md`. Terminal file drops now route through one shared `DROP_ACTIONS` registry across drag/drop, uploads, paste, and command-palette actions. The P2/P3/P4 work added read-only shell actions (`file`, `wc`, `tail -F`, `jq`, `column -t`, directory size scans), multi-file prompt actions, per-category last-action memory, Upload Preferences for read-only shell autorun and custom action lines, command-palette file actions for the active file/selection, and shared Finder/Explorer/Differ/Tabber image-row `Copy image` context-menu behavior.
- Image paste/drop option order is now exposed in Upload Preferences through `uploads.image_action_order`. The default puts agent OCR, screenshot diagnosis, image description, and `info` first so the `1..9` popup starts with the common operations; the Preference stores prompt actions as `Popup label: ; prompt text`, supports non-AI rows such as `info`, caps the list at 9, and is authoritative for image menus so undeclared fallback actions are not appended. The pasted image path/reference is inserted before the menu, so `Insert path` is not included in the image action order; stale `Server: OCR image` and `Show file type` rows are dropped.
- Server-side read-only runners now live behind `POST /api/drop-action/run`: bounded file info, text preview, log error/warning scans, CSV/TSV/JSON/NDJSON data stats with a small text chart, and OCR via `tesseract` when available. The route delegates through the app layer and the runners use the existing filesystem validation/credential guards.
- README and `docs/specs/GUI.md` now describe the file-action menu, custom action format, shell/server behavior, pasted-image insert guarantee, and image clipboard fallback. Verification for this archive entry: focused checks (`python3 tools/static_build.py`, `python3 -m py_compile yolomux_lib/drop_actions.py yolomux_lib/app.py yolomux_lib/server.py yolomux_lib/settings.py`, `node --check static/yolomux.js`, `node tests/layout_url.test.js`, and `python3 -m pytest tests/test_drop_actions.py tests/test_settings.py tests/test_server_query.py -q` with 49 passed) plus the full standard gate `python3 tools/check.py` (`CHECK PASSED in 14.23s`).

#### Editor scroll preserved across tab switches
- Completed and removed the unnumbered `DOIT.md` (markdown editor scroll snap). Editor scroll/selection now survives a Dockview tab switch away and back (`2026.md` -> Preferences -> `2026.md`); the prior behavior snapped the editor to the top of file. `captureFileEditorPanelViewState()` stores a CodeMirror `scrollSnapshot` plus `scrollTop`/`scrollLeft`/selection; CodeMirror scroll events schedule a shared `scheduleFileEditorPanelViewStateCapture(item, panel)` so "scroll, then switch away" persists state regardless of tab-event order; Dockview tab `pointerdown`/`mousedown` runs `captureDockviewPreviousPaneBeforeTabActivation(...)` before Dockview switches panels (manual Dockview tab clicks bypass `activatePaneTab()`); and `fileEditorPanelViewStateCaptureHasLayout()` blocks a pooled/disconnected/zero-height pane from overwriting a good saved scroll with `scrollTop: 0`. Restore reapplies after CodeMirror focus and across animation frames. `docs/specs/GUI.md` records the tab-switch scroll contract, distinct from the existing background-reload contract.
- Shipped on `main` as `a352c3b fix: preserve editor scroll across tab switches`. Real-click regressions in `tests/test_browser_layout.py` use real DOM tab clicks rather than `activatePaneTab()`: `test_long_markdown_editor_scroll_survives_dockview_tab_click_roundtrip`, `test_preferences_scroll_survives_dockview_tab_click_roundtrip`, `test_info_scroll_survives_dockview_tab_click_roundtrip` (`3 passed`). Also `node tests/layout_url.test.js`, `python3 tools/static_build.py --check`, `node --check static/yolomux.js`.

### UI, Theme, Preferences, and Localization

#### DOIT.62 topbar language popup stability
- Completed and removed `DOIT.62.md`. Passive topbar refreshes no longer detach a focused native topbar control, so the topbar language picker stays open while background transcript/session-file/SSE refreshes update the session buttons. The implementation added one pending-render state, a topbar-focused-control guard, and a blur flush so deferred renders still land after the user leaves the picker.
- `docs/specs/GUI.md` now records the topbar native-control rule, and README documents the user-visible language-picker behavior. Verification for this archive entry: `node tests/layout_url.test.js`, `python3 -m pytest tests/test_browser_layout.py::test_diff_overview_matches_actual_todo_codemirror_rows -q`, and `python3 tools/check.py` (`CHECK PASSED in 14.55s`).

## 2026-06-11

### Agents and Automation

#### DOIT.62 agent CLI PATH self-heal and dev1 restart owner
- Completed and removed `DOIT.62.md`. Server startup now self-heals `PATH` with `YOLOMUX_EXTRA_PATH` and an existing `~/.local/bin`, agent availability probes report `unavailable_reason: "not-on-path"` for missing Claude/Codex CLIs, the tmux menu renders that reason, and CLI startup logs one warning per missing agent. Added `tools/yolomux-restart-dev1.sh` as the documented dev1 restart owner and verified the live 8001 process restarted with `/home/keivenc/.local/bin` on `PATH`.
- Verification for this archive entry was focused rather than the full standard gate: `python3 tools/static_build.py`, `python3 -m pytest tests/test_workdir.py -q` (9 passed), `node --check static/yolomux.js`, `node tests/layout_url.test.js`, `bash -n tools/yolomux-restart-dev1.sh`, `tools/yolomux-restart-dev1.sh --print-command`, live restart of 8001, `curl -sk -o /dev/null -w "ping: %{http_code} %{time_total}s\n" https://localhost:8001/api/ping` (`401`), and `/proc/<pid>/environ` PATH inspection.

### Tmux, Terminal, YO!share, and Tabber

#### DOIT.56 neon cursor choices, focused-pane opens, and terminal tab labels
- Completed and removed `DOIT.56.md`. Cursor color now has cursor-only neon presets (`laser-lime`, `neon-green`, `neon-cyan`, `neon-magenta`, `neon-orange`) without adding neon active-pane ring colors; the same preset parent drives editor cursor, active terminal cursor, and pane scrollbar thumb, with darker light-theme variants for readability. New tabs and file opens now target the focused non-Finder pane instead of the first pane, while Finder focus still falls back to a normal content pane. Terminal pane header tabs now show the compact `Term` label while keeping the active process/window detail in title/ARIA and the Info Bar.
- Verification for this archive entry was focused rather than the full standard gate: `python3 -m pytest tests/test_settings.py tests/test_static_build.py -q`, `node --check static/yolomux.js`, `node tests/layout_url.test.js`, `python3 -m pytest tests/test_browser_layout.py -q -k "active_color_radios_recolor_live_pane_chrome or new_virtual_and_file_tabs_open_in_focused_pane or new_tabs_do_not_open_in_focused_finder or terminal_info_bar_alignment"`.

### Editor, Finder, Differ, and Files

#### DOIT.63 hover focus no longer retargets Finder/Differ context
- Completed and removed `DOIT.63.md`. Dockview active-panel changes are now passive focus updates, return during programmatic layout application, and no longer stamp `userInitiated` or call `noteFileExplorerChangesSessionInteraction`. Dockview tab click and Enter/Space activation now commit explicit tmux interaction at the gesture site, preserving click/type behavior while preventing auto-focus hover from changing Finder/Differ session context.
- Verification for this archive entry was focused rather than the full standard gate: `python3 tools/static_build.py`, `node --check static_src/js/yolomux/75_dockview_layout.js`, `node --check static/yolomux.js`, `node tests/layout_url.test.js`, and `python3 -m pytest tests/test_browser_layout.py -k sync_mode_does_not_follow_hovered_tmux_session -q` (1 passed).

#### DOIT.61 Finder pane disappearance guard
- Completed and removed `DOIT.61.md`. Fresh/default and `sessions=`-only layouts now dock Finder/File Explorer by default; explicit Finder hide actions share a localized status message showing the restore shortcut; Dockview adoption re-docks Finder when a non-user layout commit drops it while preserving the previous Finder slot when possible; and the legacy small-slot prune protects any slot containing Finder, even as a background tab.
- Verification for this archive entry was focused rather than the full standard gate: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node --check static/yolomux.js`, `node --check tests/layout_url.test.js`, `node tests/layout_url.test.js`, `python3 -m pytest tests/test_browser_layout.py -k 'pinned_tab_dragged_to_other_pane_lands_first or finder_path_is_first_and_readable_in_wrapped_toolbar or finder_diff_mode_toggle_fills_pane' -q` (3 passed), and `git diff --check`.

#### DOIT.58 D1-D3 Differ target must not follow hover auto-focus
- Audit + guard: the Differ's committed target session (`fileExplorerChangesSelectedSession` / `fileExplorerExplicitSyncSession`, read by `fileExplorerSessionFilesTargetSession()`) is moved only by explicit user intent. Verified the explicit/passive split is already correct in the code: `setFocusedTerminal` commits (`rememberFileExplorerExplicitSyncSession` + `scheduleFileExplorerActiveTabSync`) only when `options.userInitiated === true`; the hover chain (`selectPanelOnHover` -> passive `setFocusedTerminal` + `focusTerminalWhenAutoFocus` -> `term.focus()` -> the `onFocus`/`focusin` passive `setFocusedTerminal`) never passes that flag, and no `fetchSessionFiles({destination:'finder'})` call uses raw `focusedTerminal`/`focusedPanelItem`. So D1 needed no code change — the regression is already absent.
- D2 tests: extended the existing hover guard in `tests/layout_url.test.js` to cover full Differ mode (`fileExplorerMode === 'diff'`) — with session A committed, `selectPanelOnHover(B)` + passive `setFocusedTerminal(B)` + passive `selectSession(B)` all keep the Differ target on A (the source for title / session-select / cache key / fetch), while `selectSession(B, {userInitiated:true})` commits it.
- D3 docs: `docs/specs/GUI.md` Finder/Differ rules codify the explicit-intent contract. (The level-3 Tabber row pipeline already routes through the committed target, so it inherits the same guarantee.)

#### DOIT.58 Tabber — the Finder pane's third mode (B1-B6, level 3)
- New `Finder / Differ / Tabber` mode of the one shared file-pane control (`fileExplorerMode` extended to `files|diff|tabber`; `normalizeFileExplorerMode`/`readStoredFileExplorerMode` accept it; a third toggle segment; body class `file-explorer-mode-tabber` takes over the pane like Differ). The Tabber is a live tree of everything open: tmux sessions (level 0) -> their windows (`index:process`, agent-tinted, level 1) -> each window's panes (process + cwd basename, level 2) -> the touched paths grouped by repo with the git branch on the repo row (level 3, lazily fetched from `/api/session-files` and cached locally when a session is expanded, so it never disturbs the Differ target).
- Rows render through the SHARED Finder/Differ row pipeline (`renderTreeChildren` -> `updateFileTreeRow` -> `updateFileTreeRowContents`) via a `mode:'tabber'` render option whose per-row display values are precomputed as data — no forked `*RowHtml` builder. Expansion is a persisted set keyed by STABLE id-based node paths (`s_<session>/w_<index>/p_<index>/r_<n>/f_<n>`), so a session add/remove never shifts another node's expansion. `renderTreeChildren` gained a data-driven `expandedSet` option (no mode branch).
- Sort + time column come from the activity ledger (`GET /api/activity`, polled only while the Tabber is open): a session/window row's recency is `max(last_user_input_ts, last_agent_active_ts)`, set as the entry's `mtime`; the tree sorts most-recent-first (`treeSortMode:'newest'`) with the shared `None/Date/Ago` date column, re-sorting on poll without collapsing expansion. Row actions: session click focuses the session, window click switches the tmux sub-window (`tmuxWindow({windowIndex})`), context-path click opens the file (diff) in the focused pane, repo click roots Finder at the repo.
- Docs: `docs/specs/GUI.md` gained a `Tabber Pane Rules` section + Terms note; README gained a Daily-use bullet. Tests: a `t@tabber` node test renders the DOM (sessions/windows/panes, double-render reuse, recency sort, level-3 repo + openable file rows) and asserts the human labels, never the synthetic node keys; `test_browser_layout` mode list updated to the 3-way toggle. Shipped across `0.3.1`-`0.3.4`. The later D1-D3 Differ-target work is archived above.

#### DOIT.59 Uploads default into a `.uploads/` subdir
- Browser uploads (drag-drop onto a terminal, clipboard paste, the `+` button) now write into `<resolved-dir>/.uploads/` by default instead of the cwd / repo root, so pasted screenshots and dropped files stay out of source and are easy to clear. New `uploads.subdir` Preference (default `.uploads`; empty string opts back into the old cwd behavior, allowed via a `STRING_ALLOW_EMPTY` set so a blank value isn't reverted like other empty strings). One owner: `upload_target_dir` split into `_resolve_upload_base_dir` (the old resolution) + a single `_apply_upload_subdir` that reads the setting, rejects absolute / `..` escapes, `mkdir 0o700`, and falls back to the bare resolved dir on failure with the home-guard preserved; every upload caller already routes through it, so no per-call-site fork. Preferences field added in `80_panes_preferences.js` + `pref.uploads.subdir.{label,help}` across all 13 locales. Tests: 4 `_apply_upload_subdir` cases (default / empty / escape / uncreatable) in `test_app.py` + 3 sanitize cases in `test_settings.py`; 564 pytest green. Documented via the Preferences help text; README has no uploads-destination section and `docs/specs/GUI.md` had unrelated in-flight edits, so no separate persistent-doc note was added.

### UI, Theme, Preferences, and Localization

#### DOIT.64 pinned tabs stay in their current pane
- Completed and removed `DOIT.64.md`. Pinned tabs now have one shared Dockview violation helper for cross-pane and new-pane drops: same-pane pinned reorders still work, but pinned tabs cannot move into another pane or split into a new pane. The old index-0 cross-pane move exception and pointer fallback state were removed. The active UX contract in `docs/specs/GUI.md` and `README.md` now says pinned tabs stay in their current pane; historical DONE entries were left as history.
- Verification for this archive entry was focused rather than the full standard gate: `python3 tools/static_build.py`, `node --check static_src/js/yolomux/75_dockview_layout.js`, `python3 -m py_compile tests/test_browser_layout.py`, `node --check static/yolomux.js`, `node tests/layout_url.test.js`, and `python3 -m pytest tests/test_browser_layout.py -k 'pinned_tab_invalid_non_pinned_target_shows_red_dashes or non_pinned_tab_cannot_drop_between_pinned_tabs or pinned_tab_cannot_move_to_other_pane or pinned_tab_cannot_split_to_new_pane or pinned_tabs_render_first_after_pin_toggle or drag_reorders_two_pinned_tabs or first_pinned_tab_drags_after_second_pinned_tab' -q` (7 passed).

#### DOIT.60 Localization expansion order
- Added the next developer-priority localization batch: Vietnamese, Thai, Turkish, Dutch, Polish, and Italian. The topbar, login/setup, and Preferences language pickers now use the same product-priority order (`en`, Traditional Chinese, Simplified Chinese, Japanese, Korean, Spanish, German, French, Italian, Brazilian Portuguese, Polish, Dutch, Hebrew, Arabic, Russian, Hindi, Vietnamese, Thai, Turkish), with `en-XA` kept as a QA pseudo-locale at the end and excluded from the pre-auth user-facing picker. Preferences now consumes the shared `i18nLocaleChoices()` helper instead of carrying a second hand-written language list. Settings validation accepts the new locale codes; generated `static/locales/` outputs and `en-XA` are rebuilt from the expanded `static_src/locales/` set. README and development docs now describe the 19 user-facing locales.

#### DOIT.60 Help/About links to the project on GitHub
- Added a `GitHub` link to the Help -> About modal pointing at `https://github.com/keivenchang/yolomux`, mirroring the existing LinkedIn author link: new `aboutProjectUrl` const beside `aboutLinkedInUrl`, a second `.about-author.about-github` anchor in `showAboutModal`, and a `menu.help.about.github` locale key ("GitHub", brand-literal) across all 13 locales + en-XA. node guard asserts the modal renders the repo URL + the `about-github` class + the localized label.

#### DOIT.57 Info Bar dedup + window-button toggles
- Removed the duplicate agent badge from the pane Info Bar — it duplicated the `N:claude` tmux sub-window button now that the tmux sub-window bar carries agent identity (an agent sub-window's name falls back to its process label). Deleted the `sessionAgentBadgeHtml` helper (no other callers), its per-poll refresh, and the `.panel-agent-slot` / `.panel-agent-badge` CSS. The Info Bar now reads `path · dirty | tmux sub-window buttons | ×`.
- The ACTIVE tmux sub-window button reads as a pressed toggle via the shared `--active-control-*` tokens, so it follows the user's Active color preference and flips with the theme (no hardcoded hex); inactive buttons keep the plain look. `docs/specs/GUI.md` info-row contract updated, and the prior "agent badge is muted" regression pins were flipped to assert the badge is gone and the toggle uses the tokens.
- REOPENED 2026-06-18 via `DOIT.82.md`: light mode still let the generic `.tab.active` fallback override agent-tagged active tmux sub-window buttons, so the active button rendered gray instead of the shared active green.
- Verification beyond the standard gate: the live GUI check (badge absent, pressed toggle in dark + light, Active-color recolor) is pending a server restart / hard-reload; the Selenium info-bar alignment test was updated to drop the badge and still compiles.

#### DOIT.55 unified Cmd-P / Shift-Cmd-P search ranking
- Completed and removed `DOIT.55.md`. `Mod+P` and `Shift+Mod+P` now search one shared universe of files, panes/tabs, commands, and settings, with the entry point contributing only a domain-prior weight (`Mod+P` file-first, `Shift+Mod+P` pane/command-first). Open-file dedupe is preserved so a file already open as a tab appears once as the tab row with view chips, while typed queries in either entry point can blend file-index and pane/command results. Ranking weights now live in the exported `searchRankWeights` table, and the scorer adds bounded recency plus focused-repo affinity without letting those terms beat stronger text matches.
- Backend session metadata now exposes the latest agent transcript mtime, which the palette uses as the primary pane recency timestamp with summary timestamps as fallback. `docs/specs/GUI.md` now documents the unified search contract and the keyboard-shortcut line describes the two shortcuts as ranking-prior variants, not separate search features.
- Verification for this archive entry was focused rather than the full standard gate: `python3 tools/static_build.py`, `node --check static/yolomux.js`, `node tests/layout_url.test.js`, `python3 -m pytest tests/test_metadata.py tests/test_static_build.py -q`, `python3 tools/static_build.py --check`, and `git diff --check`.

### Build, Refactor, Docs, and Tests

#### DOIT.61 refactor audit follow-up
- Completed and removed the refactor-audit `DOIT.61.md`. Fixed the activity heartbeat rotation leak, preserved epoch mtimes in session-file fallback timestamps, hardened corrupt activity-ledger startup, removed dead Tabber path/detail render paths, kept the odd `1501ms` file-index-building poll cap, and added source/runtime tests for those cases.
- Consolidated shared parents for Finder/Differ/Tabber row behavior: dataset set/delete, tree indentation/depth, treeitem ARIA, stale row handler clearing, git-status normalization, Tabber session-files state, and Tabber active-window marker data. CSS audit fixes added theme tokens for file icons, soft links, menu accents, light drop-outline, and drop-suggestion z-index; routed light text through `--lt-text`; and converted the audited editor edge pins to logical inline properties.
- Backend cleanup added `error_payload(...)`, a typed error payload shape, one GET int-query route wrapper, and `sessions.active_window_for_panes(...)`. Docs now cover upload destination behavior, terminal drop suggestions, Cmd-P absolute-path targeting, Tabber non-collapsible session rows/touched-path leaves, and `GET /api/activity-summary`.
- Verification for this archive entry was focused rather than the full standard gate: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node --check static/yolomux.js`, `node tests/layout_url.test.js`, `python3 -m py_compile yolomux_lib/common.py yolomux_lib/server.py yolomux_lib/sessions.py yolomux_lib/app.py`, and `python3 -m pytest tests/test_server_query.py tests/test_sessions.py tests/test_activity.py tests/test_app.py tests/test_session_files.py -q` (117 passed).

#### Final active DOIT cleanup archived
- Completed and removed the last active `DOIT.51.md` / `DOIT.53.md` files. `DOIT.51` closed the small follow-ups: kept native blame hover by product decision, validated stale-command auto-approve behavior against the live prompt command, standardized YOLO risk labels, documented the `0.0.0.0` host default, added scoped share-token auth/revocation/layout seeding, and filtered Quick Open exact filename searches so indexed-root fuzzy noise does not bury the local match. `DOIT.53` moved the tmux sub-window bar into the pane Info Bar, rendered `index:name` labels from process-aware names, kept click-to-switch on the existing `/api/tmux-window` route, and made Finder path errors path-keyed so stale red states do not bleed into a new root.
- This archive entry is bookkeeping for the already-shipped 0.2.82 batch (`50cbd0a`); the raw DOIT notes were removed after confirming both files had no unchecked tasks.

### Mixed and Cross-Cutting

#### DOIT.58 pinned tab cross-pane drops land first
- Completed and removed `DOIT.58.md`. Dragging a pinned tab into another pane now routes through one pinned cross-pane move intent and commits via `moveSessionToSlot(..., insertIndex=0)`, so the pinned tab becomes the first tab in the target pane instead of landing behind non-pinned tabs or splitting the pane. Invalid native Dockview tab/content previews for that gesture are hidden while preserving the final drop.
- Verification for this archive entry was focused rather than the full standard gate: `python3 tools/static_build.py --check`, `node --check static/yolomux.js`, `node tests/layout_url.test.js`, `python3 -m pytest tests/test_browser_layout.py -k 'pinned_tab_dragged_to_other_pane_lands_first or non_pinned_tab_cannot_drop_between_pinned_tabs or pinned_tabs_render_first_after_pin_toggle or drag_reorders_two_pinned_tabs or first_pinned_tab_drags_after_second_pinned_tab' -q` (5 passed), and `git diff --check`.

## 2026-06-10

### Tmux, Terminal, YO!share, and Tabber

#### Terminal copy while Claude is running (OSC 52 clipboard bridge + right-click highlight)
- Root cause of "copy does nothing while Claude runs": while a TUI owns the mouse, a drag never makes an xterm selection — the copied text leaves the app as an OSC 52 clipboard escape. tmux's default `set-clipboard external` DROPS application OSC 52, and even when forwarded, xterm.js ignores OSC 52 without a registered handler. Verified empirically on a scratch tmux 3.4 server (`external` drops, `on` forwards) and end-to-end on the live server (OSC 52 from a pane reached the attached client PTY and decoded to the exact text).
- Fix: `bridge_tmux` ensures `tmux set-option -s set-clipboard on` before each attach (idempotent, self-healing); new `installTerminalOsc52Bridge` registers an OSC 52 parser handler that decodes base64 UTF-8 and writes the browser clipboard through one shared chain (`writeTerminalTextToClipboard`: synchronous `copy` event first, then async `navigator.clipboard`). `?` read-queries are consumed unanswered (no clipboard exfiltration).
- Shortcuts: Cmd-C/Ctrl-C copies the xterm/browser selection; Cmd-C with no selection is swallowed with a how-to-select hint (Option/Shift-drag) and never reaches Claude; plain Ctrl-C with no selection still sends SIGINT; explicit tmux copy-mode copy is Cmd-Option-C (Mac) / Ctrl-Alt-C (PC) plus a right-click menu item.
- Right-click no longer clears the terminal highlight: a capture-phase right-mousedown captures the selection and stops xterm from clearing it; the context-menu Copy uses that captured text.
- Opt-in copy-path instrumentation behind storage key `yolomux.debugCopy`.
- Verification beyond the standard gate: live-confirmed by the user (copy while Claude running, Cmd-C on an Option-drag selection, SIGINT fallthrough, right-click highlight, tmux-copy shortcut/menu); node OSC 52 decode/handler/clipboard + right-click + SIGINT guards; full pytest 481 passed.

### Editor, Finder, Differ, and Files

#### DOIT.53 active-pane Quick Open, pane chrome, Finder/Differ toolbar, and working-state polish archived
- Completed and removed `DOIT.53.md`. Cmd-P normal file opens now target the pane that owns the active tab while explicit split-open keeps split behavior; the session agent badge moved from the top tab-title row to the pane Info Bar; the pane-header detail toggle now says Show/Hide details instead of YO!info and syncs title/aria/pressed state; tmux panes render a per-window button bar with duplicate-name disambiguation and numeric overflow fallback; Codex working-state detection treats bottom composer/model-status chrome as chrome so the YO marker spins while the agent is actually working.
- Finder and Differ now share `Date | Expand all | Collapse all | Reload` toolbar controls through one parent path. The Expand/Collapse buttons use skinny in/out arrow SVG icons, stay narrow in all toolbar contexts, and Sync-mode Expand is bounded to affected sync-plan paths such as `yolomux.dev2` instead of recursively crawling broad home roots. Verification beyond the standard gate: the focused Finder Sync browser regression.

### UI, Theme, Preferences, and Localization

#### Dockview drag/drop hardening and GUI behavior spec
- Tightened Dockview and legacy drop validation so previews are suppressed when a target pane is too small, Finder/Differ is a reserved target except for a roomy bottom split, file drags use the same validator as tab drags, and directory drags still keep terminal path insertion behavior on normal panes.
- Restored right-root drops for stacked panes without stealing normal local pane-edge splits by using a wider cross-gutter tolerance for full-span root drops. Left and right root-edge previews now create full-height panes beside stacked content, while local pane-edge drops still split only the target pane.
- Added `docs/specs/GUI.md` as the durable pane/tab/Finder/Differ behavior spec and linked it from `README.md`. The spec includes current behavior, test coverage, and an audit backlog for future GUI specs/tests.
- Verification beyond the standard gate: `python3 -m pytest tests/test_browser_layout.py -q -k dockview`.

### Build, Refactor, Docs, and Tests

#### Refactor audit: one-source dedup, lint enforcement, god-file split (DOIT.53 refactor batch)
- Post-Dockview-rewrite refactoring audit, 39 items. (The DOIT.53 *number* was reused after the 06-09 "Differ selection/delete" batch below — this is unrelated work.) Collapsed divergent copies into single owners: approval-prompt detection moved into `yolomux_lib/approvals.py`, which de-inverts `app.py`'s import of the root `auto_approve_tmux` CLI (one shared detector, guarded by an `is`-identity parity test); one `TtlCache` behind the metadata + transcript-lookup caches; `yolomux_lib/atomic_file.py` for the flock+atomic-write idiom (adds the fsync one of the three copies lacked); shared `cmd_error` and `git_ahead_behind_counts`.
- Backend boilerplate folded into helpers (`write_app_result`, `write_validated_int_result`, `require_known_session`). Frontend shared parents: `dedupeInflight` (one cache+inflight fetcher), `paneTabInnerHtml` (Dockview + DOM tab builders), `panelDomId`/`terminalDomId`/`transcriptDomId` id helpers, `delegate()` for per-render listener loops. Split the two ~5k-line god-files into `45_file_explorer_actions.js` and `95_codemirror_editor.js` (bundle byte-identical).
- CSS: theme-aware `--shadow-rgb` (shadows now flip in light mode), `--text-muted-strong` (was `#8b95a5` in 6 places), one grouped truncation rule; `tmux-wall.css` tokens realigned to the app's.
- Enforcement added: `tools/check.py` runs the whole CPS gate as one command; `static_build.py` gained undefined-CSS-var + duplicate-declaration lints (caught and fixed 6 live bugs); the node suite got a `test()` runner so a failing assertion no longer aborts the rest and prints a pass/fail summary; `tests/ui_pins.json` + `test_ui_pins.py` are a single source for shared color pins with a drift guard; `page_html`/`app_css` de-dup the Selenium fixtures.
- Live bugs fixed along the way: `tmux_utils` used `time.sleep` without importing `time` (crashed highlight-moving auto-approve); `tmux_wall` capture dropped `-J` (wrapped lines rendered wrong); the 6 undefined CSS vars the new lint surfaced.
- Also stripped ~470 ephemeral `DOIT.N` tags from code comments (the rationale stays; reference the persistent docs instead, since DOIT files are deleted when done). Verification beyond the standard gate: `tools/check.py` green — 534 pytest, node suite exit 0, `static_build --check`, both `node --check`, whitespace clean. Shipped at 0.2.81. The approval-engine de-inversion (B8/G2) is covered by unit + parity tests but was not validated against a live approval flow.

#### DOIT.52 push-driven filesystem and session-file refresh archived
- Completed and removed `DOIT.52.md`. The browser polling path now has Preferences-backed refresh controls, `/api/session-files` and `/api/transcripts` serve cached/stale results with background refresh, explicit user refreshes use `force=1`, and the server exposes authenticated `/api/client-events` SSE events through `ClientEventBroker`.
- Added server-side watch/poll invalidation through `/api/watch/roots`, filesystem/settings/transcript signatures, cache invalidation, and push-only-on-change behavior. Post-push live-debug churn was reduced by coalescing session-file refreshes through the cached path, avoiding `fs_changed` session-file fetches, skipping expensive connected-client polling while renewing watch roots, keeping transcript directories on transcript watching, and preserving session-files cache across filesystem changes. The final user-provided debug sample held `/api/session-files` to 6 calls averaging 347.1ms, with remaining slow endpoints tracked separately.

## 2026-06-09

### Editor, Finder, Differ, and Files

#### DOIT Search formatting and editable Markdown tasks
- Quick Search now normalizes a trailing punctuation query such as `DOIT:` to `DOIT` for file search, ranks basename matches ahead of path-only fuzzy hits, and renders image hits in Popular IDE-style form such as `[Image #1] '/home/keivenc/yolomux.dev1/20260609-001.png'`.
- Markdown Preview now wires rendered task-list checkboxes back to the open file model: clicking a `- [ ]` / `- [x]` checkbox toggles the Markdown source, refreshes linked preview panes/popouts, updates CodeMirror documents, and uses the existing dirty/autosave path.
- Verification: standard gate green.

#### DOIT.53 Differ selection/delete and non-git diff counts
- Unified Differ file-row selection and file context menus onto the Finder shared parent: single-click, shift-click, and cmd/ctrl-click now use `updateFileTreeSelectionFromClick`, right-click file rows use `showFileTreeContextMenu`, and the shared delete path refreshes session-files so deleted Differ rows disappear immediately. Removed the Differ-only single selected-path state and safe-only file context menu.
- Added `diff_tracked` to session-files payloads. Counts from real `git numstat` stay green and contribute to repo totals; raw full-file counts for untracked/no-repo files stay visible per row but render neutral and are excluded from added/removed totals. Transcript-touched files outside any git repo now appear under the `Outside repo` section instead of being silently dropped.
- Verification beyond the standard gate: `python3 -m pytest tests/test_session_files.py -q`. Full `python3 -m pytest tests -n auto -q` ran 454 passing tests with two failures: the YO!agent rolling-summary test passed when rerun serially, and the remaining browser failure is the pre-existing dirty-`TODO.md` diff-overview row-offset drift.

#### DOIT.52 tab-drag and Finder sync perf
- Fixed the slow tab-drop path by moving layout render decisions into one shared scheduler (`requestLayoutRender` / `performLayoutRender`) and replacing the old `pendingPanelsRender` boolean with a structured `pendingLayoutRender` request. Same-shape drag drops now keep the cheap `syncActivePanelsInPlace` + tab-strip path, while metadata-driven `renderPanels()` calls during drag still record an explicit forced-full render request.
- Reduced the Finder sync re-render floor by adding one sync-plan parent (`fileExplorerSyncPlanKey`, `fileExplorerSyncPlanAlreadyApplied`, `markFileExplorerSyncPlanApplied`) and skipping automatic repeated root+expand-path plans after they have already applied. Explicit Sync still forces a re-apply, and manual expand/collapse/root-mode changes reset the applied key.
- Aligned Finder refresh defaults by changing the JS fallback to `5` seconds and migrating exact stale saved poll defaults (`file_explorer.refresh_seconds: 1`, old round performance poll values) to the current defaults without changing nearby custom values. Did not add the optional B3 mtime sweep because `/api/fs/info` is not a cheap stat-only endpoint today; adding it to every watched directory poll would likely make the path worse unless a dedicated stat endpoint is introduced.
- Verification beyond the standard gate: `python3 -m pytest tests/test_settings.py -q`. Full `python3 -m pytest tests -n auto -q` ran 454 passing tests and one unrelated failure in `test_diff_overview_matches_actual_todo_codemirror_rows` because the already-dirty `TODO.md` changed the hard-coded CodeMirror row offset from `45531` to `45544`.

#### DOIT.51 blame popover, quick-open dedupe, worktree identity, risk labels, and drag timing
- Inline blame now uses a styled hover popover with author, absolute date, sha, summary, and optional commit body instead of a native tooltip; blame payloads include a deduped commit-body map and skip uncommitted/empty bodies.
- Quick Search collapses an already-open file to one Tabs row with edit/preview view chips, the editor navigation stack is capped at 50 entries, linked git worktrees are identified separately from their parent checkout in session metadata, YOLO risk labels normalize to the canonical display set, and tab-drag timing instrumentation is available behind the opt-in `yolomux.debugDragTiming` local-storage flag.
- README documents the `0.0.0.0` default host behavior, the local-only `--host 127.0.0.1` opt-in, and the canonical YOLO risk-label vocabulary; CLI help also reflects the host default.
- Verification beyond the standard gate: `python3 -m pytest tests/test_filesystem.py tests/test_metadata.py tests/test_yolo_rules.py -q`.

### UI, Theme, Preferences, and Localization

#### DOIT.54 Dockview pane polish and file-drag parity
- Dockview pane separators now use the shared skinny sash line instead of a fat group border; the line thickens only on hover/drag. Inactive Dockview tabs inherit the pane tab-strip background, and Dockview/root/tab/file drag previews use the configurable pane separator color.
- Dockview now lays out to the real host size, renders YOLOmux pane controls in the Dockview header row, shows a full-span root preview before root-edge drops, preserves the root-docked Finder width when tabs move between content panes, and restores Finder/Differ file drags into panes with the same dashed split preview/open-in-editor behavior as the legacy layout.
- Follow-up: default pane spacing is now 3px; the active Dockview tab container is slightly brighter; Dockview tab hover details are restored; root top/bottom tab-drag previews no longer span into the docked Finder/Differ column; and Dockview group padding reserves pane-spacing width so the active ring does not paint over terminal/xterm content.
- Follow-up after rebase: dragging a tab over another Dockview tab now shows a 24px dashed insertion box between tabs instead of a half-tab overlay on top of the target tab; the shared separator hover line is now 5px; pane-edge drops split only the target pane while root-edge/cross-gutter drops still create full-span panes.
- Follow-up after live resize review: Dockview pane-content edge drops now route through YOLOmux `splitSessionAtSlot`, so a second same-axis split preserves `1/2 | 1/4 | 1/4` instead of flattening into equal thirds. Dockview Finder/Differ sash adoption now accepts the new root Finder percentage while preserving nested content split percentages, so moving the Finder/Differ column resizes Pane1/Pane2 proportionally.
- Verification beyond the standard gate: `python3 -m pytest tests/test_browser_layout.py -q -k dockview`.

## 2026-06-08

### Editor, Finder, Differ, and Files

#### DOIT.50 caret/active color parent cleanup
- Collapsed Active color and caret color onto one shared UI color parent with one default caret constant, removed the dead caret-color per-color locale keys, exposed backend setting choice metadata for both color settings, and made Preferences/tests derive caret ordering and labels from the shared parent. The CSS boot default now routes through the active accent token until JS applies the configured caret color, avoiding a second hardcoded Solar gold literal. Verification beyond the standard gate: `python3 -m pytest tests/test_settings.py -q`, `python3 -m pytest tests/test_browser_layout.py -q -k "scrollbar or active_color_radios_recolor_live_pane_chrome"`.

## 2026-06-07

### YO!agent

#### DOIT.47 YO!agent capability boundary
- Grounded YO!agent capability answers in a backend capability inventory. Activity payloads and prompt context state that YOLOmux can read tmux panes, poll sessions, monitor prompts/PRs/files, notify on configured transitions, and keep mutating sends behind admin-only, server-resolved, confirmation-gated paths. Updated default/reset prompt text and locale prompt catalogs, preserved legacy prompt migration, and added regressions for prompt context plus deterministic capability answers.

#### DOIT.43 YO!agent rolling transcript summaries
- Added disabled-by-default YO!agent background transcript summaries: per-session rolling state persists in YOLOmux state, updates from transcript deltas after quiet intervals, prunes dead sessions, pins cheap CLI backends, exposes the refresh interval, and injects cached rolling summaries into YO!agent context. Current builds migrate the old `yoagent.auto_refresh` boolean into `yoagent.refresh_interval_seconds` and keep only the interval visible. Added backend/settings/UI regressions for delta-only updates, idempotent no-op ticks, disabled-by-default behavior, bounded settings, and Preferences visibility.

### Editor, Finder, Differ, and Files

#### DOIT.46 Finder/Differ dark tree readability
- Brightened the Finder/Differ changes tree in dark mode without changing light mode: the shared guide-line token now uses a stronger slate alpha, repo headers use a changes-scoped dark background token, and the light theme resets both tokens to the existing values. Added layout URL guards for the dark guide line, scoped repo-head token, and light-mode unchanged contract.

### UI, Theme, Preferences, and Localization

#### DOIT.48 Startup Tips
- Added lightweight Startup Tips that show one delayed feature tip after app load for admin users with Tips enabled. The tip catalog covers drag/drop, image prompts, YO!agent, YO auto-approval, YOLO rules, `Differ`, Finder Sync/Reload, editor ref diffs, notifications, watched PRs, quick search, Markdown Preview, and rsync for large uploads. The tip uses the existing toast renderer, rotates serially through localStorage, never focuses controls, offers `Next tip`, `Hide this`, and `Turn off Tips forever`, and persists the forever-off choice through `general.startup_tips`. Preferences can re-enable it from General. Added source/runtime guards plus settings coverage.

## 2026-06-06

### Agents and Automation

#### DOIT.44 pending-approval roster badge
- Fixed the auto-approve-off roster path so pending permission prompts are visible without focusing the session. The cheap all-session status path now captures the discovered agent pane instead of the bare tmux session, derives `approval_prompt_state()` from the already-captured visible pane text, and still avoids the expensive hybrid transcript / full-pane prompt fan-out. The session-state classifier also treats `screen.key === "approval"` as `needs-approval`, so the roster lights `EXEC?` even if a future payload misses `prompt.visible`. Added backend coverage for cheap roster prompt visibility and non-active agent-pane targeting, plus a JS classifier regression for screen-only approval state. README now documents the pending-prompt attention badge behavior.

### Editor, Finder, Differ, and Files

#### DOIT.45 editor diff + FROM/TO ref picker on clean files
- Fixed the recurring bug where pressing `Differ` on a git-tracked file with history but a clean working tree showed no FROM/TO sha picker. The clean file's default HEAD-vs-working diff is empty, so `openFileDiffAvailable()` was false and `renderFileEditorPanel` force-exited diff mode back to edit, hiding `.file-editor-diff-ref-panel`. Extracted the force-exit into `diffModeShouldFallBackToEdit(path, state, item)` and gated it on `!fileStateHasUsefulGitHistory(state)`: a file with useful history now stays in diff mode so the picker is reachable and the user can compare arbitrary refs, while untracked / creation-only / outside-repo files still fall back to the editor. Added a JS guard for clean-with-history, no-history, and real-diff states plus a source-grep guard requiring the useful-history clause.

#### Finder/Differ quick roots and summary wording
- Kept the Finder quick-root buttons `~`, `/*`, and `/tmp` present whether Sync is pressed or not, with a browser regression that checks the buttons in both Sync and fixed-root states. The Differ comparison summary now says `{repo count} repos, {file count} files changed in '{session}'` (for example `1 repo, 84 files changed in '5'`) while keeping the existing `+/-/file-count` totals on the right; locale catalogs include the new repo-count phrase.

#### DOIT.39 correction: Finder Sync and diff toolbar cleanup
- Finder Sync now treats the focused tmux session's git root as the fallback working root when changed-file payloads have not arrived yet, ignores stale same-session payloads whose repo set no longer overlaps the focused git root, then expands and highlights affected repo roots/touched dirs once the current session-files payload lands. The Finder root toggle now displays `No Sync` or `Sync`, not `Root`. Finder diff mode now has only one visible Session select, Sort select, date-mode button, and Reload button, and identical session labels no longer render as `5 5`. Long changed-file rows now force filenames to ellipsize before the agent/diff/status/date metadata clips. Verification beyond the standard gate: `python3 -m pytest tests/test_browser_layout.py::test_legacy_changes_url_opens_finder_diff_mode tests/test_browser_layout.py::test_sync_mode_opens_common_repo_parent_and_expands_affected_dirs tests/test_browser_layout.py::test_sync_mode_empty_session_opens_home_not_stale_payload tests/test_browser_layout.py::test_finder_diff_mode_toggle_fills_pane tests/test_browser_layout.py::test_differ_long_filename_ellipsizes_before_date_column -q`.

#### DOIT.39 Phase 3.3 Finder Sync empty session home fallback
- Finder Sync now treats a tmux session with no cwd as an explicit home-root case instead of leaving the previous Finder root or trusting a stale session-files payload. The Sync plan keeps session identity even when `current_path` is empty, falls back to `homePath`, and clears stale affected-repo highlights. Verification beyond the standard gate: `python3 -m pytest tests/test_browser_layout.py::test_sync_mode_opens_common_repo_parent_and_expands_affected_dirs tests/test_browser_layout.py::test_sync_mode_empty_session_opens_home_not_stale_payload -q`.

#### DOIT.39 Phase 3.2 Finder Sync affected-repo highlight
- Finder Sync now highlights affected repo root rows with a stronger token-routed green tint and changed descendant directories with a lighter tint, derived from the same session-files affected set used for Sync expansion. The classes clear when Sync is turned off or the payload no longer matches the active session. Verification beyond the standard gate: `python3 -m pytest tests/test_browser_layout.py::test_sync_mode_opens_common_repo_parent_and_expands_affected_dirs -q`.

#### DOIT.39 Phase 3.1 Finder Sync multi-dir expand
- Finder Sync now computes affected directories from the session-files `repos[]` plus changed-file parent dirs, roots at their nearest common ancestor, expands each affected path, and falls back to the focused repo instead of `/` when a scattered set crosses above `~`. Verification beyond the standard gate: `python3 -m pytest tests/test_browser_layout.py::test_sync_mode_opens_common_repo_parent_and_expands_affected_dirs -q`.

#### DOIT.39 Phase 2 standalone Differ retired
- Removed the standalone Differ/Changes tab wrapper, tab type, menu/palette listing, drag/split references, duplicate session-files state, and standalone-only CSS/locale key. Legacy `changes` / `__changes__` URL tokens now resolve to the Finder pane and preselect diff mode, including virtual-only old bookmarks. Verification beyond the standard gate: `python3 -m pytest tests/test_browser_layout.py::test_finder_diff_mode_toggle_fills_pane tests/test_browser_layout.py::test_legacy_changes_url_opens_finder_diff_mode -q`.

#### DOIT.39 Phase 1 Finder `Differ` mode
- Finder now has a real `files` / `diff` mode flag instead of a hidden stacked Modified-files section. The `Differ` button is prominent, persists the mode, fills the pane with the full Differ chrome in diff mode, hides file-creation controls there, keeps file mode as a full-height tree, and switches changed-directory context-menu actions back to Finder mode before expanding. Verification beyond the standard gate: `python3 -m pytest tests/test_browser_layout.py::test_finder_path_is_first_and_readable_in_wrapped_toolbar tests/test_browser_layout.py::test_finder_diff_mode_toggle_fills_pane -q`.

#### DOIT.38 Q5 Differ long-name date clipping
- Differ/Finder shared file rows now keep the filename as the explicit flex grow/shrink ellipsis column when agent metadata is present, and the agent slot no longer uses an auto margin that can push diff/status/date columns out of view. Verification beyond the standard gate: `python3 -m pytest tests/test_browser_layout.py::test_differ_long_filename_ellipsizes_before_date_column tests/test_browser_layout.py::test_finder_differ_status_badges_share_one_column -q` and.

#### DOIT.38 Q4 blank README diff regression
- Diff panes now wait for the requested diff payload and build CodeMirror in one serialized pass instead of flashing edit mode and racing a re-entrant rebuild. The diff build also catches construction failures and falls back to raw read-only text instead of leaving an empty pane, and explicit FROM/TO refs are pinned for later refreshes so Modified-files diffs do not silently fall back to default refs. Verification beyond the standard gate: `python3 -m pytest tests/test_browser_layout.py::test_readme_diff_waits_for_payload_before_building_codemirror -q`, the Q1-Q4 focused browser test set.

#### DOIT.38 Q3 dark diff added-line fill consistency
- Dark editor diff lines now use opaque red/green fills over the editor background, and the diff-scoped active-line rule keeps the cursor line the same shade as adjacent changed lines. Verified by first reproducing the mismatch in `tests/test_browser_layout.py::test_diff_added_active_line_uses_same_fill_as_neighbor`, then passing that test plus `python3 tools/static_build.py --check` and `node tests/layout_url.test.js`.

#### DOIT.38 Q1 Finder date/reload toolbar alignment
- Finder now groups the shared date-mode toggle and Reload control as a right-aligned `Ago`/Reload cluster, matching Differ while keeping the path and creation/sort controls left-aligned. Verification beyond the standard gate: `python3 -m pytest tests/test_browser_layout.py::test_finder_path_is_first_and_readable_in_wrapped_toolbar -q`.

### UI, Theme, Preferences, and Localization

#### DOIT.41 choosable "Active color" preference
- Centralized the active/focused accent (previously one named-token salad plus ~59 raw `#76b900`/`rgba(118,185,0,α)` literals across six CSS files) into a small `--active-accent` token set (`accent`/`rgb`/`bright`/`text` + derived `dim`/`soft`) and an `--active-control-*` layer, with the raw literals swept to route through it; Green stays the exact default so the refactor is a visual no-op. Added Preferences → Appearance → Active color (`green|blue|orange|yellow|purple|white`) as a live-applied `select`: `applyActiveColor` writes the per-theme accent vars on the active theme and re-applies on theme switch, with i18n across all 13 locales + en-XA. The YOLO marker stays exactly green (not routed through the accent) and editor syntax greens stay out of scope. White is the one preset whose light-mode ring (`#9aa5b3`) diverges from its fill (`#dfe5ec`) so the focused pane still reads on a white panel. Shipped on `main` as `9b0f5d5 Recolor active chrome and polish preferences`. (The standalone `doit-41-active-color` worktree branch was a less-complete parallel take — its Phase-1 literal sweep was unfinished — and is superseded by the mainline version.) Verification beyond the standard gate: the hardened active-accent browser tests.

#### DOIT.38 Q2 dark active pane tab-container hover cue
- Dark mode now gives the active/hover-selected pane tab container a lighter token-derived background while light mode keeps the existing strip color. Verification beyond the standard gate: `python3 -m pytest tests/test_browser_layout.py::test_active_pane_tab_container_lightens_in_dark_only -q`.

### Build, Refactor, Docs, and Tests

#### DOIT.42 test-suite cleanup imported
- Imported the `doit-42-test-cleanup` worktree as a focused test-suite cleanup on top of the active-color mainline: Python source-grep tests now use behavioral or inspect-based assertions where appropriate, Selenium browser setup is module-scoped with cleanup, shared config/git/auth fixtures live in `tests/conftest.py` and `tests/_git_helpers.py`, real retry sleeps were removed from the auto-approve worker tests, tmux-utils tests moved to `test_tmux_utils.py`, active-accent browser assertions are relationship-based, pane-tab width checks are parametrized, the removed strip-hover token guard is a cheap CSS test, `web.py` has direct escaping/bootstrap coverage, and the TODO diff-overview browser test now waits for its async CodeMirror metrics under xdist load. Finished the remaining small checklist items by deriving JS pixel expectations from harness geometry, loosening a full session-files payload equality to field assertions, parametrizing visible-agent-working detector cases, adding GitHub/Linear request-building coverage, adding behavioral layout-param/deep-link edge coverage, and exercising real `do_GET`/`do_POST` server route dispatch. The DOIT.42 retrospective rejected the broad JS source-grep deletion, broad Selenium-to-vm migration, and full `node:test` migration as harmful or too risky for this pass after inspection. Verification beyond the standard gate: focused affected pytest files.

### Mixed and Cross-Cutting

#### DOIT.40 correction: inactive-pane gradient removed
- Removed the inactive-pane directional gradient after live review showed it still did not read as a gradient. The app keeps the flat inactive-pane dim, inactive-pane opacity slider, and stable visual-active pane state, but the gradient setting, Preferences row, CSS gradient tokens/rule, body class, JS direction helper, and gradient-specific tests are gone. This is deferred until it can be debugged with an explicit overlay visual. Verification: standard gate green.

## 2026-06-05

### Editor, Finder, Differ, and Files

#### DOIT.38 diff overview and inactive-pane gradient archived
- Completed and removed `DOIT.38.md`. The shipped work replaces chunk tick DOM with one CodeMirror-derived diff overview gradient, hides the overview when unchanged regions are collapsed, keeps red and green row bands non-overlapping, and tests the actual `TODO.md` diff repro plus generated large replacement chunks.
- Finished the follow-up pane behavior from the same DOIT: Finder, Differ, CodeMirror, and terminal scrollbars stay neutral until their own pane/scroll surface is hovered or focused; terminal WebSocket close now roster-confirms and prunes confirmed-dead sessions immediately.
- Added the remaining inactive-pane gradient feature: an Appearance toggle, default-on setting, localized Preferences row, slot-based direction helper, setting-gated CSS override over the existing flat dim fallback, and regression tests for the direction map, disabled state, and no-focus fallback.

### UI, Theme, Preferences, and Localization

#### DOIT.37 GUI consistency batch archived
- Completed the DOIT.37 cleanup/refactor batch and removed `DOIT.37.md`. The shipped work covers timing/token consolidation, fetch/storage helpers, file-state helpers, split-pane tokens, shared button bases, transcript normalization, duplicate-function linting, typed payload schemas, compact notifications, and TLS redirect cleanup.
- Brought Finder, Differ, and Modified-files closer to one behavior model: shared date-cycle vocabulary (`None`, `Date`, `Ago`), shared sort vocabulary (`A-Z`, `Z-A`, `new`, `old`), stable status/date placement, aligned tree-row icon columns, and Finder/Differ toolbar controls using the same visual rules.
- Tightened git editor affordances: blame and diff controls now appear together only when useful git history exists; untracked/no-history files such as `DOIT.37.md` do not show misleading blame/diff actions; same-ref/no-diff views render the file normally instead of a dead "No diff" pane.
- Fixed pane-focus and editor regressions from the live GUI audit: active green borders are stronger in both themes, dark inactive panes are lighter without changing the accepted light-mode dim, tab back-history collapses A→B→A→B into A→B, and CodeMirror Word Wrap reconfigures live without blanking the editor.

### Build, Refactor, Docs, and Tests

#### TODO and docs cleanup
- Updated `README.md` for the current editor/git-action behavior and the universal Finder/Differ date and sort controls.
- Moved completed checkbox work out of `TODO.md`; it now carries only open items plus roadmap/reference context. The one open nested follow-up under the completed session-roster item was preserved as an explicit optional event-driven session-prune item.
- Added the 2026-06-05 AGENTS.md learnings around visual consistency, shared helpers, runtime browser checks, and git-action gating.

## 2026-06-04

### UI, Theme, Preferences, and Localization

#### GUI and localization polish
- About is now a localized modal at the bottom of Help: the large brand glyph spins, Chinese uses `優` / `优` and `樂` / `乐`, Close is the visible `X`, `SHA` remains literal, and Keiven Chang links to LinkedIn.
- Localized the upload/attention toast shell, upload result messages, YO!info / YO!agent Chinese labels, user-facing PR/session status labels, fallback relative-time strings, `Refresh summary`, git/status popover text, and info-table empty/header/sort strings. Added tests so Chinese surfaces do not regress while protocol terms like `SHA`, `PR`, `CI`, `HEAD`, `git`, and `tmux` stay literal where intended.
- Recorded the vendored CodeMirror package versions and pinned the bundle dependency record so future CodeMirror upgrades are explicit.

### Build, Refactor, Docs, and Tests

#### DOIT.36 audit batch archived
- Completed and removed `DOIT.36.md`. The shipped fixes cover terminal URL hard-wrap false-joins, login-test locale isolation, block editor cursor default, Preferences control alignment, Finder/Modified-files behavior parity, repo-scoped Modified-files diff refs, explicit-session Modified-files targeting, Modified-files sorting/header polish, multi-repo pane metadata, Finder delete shortcut, indexed-search state cleanup, first-drag pane-move latency, app-menu detail-width measurement, and the Finder/Modified-files resizer token reuse.
- `TODO.md` no longer lists DOIT.36 as active; the remaining live queue starts with the non-DOIT shipped-feature follow-ups and the larger Big-Bang items.

## 2026-06-03

### Agents and Automation

#### YO ball spins on □/✓ task lists + auto-focus records nav history (DOIT.35)
- C1: a session actively working with a Ctrl-T task list showed the YO ball NOT spinning. This Claude version renders task rows with `□` (U+25A1) / `✓` (U+2713) / `✗` / `◯`, but `prompt_detector._is_prompt_trailing_ui_line` only knew the U+2610 ballot-box family — so the task rows read as new output, `visible_agent_working` flipped to False, state went `idle`, and the spin stopped. Extended the task-row glyph class + `startswith` tuple to include `□✓✔✗✘◯`. Now the task list reads as prompt-trailing chrome and the ball spins while the agent works. Regression test covers the `□`/`✓` rows + the old `☐` form.
- C2: auto-focus didn't record back/forward history (only `userInitiated` activations did), so an auto-focus jump was invisible to Back. Added a debounced `recordAutoFocusNav` in `setFocusedPanelItem` (gated on `autoFocusEnabled`): it records the focus that LANDS after a ~500ms dwell, so rapid auto-focus flapping doesn't flood the stack and a back/forward re-activation no-ops via the consecutive-dedupe. Node guard covers the wiring.

#### Auto-approve: `extract_command` read the wrong (stale) command (DOIT.17)
- A Bash permission prompt was detected as `approval`, but `extract_command` walked its backward `● Bash(…)` search past the `─────` separator and the prior `● Done.` into the PREVIOUS step, returning that step's stale (safe) `chmod` instead of the pending `cp -r src/ dist/`. The worker then classified danger on the wrong command — a real safety hole (a dangerous `cp -r` approved on the strength of an unrelated safe command) and a cause of ASK? not lighting under auto-approve. Fix (detector only — the mock correctly matches real Claude): bound the `● Bash(…)` search to the current prompt block (stop at the separator/box-top), stop the box-body path at the tool description instead of folding it into the command, anchor `_SKIP_LINE` as `Bash command\b` so the `(unsandboxed)` header is always skipped, and make the Codex path accept an un-prefixed command line. Regression test (`test_extract_command_does_not_cross_separator_into_prior_step`) added; detector+worker suites 60 passed. Remaining is user-gated live validation (auto-approve ON/OFF on a real agent screen), tracked in TODO.

#### Auto-approve (YOLO) reliability
- Claude `PreToolUse` permission hook shipped (DOIT.11): `yolomux_lib/claude_permission_hook.py` decides allow/deny/ask from the agent's structured request via the existing `yolo_rules` engine (no TUI scraping, no keystrokes), with 17 tests pinning the mapping + the hard-floor + fail-safe. **Remaining is user-gated / deferred and tracked in TODO Big-Bang #1**: the manual `~/.claude/settings.json` install (must not be auto-edited), live validation, the keystroke-worker stand-down (gated on the hook being live), and the Codex `app-server` re-architecture.

### Tmux, Terminal, YO!share, and Tabber

#### Session project follows the live pane cwd, not the session-number default (DOIT.32)
- `cd`-ing a tmux pane into a different repo didn't change the displayed project — `candidate_session_cwds` listed the static session-number default workdir (session "1" → `~/dynamo/dynamo1`) FIRST, so `session_git_inventory` returned it before ever reaching the pane's live `cd`'d path. Reordered so the focused pane's live cwd (then agent/other-pane cwds) wins, and `session_workdir`/`numbered_session_workdir` are FALLBACKS used only when no pane/agent sits in a repo (a fresh shell in home still shows the dynamoN convenience default). Now the project/branch/PR follow the pane within one metadata poll. pytest covers the live-cwd-wins ordering + the fallback.

#### Dark terminal: light-on-white agent composer forced readable (DOIT.30)
- The Codex composer (light text on an ANSI-white input box) was white-on-white in the DARK terminal theme because `terminalMinimumContrastRatio` returned 1 (no adjustment) for dark — so xterm left the ~1:1 cell alone. (Light already used 4.5, which is why a light terminal auto-darkened it.) Confirmed the white box is real Codex's own composer, not the local mock renderer. Fix: dark now returns a moderate 3:1 floor — enough to force the composer to a readable foreground, low enough that intentionally-dim dark-palette text (already ≥3:1) is mostly untouched; light stays at the stricter 4.5. Node guard updated (dark 1→3).

### Editor, Finder, Differ, and Files

#### Code-audit follow-ups: readonly policy, blame lifecycle, watch-list logging, TODO drift (DOIT.34)
- #1 `/api/blame` bypassed the readonly guard (only `/api/fs/*` was blocked for readonly), so a readonly identity could read repo file history. Added `/api/blame` to the admin-only GET gate; `test_login_auth` now asserts readonly gets the same FORBIDDEN as `/api/fs/list`.
- #2 The readonly file UI offered Download / open-image even though the server 403s every `/api/fs/*` read (readonly = terminal-only). Disabled `downloadDisabled`/`openInNewTabDisabled` in readonly so the UI matches the server; node guard updated (readonly download/image-open now disabled).
- #3 Inline blame didn't annotate a file opened AFTER the toggle was already on (the ViewPlugin built before any fetch populated the cache). Added `ensureEditorBlameForPath` — on a text-editor render with blame on and the path uncached, it fetches (now deduped per path via `editorBlameFetches`) then dispatches a selection-preserving transaction so the blame plugin recomputes; the annotation appears without a manual toggle.
- #4 Watched-PR truncation logged `watched_pr_truncated` on EVERY poll. Now tracked by a `(count, refs)` signature and logged only when the capped state changes (test asserts: log once on first cap, no re-log on an unchanged poll, new log on a changed count).
- #5 TODO.md still listed DOIT.29/30/31 (and the rest of DOIT.13–33) as ACTIVE BATCHES while DONE.md archived them. Rewrote the orientation: ACTIVE BATCHES is now "none — DOIT.11–33 complete/archived; only DOIT.11's hook wiring (user-gated) remains," matching the archive.

#### Finder: symlink arrow badge + target tooltip (DOIT.31)
- Symlinks rendered identically to regular files/dirs. Backend `_entry_info` already detected symlinks (`is_symlink`, target-resolved `kind`, `kind="symlink-broken"` for dangling); added `symlink_target` via `os.readlink` (best-effort). Frontend: the file-tree row now toggles `is-symlink` / `symlink-broken` and sets a native `name → target` title (broken flagged), keeping the target-type icon (a linked dir keeps the dir icon, a linked `.py` the code icon) and overlaying a small `↪` arrow badge via `.file-tree-icon::after` — `--muted` normally, `--bad` (red) + struck-through name when broken. All colors via tokens. New `finder.symlink.broken` key in 13 locales. pytest covers the backend (dir/file/broken targets + non-symlink unflagged); node guards the row toggles + title + CSS badge.

#### Terminal: an agent hard-wrapped URL links across both rows (DOIT.27)
- A long URL that Claude's TUI HARD-wraps with a hanging indent (`…/runs/26` then a separate indented `919558600/job` row) was only linked on its first row — the continuation is its OWN logical line (`isWrapped === false`), so `terminalWrappedLineGroup`'s soft-wrap sweep missed it and the regex matched only the truncated `…/runs/26`. Fix (`10_core_utils.js`): the group walk now also stitches hanging-indent URL continuations — backward sweep extends past an indented row whose previous row's tail is an unterminated url token, and the forward build, while the joined text still ends mid-URL, pulls in following indented URL-valid rows (indent stripped for matching, recorded so the underline maps to the row's REAL columns). Gated tightly (only EXTENDS a url token running off the right edge; `terminalTailIsUnterminatedUrl` + a leading-whitespace-then-URL-char shape check) so prose and adjacent independent lines are never merged. The whole URL is now ONE clickable link, underlined across both rows, active from either row. Existing true-soft-wrap behavior preserved. Node tests cover the hard-wrap stitch (both query rows + the no-merge prompt row) and the prose-not-merged / standalone-indented-URL cases.

#### Editor back/forward moved to the global topbar (DOIT.21 follow-up)
- The ← / → editor history buttons were relocated from the per-editor-pane toolbar to the GLOBAL topbar, immediately left of the universal search box (one file-history control for the whole window, like a browser's). `createTopbarNav()` builds `#topbarNavBack`/`#topbarNavForward` (wired to `editorNavBack/Forward`); `updateEditorNavButtons()` now toggles their `disabled` from the global `editorNav` stack instead of per-pane; the per-pane `.file-editor-nav-control` template, handlers, separator slot, and CSS were removed. Nav + search center as a PAIR (`margin-inline-start:auto` on the group + `margin-inline:6px auto` on the search; logical props so RTL mirrors). Node guards updated (#29 centering + the relocation/wiring assertions).

#### Modified-files "M" badge legibility (image 046)
- The Modified status badge used the theme-aware `--accent-gold`, which flips to a dark brown (`#b7791f`) in light mode — near-black "M" on dark brown read as muddy/illegible. Switched `.changes-status-m` to the fixed bright amber `#f5c542` (matching the other vibrant fixed status-badge fills), so the near-black glyph pops in both themes.

#### AI/markdown: bare URLs become links + right-click Copy URL (DOIT.15)
- Bare http(s) URLs in rendered markdown (YO!agent replies + file previews) are now real `<a>` links: `renderMarkdownPreviewInto` sanitizes into a fragment, then `linkifyBareUrls` walks text nodes and wraps URLs in safe anchors (`target=_blank rel=noreferrer noopener`), reusing `markdownPreviewUrlAllowed` so only safe schemes link. Skips text already inside `<a>/<code>/<pre>` (code samples and existing links untouched) and drops trailing sentence punctuation. Version-proof against marked's GFM autolink missing them under the per-line source anchors.
- A long URL now wraps as ONE `<a>` (`.markdown-body a { overflow-wrap:anywhere; word-break:break-word }`) instead of shattering into plain text across lines — selectable/copyable as a unit.
- Right-click menu on links: a 4th `linkContextMenu` controller + `showLinkContextMenu`/`installLinkContextMenu` (bound in `renderMarkdownPreviewInto`) offer "Copy URL" / "Open URL", localized in all 13 locales. Verified the shipped `linkifyBareUrls` against sample DOM (2 URLs linked incl. a 90-char consent URL as a single node, code-block URL skipped, trailing dot dropped, `overflow-wrap:anywhere` computed).

#### Editor inline git blame, Popular IDE-style (DOIT.26)
- A toolbar toggle (`.file-editor-blame-panel`, green when on, edit-mode-only, persisted `fileEditorBlameEnabled`) shows a dim annotation at the END of the caret's current line: "Author, <relative time> • <summary> (#PR)". Backend `/api/blame?path=&ref=` runs `git blame --line-porcelain`, parses per-line `{sha, author, time, summary, pr}` (PR from `(#N)`, all-zero sha → "You"/"Uncommitted changes"), cached per (path, HEAD, mtime, ref). Frontend: a CodeMirror ViewPlugin decorates the caret line (recompute on selectionSet/docChanged) with `data-blame` + a `title`; the annotation renders via a `.cm-line[data-blame]::after` (no WidgetType in the bundle), colored by the theme-aware `--code-comment` token (the same gray common in Popular IDEs), `user-select:none` so it's never copied as code. Blame is fetched lazily on toggle, re-rendering open text editors. Locale key in 13 locales. Backend unit-tested (parser + real-file blame); annotation/button verified via probe. Hover is the native `title` with the full commit (a styled popover is a deferred follow-up — the bundle exposes no `hoverTooltip`).

#### Modified-files panel: collapsible repo headers (DOIT.23)
- Repo group headers in the Changes panel (`~/ai-config`, `~/dynamo/frontend-crates`, …) are now collapse toggles, mirroring the uploaded-group toggle: a `<button>` with a `▸/▾` caret, per-repo collapse state persisted by path (`changesRepoCollapsed`), the file tree omitted when collapsed. Folder-level collapse still works independently. Light + dark.

#### Quick-open palette: one row per file, not duplicate editor+preview rows (DOIT.22)
- A file open as both an editor tab and a preview tab showed TWO identical palette rows (same name + path) — `commandPaletteAllTabItems` deduped by layout-item ID, and the editor item (`fileEditorItemFor`) and preview item (`filePreviewItemFor`) are distinct IDs. Now `commandPaletteCommandItems` groups file tabs by PATH and emits ONE `file:<path>` row, surfacing the open views as `edit`/`preview`/`diff` chips (via `editorViewModeFor`); selecting it focuses the editor view. Non-file tabs (sessions, Finder/Info/Prefs/Changes) stay one row each. Chips reuse the existing `editor.mode.*`/`editor.diff` labels (no new keys) and read in both themes (contrast 7.0 light / 11.9 dark). Node test guards the grouping; chips informational for now (clickable-to-jump + RECENT-row suppression are confirm-with-user follow-ups in TODO).

#### Just-updated recency pulse reuse
- Finder, Differ, and Tabber now pulse the `.file-tree-date` cell only for the new `just-updated` `<15s` recency tier in Ago and Date modes, using the shared `.attention-pulse` parent (`attention-ring-fade`, `--red-reminder-duration`, `--red-reminder-easing`, and `attentionAnimationDelay`) instead of the old standalone `file-tree-recency-pulse` one-shot. Rows settle into the existing graduated recency colors after the pulse window, and selected/current rows suppress the pulse so their colors win. Verified with `python3 tools/static_build.py`, focused Node suites for editor preview / Tabber / share theme, and `python3 tools/check.py`.

#### Editor back/forward navigation (DOIT.21)
- Popular IDE-style editor history: ← / → buttons at the far left of the editor toolbar walk back/forward through visited files. One global `editorNav` stack records every user open via `openFileInEditor` (dedupes consecutive same-file; a new open after Back drops the forward tail; recording is suppressed during a back/forward re-open). Back/forward re-open through the normal open path (reopening a since-closed file). Buttons show once there's history and disable at the ends; the nav group keeps the toolbar visible in preview mode too. Localized in 13 locales; node test covers the stack push/dedupe/truncate/navigating-suppression logic.

#### Find panel, Preferences/editor (DOIT.14, DOIT.16)
- Find-panel toggle chips robustness (DOIT.14): the overlap was already gone (the `.cm-panel` specificity override hides CM's native text), but added belt-and-suspenders — `overflow:hidden` clips any un-collapsed text, and the chip glyph is now bound to the checkbox `name` (`:has(input[name="case"/"re"/"word"])`) instead of `:nth-of-type`, so a CodeMirror control reorder can't mis-map the Aa/.*/word glyph.
- Editor Font Size no longer flips a Preview/diff pane into an editor (DOIT.16 C1): `applyEditorWrapPreference` + `setDiffExpandUnchanged` now re-render each panel with its OWN `panel.dataset.layoutItem`, not the editor item. Preview already tracks `--editor-font-size` (C2, no change needed).
- Inactive-pane dim now covers the full pane when scrolled (DOIT.16 C3): the dim is a `.panel-overlay-root::after`, but `.preferences-body`/`.changes-body` were both the overlay-root AND the scroll container, so the dim scrolled away. Decoupled — the overlay-root is now a non-scrolling flex layer (`overflow:hidden`) and scrolling moved to an inner `.preferences-scroll`/`.changes-scroll` (scroll-position preservation retargeted). Added the "overlay-root must not scroll" convention comment + node guards. Finder/Info/editor-body were already correct (overflow on inner children).
- Preferences: "Finder font size" row moved to sit right after "Editor font size" (DOIT.16 C4).

#### Diff
- Diff "expand / collapse all unchanged" toolbar toggle (DOIT.12 B4): a diff-only button (persisted `diffExpandUnchanged`, green when on) that shows full context (omits `collapseUnchanged`) or restores the collapsed runs, in both side-by-side and unified diff; the config signature includes it so toggling rebuilds. Localized in all 13 locales.
- Diff overview-ruler ticks were misaligned and went stale after expanding an "N unchanged lines" fold (DOIT.12 B3). Now positioned from the editor's rendered geometry (`lineBlockAt`/`contentHeight`, so they track folded space; line-fraction fallback before first measure), click-to-scroll jumps to the chunk's real rendered top, and a CM `updateListener` rebuilds the ticks on any geometry/height change (incl. fold expand/collapse) on both the unified and side-by-side views.

### UI, Theme, Preferences, and Localization

#### Menu dropdowns: width/wrap fix, new-session labels, Term always offered (images 055/056)
- App-menu detail lines wrapped (`white-space: normal`), making each item 3-4 lines tall AND keeping the menu narrow (a wrapping block's max-content is ~one word, so `fitAppMenuPopover` computed a tiny width). Made `.app-menu-detail` single-line + ellipsis (`max-width: 42ch`), so the fit-width grows to the longest detail → menus are wider and much shorter; full text on hover via a `title`. Shortened the verbose "Needs me first" sort detail. Fixes both the View ▾ and tmux ▾ menus.
- New-session items dropped the useless "+" prefix (label is just "Claude" / "Codex" / "Term"); when launchable, the detail now shows the params actually passed (e.g. `--dangerously-skip-permissions`, `--dangerously-bypass-approvals-and-sandbox`) via a new `agentLaunchCommands` bootstrap map (`agent_command(agent, dangerously_yolo)` from the server) + `agentLaunchParams`.
- Term is now always offered (a plain shell is always launchable) instead of greyed "unavailable" — `available_agent_commands()` always includes `term`, so both the menu and the launch validation accept it.
- pytest covers the launch-command bootstrap + Term-always-available; node guards the no-"+" label + params detail.

#### Menu-bar: View → Sort + [S]-item reconciliation (P0)
- View → Sort tab list: Default / Needs me first / By name ordering for the Tabs ▾ navigator.
- Reconciled the P0 menu-bar [S] items: File ▾ / Tmux ▾ / Help ▾ already satisfy the design tree (ticked); the proposed Settings ▾ was decided against (folded into View ▾). View ▾ remains partial (a Panel-tabs visibility toggle + an Inactive-tabs control are the last gaps).
- NOTE: a per-pane left dropdown (a `▾` caret before each pane's tab strip) was built the same day then REVERTED at the user's request — the caret took its own line above the tab strip (wasted vertical space) and wasn't wanted; a node guard keeps `.pane-tabs-menu-caret` absent.

#### Pane/Tab search finds a session by an other-branch PR (DOIT.33)
- Pane/Tabs search couldn't find a session by a PR (e.g. `#10289`) that lives on one of the repo's OTHER branches — `tabSearchFields` only indexed `displayPullRequest(info)` (the current/default-branch PR). Extended it to also fold in `info.project.git.other_branches.branches[]` (the same data YO!info renders, already in the metadata payload — no extra fetch): each branch's name, PR title, PR number in every typed form (`#N`/`PR#N`/`PR N`/`N`, via a shared `prNumberSearchForms` helper), and Linear IDs. Searching `#10289`, `10289`, the branch name, or `DIS-2193` now surfaces the session. Node test covers the other-branch PR/branch/Linear indexing + fuzzy match.

#### YO!info color audit: sub-tab band + current-branch row (DOIT.28)
- C1: the `YO!info | YO!agent` segmented toggle band hardcoded a near-black `#11151d`, so it was a black slab on the light pane. Routed `.info-subtabs` background through `var(--panel2)` (flips light/dark); the inactive-hover white tint (invisible on light) became a token tint `color-mix(in srgb, var(--text) 8%, transparent)`; the active pill text uses `var(--pane-tab-active-text)` for consistency. C2: the current-branch row used a dim navy `#161f2c` that barely stood out on the dark table, while light mode already overrode it to the selection token. Made the base `.info-row.current` rule reuse the shared selection tokens for BOTH themes (`--file-selection-bg`/`--file-selection-text`) + an `inset 3px 0 0 var(--file-selection-border)` left accent bar (mirroring the Finder's selected-file), and deleted the now-redundant `body.theme-light .info-row.current` override. Current branch is a prominent green selection in both themes; no per-theme hardcoded overrides remain. Light-mode browser contrast guard (`info-cur`) still green.

#### YO!info table light mode (DOIT.18 C1)
- The YO!info table hardcoded dark-mode colors with NO `body.theme-light` override (missed in the AUDIT-LIGHTMODE sweep), so on the white pane the non-current rows were near-white-on-white (invisible), the header/current rows were dark bars, and the PR/branch links were low-contrast pastels. Added light overrides: rows use `--text` on the white surface, the header uses `--muted` on `--panel2`, the current row uses the pale `--file-selection-bg` tint (not a dark navy bar), and the links/`pr-status-*` get darker light-tuned variants (link #1d4ed8, passing #1a7f37, failing #c0143c, merged #8250df, closed #57606a). Verified contrast 4.8–15.4 on the light pane; added the info table to the `test_browser_layout.py` light-mode guard fixture. (C2 — a "black line below the tab bar" — was investigated and is terminal CONTENT, an agent/mock `─` rule rendered dark-on-light, not a chrome bug; a live confirm is tracked in TODO.)

#### Transcript / AI-Transcript / event-log → shared theme tokens, not hardcoded hex (DOIT.25)
- These three views were dark in light mode because they hardcoded hex instead of consuming the shared theme tokens that flip via `body.theme-light`. Routed `.transcript*`, `.summary*`, and `.event-*` through `--bg`/`--panel`/`--panel2`/`--text`/`--muted`/`--line` — so they now flip light/dark automatically with ZERO per-view overrides (the AUDIT "Durable Fix A" applied; supersedes the per-component-override style). The 6 per-type transcript tints keep their accent border but derive the bg via `color-mix(in srgb, <accent> 12%, var(--panel))`, so the tint is pale on light / deep on dark with no theme rule. The event-log command now wraps across lines (`white-space:normal; overflow-wrap:anywhere`) instead of one-line ellipsis, with the time/type columns top-aligned. Verified light: bg lum 248, dark text lum 31, pale tint, command wraps to 5 lines.

#### Adjacent pane rings touch at the seam + ring opacity default 50 (DOIT.24)
- DOIT.20 had moved the pane ring to a content-edge `.panel::after` overlay, which put an active (green) pane's ring a full gutter away from a neighboring needs (red) pane's ring (~6px white gap, image 038). Reverted the ring to the `--pane-split-gap` GUTTER BORDER, colored per state by the unified `--panel-ring-color` and faded by `--pane-ring-opacity` (kept from DOIT.20). Because it fills the pane's outer edge, an active pane and a needs pane now meet across the 1px resizer — they touch. Still one marker (no "red inside green"); the needs pulse animates the border-color. Verified: active green @0.5, needs red pulsing, active+needs red, inactive transparent, all 4px. C2: ring opacity default 70→50 (settings + JS fallback + pins). The DOIT.20 TODO eyeball note anticipated this exact "revert to the gutter border" call.

#### Pane rings → one translucent overlay + opacity setting (DOIT.20)
- The green/red/white pane rings are now ONE translucent overlay drawn over the content edge (`.panel::after`), colored by the per-state `--panel-ring-color` and faded by a new `--pane-ring-opacity` (Preferences "Pane ring opacity", 20–100%, default 70). The focus ring is no longer the real border (the transparent `--pane-split-gap` border is kept purely for inter-pane spacing). One ring permanently removes the "red inside green" nesting — a focused needs-attention pane just resolves `--panel-ring-color` to red. Default ring width 4px (`--pane-tab-panel-ring-width`) and pane spacing reverted to 4px. Verified per-state via probe: transparent (inactive), green @70% (active/typing-ready/preview), red pulsing (needs), red for active+needs and typing+yolo+needs (attention beats the yolo-ready green). Node guards updated for the overlay model; test pins moved 2→4.

#### YO!info merged-PR link purple in light mode (DOIT.19)
- A MERGED PR link in the pane detail bar / popover / summary / branch-meta rendered the generic light-mode blue (`body.theme-light .meta a` out-won the dark `.meta a.pr-status-merged{#c084fc}` on a same-specificity tie). Added `body.theme-light …a.pr-status-merged { color: #8250df }` (0,3,1) so it reads GitHub-merged purple, matching the purple MERGED tab badge; OPEN/passing/failing keep their colors. Verified: merged `rgb(130,80,223)`, passing stays blue.

#### Setup screen language picker (DOIT.13)
- The first-run setup ("Set up YOLOmux … Waiting for auth.yaml changes") screen now has the same endonym-labeled language picker as the login screen / topbar. Factored a shared `locale_field_html(current, css_class)` (login + setup), positioned `.setup-locale` top-right (RTL-aware via `inset-inline-end`). The screen was already localized server-side (Steps 1/5/6, prior commit); this adds Steps 2–4: the picker carries the choice in a short-lived `yolomux_locale` cookie (NOT a settings write — the screen is pre-auth/pre-users), `request_locale_pref()` resolves cookie → saved `general.language` and feeds both setup and login serve sites, and the existing post-sign-in `save_login_locale` makes it permanent (Max-Age=600 backstop caps a stale cookie). Verified: picker reflects the locale (ja/ar selected, ar RTL), cookie carrier resolves correctly, invalid cookie falls back to the saved pref.

#### Pane chrome — one shared, state-driven theming system (no per-button/per-bar one-offs)
- All pane-head BARS (tab strip, Info Bar, editor toolbar, CodeMirror find panel) read one `--pane-bar-bg`: the bright tab-strip green when the pane is focused, neutral gray when not — so the Info Bar/find no longer stay green when unfocused (DOIT.12 B5 / images 010/011).
- All control BUTTONS (terminal/agent pill, window-step, actions, info toggle, minimize, expand `+`, close) read `--pane-ctl-*`: rest = white (light) / near-black (dark), pressed/active = green. Killed the navy active-tab and always-green `+` one-offs (DOIT.12 B5 toolbar `.tab.active`; image 009). The agent pill is green when active, `+` is white/black.
- Light-mode tab text fixed (was white-on-white): `--pane-tab-text` dark in light, plus a dark override for the `.session-button-name/dir/detail` spans that hardcoded near-white. Added a headless contrast guard (computed text color vs tab bg) — the gap that let it ship was a browser test measuring backgrounds but never nested text colors.
- Inactive-pane dimming is ONE CSS rule keyed off the uniformly-toggled `.focused-pane` class; deleted the per-pane JS overlay + `isVirtualItem` special-casing (DOIT.12 B2).

#### Topbar
- The "big space" above the green tabs was the topbar's own cool-gray background showing where the shorter pills sit centered in a too-tall bar (DOIT.12 A1/A2). Painted the topbar the green tab-strip color (`--pane-tab-strip-bg`) in both themes so it's one continuous green with no band, tightened its height (`+4px`, `line-height:1` on the brand title), and completed the Notify-toggle active state. Guard in layout_url.
- Topbar light-mode coloring sweep (DOIT.12 A4): defined `--accent` per theme (was undefined → literal light-blue hover hairline in both themes); and light overrides for the menu-count badge halo, the near-black `.transport-warning` tooltip, the garish `#tabMetaToggle.active` green (→ `#5f9800`), and the invisible menu row separator.

#### Light mode — full surface sweep (DOIT.12 B5, all HIGH items in AUDIT-LIGHTMODE.md)
- Cleared every HIGH "dark box / invisible text on white" bug. The root cause was component rules hardcoding a dark color literal with no `body.theme-light` / `body.editor-theme-light` counterpart, so the dark value rendered on the white surface. Fixed surfaces: the whole session/tab/file hover popover (near-black-green box + pale-green text → light card, dark text, re-tinted the label/value/desc/legend cluster — the audit's "highest single fix"); the neutral session-state badges + inactive YO marker (dark-slate chips → light gray); the command palette + keyboard-shortcuts dialog (were dark-on-dark: dark fill with theme-aware `--text` → light cards, dark rows/kbd); the global-reset warning bar (cream-on-brown wash → light amber tint, dark amber text); the `.agent-icon` glyphs (white → dark); the drop-target fill, the file-missing tab (dark maroon → light error tint), the server-update banner (dark navy slab → white), the file-tree current/repo-non-main/indexed rows (near-white-green / washed-amber text → darkened) + a visible selection ring; the rename input (near-black fill with `--text` → white); YO!agent markdown code blocks (`#0b0e14` → light); and in the light editor: the image backdrop + transparency checkerboard (dark/inverted → light), the diff overview-ruler ticks + inline diff decorations (saturated/dark → matched to the light `--code-diff-*` fills).
- Made it durable: two headless `test_browser_layout.py` guards (`test_light_mode_surfaces_are_readable_not_dark_boxes`, `test_light_editor_image_backdrop_is_light`) build each surface in light mode and assert backgrounds are light AND nested text meets a real contrast ratio (≥3.0). This closes the exact gap that let the white-on-white tab text ship — the prior tests measured backgrounds but never nested text colors. Verified live with a headless computed-color probe across all surfaces (every fixed text 5–15:1 on its surface). The pane-head `.tab.active` and white-on-white tab text were already cleared via the pane-chrome refactor (above); CSS brace/parse enforcement (audit Durable Fix C) shipped as `check_css_braces()`.

### Build, Refactor, Docs, and Tests

#### Build / structural
- `10_topbar_menus.css` had a TRUNCATED `.notify-toggle.active {` rule whose body was split into the next partial — it only rebalanced by accident in the bundle (DOIT.12 B1). Completed the rule, removed the orphaned body, and added a `check_css_braces()` build step that fails on any brace-unbalanced CSS partial (with a regression test). The check immediately caught the latent split.

### Mixed and Cross-Cutting

#### Watch explicit PRs independent of sessions + notify on merge/CI/review (DOIT.29)
- YOLOmux only tracked PRs tied to an open session's branch and only displayed status. Added a PR watchlist that polls arbitrary PRs and notifies on transitions. Backend: a `github.watched_prs` list setting (`owner/repo#N` or a full PR URL) + a separate, longer `performance.watched_pr_refresh_ms` (default 60s, 15s–600s) so a big watchlist doesn't burn the GitHub rate limit; `notifications.notify_transitions` now also accepts `pr-merged`/`pr-ci-failing`/`pr-review` (new `PR_TRANSITION_KEYS`/`NOTIFY_TRANSITION_KEYS` allowlist). `metadata.parse_pull_request_ref` normalizes refs/URLs → canonical `owner/repo#N` (rejects non-github hosts, issue URLs, PR #0); `metadata.watched_pr_metadata` dedupes, caps at `WATCHED_PR_LIMIT=20`, fetches each via the existing `github_pull_request_by_number` (shared metadata cache), and reports `truncated`/`invalid`. `app.watched_prs_payload` (logs once when capped) is served at `GET /api/watched-prs`.
- Frontend: polls `/api/watched-prs` on its own runtime interval; a "Watched PRs" section in YO!info (own `#info-watched` container, reuses the `pr-status-*` badges) with a remove (×) button and an empty/truncated state; right-clicking a PR in the branch table offers "Watch this PR"; Preferences gets a GitHub section (`github.watched_prs` list field) + the `watched_pr_refresh_ms` number field. Notifications: each poll diffs the per-ref last-seen status (`watchedPrLastStatus`) and fires `pr-merged` / `pr-ci-failing` (CI flips INTO failing) / `pr-review` (decision changed) through the shared channel — toast (click opens the PR) + browser `Notification` (also click-to-open via a new `options.url` on `sendBrowserNotification`), gated by `notify_transitions`, deduped + throttled by `notifications.throttle_seconds`. First sighting of a ref only records a baseline so a load with already-merged/failing PRs doesn't storm.
- 18 i18n keys added to all 13 locales (build parity enforced). Tests: pytest (`parse_pull_request_ref` accept/reject, `watched_pr_metadata` dedupe/cap/invalid/fallback shape, settings defaults + clamp + notify-key allowlist + round-trip, `watched_prs_payload` shape + truncation log) and node (`normalizeWatchedPrRef` mirror, `watchedPrStatusSnapshot`, pure `watchedPrTransitionKeys` detector, `shouldNotifyTransitionKey` opt-in gating, wiring grep, Preferences section order Performance→GitHub→YO!agent). 348 pytest + node gate green.

## 2026-06-02 (versions 0.1.76 – 0.1.101)

### Agents and Automation

#### Auto-approve & prompt detection
- Auto-approve fires with the Ctrl-T task overlay shown below a live approval prompt (bounded-overlay break instead of treating the overlay as newer output).
- Footer-hint matching accepts multi-key + parenthetical footers (e.g. `(ctrl+b ctrl+b (twice) to run in background)`) so a live prompt stays auto-approvable.
- `capture-pane` uses `-J` so a wrapped command is captured as one logical line (a wrap can otherwise split a token and flip a danger verdict).
- YO marker spins only while working (slow, configurable period), never when idle.
- Backend safety/correctness pass (P0/P1/P2): YOLO hard-floor always-on, takeover re-acquire, send-action re-verify, worker stop-join, WS frame cap, metadata TTL + bounded cache, settings coerced-keys reporting, transcript tail windowing, and related hardening.
- DECISION (DOIT.10, resolved/archived): keep the fail-safe TUI prompt-liveness guard as-is. Rejected the "default-to-live" rework — it flips the safety bias toward typing into a stale prompt (false-positive keystrokes), which is worse than the recurring footer whack-a-mole it would fix. The footer-hint band-aid + `-J` capture are the standing solution; the recurring breakage is the correct price of failing safe.
- Reliable-auto-approve groundwork (DOIT.11, partial): the Claude `PreToolUse` permission hook (`yolomux_lib/claude_permission_hook.py`) + 17 tests landed — reuses the existing rule engine, returns allow/deny/ask programmatically (no keystrokes, no TUI scraping), fails safe. Still OPEN and user-gated (tracked in TODO Big-Bang #1): wiring it into `~/.claude/settings.json`, live validation, standing the keystroke worker down once live; Codex (`app-server` JSON-RPC) deferred.

### Editor, Finder, Differ, and Files

#### Editor, diff & markdown
- The editor is plain by default — no auto-loaded diff and no inline diff coloring on open; changes appear only in the explicit diff view (the diff button / Modified-files menu).
- Clicking a relative link in a rendered markdown preview opens+renders the target file in the same pane (with a path normalizer; out-of-root links are rejected server-side and toast).
- Tightened YO!agent markdown spacing (loose lists render compact); block cursor fills the full character cell.
- README opens in rendered preview; View→Theme re-themes open editors and terminals in lockstep; theme is chosen via macOS-style preview cards.

### UI, Theme, Preferences, and Localization

#### Internationalization (i18n)
- Extracted every user-facing string in the app to a key-based `t()`/`tPlural()` catalog — menu bar (incl. the whole tmux menu), Modified-files panel, diff-ref, comparison, file-explorer + editor toolbars, command palette, keyboard-shortcuts overlay, pane chrome, rename dialog, branch list, version banner, PR-review chips, YO!info/YO!agent panel + chat, Preferences, file-editor dialogs, file-tab tooltips, hover-popover labels, session-state labels, toasts/status lines. The `en-XA` pseudo-locale shows zero plain English except intentional proper nouns (Codex/Claude/tmux/git/PR/README/theme names).
- Shipped 12 UI locales at full key parity (build-enforced): English, Traditional Chinese, Simplified Chinese, Spanish, Japanese, German, French, Brazilian Portuguese, Russian, Korean, Hindi, Arabic — plus the `en-XA` pseudo-locale. The five least-common were parallel-translated by subagents.
- Three language entry points, all endonym-labeled, all writing the same `general.language` setting: the login-screen picker (persists after sign-in), a top-right topbar switcher, and the Preferences picker. `system` resolves against the browser locale.
- Right-to-left support: Arabic drives `dir="rtl"` (client + server-rendered shell); converted all physical margin/padding/border-left|right and text-align to logical properties so the layout mirrors under RTL with no LTR change.
- Locale-aware formatting: relative time via `Intl.RelativeTimeFormat`, dates via `Intl.DateTimeFormat` (LA timezone), counts via `tPlural`/`Intl.PluralRules`.
- Chinese brand glyphs (優/优 marker, 優樂mux/优乐mux wordmark) render and re-render on a language switch; the login screen localizes server-side; the YO!agent LLM backend gets a "Respond in <language>" directive and the deterministic backend's fixed framing localizes.

#### Drag, layout & panes
- Fixed the real tab-drag root cause — a full panel re-render mid-drag was wiping the grid and aborting the native drag; it now defers and flushes on drop.
- Tab drag-reorder works in both directions (left→right and right→left), from any pane including Preferences.
- Every pane keeps its active tab highlighted (not just the focused pane); the focused pane keeps an extra ring.
- File-menu Finder entry toggles; File→Finder/Preferences/etc. open in place.

#### Tabs, badges, search & menus
- Light-mode tab badges are legible; removed the redundant "PR" pill; killed the duplicate native tooltip on hover; the session popover shows review status and reviewer.
- The default search bar blends matching commands/tabs/settings with file results; duplicate file-search results (mirrors/symlinks) collapse.
- "Branch Info" renamed to "YO!info"; merged YO!info + YO!agent into one pane with a sub-tab toggle; Preferences gained a max-tabs-per-pane field and the Performance section sits above YO!agent.

### Build, Refactor, Docs, and Tests

#### Performance
- Tab moves no longer take several seconds: a layout change no longer re-polls the server, and same-shape changes (reorder/activate/move) swap only the affected panes in place instead of tearing down and rebuilding the whole topbar + grid. Markdown preview renders are guarded by a path+content signature.

#### Process & learnings
- Recorded recurring-failure lessons from the batch (stale-backend pitfalls, build/restart discipline, falsely-marked-done detection) and folded them into the working notes.
