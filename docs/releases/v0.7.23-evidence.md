# v0.7.23

Landing record for annotated release `v0.7.23`. The release removes a redundant Finder metadata request that delayed the first Git Diff interaction under contention.

## Shipped scope

- Finder directory `Delta/Show Diff` opens the Diff tab without starting a second forced `fetchFilePathInfo` request. The context menu's existing metadata refresh remains the sole metadata request for that click.
- Finder file `Delta/Show Diff` uses the same request ownership rule.
- The served JavaScript bundle and its source partial remain synchronized.

## Verification

- `python3 -m pytest tests/test_browser_finder.py::test_finder_repo_context_menu_and_diff_tab_are_immediate_while_metadata_is_deferred -q` passed.
- `node tests/layout_url.test.js` passed all 20 shards.
- `node tests/cross_surface_state.test.js` passed 56 checks.
- `python3 tools/static_build.py --check` passed.
- `git diff --check` passed.

The full canonical gate was not used as release evidence because another check process already owned the shared expensive-tool lock and did not finish within the bounded wait. The focused regression and static checks above ran on the exact release subject before tagging.

## Release boundary

Both runtime version owners report `YOLOMUX_VERSION = "0.7.23"` in the release commit. The annotated tag `v0.7.23` and `origin/main` must resolve to that same final release commit after landing.
