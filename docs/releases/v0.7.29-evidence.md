# YOLOmux 0.7.29 Evidence

Release commit: `v0.7.29^{commit}`.

This release adds first-class visible OpenCode support alongside Claude and Codex. It adds a shared client capability registry, real OpenCode process and pane identity handling, read-only OpenCode SQLite usage accounting, reusable OpenCode 1.18.25 capture fixtures, idle/working/input/blocked screen classification, native OpenCode context-menu preservation, Switchyard pricing resolution, and related status and attribution regressions. Directory Diff now fills its available viewport and prefetches two additional viewport-sized pages.

Verification included focused Python suites with 529 passing tests, OpenCode detector and capture coverage with 352 passing tests, OpenCode session and statistics coverage with 99 passing tests, the JavaScript layout suite with 20 passing shards, the Tabber suite with 49 passing tests, static asset validation, Python compilation, and `git diff --check`. The full functional `python3 tools/check.py` lanes completed successfully. Exact-SHA certification was not admitted because the checkout was intentionally dirty during development (`dirty_start_checkout`).

Runtime evidence used the active `yo7220` takeover target on port 7220 because port 7773 is not configured on this host. The guarded restart served version 0.7.28 before the version bump from this worktree, from the expected checkout, with tmux discovery `all`. The final release tag must point to the release commit after the version bump and landing.
