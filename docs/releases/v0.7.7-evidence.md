# v0.7.7

Landing record. The durable Markdown-audit evidence is retained in [`../DONE/2026-08/DONE.0-7-7-active-queue-reconciliation.md`](../DONE/2026-08/DONE.0-7-7-active-queue-reconciliation.md), and unresolved spec-versus-implementation findings remain explicit in [`../specs/V0.7.7_IMPLEMENTATION_DISCREPANCIES.md`](../specs/V0.7.7_IMPLEMENTATION_DISCREPANCIES.md).

- Both runtime version owners report `YOLOMUX_VERSION = "0.7.7"` at annotated tag `v0.7.7`. The local and origin tag object is `82deaff9d472e7fed6fac43073e1ffda582a5aac`, and both peel to release commit `fb58cac56635df31fa6fd2c94f92cb971589e307`; `origin/main` also resolved to that commit after publication.
- The stable v0.7.7 finish line closed at 8/8. Root-level `PLAN.0.*.md` and `DOIT*.md` counts are zero; 42 concrete queues preserve 331 open requirements under `queues/backlog/`, with zero requirements incorrectly checked complete.
- The pre-commit working tree that became the release candidate passed all nine functional lanes in 593.43 seconds, including E2E in 199.05 seconds, non-browser pytest in 403.50 seconds, browser pytest in 486.77 seconds, and the timing-sensitive serial lane in 7.15 seconds. Static and whitespace lanes passed again after the final plan-to-backlog reconciliation.
- The clean exact release commit passed all seven exclusive latency-certification units on qualified preflight and postflight; `python3 tools/check.py --certification-only` exited 0 in 95.20 seconds. The retained evidence directory is `/tmp/yolomux-certification/cert-1786816252487170521-1418243`.
- No live YOLOmux port was restarted or promoted as part of this documentation-only release.

## What it carried

- A repository-wide audit of every Markdown document and every active DOIT requirement, with completed requirements archived and broad TODO backlogs retired.
- `STATUS-REPORT.md` as the sole aggregate release ledger, backed by concrete priority queues instead of root-level plan files.
- A complete specs audit against the current implementation, direct corrections for documentary drift, and a dedicated non-queue guide for discrepancies that still require later implementation work.
- Synchronized documentation maps, DONE records, release references, local links, queue counts, and repository workflow guidance.
- The former v0.7.8 seed and three additional P0/P1/P2 findings preserved in the measured backlog without retaining a parallel release plan.

## Release boundary

The v0.7.7 product release ends at `v0.7.7`. This post-tag evidence closure advances `main` without moving or recreating the tag; later queue work remains under `queues/backlog/` until explicitly promoted.
