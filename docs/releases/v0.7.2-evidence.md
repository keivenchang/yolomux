# v0.7.2

> **CURRENT TAG STATE 2026-08-15:** Annotated `v0.7.2` now peels locally and on origin to rewritten release commit `63b0b244ad7b908fcc85e0cfce254ccf7f797cc0`. The `926e4a166` value below is the original release-line identity retained in the landing record.

Landing record. Nothing on this page was re-measured when it was written; every claim is sourced to [`../DONE/2026-08/DONE.yolomux-0-7-2-shipped-waiver-daemons-system-status-rework.md`](../DONE/2026-08/DONE.yolomux-0-7-2-shipped-waiver-daemons-system-status-rework.md).

- Version `YOLOMUX_VERSION = "0.7.2"`, tag `v0.7.2`, release commit `926e4a166`.
- Landed on local `main` as one squashed commit over the inclusive range `9a960fc2c..98741bcb2` (32 commits; `9a960fc2c` = `v0.7.1`).
- The 0.7.2 queue (`DOIT.p0.v0.7.2.md`) was gitignored and removed at land; its 24 deferred items moved to the 0.7.3 queue with evidence and exit criteria intact.

## What it carried

- Daemons/system-status rework: real per-service metrics including the YOLOmux web process, unmeasured values rendered as `—` with a machine-readable reason code rather than a fabricated `0`, `/api/system-status` served as a background-published immutable snapshot, lazily-fetched advanced diagnostics.
- Two security closures, each reproduced before being fixed: the cross-port jobd confused deputy (access policy captured at HTTP admission and enforced at point of use through one `FilesystemAccessPolicy` parent), and the watchd cross-policy path disclosure (revision consumer intersects roots and changed paths against the server's own authorized roots).
- Interactive-API lane isolation: per-lane 64-slot queue cap plus a dedicated mutation lane; jobd protocol 20 to 21.
- Gate-harness hardening (F8): flock acquired before the worktree writer lease, and the reaped-container guard signals the whole session group.

## Shipped without closure

**This release shipped by waiver on a red gate.** The full `python3 tools/check.py` at candidate `98741bcb2` failed four nodes under 16+10-worker saturation. Each passed when run isolated on a quiet box at that exact HEAD, and both causes — a contention-blind jobd RPC/health deadline and a Selenium stale-element re-render — were moved into the 0.7.3 queue as gate-hardening owners rather than fixed here.

A green isolated rerun is not a green gate. Treat "0.7.2 passed" as false; the accurate statement is that 0.7.2 shipped with four known load-only failures and a written waiver.
