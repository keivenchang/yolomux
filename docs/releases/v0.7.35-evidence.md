# YOLOmux 0.7.35 Evidence

This release carries the source-backed Markdown ViewEditor, including ProseMirror-only editing, delayed TextEditor/ViewEditor synchronization, Markdown round-tripping, image paste/upload/rendering, and safe read-only fallback diagnostics. It also corrects Linux startup capacity admission so stale load averages from blocked NFS I/O do not reject a host with available CPU and low current PSI pressure, while retaining startup ownership, readiness, and listener checks.

The release was prepared from the exact origin/main ancestry and is intended to ship as annotated `v0.7.35`. The final tested subject is the release commit itself; after tagging, the immutable release target is `v0.7.35^{commit}`.

Verification: focused editor/backend tests, startup-gate tests, static bundle rebuild/check, JavaScript syntax checks, and the canonical `python3 tools/check.py` gate are required before landing. Live port 7220 was restarted from this worktree and returned `/healthz -> 200`; unauthenticated `/api/ping` returned `401`.
