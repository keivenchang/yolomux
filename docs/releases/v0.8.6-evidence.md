# YOLOmux v0.8.6 Evidence

## Release

- Version: `0.8.6`
- Tag: `v0.8.6`
- Tested subject: `2cdb3452c3317040876a076361aefae1e9dda745`
- Final tag target: `v0.8.6^{commit}`

## Scope

Changes since v0.8.5:

- Coalesce concurrent cold `/api/session-metadata` and compatibility `/api/transcripts` reads through one lightweight metadata future instead of rebuilding session metadata for every request.
- Preserve forced refresh generation, topology invalidation, typed failure propagation, and teardown behavior while releasing waiters when an in-flight lightweight read is invalidated or stopped.

## Verification

- Focused cold metadata tests: `4 passed`.
- Application and metadata route tests: `733 passed`.
- Python compilation and whitespace checks passed.
- Full canonical `python3 tools/check.py`: red because of a timing-sensitive tmux test and host certification refusal for disk-busy, memory-stall, and dirty-checkout conditions. The changed-area tests passed.
- Runtime acceptance: `7111` restarted from the changed worktree; the idle post-restart measurement showed no continuing transcript metadata request storm, but no controlled replay of the original client load was performed.

## Evidence Status

This page records the release scope and measured limitations for the annotated `v0.8.6` release. The final tag target is `v0.8.6^{commit}`.
