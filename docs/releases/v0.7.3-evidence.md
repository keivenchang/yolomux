# v0.7.3

Landing record. Nothing on this page was re-measured when it was written; every claim is sourced to `docs/DONE/DONE.0-7-3-release.md`.

- Release commit `7bf385828`, 2026-08-12. **No git tag exists for this version** — `git tag -l 'v0.7*'` returns `v0.7.0`, `v0.7.1`, `v0.7.2` only, verified 2026-08-12.
- Landed on `origin/main` and `origin/dev/0.7.2`; deployed to 7770 (production) and 7771.

## What it carried

- All 15 W packages from `DOIT.p0.v0.7.3.md`, plus breadth-first Quick Open indexing from `DOIT.fs-interactivity.md`. Both queues are archived under `docs/DONE/`.
- Post-audit gaps closed at land: statusd stale blocked-start latch, watch-diff jobd interaction lease, quiet-gated stats-DB compaction, share-scoped host-poller suppression.
- Streaming Quick Open using authenticated cursor deltas plus redacted progress signals, with live 7773 acceptance.
- Bounded watchd scope: recursive workspace registration removed, measured live descriptor count reduced from about 102,769 to 14, idle CPU measured at 3.97% of one core, one shared exclusion owner retained.
- Certification machinery CERTIFIED on a fresh exact-SHA checkout.

## Shipped without closure

External acceptance was open at land and non-blocking by decision:

- W11 real Darwin populations.
- W14 real Darwin checkout plus a soak of 600s or more.
- W15 a fully-green gate on a quiet host.

Deferred to 0.7.4: statsd listener-thread maintenance de-serialization, ack full-snapshot fsync, identical watch-root writes, watchd bounce root cause, CPU causal attribution.
