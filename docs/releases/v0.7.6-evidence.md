# v0.7.6

Landing record. The durable implementation, measurement, gate, browser, and production evidence is retained in [`../DONE/2026-08/DONE.0-7-6-performance-release.md`](../DONE/2026-08/DONE.0-7-6-performance-release.md).

- Both runtime version owners report `YOLOMUX_VERSION = "0.7.6"` at signature-bearing annotated tag `v0.7.6`, which peels locally and on origin to release commit `35907e6f344c2130f6dbbdb33f35eea8247fa3f4`.
- The five release queues closed at 59/59 requirements and the stable release finish line closed at 8/8. The post-release documentation commit archived the queues and retained `STATUS-REPORT.md` as the sole aggregate source of truth without moving the product tag.
- Clean source parent `06cbfd9ed84ab279c48a793ca594a5e2c92af17f` passed all eight functional lanes and all seven exclusive latency-certification units on qualified preflight and postflight; `python3 tools/check.py` exited 0 in 529.12 seconds. The tagged release commit changes the two runtime version constants to 0.7.6 and updates the release ledger.
- Production 7770 restarted from the tagged release commit as PID `884568` and served JavaScript SHA-256 `317c331931faa4c9863c8f471bdf64c666bfbd07b90935e60268f296bf73a366`. Authenticated Chrome replayed the last-window removal patch with the session record retained and `windows=[]`; the fallback kept `0:claude`, SSE connected, and browser/render diagnostics remained empty.

## What it carried

- Bounded layout and terminal completion waits, lossless completion-generation coalescing, and server-successful rename handling across client supersession.
- Tmux window-strip fallback preservation when an empty signal snapshot survives after the last matched window is removed, with typed diagnostics for silent-empty paths.
- External indexed Quick Open matching restricted to the basename, stem, or one relative-path segment, with full hover titles retained for clipped indexed roots.
- Activity-tiered statusd capture intervals with immediate active promotion and bounded jitter; measured quiet capture work fell from 315.0 to 87.0 calls per minute and statusd CPU fell from 3.82 to 1.35 CPU-seconds over comparable 60-second runs.
- A 50% canonical pytest worker budget with disjoint default lane ownership, and a controller-to-DOM paint fence preventing stale YO!stats ring results from overwriting the current request.

## Release boundary

The 0.7.6 product release ends at `v0.7.6`. Later documentation closure on `main` does not move or recreate the tag, and later findings remain in `queues/backlog/` unless explicitly promoted.
