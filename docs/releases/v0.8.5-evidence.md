# YOLOmux v0.8.5 Evidence

## Release

- Version: `0.8.5`
- Tag: `v0.8.5`
- Tested subject: `5e65e252c7b7f58aef43ed902d3d6c018fa61aa6`
- Final tag target: `v0.8.5^{commit}`

## Scope

Changes since v0.8.4:

- Correct OpenCode and session-level token attribution using machine-wide process/session discovery, preserving unavailable spans and source gaps.
- Add the internal 200-record pricing snapshot and normalized 98-model catalog; 176 records carry prices, including `openai/openai/gpt-5.6-luna`.
- Prevent autosave self-write events from producing false reload/conflict prompts during continued editing.
- Make terminal URLs continue across physical rows when a period ends the first row, including the `.html` suffix.
- Ensure Dockview center drops show one native center preview instead of overlapping custom full-pane overlays.
- Add regression coverage, importer validation, and durable interaction-testing guidance.

## Verification

- Internal rates importer: `200` records validated, `176` priced, `98` normalized models.
- Focused pricing identity tests passed.
- Focused terminal/editor/layout tests passed during implementation.
- Focused pricing/stats/session tests: `235 passed`.
- Layout suite: `116 passed`; ProseMirror editor suite: `5 passed`.
- Full canonical `python3 tools/check.py`: red in the release environment due to a timing-sensitive tmux test and host memory-stall certification; the changed-area focused tests passed.
- Runtime acceptance: `7220` served version `0.8.4` during final pre-release work; post-release restart is pending.

## Evidence Status

This page records the final tested subject before tag creation. Record the resolved tag SHA after the annotated tag is created.
