# v0.7.4

Landing record. The durable implementation and acceptance evidence is retained in [`../DONE/2026-08/DONE.0-7-4-behavior-preserving-architecture-cleanup.md`](../DONE/2026-08/DONE.0-7-4-behavior-preserving-architecture-cleanup.md).

- Version `YOLOMUX_VERSION = "0.7.4"` in both runtime owners, with signed annotated tag `v0.7.4` on this release commit.
- The frozen cleanup queue reached 26/26 and was archived before landing. No 0.7.5 queue was started on the release candidate.
- Release admission requires one unmodified canonical gate with every functional lane green and all six exclusive certification units qualified on the exact clean SHA.
- The signed tag is created only after that same commit is restarted on 7771 and accepted in a real authenticated browser against its exact served bundle, followed by a 600-second clean soak and the required attributed negative probe. The tag annotation retains those exact measured facts without mutating the accepted release commit.

## What it carried

- Named backend owners for daemon dispatch, service projection, registry lifecycle, immutable published caches, HTTP adapters, stats query/projector boundaries, and app-domain state.
- Named frontend owners for browser state/resources, tree rows and controls, Debug views, preview renderers, async reads, lifecycle resources, fixture scenarios, and gate lanes.
- Structural ratchets and characterization manifests that preserve behavior while preventing reviewed source and browser-test owners from regrowing.
- Final lifecycle fixes for stale Dockview adoption, filesystem retirement severity, browser retirement diagnostics, Finder Sync directory listing, stats fixture transport ownership, and exclusive 24-hour stats latency certification.

## Release boundary

The 0.7.4 release ends at this tagged commit. At publication, the ordered 0.7.5 queues tracked in `STATUS-REPORT.md` remained separate and started only after this release evidence was complete; that later status ledger has since been retired.
