# YOLOmux v0.8.3 Evidence

## Release

- Version: `0.8.3`
- Tag: `v0.8.3`
- Tested subject: `8a819bdfa5b150f04d34f8e981c88a3e9231d4ea`
- Final tag target: `v0.8.3^{commit}`

## Scope

This release carries the squashed work since v0.8.2:

- ProseMirror Markdown link editing and menu behavior, including direct-click handling and URL editing.
- Finder and Dockview interaction fixes, including restored tab reordering and removal of immediate Finder deletes.
- Search-index safety refresh reduced to five minutes, aggressive default exclusions, and explicit indexed-root behavior that avoids implicit home-directory indexing.
- Startup admission uses current CPU signals and subtracts blocked Microsoft Defender threads from the load signal.
- Related browser, backend, indexer, settings, and startup-gate regression coverage.

## Verification

- Focused restart-gate tests: `26 passed`.
- Focused indexing/settings tests: `114 passed`.
- ProseMirror link browser regression: `1 passed`.
- `git diff --check`: passed.
- Full canonical `python3 tools/check.py`: pending.
- Runtime acceptance: 7220 was restarted and `/healthz` returned HTTP 200 before the final release rebase; a post-release restart is pending.

## Evidence Status

The canonical gate ran against the squashed release tree. The gate remained red because of pre-existing cross-surface Node fixture failures, architecture-budget ratchets, a browser fixture teardown timeout, host memory-stall admission, and dirty-start certification. Focused changed-area tests passed, including 113 index/settings/startup tests and the ProseMirror editor suite.

The final annotated tag target is recorded after tag creation in the release handoff.
