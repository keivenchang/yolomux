# v0.7.20

Landing record for annotated release `v0.7.20`. The final release commit is the object resolved by `v0.7.20^{commit}` after this record, the version owners, and the rebuilt bundle are committed, landed on `main`, and tagged. The release handoff records the exact peeled tag target and pushed `origin/main` SHA.

## Shipped scope

- Finder context menus now mount before deferred path-info and relative-copy resolution. Their existing menu lifecycle re-renders only the still-connected menu for the same selected paths when that metadata returns.
- A single listed Git directory exposes `Show Diff` immediately from its existing repository marker. Clicking it creates or activates the Diff tab immediately; the tab uses the shared animated `Loading...` ellipsis while bounded Git history loads.
- File Edit and Preview actions also remain immediately usable before optional Git metadata. File Diff retains its capability gate; non-Git directories retain no Diff action.

## Verification and release state

- `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/cross_surface_state.test.js` (56 passed, 0 failed), `python3 tools/architecture_budgets.py`, and `git diff --check` passed after the final source/test changes.
- The Finder browser subset passed: `test_finder_repo_context_menu_and_diff_tab_are_immediate_while_metadata_is_deferred`, `test_finder_context_diff_repo_eligibility_and_touch_long_press`, `test_finder_context_file_actions_share_one_dirty_editor_tab`, and `test_finder_context_relative_copy_uses_visible_root_and_fails_closed` reported 4 passed. The new held-request test proves the native menu is suppressed, `Show Diff` is enabled before path metadata, its tab is active, and the shared moving ellipsis is present before Git history resolves.
- The first canonical gate ran before the final menu-contract test and architecture-budget corrections. Syntax, whitespace, compilation, static-build, Node layout, and serial timing passed; it then reported stale budget/version expectations and existing broad browser/E2E failures. The exact report is `/tmp/yolomux-check-runs/check-1787898853753399139-3686413.json`; its host certification was `NOT CERTIFIABLE: host_unqualified_preflight` because CPU some-stall measured 0.158185 above the 0.067 limit. This record does not represent that first red attempt as a green retry.
- Requested 7771 runtime acceptance completed on 2026-08-27: PID 254987 listened on 7771 from `/home/keivenc/dev/yolomux.dev7771`; the served `static/yolomux.js` SHA-256 was `f46bdc738141438d883430cb4155219cd0541f190c50a8fcb44b2b084029db40`, identical to the candidate. An authenticated Chrome session right-clicked `/home/keivenc/dev/notes` and `/home/keivenc/dev/yolomux`; both immediately suppressed the native menu and exposed enabled `Show Diff`. Clicking `notes` immediately activated its Diff tab with `loading...` and the shared moving ellipsis. Console errors and unhandled rejections were empty.
- The completed queue record is [DOIT.p0.fix-right-click-finder.md](../DONE/2026-08/DOIT.p0.fix-right-click-finder.md).

## Release boundary

Both runtime version owners report `YOLOMUX_VERSION = "0.7.20"` at the annotated tag. The intended tag message is `YOLOmux v0.7.20 release`. Before publication, `v0.7.20^{commit}` and pushed `origin/main` must resolve to the same release commit.
