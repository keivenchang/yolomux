# v0.7.21

Landing record for annotated release `v0.7.21`. The final release commit is the object resolved by `v0.7.21^{commit}` after this record, the version owners, and the rebuilt bundle are committed, landed on `main`, and tagged. The release handoff records the exact peeled tag target and pushed `origin/main` SHA.

## Shipped scope

- Finder repository history opens incrementally, and one physical file remains one editor tab across canonical paths, aliases, and hard links.
- Session-files refreshes use bounded shared disk/cache work. Owner and follower processes converge on one published generation without repeated Git production or opaque-cache reads.
- Jobd retains its status while executors retire, and fixture teardown joins app, scheduler, local-service, and transcript workers before their private root is removed.
- The transcript payload refresh owner fences teardown admission, retains superseded workers until they finish, and prevents client-watch snapshots from starting after the fence.
- The isolated Docker gate supports the NFS-backed checkout through rootless Docker, while retaining writer-protection coverage for a true read-only bind mount.

## Verification and release state

- `python3 tools/check.py --lane pytest-e2e` passed in 219.61 seconds.
- `python3 tools/check.py --lane pytest-browser` passed in 361.54 seconds.
- The required concurrent transcript/session-files regression group passed: 5 tests in 28.95 seconds.
- `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, `tests/test_gate_worktree_protection.py`, and `tests/test_architecture_budgets.py::test_checked_architecture_manifest_matches_the_worktree` passed.
- A final full `python3 tools/check.py` ran all functional lanes. Its only non-green state was exact-SHA certification refusal because the release subject was still dirty. Certification is rerun from the clean committed release subject before publication.
- Requested 7220 acceptance completed before release: PID 1342531 served this candidate from `/nfs/keivenc/dev/yolomux.e3-candidate.YqegFY`; unauthenticated `https://localhost:7220/api/ping` returned 401 in 43 ms. Native filesystem watching remained disabled on this NFS host through the established polling fallback.

## Release boundary

Both runtime version owners report `YOLOMUX_VERSION = "0.7.21"` at the annotated tag. The tag message is `YOLOmux v0.7.21 release`. Before publication, `v0.7.21^{commit}` and pushed `origin/main` must resolve to the same release commit.
