# v0.7.32

Landing record for the post-v0.7.31 gate and OpenCode reliability fixes.

## Shipped scope

- OpenCode terminal URL references retain their full logical URL across wrapped, indented, quote-gutter, and repeated-gutter rows without merging complete URLs with following prose.
- The OpenCode mock renderer paints a contiguous newest viewport, and the live launch gate recognizes the Python-backed mock without weakening native executable validation.
- YO!cost ring reconstruction derives cost reports from exact persisted bucket details, and ring landing checks read the server generation's cost report after restart.
- Linked-worktree isolated test containers mount the recorded checkout path so Git identity checks work from the test mount.
- Editor, Finder, Differ, transcript identity, and browser gate fixtures retain their current state and timing contracts.

## Verification

- The focused OpenCode, session, ring, and gate suites passed during candidate construction.
- The Node layout suite passed with all 20 shards passing after the final bundle rebuild.
- The full repository gate passed all functional lanes on commit `0cf598178`, and the seven latency certification units passed on a fresh checkout. One earlier parallel run had an environment-sensitive ring sidecar identity failure; the exact ring landing test passed in isolation afterward. The final release commit also preserves the local `main` macOS Finder fixes that were present when the release was landed.

## Release boundary

Both runtime version owners report `YOLOMUX_VERSION = "0.7.32"` in the release commit. The tested subject SHA is `0cf5981788c3539e2c26b5976ef74526b72614c0`. The final release commit is the annotated tag target `v0.7.32^{commit}` after landing on `main`.
