# OpenCode Agent Detection P0

Completed the OpenCode visible-pane integration and YO!stats support for the P0 queue. The implementation recognizes real OpenCode process identity, preserves breadth-first pane ownership, records pane and session metadata, fails closed on ambiguous SQLite attribution, classifies idle/working/needs-input/blocked states, preserves native OpenCode context-menu behavior, and uses the shared client capability registry. Real OpenCode 1.18.25 plain and styled captures are preserved as a reusable library in `tests/fixtures/prompt_corpus/captures/`, including live idle and working pane shapes and the `capture-pane -J` wrap contract. Verification included `529` focused Python tests, `352` detector/capture tests, `99` session/stats tests, `20` JavaScript layout shards, `49` Tabber tests, `python3 tools/static_build.py --check`, and the full `python3 tools/check.py` functional lanes. The final live takeover was performed on the active `yo7220` target rather than unavailable `yo7773`: guarded restart reported version `0.7.28`, served from this worktree, with tmux discovery `all`; port 7773 is not configured on this host.
