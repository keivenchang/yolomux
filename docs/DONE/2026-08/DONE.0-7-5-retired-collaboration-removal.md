# 2026-08-14 retired collaboration feature removal

- Removed the retired frontend, backend routes and WebSockets, auth exchange, configuration, source locales, CSS, upload sink, feature tests, and specifications. The completed removal is net `-22,368` non-generated lines (`+1,299/-23,667`).
- Preserved the normal-product owners that had shared retired files: layout URL capture/restore, font readiness, producer-neutral diagnostic redaction, preference ordering, common conversation behavior, YO!info assertions, and 48 cross-surface contracts.
- Added one fail-closed reappearance guard covering retired identifiers in filenames, source, generated content, locale keys, binary content, HTTP routes, and WebSocket routes. The only surviving similarly named owners are `shared_config_lock.py` and `test_shared_config_lock.py`; the zero-hit search excludes only this historical archive.
- Verification retained 778 Node contracts across all 19 shards, passed 12,071 focused Python tests and 182 architecture-budget tests, and matched the frozen browser `-45`, non-browser `-518`, and Node `-21` collection deltas without deleting unrelated assertions.
- Authenticated Chrome on exact candidate `7228cb20e` exercised Finder, editor, Differ, YO!stats, Daemons, Logs, and a terminal socket. `/share/`, `/api/share`, and all three real WebSocket Upgrade attempts returned 404; no retired socket opened and no unexpected browser/server warning or error appeared.
- Release gate evidence: rewritten product commit `5c899077d` has the exact tree tested as source candidate `1ab141ea9`, which passed all eight functional lanes in 436.98 seconds with zero red lanes. Product-identical source parent `199d36cd2` certified all six fixed units on a qualified host; later host-saturated attempts were refused before measuring units.
