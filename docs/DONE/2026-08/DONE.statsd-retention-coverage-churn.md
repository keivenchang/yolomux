# Statsd retention and coverage churn completion

Completed the follow-up after correcting the initial diagnosis. The copied-production py-spy capture was cold-start dominated, warm unchanged input already scheduled no work, and dirty observation/usage reads already used SQL pushdown. The measured warm defect was duplicate retention ownership: every RPC and host append passed `retention_now`, repeatedly clipped old coverage, invalidated the coverage cache, and made each 1 Hz incremental build reread the retained coverage corpus.

The existing 60-second `_prune_if_due()` sweep is now the sole service-level retention owner; explicit `Store.append_batch(..., retention_now=...)` callers remain supported. Materializer-worker startup or prune failure fail-stops the listener, and exact half-open cutoff handling deletes closed coverage/unavailable intervals ending at the cutoff while keeping point facts stamped exactly at it. This also fixes the reproduced zero-length unavailable-span SQL-check rollback.

Full materialization now lower-bounds point facts at the largest renderable window and reads coverage/unavailable facts by strict positive overlap. Spanning epochs remain visible, sources whose first overlap starts inside the window retain one indexed predecessor for left-edge gap geometry, and ancient-only sources are deliberately retired from the bounded model because preserving their identities would require the schema/index summary excluded from this queue.

The permanent accelerated regression holds a 4,096-row warm coverage model, proves unchanged input schedules no work, and performs eight 1 Hz incremental builds with zero coverage-history rereads. On the frozen production-shaped fixture, startup returned 57,312 coverage rows instead of 108,146; eight warm appends returned zero coverage rows instead of 865,168 and projected 9.37% instead of 71.11% of one core. `python3 -m pytest tests/test_stats_current_service.py tests/test_stats_current_storage.py -q` passed 180 tests, the final current-tree `python3 tools/check.py --lane pytest --cpu-percent 50` passed in 264.15 seconds, and the architecture ratchet test passed after regenerating the reviewed counts.

Only 7771 was restarted. Web PID 1507512 served `/home/keivenc/dev/yolomux.dev7771` on port 7771, unauthenticated `/api/ping` returned the expected 401 in 44 ms, statsd PID 1508469 reported ready with zero failed builds and cache/source convergence, and its first post-start 20-second CPU sample averaged 11.0% of one core. Production 7770 was not touched.
