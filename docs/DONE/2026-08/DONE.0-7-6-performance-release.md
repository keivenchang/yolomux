# 2026-08-14 v0.7.6 performance release

- Published `origin/main` and annotated `v0.7.6` at `35907e6f344c2130f6dbbdb33f35eea8247fa3f4`. The release contains +1,069/-243 non-generated lines, net +826, excluding generated bundles, the architecture ratchet, and the progress ledger.
- Bounded the tmux rename completion transaction at both layout and terminal waits, retained begun completion generations through render coalescing, made rejected settlement observable, and treated a server-successful superseded client transaction as success. The exact pending-render browser reproduction changed from an open errorless dialog to one settled generation and a closed dialog.
- Kept tmux window strips when a patch removes the last signal window but leaves its session record. Empty signal authority now preserves populated transcript panes, rejected non-empty inputs emit typed diagnostics, and the numeric fallback remains intact. Authenticated Chrome on production 7770 at the release SHA retained `0:claude` with `sessions["strip-live-7770"]` populated and `windows=[]`, connected SSE, and zero browser/render diagnostics.
- Landed Quick Open branch `4a7f11a07` through merge `6a1f48234`. External indexed matches now require the basename, stem, or one relative-path segment instead of assembling a fuzzy match across unrelated absolute-path directories; clipped indexed-root groups expose their full title. The post-merge ratchet was regenerated and both served bundles were rebuilt from source.
- Added one shared activity-tiered statusd capture policy: active sessions retain 5.003-second reconciliation, then recent/quiet/cold sessions use approximately 10/30/120 seconds with bounded jitter and immediate promotion on new activity. In comparable 60-second measurements, successful `capture-pane` calls fell from 315.0 to 87.0 per minute and statusd CPU fell from 3.82 to 1.35 CPU-seconds.
- Set the canonical default pytest pool budget to 50% on the shared 32-core host after measuring 100% versus 50% at comparable starting load. Peak load fell from 76.016 to 49.380 with 456.843s versus 504.499s wall time; both configurations executed the same 17,919 unique pytest node IDs with zero added, removed, skipped, or deselected tests. Focused aliases remain available but are not duplicate default lanes.
- Repaired the parallel-only YO!stats ring controller-to-DOM paint fence found during integration so a stale request cannot overwrite the current result. The deterministic owner passed 72/72, real Chrome passed 1/1, three-worker E2E passed 122/122, and Node plus architecture checks passed.
- Final acceptance used clean exact-SHA `06cbfd9ed84ab279c48a793ca594a5e2c92af17f`: all eight functional lanes passed and all seven exclusive latency-certification units passed on qualified preflight and postflight; `python3 tools/check.py` exited 0 in 529.12s. Version equality passed 1/1 after the release bump, generated assets matched, production 7770 restarted as PID `884568` from the release SHA, and its served JS SHA-256 was `317c331931faa4c9863c8f471bdf64c666bfbd07b90935e60268f296bf73a366`.
- Existing owners checked and retained: canonical `TestPhaseSpec` phase partitioning, `polling_policy.quiet_poll_interval`, layout-mutation generation settlement, keyed tmux-signal patching, transcript-pane fallback rendering, external indexed-match filtering, generated static-build ownership, and the stats ring controller-to-DOM fence. Negative searches found one quiet-interval parent, no retired strict indexed-query gate, no lost undefined completion-generation option, and no duplicate tmux window fallback owner.
