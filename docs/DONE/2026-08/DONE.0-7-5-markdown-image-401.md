# 2026-08-13 v0.7.5 Markdown preview image recovery

- Captured the real authenticated browser requests on isolated 7772 before changing code. `/tmp/yolomux-075-markdown-image-headers-7772.json` records `yolomux_auth_7772` present and unblocked on both requests: `/api/ping` used `Sec-Fetch-Dest: empty`, `Sec-Fetch-Mode: cors`, and returned 200; the image used `Sec-Fetch-Dest: image`, `Sec-Fetch-Mode: no-cors`, passed authentication, and returned typed 503 from `local_services.jobd` because `ValueError: result too large`. The historical 7771 401 remains unexplained; 7771 was not touched.
- Added bounded file-backed jobd artifacts and chunked RPC so an authenticated `/api/fs/raw` response can carry the reported 1,957,801-byte PNG without exceeding the generic result budget. `test_authenticated_png_subresource_above_generic_job_budget_returns_image_bytes` drove the real HTTP handler and persistent jobd: unchanged HEAD failed `503 != 200`, while the completed tree returned 200, identical bytes, and `image/png`.
- Routed Markdown, Finder/Differ hover media, editor/native media, Open, and Download through one authenticated raw-Blob owner. The owner preserves typed 401, 404, and 413 messages, aborts stale work, revokes each Blob URL exactly once, and awaits image decode. Markdown rewrites local sources while the sanitized fragment is detached, so an HTML-authored relative image cannot start an obsolete browser request before the authenticated Blob owner takes control.
- Restarted 7772 and verified the reported Differ Markdown preview in real Chrome. `/tmp/yolomux-075-markdown-differ-preview-7772.json` records HTTP 200 for all 1,957,801 bytes, `image/png`, a decoded Blob image at 1672x941, zero fallback nodes, and no `Image unavailable` text.
- Existing parent checked: `installRawFileMediaSource()` and `renderMarkdownPreviewInto()`. Searches: `rg -n "installRawFileMediaSource\\(" static_src/js/yolomux tests`, `rg -n "rewriteMarkdownPreviewImages|_previewAsync|markdown-preview-image" static_src/js/yolomux tests`, and a negative search confirmed exactly one `function fetchRawFileBlob(`. The final decode/fragment correction added six net non-generated lines and no parallel media owner; no aggregate line count is claimed because Queue 0 was developed on top of the concurrent macOS merge and their overlapping files do not provide a stable standalone denominator.
- Verification: `python3 tools/static_build.py`; all 20 `node tests/layout_url.test.js` shards; five focused browser/route tests passed with one environment skip; the formerly failing Markdown test and the authenticated rename journey passed together; final `python3 tools/check.py` passed all eight functional lanes in 388.04 seconds. Report: `/tmp/yolomux-check-runs/check-1786656969524221696-2228487.json`. Exclusive performance certification refused before measuring because host disk-busy fraction was 0.980352 over its 0.9 admission ceiling; exact-SHA admission is also unavailable during the dirty merge, so release certification remains separate from this completed functional queue.
