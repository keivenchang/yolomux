# 2026-08-14 first-launch agent auth and setup brand

- Made the isolation decision explicit: `YOLOMUX_ROOT` isolates YOLOmux-owned configuration, state, cache, and runtime paths; Codex credentials and configuration remain user-owned at `~/.codex` unless `YOLOMUX_CODEX_HOME` explicitly opts into another home.
- Routed runtime launch and read-only auth probes through that policy. A fresh rooted instance recognizes an existing `~/.codex/auth.json`, and the status probe no longer creates an empty instance Codex home as a side effect.
- Replaced divergent `logged_in` checks with the shared server-owned tri-state availability predicate. Installed agents with `logged_in=None` stay usable and render no login banner; only confirmed unavailable state disables the agent or names a login command.
- Removed setup-screen rotation while retaining the measured pulse. Real Chrome sampled 14 frames: `setup-brand-yolo-pulse` changed background, shadow, and filter over time while every transform and rotation remained `none`.
- Retained red/green evidence: the pre-fix unknown-auth regression failed, then the rooted-auth, unknown-banner, availability-projection, and brand assertions passed with the focused Python owners, 84 Node layout cases, generated/static checks, and the architecture gate.
- Live acceptance on exact candidate `1a4a85edf` showed Codex installed/logged-in/available on the main page, no login command on the login or main page, clean browser and server deltas, a rooted probe using `/home/keivenc/.codex` without mutating the historical instance Codex directory, and unchanged 7771 PID `3613344`.
- Release gate evidence: rewritten product commit `5c899077d` has the exact tree tested as source candidate `1ab141ea9`, which passed all eight functional lanes in 436.98 seconds with zero red lanes. Product-identical source parent `199d36cd2` certified all six fixed units on a qualified host; later host-saturated attempts were refused before measuring units.
