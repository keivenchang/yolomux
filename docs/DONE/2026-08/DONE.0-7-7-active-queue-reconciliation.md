# 2026-08-15 v0.7.7 active-queue reconciliation

- Removed `DOIT.p0.fix-png.md` as a duplicate of the shipped v0.7.5 Markdown image recovery already recorded in `DONE.0-7-5-markdown-image-401.md`. Current HEAD still uses bounded file-backed jobd artifacts and chunked response forwarding while `JOBD_MAX_RESULT_BYTES` remains 512 KiB. Current verification: the authenticated 1,957,801-byte PNG route, raw/ZIP artifact chunking, and response-lease close paths passed 5/5.
- Removed `DOIT.p0.activity-summary-disable.md` because the terminal disabled contract is now the shipped baseline. `GET /api/activity-summary` returns typed `503 feature_disabled` with `async_replacement_required`; browser, watcher, and client-event demand remain absent. Current verification: `tests/test_activity_summary_disabled.py`, `tests/test_activity_summary.py`, and the Node request/push contract passed as part of 25 pytest and 156 layout-async checks. The separate asynchronous replacement queue remains open.
- Removed `DOIT.p0.test-gate-no-hidden-retries.md` because current main has no retry plugin, retry-attempt constant, timeout multiplication, or `contention_prone` marker. The canonical pytest owner applies one 180/300/600-second class timeout with a 120-second faulthandler boundary. Current verification: the timeout-class and faulthandler regressions passed; negative searches over `pytest.ini`, `tests/conftest.py`, `tools/check.py`, and `tests/` found no canonical retry owner.
- Removed `DOIT.p2.browser-diagnostic-redaction.md` because the shared browser/Python diagnostic contract and producer/retention/render/copy paths shipped in v0.7.3 and remain current. Current verification: the six-case Node conformance suite passed, and the retained fixture still covers credentials, headers, assignments, URLs, nested structures, exact-key negatives, UTF-8, and idempotency.
- Removed `DOIT.p2.copy-feedback.md` because every inventoried text, image, terminal, Logs, API/SSE, editor, menu, and popover copy path now routes through the shared feedback parent or its synchronous activation adapter. Current verification: the copy ownership and behavior assertions passed inside `tests/layout_async.test.js` (156/156), including exactly one raw text clipboard owner, image feedback, and terminal synchronous activation.
- Removed `DOIT.p2.terminal-wheel-consistency.md` because discrete pixel-wheel input, continuous fractional input, direction, clamping, route transitions, and alternate-screen versus tmux routing are implemented and documented. Current verification: `tests/editor_preview_tmux.test.js` passed 60/60, and the durable contracts remain in `docs/DEVELOPMENT.md`, `docs/specs/GUI.md`, and `docs/specs/TMUX.md`.
- Split `DOIT.p0.interactive-api-lanes.md`. Per-lane jobd admission and the dedicated bounded mutation lane shipped in v0.7.2, and per-lane completion-service reservations shipped in v0.7.3. Its four unresolved and differently owned requirements now live in `DOIT.p1.operation-journal-port-scope.md`, `DOIT.p1.queued-operation-pending-indicator.md`, `DOIT.p1.filesystem-delete-lane-split.md`, and `DOIT.p2.jobd-bulk-fairness.md`; no P0 lane finding remains in the audited umbrella.
