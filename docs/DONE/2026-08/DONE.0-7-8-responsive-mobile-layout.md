# 2026-08-17 Responsive mobile layout, Preview scrolling, and terminal input

Closed the v0.7.8 mobile queue after Keiven accepted the corrected build on an iPad running iPadOS 26.6 in a split pane. Preview scrolling, terminal input, and copy/paste were reported perfect. Browser, orientation, viewport dimensions, and a pre-fix real-device trace were not retained; Keiven directly instructed the remaining boxes to be marked done, so those missing fields are recorded rather than inferred.

Preview snap-back was a stale deferred-write transaction. `restoreElementScrollPosition()` and `restorePaneElementScrollState()` could capture an old position, allow a native touch scroll, then write the old coordinate from animation-frame or timeout work. The deterministic red-first ledger reproduced `148 -> 100`. The shared deferred-scroll owner now requires both live geometry and the Preview touch-ownership generation to remain current across layout restoration, renderer hydration, zoom, split reflection, popout reflection, and URL-layout restoration; a real vertical touch invalidates stale work before WebKit reports its later geometry.

Mobile terminal typing stays on the existing `focusTerminalFromUserAction()` and xterm transport owners. An unclaimed tap focuses the hidden textarea, while a classified vertical pan retains terminal scrolling and the existing synthetic-mouse suppression. The trusted-touch browser journey uses a real xterm, WebSocket, tmux PTY, native text, IME, Backspace, Return, paste, pan, Preview/pane switching, accessory open/close, keyboard viewport resize, and rotation, and checks that each marker reaches the PTY once.

Responsive phone/tablet behavior uses the existing viewport/layout owners rather than a second mobile state store. The final iPad refinements removed special Editor/Preview/Diff button sizing, kept the Preview-font group in flex flow to prevent overlap, and routed Edit-only Upload through the same compact 20 px toolbar geometry so Edit and Preview retain one row height. Terminal accessory keys and pane-resize handles keep their interaction-specific touch geometry.

The final integrated `python3 tools/check.py --cpu-percent 35` attempt is `/tmp/yolomux-078-final-gate.json`. Node syntax, whitespace, Python compilation, all 19 Node shards, E2E, and the timing-sensitive serial lane passed; 17,327 non-browser tests had one architecture-ratchet failure, and the mobile boot lane had only three stale 44 px assertions after Keiven rejected special iPad sizing. After regenerating the final architecture manifest and changing those assertions to desktop geometry, the static lane passed in 20.89 seconds and the complete mobile boot lane passed in 73.44 seconds, recorded in `/tmp/yolomux-check-runs/check-1786992189557848457-298673.json`. The exclusive phase refused the dirty host because disk busy measured `0.921972` against the `0.9` limit; it was not rerun or represented as certified under Keiven's explicit reduced-evidence instruction. After queue removal, 7771 restarted as PID 511459 from `/home/keivenc/dev/yolomux.dev7771`; served/source JavaScript both hash to `53c63fc23547923e6d613d442ed84ba01cc56929a559d2d0b38fc535a49cf03b`, served/source CSS both hash to `3cdf1a10a0b0af994044e8cbb08f6918dbb2e3e0eaa3b5aefd05111af1cdef84`, and unauthenticated `/api/ping` returns the expected 401.

Existing parents checked: deferred layout/pane scroll restoration plus its Preview touch generation; `focusTerminalFromUserAction()` plus the existing xterm transport; `normalizeLayoutSlotsForViewport()` for responsive focus; and the shared compact file-editor toolbar selectors for controls. Searches covered every `scrollTop` writer, Preview renderer lifecycle, terminal touch/focus/input stage, coarse-pointer editor selector, and generated-source owner. No parallel mobile terminal sender or mobile layout store was added. The coordinated non-generated v0.7.8 worktree diff at closure measured `+3867/-404`; mobile-only line attribution is unavailable because the release items share the same uncommitted integration batch.
