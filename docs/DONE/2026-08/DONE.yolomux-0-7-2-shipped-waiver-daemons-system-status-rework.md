# 2026-08-10 YOLOmux 0.7.2 shipped by waiver — Daemons/system-status rework and two security closures

- Shipped as `YOLOMUX_VERSION = "0.7.2"` (`yolomux_lib/common.py:7`, `yolomux_lib/infra/common.py:64`), tag `v0.7.2`, landed on local `main` as one squashed commit over the inclusive range `9a960fc2c..98741bcb2` (32 commits; `9a960fc2c` = `v0.7.1`). This entry is the durable acceptance record: the 0.7.2 queue (`DOIT.p0.v0.7.2.md`) is gitignored and was removed at land, and its 24 deferred items moved to `DOIT.p0.v0.7.3.md` with their evidence and exit criteria intact.
- Daemons/system-status rework shipped: real per-service metrics including the YOLOmux web process, with unmeasured values rendered as `—` plus a machine-readable reason code (never a fabricated `0`); `/api/system-status` is now a background-published immutable snapshot read O(1) on the request thread (measured cold 51.622→1.150ms handler, steady p99 20.233→1.148ms, body 69505→57670B on an isolated fixture server — the live-authenticated p99 was NOT taken and is deferred to 0.7.3); Advanced diagnostics fetched lazily only while the disclosure is open; the summary strip's CPU sum and "of N" count now use one source matching the render.
- Two security closures, each reproduced then fixed: (1) the cross-port jobd confused deputy — filesystem job descriptors executed under the launcher's environment; the accepting server's immutable access policy is now captured at HTTP admission, carried on the descriptor, hashed into the coalesce key, and enforced at point of use through one `filesystem.paths.FilesystemAccessPolicy` parent (broad-daemon/narrow-caller and inverse red-then-green, six-reason fail-closed matrix); (2) the watchd cross-policy path disclosure — `wait_revision`/`apply_watchd_revision` fanned the caller-independent union of roots and changed_paths to a narrow server's browser; the revision consumer now intersects roots/changed_paths/signature against the server's own authorized roots before publishing `fs_changed`/`filesystem_history`, with the S0 content-admission 403 preserved.
- Interactive-API lane isolation: per-lane 64-slot queue cap (a full bulk queue no longer refuses an idle point read) plus a dedicated mutation lane off the capacity-1 interactive lane; jobd protocol 20→21.
- Gate-harness hardening (F8): the pytest harness now acquires the tool flock before the worktree writer lease so a queued run holds no lease while it waits, and the reaped-container guard signals the whole session group so a leaked `docker run` wrapper cannot keep holding the lock.
- Honest acceptance: the full gate `python3 tools/check.py` at candidate `98741bcb2` did NOT pass — it FAILED four nodes under 16+10-worker saturation: `test_service_module_entrypoint_exits_cleanly_on_sigterm[jobd]`, `test_a7_missing_file_is_typed_404_not_transport_failure`, `test_generated_share_link_mirrors_interactive_ui_surface_matrix[chrome]`, and `test_live_compact_menus_root_opens_on_touch_sized_topbar`. Each was run isolated on a quiet box (load < 4) on that exact HEAD and passed (sigterm[jobd] 1.54s, a7-404 4.73s, share_mirror 15.98s, compact_menus 14.77s; four `/tmp/iso_*.log` retained with SHA256s). They are load-only contamination root-caused to a contention-blind jobd RPC/health deadline and a Selenium stale-element re-render, both moved to `DOIT.p0.v0.7.3.md` as gate-hardening owners. `python3 tools/static_build.py --check` exits 0.
- Release is by the user's explicit 2026-08-09 waiver narrowing 0.7.2 to the security closure, NOT on a green gate. Authenticated real-Chrome smoke on 7772 served from dev7771 HEAD `98741bcb2` recorded zero JavaScript errors or unhandled rejections and zero new server Warning/Error rows (evidence `/home/keivenc/.claude/jobs/e8f5a76b/tmp/qa_smoke_072.{py,json}`, hashed). No 600s `live_browser_soak` was run on this SHA — the 600s tool floor stands and was not lowered; the soak, the live p99, and 22 other deferred items move to `DOIT.p0.v0.7.3.md`. 7772 is prior-candidate QA at `98741bcb2` and is not exact-final-SHA (squashed) evidence.
