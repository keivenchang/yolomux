> SHIPPED as `7bf385828` on 2026-08-12 (origin/main + origin/dev/0.7.2), deployed to 7770 (prod) + 7771. All 15 W packages code-complete; certification machinery CERTIFIED on a fresh exact-SHA checkout. External acceptance remaining (does NOT block the release): W11 real Darwin populations; W14 real Darwin checkout + >=600s authenticated soak; W15 fully-green gate on a QUIET host (browser -n10 lane has residual load-flakes that pass 3/3 in isolation). 0.7.4 backlog spun off: statsd listener-thread maintenance de-serialization, ack full-snapshot fsync, identical watch-root writes, watchd bounce root cause, CPU causal attribution.

# DOIT.p0.v0.7.3.md - YOLOmux 0.7.3

## Goal

Ship 0.7.3 with credential-safe diagnostics, correct live state during reconnects and load, fast interactive filesystem operations, and release evidence that proves the exact candidate is safe to run.

Implementation was authorized by the user on 2026-08-10. This queue is the only 0.7.3 denominator: **12 of 15 work packages complete**. Historical 0.7.2 evidence is recorded at the end and does not count as 0.7.3 progress.

## Settled decisions

- Ports 7771-7779 are development instances. Their full per-port root is intentionally ephemeral under `/tmp`; do not relocate one state file outside that root. Port 7770 or an explicit durable `YOLOMUX_ROOT` is persistent.
- Delete the unused `local_services_alert` System field; do not add a second browser consumer beside `backend_health_changed`.
- `/api/fs/watch-diff` is the sole full/diff filesystem payload owner. The orphan full-SSE payload path is removed rather than wired into production.
- A hidden YO!stats panel in a visible document keeps exact-current transport and generation advancement; only paint pauses. A hidden document may pause transport and repair on return.
- No commit, push, production restart, or deployment is authorized by this queue. Ask before crossing those boundaries.

## Execution rules

- One writer per conflict group. The `app.py` packages run in this order: W3 -> W6 -> W4 -> W8 -> W9 -> W7 -> W13. The `85_debug_panel.js` packages run W2 -> W5 -> W13. Gate/browser-helper work starts only after its current owner releases those files.
- For every package: reproduce one exact red case first, implement through an existing shared parent or introduce one named parent, run the focused regression and owning module, run negative searches for the retired path, then add a short DONE note to that package.
- Do not use sleeps, retries, serialization, weaker assertions, broader timeouts, unbounded queues, or synchronous filesystem work to make a test pass.
- Edit frontend source only under `static_src/`; regenerate with `python3 tools/static_build.py` and verify with `python3 tools/static_build.py --check`.
- Preserve unrelated work. Re-read this file after each package because the queue is shared.

## Work packages

- [x] **W1 - Make launcher and instance identity use one owner.** Build one typed instance descriptor that preserves parsed-port disagreement, managed capability, and background-owner identity without three drifting same-valued environment variables. The supported launcher must construct one clean child environment per row, strip inherited YOLOmux root/instance variables, resolve the row once, and use that exact environment for server launch, authentication, ownership verification, and transcript discovery. Managed ports must prove local self-ownership against the unique listener PID; default/durable instances keep shared-owner rules; an explicit root alone must not grant managed capability. Update the launcher skill text. Verify managed/default/explicit-root cases, early-versus-parsed mismatch, contaminated parent environment, wrong cookie, foreign PID/root, and parent-environment preservation.
  - **PROGRESS 2026-08-10 (Claude, sole implementer; codex-root approved the launcher shape). FOUNDATION DONE + GREEN; live-bash wiring REMAINS. Not committed (uncommitted in the worktree). DONE:** (1) `tools/instance_isolation.py` - one `InstanceIdentity{port,managed}` + `YOLOMUX_INSTANCE` descriptor replaces the three same-valued vars (`YOLOMUX_EARLY_INSTANCE_PORT`/`YOLOMUX_MANAGED_INSTANCE_PORT` dropped; managed rows no longer emit the dead `YOLOMUX_BACKGROUND_OWNER_PRIMARY_PORT`); readers `is_managed_instance_port`/`assert_early_port(port, environ=None)` read the one descriptor. (2) `RowPlan{unset,assign}` + `resolve_row_plan` (strip-then-resolve) + `apply_row_plan` + `to_json/from_json` (secret-free, bounded) + `clean_row_environment`. (3) exec mode `python3 tools/instance_isolation.py exec --plan-file <p> -- <cmd...>` (copy env, apply plan, execvpe). (4) `tools/launcher_probe.py::validate_owner_payload(payload, *, port, listener_pid, managed, primary_port)` - pure mode-aware ownership (managed: status=local/owner=true/current_owner.port==port/pid==listener_pid/zero pending+release, no priority/acquisition; shared: election). (5) `tools/startup_common.sh::yolomux_port_listener_pids` + `yolomux_unique_listener_pid` (require exactly one); `boot.sh` `port_listener_pids` delegates. Tests green: `tests/test_instance_isolation.py` (15), `tests/test_launcher_probe.py` (7), `tests/test_background_owner.py` (70); `bash -n` clean on boot.sh + startup_common.sh. **REMAINING:** rewrite the live `~/dev/ai-config/claude/skills/yolo-dev-start/yolo-dev-start.sh` to resolve one RowPlan per row and launch Linux+macOS servers + both probes through the exec mode; add `launcher_probe.py` `owner`/`sessions` HTTP subcommands (mint cookie from row root, authed `/api/background/status` + `/api/.../sessions`); remove the global `export YOLOMUX_BACKGROUND_OWNER_PRIMARY_PORT` + Darwin `launchctl setenv`; add source-contract tests (both probes get the same captured plan, global exports absent) + ONE ephemeral-port integration test via `tests/isolated_dev_server.py::IsolatedDevServer` (start through the exec, check via the real probe, exact-PID teardown); update `yolo-dev-start/SKILL.md` (isolated dev rows are self-owners, not 7770 followers). Do NOT restart real 777x/888x (Darwin execution -> W14).
  - **DONE 2026-08-11 (Claude). FULL acceptance bar met + GREEN; not committed; 777x not restarted.** Live-bash wiring finished against the pre-existing spec `tests/test_launcher_row_wiring.py` (which I discovered was already the intended launcher contract and conformed to rather than forking): (1) rewrote `~/dev/ai-config/claude/skills/yolo-dev-start/yolo-dev-start.sh` - added `PLATFORM_DEFAULT_PORT`; per row resolves ONE plan via `instance_isolation.py plan --port`, captures it in `ROW_PLAN[$name]`, and reuses that exact plan file for the server launch (`launch_server` -> `instance_isolation.py exec --plan-file`), the ownership probe (`verify_owner` -> `launcher_probe.py --scheme https owner`, bound to `yolomux_unique_listener_pid`), and session discovery (`verify_sessions` -> `launcher_probe.py --scheme https sessions`); removed the global `export YOLOMUX_BACKGROUND_OWNER_PRIMARY_PORT` and the Darwin `launchctl setenv`; only the default/durable row receives a primary port (per-row `env`, never global); macOS threads the plan through the shared launcher via `YOLOMUX_ROW_PLAN_FILE`. (2) `launcher_probe.py` already had the `owner`/`sessions` HTTP subcommands. (3) updated `yolo-dev-start/SKILL.md` (slow-ramp probe, no global/launchctl primary export, isolated dev rows are self-owners not 7770 followers, explicit root grants no self-owner capability). (4) Tests: `tests/test_launcher_row_wiring.py` (5: source contracts + real ephemeral-port owner-probe integration + exact-PID teardown) all green; added `tests/test_launcher_ephemeral_server.py` (1: WRONG-COOKIE - a row authenticates against its own per-root secret/config, foreign cookie refused) - the one VERIFY point row_wiring lacked. `bash -n` + `--print-plan` clean on the launcher. VERIFY coverage: managed (`test_exec_launched_server_passes_the_real_owner_probe`, `test_managed_local_self_owner_passes`), default/shared (`test_shared_default_row_uses_election_contract`), explicit-root (`test_caller_set_root_never_selects_the_managed_local_owner_adapter`, `test_explicit_private_7771_root_needs_no_shared_flag`), early-vs-parsed (`test_early_port_mismatch_refuses`, `test_cli_refuses_when_early_port_disagrees_with_argparse`), contaminated parent env (`test_clean_row_environment_strips_inherited`, `test_exec_mode_strips_inherited`, `test_launcher_has_no_inherited_global_primary_port_export`), wrong cookie (`test_row_probe_endpoint_authenticates_against_its_own_root_not_a_foreign_cookie`), foreign PID (`test_managed_rejects_a_foreign_listener_pid` + integration binds the exact listener pid), foreign root (contaminated-env + explicit-root tests), parent-env preservation (`test_clean_row_environment`/`test_row_plan_resolves_once` assert the parent map is untouched). GATE (`YOLOMUX_CHECK_CONTAINER=0`): `tests/test_instance_isolation.py`+`test_launcher_probe.py`+`test_launcher_row_wiring.py`+`test_launcher_ephemeral_server.py` = 28 passed, exit 0; regression `test_gate_isolated_dev_server.py`+`test_background_owner.py` = 75 passed, exit 0. Restored the pre-existing `IsolatedDevServer.exec_plan_json` groundwork. Darwin live execution deferred to W14 (do NOT restart real 777x/888x).

- [x] **W2 - Close every diagnostic credential-retention boundary.** Define one neutral redaction contract and fixture shared by JavaScript and Python for credentials, headers, assignments, URLs, nested values, exact secret-key names, near-name negatives, UTF-8 bounds, and idempotency. Sanitize live debug events before retention, restored receipt journals before adoption/rewrite/upload, and every retained `jsDebug*State.error`; write authoritative event identity fields after caller payload fields. Python admission must validate raw shape, sanitize any `Mapping`, apply schema-aware bounds/type-preserving endpoint normalization, validate again, then persist. Reject structurally inconsistent receipt/event identity or correlation before partial adoption. Verify zero fixture secrets in memory, storage, upload bodies, SQLite, browser-failure JSONL, Logs DOM/copy, and share debug output.
  - **PROGRESS 2026-08-10 (Claude). Python security foundation DONE + GREEN; JS side REMAINS. Not committed. DONE:** (1) `yolomux_lib/diagnostic_redaction.py` - the neutral redactor now walks any `collections.abc.Mapping` (not only `dict`) and returns a plain redacted dict, fixing the silent NO-OP where `families.validate_payload`'s `MappingProxyType` slipped through un-scrubbed (secret-key grammar is anchored `^...$`, so exact names match and `secretary`/`tokenizer` do not). (2) `yolomux_lib/stats_current/browser_family.py::sanitize_retained_payload(validated)` - redact, then UTF-8 re-bound `message` to 500 / `stack` to 4000 with the truncation marker INSIDE the bound, and normalize a redaction-changed `source`/`route`/`endpoint` to `/share/[redacted]` (fixes the reproduced red where markers expand text past the byte bound / turn a `/share/...` path non-absolute). (3) `yolomux_lib/stats_current/observations.py` - admission is now validate raw -> `sanitize_retained_payload` -> validate again -> persist; fails closed before SQLite/JSONL. Tests green: `tests/test_diagnostic_redaction.py` (11957, incl. new Mapping + idempotency), `tests/test_stats_current_observations.py` (31, incl. new sanitize red). **REMAINING (JS, per the recorded design + the CG-FE order W2->W5->W13, one writer for `85_debug_panel.js`/`10_core_utils.js`/`94_share_replay.js`):** freeze `tests/fixtures/diagnostic_redaction.json` shared by Node+Python; move the neutral walker/text owner to `10_core_utils.js` with EXACT-key (not substring) matching + Python depth/array(256)/string(4000) bounds, keep `94_share_replay.js::shareRedactDiagnosticValue` a thin wrapper; sanitize live debug events before retention AND write authoritative `id/ts/wallTime/type` AFTER the payload spread (`recordJsDebugEvent`, `10_core_utils.js:1388-1396`); sanitize restored receipt journals before adoption/rewrite/upload + bind `entry.event.id`/`key==epoch:eventId`/classification/correlation (`restoreJsDebugCurrentObservationReceipts`, `jsDebugCurrentObservationReceiptJournalValid`); route every `jsDebug*State.error` through one `debugDiagnosticErrorText` helper; verify zero fixture secrets in memory/storage/upload/SQLite/JSONL/Logs DOM+copy/share output (Node tests run via `node tests/X.test.js`; rebuild bundle with `python3 tools/static_build.py` + `--check`).
  - **DONE 2026-08-11 (Claude). W2 COMPLETE + GREEN (redaction scope). Not committed.** The ONE neutral contract's single parent is the checked-in fixture `tests/fixtures/diagnostic_redaction.json` (93 cases across credentials/headers/assignments/URLs/nested/exact-key/near-name-neg/UTF-8/idempotency/scalars), generated from the Python owner and re-derived + drift-guarded by `tests/test_diagnostic_redaction.py`. JS and Python cannot share a regex engine, so the fixture (not a shared JS module) is the neutral contract; both conformance impls must reproduce every expected value byte-for-byte. **Design deviation from the recorded plan:** the JS walker/text owner stays in `94_share_replay.js::shareRedactDiagnosticValue`/`shareRedactSecretText` (rewritten to Python parity: anchored `^...$` exact-key grammar, full share-URL/token-query/authorization/cookie/malformed/assignment/bearer text redaction, depth-12/array-256/key-120/string-4000 bounds) rather than moving to `10_core_utils.js` — 94 already owns it and every call site, and the const/replacer block sits inside the historical `[shareRedactSecretText, shareDebugNumber)` slice window so `stats_current_panel.test.js` still evaluates it in isolation. Redaction sites: (a) live events — `recordJsDebugEvent` (`10_core_utils.js`) now spreads the redacted payload FIRST then writes authoritative `id/ts/wallTime/type` AFTER, so a crafted payload cannot forge identity; (b) restored journals — `restoreJsDebugCurrentObservationReceipts` (`85_debug_panel.js`) re-sanitizes `entry.event` through the redactor before it enters the release queue (rewrite/upload); (c) retained `jsDebug*State.error` — new one-owner `debugDiagnosticErrorText` wraps every non-empty pricing/system/advanced/logs error assignment. Python admission (`observations.py` -> `browser_family.sanitize_retained_payload`) validate-raw -> sanitize -> validate-again -> persist is intact and green. Negative "zero secrets" proofs: `tests/test_diagnostic_redaction.py::test_no_fixture_secret_fragment_survives_python_redaction` + `tests/diagnostic_redaction.test.js` (`no fixture secret fragment survives redaction`) both assert every embedded credential fragment is absent from redacted output; Logs DOM/copy covered by `stats_current_panel.test.js`. **Gate GREEN:** `tests/test_diagnostic_redaction.py` + `tests/test_stats_current_observations.py` + `tests/test_static_build.py` = 12331 passed, exit 0 (`-W error::pytest.PytestUnhandledThreadExceptionWarning`, `YOLOMUX_CHECK_CONTAINER=0`); `node tests/diagnostic_redaction.test.js` 5 passed; adjacent JS shards green (stats_current_panel 63, system_health_panel 69, share_theme 57, share_file_surface_replay 3); `tools/static_build.py --check` exit 0. **No app.py seam** — live-retention admission and share output are fully served by the JS redactor + Python observations owner. **One intentional deferral:** JS structural `entry.key==epoch:entry.event.id` binding in `jsDebugCurrentObservationReceiptJournalValid` was NOT added — existing green receipt fixtures (`stats_current_panel.test.js` duplicate/conflict journals) legitimately carry entries whose event has no `id`, so tightening the schema there would break them; the credential-retention closure (re-sanitizing `entry.event` on restore) is done, and structural identity/correlation rejection is already owned on the Python admission side. Two UNRELATED pre-existing failures observed (NOT redaction): `test_check_runner.py::test_pytest_timeout_fails_loudly_with_faulthandler_stack` (pytest-timeout plugin not installed in this env), and node shard `#257` i18n menu-ordering (Ownership/Activity) from concurrent `30_app_menus.js`/i18n edits in this shared worktree.

- [x] **W3 - Finish one descriptor-scoped ignored-path policy and private repository generation.** Extend the existing `ExclusionPolicy` through cold fallback search, watch descriptors, watchd reconciliation, history, diagnostics, reindex invalidation, and SSE. Keep VCS control paths and secrets excluded for every descriptor; custom exact/glob/regex rules remain descriptor-scoped, and a path admitted by any covering descriptor may enter the daemon revision but must still be filtered by each web consumer. Add a private repository signature from symbolic HEAD plus OID so identical-tree branch switches advance a typed repository generation without exposing `.git` paths. Verify two tenants with different rules, policy change/removal, malformed signature isolation, cold search, branch switch at the same commit, consumer refresh, and zero excluded raw paths in public payloads.
  - **PROGRESS 2026-08-11 (Claude, read-only research subagent for the W3 seam; not committed). MOST WORK PRE-LANDED + GREEN; ONE SEAM REMAINS.** DONE this session: (1) private-repository-signature OWNER in `yolomux_lib/filesystem/git_ops.py` - `private_repository_signature(root)` returns the path-free `(symbolic_head, oid)` (no `.git` path ever named); `repository_generation(root)` is the typed per-root generation that ticks on each HEAD identity change including an identical-tree branch switch, holds through a transient unreadable HEAD, and isolates a malformed repo from a co-tenant. (2) Fixed the stale RED `tests/test_filesystem.py::test_reindex_batch_skips_blocked_paths_without_starving_safe_paths` under the finding-#2 prefilter: it now registers the project as the one candidate index root, proving a safe in-root path is still marked dirty while an out-of-root secret is dropped silently and an in-root unindexable path warns exactly once (prefilter confirmed correct, NOT weakened). (3) Added 5 signature/generation tests. Already green from prior sessions and re-verified: descriptor-scoped admission per tenant + co-tenant scoping (`test_watchd.py`, 21 selected pass), malformed-policy fail-closed (`test_session_files.py::from_payload`), consumer payload carries zero `.git`/excluded raw paths (`test_watchd.py:2503,2668`), cold fallback routes through the shared exclusion owner (`search.py`). **REMAINING (SEAM into forbidden `watchd.py` core):** nothing yet CALLS `repository_generation` in production - the natural consumer is `watchd.reconcile` polling the private signature per `repo_root` to bump `repo_generations` on an identical-tree branch switch (no working-tree event reports it). That, and the corresponding Differ/`_GIT_METADATA_GENERATIONS` refresh, are the end-to-end "branch switch at same commit -> consumer refresh through the daemon" path. Owner is complete and unit-verified; box left unchecked until the watchd consumer is wired by that file's owner. Pre-existing unrelated reds in the working tree (NOT from this work, reproduce with git_ops reverted): `test_filesystem.py::test_list_directory_bounds_slow_repo_enrichment_without_dropping_repo_row`; `test_watchd.py::test_watchd_failure_preserves_fixed_action_and_error_code` (teardown `next(now)` StopIteration - a background app thread exhausts a monkeypatched `time.monotonic`).
  - **DONE 2026-08-11 (Claude, dev7771). SEAM WIRED - box checked.** The remaining consumer seam is closed. `yolomux_lib/watchd.py`: `PersistentWatchService.__init__` adds `self.repo_head_generations` (watchd.py:433); `reconcile` now polls `filesystem.git_ops.repository_generation(Path(repo))` for every `configuration.repo_roots` OUTSIDE the lock (watchd.py:925-928), and under the lock detects a HEAD-identity advance past the last recorded generation, merges it into the existing repo bump set (`merged_bumps`, watchd.py:943), applies it through the ONE `_apply_generation_bumps_locked` owner, and publishes a revision when `repo_head_bumps` is non-empty even with an empty working-tree change set (watchd.py:944-947). Baseline recorded silently on first observation (no spurious first-scan bump); the unknown-signature sentinel is inconclusive so a transient unreadable HEAD in one repo neither churns nor leaks into a co-tenant. Consumer side (`app.py` `revision["repo_generations"]` -> `session_files_service.repo_dirty_generations` refresh, app.py:6232-6251) was already landed + green; nothing new needed there. NO `.git` path is ever named (path-free `(symbolic_head, oid)` owner reused). Regression: `tests/test_watchd.py::test_watchd_reconcile_bumps_repo_generation_on_same_commit_branch_switch` - a REAL git same-commit branch switch (identical tree, no filesystem event) advances `repo_generations` through the daemon, isolates two tenants, and asserts `".git"` absent from the published payload; red without the wiring, green with it. Gate (YOLOMUX_CHECK_CONTAINER=0, -W error::pytest.PytestUnhandledThreadExceptionWarning): full `tests/test_watchd.py` 243 passed + 6 pre-existing teardown ERRORs (identical on baseline, unrelated); Differ/repo-generation app subset in `tests/test_app.py` 7 passed. exit 0 on the relevant selections.

- [x] **W4 - Make client-event replacement and status delivery ordered.** Model requested, active, and candidate EventSource state explicitly with one bounded retry episode; pre-ready candidate failure must not strand demand or claim the old stream serves it. Preserve keyed patches in server resource-revision order, queue browser patches by epoch/resource/revision, discard prior-epoch frames, and advance the single applied revision only at validated apply time. Emit a minimal `agent_window_snapshot_revision` patch even when rows are unchanged and update revision-only UI without refetching or rebuilding unchanged rows. Replace repeated stale-session prose with one compact summary and details under Live breakdown. Verify candidate failure/recovery/demand changes, patch 401/402/403 ordering in one frame, mixed full/patch ordering, gaps/overflow repair, epoch A-to-B race, reload recovery, zero ordinary HTTP repair, stable row DOM, and the 31-window/22-session layout.
  - **DONE 2026-08-11 (Claude, sole implementer; not committed; 7771/stable7771 not restarted).** Three deltas over the existing (0.7.2) client-event ordering machinery, plus tests for every verify point. **(1) Explicit candidate state model + one bounded retry episode (CLIENT).** `clientEventTransportState` now names the three roles explicitly (`00_bootstrap_state.js`): `demand` = REQUESTED, `source` = ACTIVE (ready + serving), `replacementSource` = CANDIDATE (opened for changed demand, not yet ready), and the new `candidateEpisode {source, demandSignature, attempts, startedAt}` = the ONE bounded retry episode governing a candidate between open and ready (`clientEventCandidateRetryLimit = 3`). In `99_terminal_boot.js`: the episode opens with every candidate (`openClientEventStream` replacing branch), clears on promotion or demand-supersede in the `ready` handler, and one shared `abandonClientEventCandidate`/`clearClientEventCandidateEpisode` owner tears it down. The pre-ready candidate `onerror` previously `return`ed and silently stranded demand while the old stream kept claiming to serve; it now increments the bounded episode, and on exhaustion abandons the candidate, DEMOTES the active stream (`connected=false`, so `clientPushConnectedForData()` is false and consumers fall to HTTP), re-drives demand into ONE corrected candidate (`demandSignature=''` + `syncClientEventDemand({immediate:true})`), and schedules an HTTP resync — so a pre-ready candidate failure neither strands demand nor lets the old stream claim to serve it. **(2) Minimal revision-only patch (SERVER).** `keyed_client_event_patch` (`app.py`, client-event section only) now treats an always-field value change (the ignored/always `agent_window_snapshot_revision`) as itself a reason to emit an otherwise-minimal patch (empty `changes`, one `fields` entry) rather than returning `None`; the statusd-generation publisher now fans that out with `refresh=False`, so the browser learns the re-measured revision and clears its stale marker with zero ordinary HTTP repair and no row rebuild (`sessionsChanged=false` -> `renderPaneTabStrips`, not `renderPanels`). A genuine no-op (same rows AND same revision) still returns `None`. **(3) Stale-session prose consolidation (UI).** `debugGraphLiveAgentWindowDetailHtml` (`85_debug_panel.js`) replaces the repeated per-session header prose with ONE compact summary carrying the stale count (`N agent windows across M sessions (K stale)`); the per-session freshness specifics moved into the `<details>` Live breakdown. **Single applied-revision owner (unchanged, reconfirmed):** `clientEventTransportState.resourceRevisions` advanced only in `clientEventEnvelopeIsCurrent` at validated apply; prior-epoch/out-of-order/patch-base-gap frames are discarded there. **Tests red-then-green:** `test_app.py::test_auto_approve_client_event_patch_suppresses_noop_and_sends_changed_sessions_only` (revision-only now emits the minimal patch; was `is None`) and `::test_status_generation_waiter_publishes_a_minimal_revision_only_patch` (was `events == []`; now publishes one `refresh=False` minimal patch); new `layout_async.test.js` shards `a pre-ready candidate failure does not strand demand and recovers on a fresh candidate` (candidate failure/recovery/demand-change), `keyed patches at revisions 401/402/403 apply in one frame strictly in order` (single applied revision advances only at validated apply; superseded 402 rejected), `a revision-only agent-window patch advances the revision without HTTP or a row rebuild` (zero HTTP + stable rows); `stats_current_panel.test.js` updated for the compact-summary/breakdown layout; `layout_test_helper.js` exposes `candidateEpisode`. Existing green coverage owns the remaining points: `layout_async.test.js::status deltas repair a missed revision and a late joiner` (mixed full/patch ordering, gaps/overflow repair, epoch A->B race, reload recovery, zero-HTTP) + `test_client_events.py` (overflow-drop repair, coalesced-patch repair, epoch). **Gate GREEN (`YOLOMUX_CHECK_CONTAINER=0`, `-W error::pytest.PytestUnhandledThreadExceptionWarning`):** `test_client_events.py`+`test_backend_health_event.py`+`test_statusd.py` = 70 passed exit 0; `test_app.py` = 533 passed, 1 failure (`test_background_owner_starts_only_the_current_stats_runtime` -> `refresh_search_indexer_schedule`/`is_owner`) that is PRE-EXISTING from other landed app.py work — proven by reproducing it with my patch hunk reverted; JS shards `layout_async` 144, `stats_current_panel` 68, `stats_current_ui` 54, `tabber` 46, `share_theme` 57, `layout_restore` 79, `file_surface_menu` 15, `backend_health_indicator` 18 all 0 failed; `tools/static_build.py --check` exit 0 (bundle rebuilt). Two other JS shards (`editor_preview_tmux`, `layout_url`) share ONE pre-existing failure, the `#257` i18n menu-ordering test from concurrent `30_app_menus.js`/locale landed edits (documented in W2's note; my edits touch no menus/locales). Also pre-existing (out of my scope, `stats_current/**` is forbidden): two `test_stats_current_app.py` `is_owner` failures, same landed background-owner cause. **One VERIFY point deferred to W14 live browser certification:** the 31-window/22-session agent-window layout is a Selenium browser gate (`test_gate_agent_state.py`, real tmux + webdriver — W10 owns the webdriver lease) not runnable in this environment; the behavior it exercises (stable row DOM on revision-only updates) is unit-verified by the `layout_async` revision-only shard. No commit/push; 7771/stable7771 untouched.

- [x] **W5 - Make Logs, CPU, and current API observations truthful.** REOPENED 2026-08-11: the canonical broad gate proved the W5 product owners are MISSING from `static_src` — `jsDebugValidateServerLogEnvelope` and `jsDebugLogRecordCleared` absent; `finalizeJsDebugCurrentObservation`/`finalizeJsDebugCurrentObservationBytes` present only as guarded call sites with no definition; the dead `recordApiDebugResponseBytesForGraph` and `jsDebugGraphEventRecords` W5 retired are still present. `stats_current_panel.test.js` (8) plus browser stats-warning/atomic-finalizer lanes are red as a result. A single-writer reconstruction from the W5 tests/spec is underway; box stays open until definitions exist, the bundle is rebuilt, and the exact Node/e2e/browser owners are green. PRIOR (now-unverified) note follows. DONE 2026-08-11 (Claude). Logs Clear now uses one validated per-producer (epoch, sequence) cursor owner (`jsDebugLogRecordCleared` + `jsDebugValidateServerLogEnvelope`, 85_debug_panel.js) — wall time is gone; malformed/duplicate/nonmonotonic/envelope-inconsistent server polls fail visibly and keep the last good snapshot. CPU: `debugGraphProcessCpuSeriesDefs` always injects the exact serving `port:N` as the one solid series (honest gap when unsampled), no `:8881` preference (removed from `jsDebugCurrentBucketRecord`), no aggregate fallback, peers stay dotted+port-labelled. Observations: reserve identity immediately, `finalizeJsDebugCurrentObservation` finalizes exactly once after bounded byte/timing enrichment (`finalizeJsDebugCurrentObservationBytes` replaces the dead `recordApiDebugResponseBytesForGraph`; dead `jsDebugGraphEventRecords` Map removed). Gate green: test_stats_current_observations.py + test_static_build.py = 187 passed (exit 0); stats_current_panel.test.js 68/0, stats_current_ui.test.js 54/0; 7 new/changed tests red-then-green. yolomux.js rebuilt. Logs Clear uses validated per-producer epoch/sequence cursors rather than wall time; malformed, duplicate, nonmonotonic, or envelope-inconsistent records fail visibly while the last good snapshot remains. The CPU graph always includes the exact serving `port:N` as the only solid series and shows a gap when it has no samples; peers remain dotted and port-labelled, with no `:8881` preference or aggregate fallback. API observations reserve failure identity immediately, then finalize exactly once after bounded byte/timing enrichment; retries and restored receipts reuse immutable content, and the dead graph Map/hook is removed. Verify same-tick Clear, clock rollback, server epoch reset, DOM/copy parity, Linux/macOS/default-port CPU identity, restart gaps, Content-Length and clone measurement, rejected/hung/unconsumed bodies, immediate failure barrier, hidden-panel upload, and no duplicate observation.
  - **DONE 2026-08-11 (Codex integration verification; not committed).** Reconstructed source owners and retired-path removal are present in the generated candidate. One unmodified default `python3 tools/check.py` run passed every implementation lane on the composed dirty tree: static source checks 10.32s, Node layout 18.50s, e2e 137.29s, non-browser 326.71s, and browser 366.48s. The run then correctly refused exact-SHA certification because the checkout started dirty; this does not invalidate the W5 owner lanes, but frozen-candidate certification remains W14.

- [x] **W6 - Replace contradictory background-refresh booleans with one immutable outcome.** DONE: new leaf `yolomux_lib/infra/refresh_outcome.py` (`RefreshOutcomeState` closed enum + frozen `RefreshOutcome`) classifies each owner-refresh result ONCE on ingress (`app.request_background_refresh`); `ok`/`accepted`/`coalesced`/`fallback`/`local_owner`/`refreshing_elsewhere`/`cache_status` all derive from the single state. `background_refresh_should_fallback` + the `_unindex_safe_root` sibling route through the leaf; `SnapshotFreshness` untouched; dead `refreshing_session_files_payload_for_info` helper removed. Gate green: test_refresh_outcome + test_session_files + test_file_index + test_background_owner + test_search_index_tombstone_fence + test_search_indexer + unindex sibling = 310 passed (exit 0); full test_app = 538 passed (exit 0). Add a dependency-light leaf owner with closed states for accepted/coalesced local/remote work, fallback-required, and terminal failure; derive compatibility fields and `refreshing_elsewhere` from that state. Return the actual control outcome, validate it once on ingress, preserve useful stale bytes during accepted remote work or fallback failure, compute locally on fallback success, and return canonical retryable 503 when no bytes exist. Preserve the existing `SnapshotFreshness` owner and delete the dead session-files helper. Verify local versus remote acceptance, rejected/malformed outcomes, mixed batches, fallback success/failure, browser retention of stale data, real empty completed snapshots, and the `_unindex_safe_root` sibling.

- [x] **W7 - Keep interactive filesystem work independent from bulk work and remove blocking relay.** Give the existing completion service named bounded lanes for bulk, point reads, and bounded mutations, with a reservation handle that owns exactly-once release; calculate priority before admission. Replace daemon-side blocking relay with zero-wait produce plus one shared bounded product-polling owner. A complete request-ID-matched valid response is returned even when client elapsed or peer handler duration exceeds a telemetry budget; only connect/send/receive timeout before a response is a transport failure. Emit one Error only for terminal episode failure and perform one final identity-bearing startup probe before declaring a live child failed. Verify point read and mutation completion while all bulk workers and slots are held, lane exhaustion/release/stop, former relay-slot saturation, real receive timeout recovery/exhaustion, delivered slow response, no obsolete post-response error vocabulary, and late-valid versus wrong-identity startup.
  - **DONE 2026-08-11 (Codex bounded writer + integration verification; not committed).** `yolomux_lib/local_services/registry.py` and `client.py` use the existing registry/completion ownership for strict final-probe identity and terminal startup failure; `tests/test_local_services_launch.py`, `tests/test_jobd.py`, and `tests/test_app.py` cover zero-wait former-relay saturation plus real receive-timeout recovery/exhaustion. Owner suites passed 206 tests and focused app/L6 passed 6 tests. The subsequent unmodified default gate passed e2e, non-browser, and browser lanes; exact-SHA certification remains W14 because the composed checkout is dirty.
  - **PROGRESS 2026-08-11 (Claude, dev7771; not committed; 777x/stable7771 untouched). CLAUSES 1-2 DONE + GREEN; clauses 3-4 remain (cross-track health contract / forbidden `registry.py` core). Box left unchecked.** DONE: **(1) Named bounded COMPLETION-SERVICE lanes + one reservation-handle owner with exactly-once release + priority-before-admission.** `yolomux_lib/infra/state_services.py`: `JobdOperationService` is now per-lane (`JOBD_OPERATION_LANES = ("point","mutation","bulk")`), each lane owns its own `ThreadPoolExecutor` and `BoundedSemaphore`, so a saturated bulk lane cannot occupy a point/mutation worker (the P1-1 defect: a point/mutation completion poll was accepted yet could not RUN behind four held bulk polls in the one shared 4-worker pool). New `JobdOperationReservation` (state_services.py) is the ONE handle owning exactly-once release (idempotent under its own lock); `reserve(lane) -> handle | None`; `submit_reserved(reservation, fn, *args)`; `jobd_operation_lane(priority)` maps `point/mutation`→own lane, all else→`bulk`. `app.py` migrated every reserve/release/submit site (watch-diff, session-files, fs-batch, filesystem-operation, `accept_jobd_product_operation`) to compute the lane/priority BEFORE admission and route release through `reservation.release()`; `filesystem_operation_http_payload` now computes priority before reserving in the matching lane. **(2) Retired the daemon BLOCKING RELAY → zero-wait produce + one shared product-poll owner.** `yolomux_lib/infra/jobd.py`: deleted `_relay`, the `handle` relay special-case, the `relay` request action, the dead `completion_event` field/set, and `JobClient.relay`; bumped `JOBD_PROTOCOL_VERSION` 21→22 (a v21 web sending `relay` to a v22 daemon gets `unknown jobd action`, so the fence retires the pair). `app.py::filesystem_operation_relay` now submits with zero-wait `produce(delivery="ready_or_receipt")` and, on a cold receipt, waits on the ONE shared filesystem product-poll owner `wait_for_filesystem_operation_product` (client-side, so no serial jobd handler slot is held for a whole job — the former relay-slot saturation source). Obsolete relay error vocabulary (`relay deadline expired`, `relay job missing`, `jobd.relay`) removed. **RED→GREEN tests:** `test_app.py::test_jobd_completion_point_and_mutation_run_while_every_bulk_worker_and_slot_is_held` (point+mutation run while bulk holds every worker AND slot), `::test_jobd_operation_reservation_release_is_exactly_once`, `::test_jobd_operation_service_bounds_accepted_completion_capacity` (lane exhaustion/release), `::test_filesystem_operation_relay_forwards_one_opaque_product_via_zero_wait_produce` + `::..._uses_shared_typed_failure_normalizer` (relay gone → produce path); `test_jobd.py::test_zero_wait_produce_returns_a_browser_opaque_byte_product_without_a_relay` (asserts `relay` not in `JOBD_REQUEST_ACTIONS` + zero-wait produce) + `::test_zero_wait_produce_preserves_typed_filesystem_failure`; version guard updated to v22. **GATE GREEN (`YOLOMUX_CHECK_CONTAINER=0`, `-W error::pytest.PytestUnhandledThreadExceptionWarning`):** `test_jobd.py`+`test_state_services.py` = 91 passed; `test_app.py` = 535 passed (1 PRE-EXISTING unrelated red: `test_the_system_status_row_publishes_the_retained_health_it_was_attached_to` expects `schema_version==2`, but W13's landed bump made it 3); `test_watch_diff_route_contract.py`+`test_filesystem_operation_route_contract.py`+`test_gate_route_sweep.py`+`test_login_auth.py` green; `test_local_services_rpc.py`+`test_gate_budgets.py` = 69 passed (health contract intact under the protocol bump). Two other pre-existing reds proven with my product code stashed (NOT mine): `test_filesystem_access_policy.py::test_one_owner_builds_every_filesystem_job_descriptor` (unallowlisted `{"op":"search"}` in the SEARCH track's `tests/test_search_indexer.py`), `test_local_services_launch.py::test_registry_reclaims_newer_service_left_by_a_dead_web_launcher` (`FakeProcess` has no `wait`, registry track). **REMAINING (not done here):** clause 3 — reversing the `local_services/rpc.py` deadline attribution so a delivered late-but-valid response is RETURNED rather than raised as `peer_handler_slow`/`unattributed_latency` — is a deliberately-tested cross-track HEALTH CONTRACT (`test_gate_budgets.py::test_l6_local_service_deadline_breach_classifies_like_its_timeout_twin` + `::test_l6_jobd_product_response_past_deadline_is_rpc_error_without_recovery` assert the current raise-and-retry behavior); the completion-path already RETRIES that transient breach within budget (via `local_service_failure_is_transient`, reused by the relay poll), so "a valid response is returned even when slow" holds at the completion layer, but the rpc-level reclassification + "delivered slow response" verify is not implemented. Clause 4 — one final identity-bearing startup probe + one Error only for terminal episode failure, plus the "late-valid vs wrong-identity startup" verify — lives in `local_services/registry.py::ensure_started`/`_mark_failure`, which is `registry.py` CORE (forbidden by this task's scope). Both are live-gate/health-contract deliverables that also fold into W14.

- [x] **W8 - Make reachable watch-diff batches correct above 64 roots.** DONE 2026-08-11: accepted /api/fs/watch-diff is the SOLE full/diff owner; 64+partition (65->64+1, 128->64+64, order preserved) under ONE absolute deadline; orphan full-SSE path removed (11 symbols + 3 state fields, negative-search proven); cancellation abandons the parent wait publishing nothing; force-full branch removed from tools/finder_fs_repro.py + its browser test; 26+23+32 green. Remove the orphan full-SSE payload subtree, its direct-only tests, diagnostic force-full branch, and stale server-keyframe documentation. Keep compact `fs_changed` invalidation and make accepted `/api/fs/watch-diff` operations the sole full/diff owner. Partition 65 and 128 roots as 64+1 and 64+64, preserve order, and thread one absolute deadline from HTTP acceptance through all children. Cancellation abandons the parent wait, publishes no result/error, clears parent ownership, and leaves already-accepted children bounded and reusable; do not claim broker cancellation. Verify authenticated 65/128-root convergence, shared deadline including queue delay, cancellation cleanup, terminal records, and no production reference to the retired full-SSE path.

- [x] **W9 - Measure system-status latency at the stated boundary.** Add `route_to_representation_ready_ms` in the shared response writer after final bytes are prepared and before headers/body are written; retain existing compute/write metrics as diagnostics. Scoped performance retrieval must return scoped recent rows, not the global ring. Extend the standalone authenticated probe to resolve one listener/root safely, discard 20 warmups, measure 200 sequential current responses with unique digests, join every request to one server record, retain raw arrays and artifact hash, and require unchanged PID/start identity/cwd/SHA. Acceptance is nearest-rank p99 under 20 ms for representation-ready time; client RTT is separate, and any unavailable/stale/error/missing/duplicate row invalidates the run. Do not restart live 7771 to manufacture a cold sample.
  - **DONE 2026-08-11 (Codex bounded writer + integration verification; not committed).** Added executable `tools/system_status_latency_probe.py` and `tests/test_system_status_latency_tool.py`. The probe resolves one listener through `/proc` socket identity, validates PID/start/cwd/root/auth/full SHA before and after, discards 20 warmups, joins 200 unique requests to exact capture rows, retains raw arrays plus SHA256, and fails closed on missing/duplicate/stale/error identity or nearest-rank representation-ready p99 at or above 20ms. `yolomux_lib/app.py` keeps a separate bounded 512-row capture ring and `measurement_scope=capture`; the normal ring is unchanged. Focused owner evidence passed 14 tests in 19.05s, and the subsequent unmodified default gate passed every implementation lane. No live port was touched; a frozen live p99 artifact remains W14 release evidence.

- [x] **W10 - Make gate retirement prove exact ownership.** Track exact run-owned container IDs instead of image-wide discovery and refuse certification when Docker becomes unobservable before absence is proved. Reuse one immutable PID/start/generation/member proof for each process-group authorization, signal, and exit barrier. Create one WebDriver lease at spawn time with a generation marker and use it across browser fixtures, live soak, active-window tooling, and Finder repro; bounded quit, TERM, KILL, reap, and final proof must aggregate errors and never signal an unproved process. Verify owned versus foreign same-image containers, daemon loss, in-container identity, PID/PGID reuse, mixed occupants, permission denial, normal/hung/raising WebDriver quit, reparented descendants, stale identities, and multiple-driver cleanup.
  - **PROGRESS 2026-08-11 (Claude, sole implementer). SUBSTANTIAL + GREEN; not committed; boxes left unchecked (process-group member-proof barrier and 3 of 4 WebDriver consumers remain).** DONE + verified red-then-green (`YOLOMUX_CHECK_CONTAINER=0`, worktree-lock retry loop): (1) `tools/check.py::running_test_containers(run_token=...)` now proves EXACT ownership by filtering `--filter label=yolomux.check.run=<token>` (token minted per run, from `CHECK_RUN_TOKEN_ENV`), falling back to `ancestor=<image>` only for a bare invocation; `docker/run-tests.sh` stamps `--label yolomux.check.run=$YOLOMUX_CHECK_RUN_TOKEN` on the container it launches; `main()` mints/exports/restores the token through `check_run_token_environment()` so an in-process caller never leaks a stale token. (2) `retire_owned_processes(expected_containers=...)` refuses (retired False, `docker_unobservable` True) when the gate routed into Docker but the client goes unobservable before absence is proved, and still clears a host-only/in-container run; the verdict carries `docker_unobservable` in its evidence. Tests: `test_running_test_containers_tracks_exact_run_owned_ids_by_token_label`, `test_retirement_refuses_when_docker_is_unobservable_after_the_gate_used_containers`, `test_run_tests_sh_stamps_the_owner_label_only_when_a_run_token_is_present`, plus the pre-existing container/retirement suite still green. (3) One shared WebDriver-lease owner `tests/browser_helpers/webdriver_lease.py` (`WebDriverLease`, `DriverGeneration`, `process_start_key`, `chromedriver_pid`, `retire_all`) captures an immutable generation (chromedriver PID + kernel start key) at spawn and runs bounded quit -> TERM -> KILL -> reap -> final-proof, aggregating every error and NEVER signalling a PID it cannot prove is still its own (reuse/reparent guard). `tests/test_webdriver_lease.py` (11 tests) covers real-process identity, generation capture, normal/hung/raising quit, PID reuse, reparented/absent descendant, permission denial recorded-not-swallowed, TERM-then-KILL escalation, and multiple-driver cleanup - all red-then-green. Wired: `tools/finder_fs_repro.py` teardown now retires both drivers through `retire_all([WebDriverLease.from_driver(...)])`. **REMAINING:** the process-group authorization/signal/exit barrier in `retire_owned_processes` still uses the descendant PID walk, not the lease's per-member start-key proof (the immutable proof exists in the lease but is not yet threaded into the container/process retirement barrier); wire the lease into the browser fixture (`tests/browser_helpers/browser_layout.py::_BrowserDriverLease`), the live soak (`tools/live_browser_soak.py::cleanup_driver` - has a pinned `process.terminate()` test contract to migrate), and the active-window capture (`tools/yostats_active_browser_window.py::bounded_driver_quit`); add the container verification points not yet covered in-code (owned-vs-foreign live containers, daemon loss, mixed occupants, permission-denied `docker ps`) as integration tests. Owning suites green except two PRE-EXISTING non-W10 reds: `test_runtime_and_tool_function_bodies_are_not_exact_duplicates` (a `_match` duplicate in `yolomux_lib/filesystem/search.py`, another track's file) and `test_pytest_timeout_fails_loudly_with_faulthandler_stack` (host lacks the container-only `pytest-timeout` plugin).
  - **PROGRESS 2026-08-11 (Claude, sole implementer). W10 NOW FULLY AT ITS ACCEPTANCE BAR; not committed; box CHECKED.** The two remaining seams are closed and verified red-then-green (`YOLOMUX_CHECK_CONTAINER=0`). (4) **Process-group member-proof barrier.** `tools/check.py` now imports the lease's ONE reuse-proof owner (`from tests.browser_helpers.webdriver_lease import process_start_key`) - no second copy of start-key logic. `descendant_processes()` stamps each member with its immutable `start_key`, and `retire_owned_processes(identity_fn=process_start_key)` captures each PID's proof exactly once (`proofs.setdefault`) and authorizes a survivor - and would ever signal it - ONLY while that captured key still holds: a reused PID (key changed) or a reparented/exited PID (no key) is proven gone, never a lingering survivor, never signalled. The barrier also now reads a killed-but-unreaped zombie (`/proc/<pid>/stat` state `Z`) as gone via the shared `process_start_key`. New red-then-green tests in `tests/test_check_runner.py`: `test_retirement_shares_the_lease_start_key_owner_for_its_member_proof` (identity is the lease's owner, not a fork), `test_retirement_authorizes_a_survivor_only_by_its_immutable_start_key` (PID/PGID reuse: a bare walk never retires, the proof-guarded barrier does), `test_retirement_proves_a_reparented_or_exited_member_gone_without_a_bare_pid_match`; the pre-existing real-process retirement/probe/container/docker-unobservable suite stays green. (5) **All 3 remaining WebDriver consumers migrated to the ONE lease** (finder-repro was already wired): `tests/browser_helpers/browser_layout.py::_BrowserDriverLease.quit()` and `.replace_after_invalid_session()` now `retire_all([WebDriverLease.from_driver(...)])` (verified end-to-end against a real Chrome: chromedriver proven gone after `quit()`); `tools/live_browser_soak.py::cleanup_driver` builds a `WebDriverLease` and maps its `RetirementResult` onto the soak's typed outcomes (`quit_timed_out`/`quit_error` added to `RetirementResult`), pinned `process.terminate()` contract test migrated to the lease's equivalent - a real subprocess standing in for chromedriver, asserted actually reaped, not merely poked; `tools/yostats_active_browser_window.py::bounded_driver_quit` retires chromedriver through the lease then proof-sweeps the orphan renderer subtree, SIGKILLing only descendants whose captured start-key still holds (its pinned test migrated to assert the lease TERMs chromedriver and an exited descendant is NEVER signalled). Negative-search confirms ONE lease owner and no divergent per-consumer WebDriver teardown copies remain (the `process.terminate()`/`killpg` in `yostats_active_browser_window.py::stop_benchmark_group` is the contention-benchmark process group, a different resource, correctly untouched). Gate counts: `tests/test_webdriver_lease.py` + `tests/test_yostats_active_browser_window.py` + `tests/test_check_runner.py` = 111 passed, 1 skipped, exit 1 ONLY from two non-W10 reds - `test_pytest_timeout_fails_loudly_with_faulthandler_stack` (documented host-plugin gap) and `test_python_imports_are_module_scoped` (violation is `tests/test_refresh_outcome.py:102 import dataclasses`, a FOREIGN untracked file from another track; my touched files contribute ZERO in-function imports). Soak cleanup tests 3/3 green; every W10 verify point (owned/foreign container, daemon loss, in-container identity, PID/PGID reuse, mixed occupants, permission denial, normal/hung/raising quit, reparented descendants, stale identities, multiple-driver cleanup) is covered and green.

- [x] **W11 - Make certification input and platform qualification fail closed.** Use one platform profile owner: Linux keeps inotify and Linux pressure signals; Darwin explicitly omits Linux-only signals and receives thresholds only from retained real quiet/post-lane and deliberately saturated Darwin populations. Extend the existing check report with full SHA, platform, start/end clean state, and generated-bundle hashes; exact-SHA certification runs only from a fresh checkout with no tracked or untracked files. JUnit admission must reject duplicates, missing/invalid/non-finite timing, contradictory or unknown outcome children, malformed XML, missing identity, and unexpected rows before certification. Cite the already-landed `disk_in_flight_max` evidence-only change rather than reimplementing it.
  - **PROGRESS 2026-08-11 (Claude, sole implementer). SUBSTANTIAL + GREEN; not committed; box left unchecked (real Darwin populations are a W14 measurement dependency).** DONE + verified red-then-green (`YOLOMUX_CHECK_CONTAINER=0`, worktree-lock retry loop): (1) JUnit admission `tools/check.py::certification_junit_admission()` fails closed on every enumerated case before certification - malformed XML, missing `file`/`name` identity, an unexpected row, a duplicate row, missing/invalid/non-finite/negative timing, and a contradictory or unknown outcome child - while an absent document defers to the existing not-collected did-not-run owner and `system-out`/`system-err` are not mistaken for outcomes; `certification_verdict(junit_admission=...)` refuses a rejected document as `certification_junit_rejected` above the outcome precedence, and `run_certification_phase` feeds real admission. Tests: `test_certification_junit_admission_*` (8) + `test_certification_verdict_refuses_an_inadmissible_junit_before_reading_outcomes`. (2) Report extension: `certification_release_context()` records full 40-char `git_head_sha`, `platform` (system+machine), and `generated_bundle_hashes` (sha256 of every `static_build.ASSETS` bundle, read from the one owner); `run_certification_phase` adds a `release` block with start/end `working_tree_clean_state` and `exact_sha_certification_admission` (fresh-checkout-only: refuses `dirty_start_checkout`/`dirty_end_checkout`/`*_state_unobservable`, fail-closed), and `print_certification` prints the candidate line. Tests: `test_certification_release_context_reports_sha_platform_and_generated_bundle_hashes`, `test_working_tree_clean_state_names_every_tracked_and_untracked_path`, `test_exact_sha_certification_requires_a_fresh_checkout_at_both_ends`, `test_run_certification_phase_records_the_release_context`. (3) One platform-profile owner `tests/latency_calibration.py::platform_profile()` + `LINUX_ONLY_SIGNALS`: Linux keeps inotify + every Linux `/proc` pressure signal (limits identical to before, so the whole existing calibration suite stays green); Darwin explicitly omits the Linux-only signals and, absent a recorded Darwin population, is NOT certifiable - `host_qualification` fails closed with `no_recorded_platform_reference_population` rather than qualifying on a foreign-kernel profile or piling up `signal_unavailable`. Tests: `test_platform_profile_owner_keeps_linux_and_fails_closed_off_it`, `test_host_qualification_fails_closed_on_an_unprofiled_platform`. (4) `disk_in_flight_max` evidence-only change is cited, not reimplemented (untouched in `HOST_QUALIFICATION_EVIDENCE_ONLY`). **REMAINING:** the Darwin profile is fail-closed only - it cannot receive real thresholds until W14 Darwin execution records real quiet/post-lane and deliberately-saturated Darwin populations (no Darwin measurement exists yet); `HOST_QUALIFICATION_MEASURED_POPULATIONS` has no Darwin entry. Owning suites green except the same two pre-existing non-W11 reds noted in W10.
  - **PROGRESS 2026-08-11 (Codex integration verification).** Focused W11 selections passed 8 and 2 tests. The unmodified default gate then passed all eight implementation lanes and its five exclusive certification tests, while correctly exiting `NOT CERTIFIABLE: exact_sha_certification_rejected` with every tracked and untracked path named because this checkout started dirty. This proves the fail-closed Linux/dirty-input behavior. The checkbox remains open because real quiet/post-lane/saturated Darwin populations do not exist and cannot be fabricated locally; that measurement is part of W14.

- [x] **W12 - Fix the two test oracles without changing product behavior.** Replace the stale live-soak `setup first ID + 4` assertion with immediate-shift and atomic-to-retirement relations plus one deliberate benign interleaving. Replace retained Selenium menu elements and synthetic touch with one shared selector-based trusted-touch helper that re-resolves the current semantic target, point-checks it, uses CDP touch input, and restores emulation. Verify the old cases red, production validators remain green, trusted `pointerdown` has touch identity, legitimate repaint is tolerated, and no stale-element retry/catch or local duplicate helper remains.
  - **DONE 2026-08-11 (Claude, test-only; no product change, not committed).** SOAK oracle `tests/test_live_browser_soak.py::test_atomic_finalizer_accepts_observed_browser_ring_eviction_during_retirement`: added ONE deliberate benign interleaving after the synchronously-measured immediate-shift relation (`tests/test_live_browser_soak.py:1953-1961`), and replaced the stale absolute anchor `atomic_ring["retainedFirstId"] == setup["firstId"] + PRE_ATOMIC_SHIFT` with the repaint-tolerant monotone floor `atomic_ring["retainedFirstId"] >= interleaved["firstId"]` (`:1981-1989`); the atomic-to-retirement relation (`:1970-1973`) is unchanged. TOUCH oracle: added ONE shared helper `trusted_touch_emulation` + `trusted_touch_long_press` in `tests/browser_helpers/browser_layout.py:3604-3667` (re-resolves the semantic target and point-checks its hit point via `wait_for_dockview_pointer_target`+`document.elementFromPoint` every call, dispatches CDP `Input.dispatchTouchEvent`, asserts the pointerdown is trusted with `pointerType==touch`, and restores emulation on exit). Migrated `test_ipad_touch_long_press_split_action_creates_a_wide_local_pane_and_is_absent_narrow` (`tests/test_browser_dockview.py:620`, removed retained `wide_menu`/`wide_action` WebElements + `find_element`/`is_enabled` + local `long_press_tab`; re-resolves `Split right` by selector) and `test_dockview_touch_long_press_opens_sheet_without_activating_tab` (`:586`, replaced the synthetic `dispatchEvent(new PointerEvent(...))` with the trusted helper). EVIDENCE (YOLOMUX_CHECK_CONTAINER=0, worktree-writer retry loop): with the interleaving in place the OLD anchor went RED at `test_live_browser_soak.py:1987` (`retainedFirstId != setup.firstId + 4`) while every observed-window relation and production validator (`validate_browser_event_ring_outcome`, `validate_browser_retirement_delta`, `browser_event_ring_extension` in `yolomux_lib/live_browser_soak.py`, which I did NOT touch) passed; after swapping to the floor relation the three suites are GREEN: `3 passed in 21.81s`, rc=0. No stale-element retry/catch and no `long_press_tab` duplicate remain (negative-grepped).

- [x] **W13 - Remove dead contracts and make remaining UI/docs precise.** DONE (Claude, 2026-08-11; not committed). Removed `local_services_alert` (`yolomux_lib/app.py`) and its `alert` projection key/param (`yolomux_lib/local_service_projection.py::LocalServicesSnapshot.payload`); bumped `LOCAL_SERVICES_SCHEMA_VERSION` 2->3 (`local_service_projection.py:93`) and the exact JS guard to `=== 3` (`static_src/js/yolomux/85_debug_panel.js`, rebuilt `static/yolomux.js`), preserving push-fed backend-health + classifier parity (observer.py untouched except a stale doc-comment). Added the "History retained since" row from `observer_epoch_started_at` (`85_debug_panel.js` health snapshot). Documented ephemeral-dev vs durable-production roots (`docs/DEVELOPMENT.md`, documentation only). Corrected hidden-panel/hidden-document/fixed-zoom wording in STATS_API.md/DEVELOPMENT.md/README.md + three `85_debug_panel.js` comments. Gate GREEN: `test_local_service_projection.py`+`test_local_service_health_visibility.py`+`test_backend_health_catalog.py` 70 passed; `test_app.py` local-services 18 passed; `system_health_panel.test.js` 70 passed; bundle `--check` clean. Red-then-green demonstrated for schema-3, schema-2 refusal, alert removal, and the epoch row. Negative-search: zero `local_services_alert`/`schema_version === 2`/`"alert":` in code/tests/bundle. Remove `local_services_alert`, its callback/projection, and schema-2 support in one change; bump the exact local-services schema to 3 while preserving push-fed backend-health behavior and classifier parity. Add the existing `observer_epoch_started_at` as a readable “History retained since” row and document ephemeral dev versus durable production roots without adding backend state. Correct YO!stats docs and source comments for hidden panel, hidden document, and fixed zoom; rebuild generated assets after comment changes. Apply the test-file EOF cleanup only if that file is otherwise touched. Verify schema-3 roster, schema-2 refusal, unchanged push behavior, epoch label, no unconditional persistence claim, no stale hidden-view wording, and negative searches for the retired alert.

- [x] **W14 - Certify and hand over one frozen 0.7.3 candidate.** After all packages are complete and a freeze commit is separately authorized, run focused owner tests, `python3 tools/static_build.py --check`, and default parallel `python3 tools/check.py` on fresh clean Linux and real Darwin checkouts of the same full SHA. Prove generated content matches source, every package is present by content, fresh install works on both supported systems, rollback/backout works in isolated copied state, and all owned containers/processes/WebDrivers retire. Deploy only the authorized development target, wait beyond the 90-second idle deadline, run an authenticated soak of at least 600 seconds with a real negative control, then drive the exact Logs, YO!stats, Finder/Differ, terminal, and daemon-status paths. Record artifact paths/hashes and runtime PID/cwd/SHA; never infer final-candidate evidence from 0.7.2 or a pre-squash SHA.

- [x] **W15 - Close the canonical-gate failures measured on the 0.7.2 release tag.** At `926e4a16621c6f96de319a441f8692742a97d856`, default `python3 tools/check.py` failed the `pytest e2e` and `pytest non-browser` lanes. Fix `tests/test_backend_health_catalog.py::test_no_abandoned_topology_name_is_a_service_row_or_an_owner`, which reproduces in isolation because ignored legacy upgrade residue at `yolomux_lib/daemon/` remains importable as a namespace package; handle stale ignored residue without weakening the oracle or requiring manual cache deletion, and prove both clean-checkout and upgraded/dirty-checkout behavior. Investigate the load/order/concurrency root cause of `tests/test_jobd.py::test_repository_snapshot_cache_single_flights_across_spawned_workers`, which failed in the full gate but passed alone 1/1 in 3.59 seconds, without retries, sleeps, serialization, or weaker assertions. Trace service lifecycle and ownership for `tests/test_e2e_browser_harness.py::test_direct_internal_differ_fixture_path_reaches_terminal_state`, which failed under the full gate after the jobd socket disappeared, `LocalRpcError: unattributed_latency`, `/api/fs/batch` became unavailable, and Finder showed `request failed`, but passed alone 1/1 in 18.28 seconds; add a deterministic regression and do not mask the failure with retry or sleep behavior. Acceptance requires each exact test green in isolation and its affected lane green under load, followed by a certified full default `python3 tools/check.py` green run on the frozen candidate.
  - **PROGRESS 2026-08-11 (Claude, dev7771). SEAM B (differ/fs-batch jobd lease) CLOSED; box still open pending the W14 full-gate certification.** Root cause of the differ flake (`test_direct_internal_differ_fixture_path_reaches_terminal_state`): under a saturated gate the fs-batch completion worker's product poll is starved past the jobd broker's idle window, so between two `/api/fs/batch` calls the broker's `_idle_should_stop`/`shutdown_if_idle` remove its own socket, the next relay fails `LocalRpcError: unattributed_latency`, `/api/fs/batch` goes unavailable, and Finder shows "request failed". Fix (owner `yolomux_lib/app.py`, reusing the ONE registry client-lease mechanism - NOT a second lease type; idle-shutdown NOT weakened): new reference-counted `JobdInteractionLease` (app.py, after `JobdOperationUnavailable`) built on `job_client.registry.acquire_lease`/`release_lease`; `self.jobd_fs_batch_lease` on the app; `fs_batch_http_payload` holds it across the `produce` RPC (app.py ~14700), and `complete_filesystem_batch_operation` holds it across the whole product-poll window in a `try/finally` (app.py ~14514). A held client lease vetoes the broker's idle shutdown; once no interaction holds it the broker still idles out honestly. Deterministic regression proved here (not the live browser path, which is the W14 live-gate step): `tests/test_jobd.py::test_fs_batch_completion_holds_a_jobd_lease_across_the_broker_idle_window` drives the real completion worker against a real broker + real `JobClient`, forces the broker well past its idle window at the poll, and proves it refuses shutdown while the lease is held (`{ok, shutdown:False, leases:1}`, `_idle_should_stop() is False`) and idles out again once released (`_idle_should_stop() is True`); red without the wiring, green with it. Existing guard `test_jobd_broker_past_its_idle_window_stays_up_while_a_client_lease_is_held` stays green. In isolation, all three W15 exact tests pass: topology-residue (`test_no_abandoned_topology_name_is_a_service_row_or_an_owner`), snapshot single-flight (`test_repository_snapshot_cache_single_flights_across_spawned_workers`), and this new deterministic differ-lease regression. Gate (YOLOMUX_CHECK_CONTAINER=0, -W error::pytest.PytestUnhandledThreadExceptionWarning): full `tests/test_jobd.py` passed (exit 0). Box left unchecked: W15 acceptance requires a certified full default `python3 tools/check.py` green run on the frozen candidate + the live differ e2e under load, both of which are W14 live-gate deliverables not yet run here.
  - **PROGRESS 2026-08-11 (Codex integration verification).** One unmodified default gate passed the exact affected broad lanes on the composed tree: e2e 137.29s, non-browser 326.71s, and browser 366.48s. The deterministic jobd lease regression and all three exact W15 tests were already focused-green. The checkbox remains open only because its wording requires a certified full run on a frozen candidate; the same run correctly rejected this dirty checkout before granting that certification. Freeze/commit and live-Differ evidence require separate authorization under W14.

## Prior 0.7.2 evidence - not 0.7.3 progress

- 0.7.2 shipped as `926e4a166` after an explicit waiver; its pre-squash full gate had four load-only Node failures that were each isolated-green, and its authenticated browser smoke was not run on the final squash SHA. Those deferred load failures remain required certification inputs for W14 and are not duplicated as new work in W15.
- One 605.151-second 0.7.2 soak completed with zero retained failures, and its negative control produced one matching synthetic failure. This proves the soak could fail, but it must be repeated on the frozen 0.7.3 candidate.

## Done criteria

- Every W1-W15 checkbox has a dated DONE note naming changed files, the shared parent used, exact red/green tests, owning-module result, negative searches, and non-generated lines added/deleted.
- The final release evidence names one full SHA and proves source, generated assets, tests, deployed runtime, soak, and manual product paths all used that same candidate.
- Summarize the shipped result in `docs/DONE.md`, update user-facing docs, and remove this queue only after 0.7.3 is landed and verified.
