# 2026-08-13 0.7.4 behavior-preserving architecture cleanup

- Consolidated daemon action dispatch, six-service runtime projection, registry state, published JSON caches, HTTP adapters, stats query/projector seams, browser state/resources, shared tree rows/controls, fixture lifecycle, and declarative gate ownership behind named parents. `TmuxWebtermApp` remains the explicit compatibility facade over `WatchBridge`, `SessionFilesCoordinator`, `ActivityCache`, and `SystemStatusProjector`; its field count fell from 99 to 96 and `app.py` fell from 17,600 to 17,598 lines without removing its 602 facade methods.
- Added extension maps for daemon actions/runtime rows, routes/adapters, app domains, Debug subviews, preview renderers, async reads, lifecycle resources, tree rows/controls, fixture scenarios, and gate lanes. Rejected big-bang/framework rewrites, mixins/service locators, an ORM or second cache, consolidated daemons, boolean-heavy helpers, false CSS merging, SQL in projectors, and retry/sleep/serialization test masks.
- Frozen baseline `e68c0d709a7780ea4896685288c14ec44febec30` still verifies exactly against its versioned characterization manifest. The accepted current structural comparison records the deliberate parent/registry/package changes by category; architecture budgets remain green without growing the reviewed source or browser-test owners. Across the cleanup, the non-generated diff is +31,609/-18,852 lines. The closing Finder Sync defect reused `fileExplorerSyncPlanForFile` and its existing render/list pipeline, changed zero net non-generated lines, and now expands/lists the file's parent directory rather than issuing a directory request for the file itself.
- Focused closure evidence passed 23/23 architecture and Finder browser tests, the supporting Node owner 57/57, generated-asset freshness, py_compile, and whitespace checks. Earlier exact current-SHA recovery passed 17,596 non-browser tests and the browser lane in 378.63 seconds; the exact 5/5 certification artifact is `/tmp/yolomux-certification/cert-1786602249138923799-2044024`. The queue reached 26/26 in checkpoint `81c6d9fe1` before deletion.
- Restarted 7771 acceptance `/tmp/yolomux-074-manual-7a1d558e1-r22.json` is mode 0600 and clean on PID 4189697, commit `7a1d558e1fa22f9f486d8d14846cf30c53b6800b`, and bundle `d784f4753ce72286464d5b578f996c34e541d278ff2cee6bd75b21f8dce7380b`. It passed Terminal transport and wheel scrolling, Finder accessibility/focus/scroll, an 8,391-byte CodeMirror open/focus/close, Differ, Layout change/restore, Debug Graph/API/Daemons/Logs, an accepted YO!stats delta with exact current-generation paint, stable owner/roster/ledger identities, positive API bytes, quiescent diagnostic receipts, zero incremental Warning/Error/integrity failures, and clean WebDriver retirement. The 603.29-second clean soak and sole-cause redacted negative probe remain in `/tmp/yolomux-074-clean-8ac6110b3.json` and `/tmp/yolomux-074-negative-8ac6110b3.json`.
- Release closure after this archive commit still requires one unmodified `python3 tools/check.py` on the clean closure SHA, followed by a 7771 restart and real-browser verification on that exact SHA. `PLAN.0.7.5.md` keeps 0.7.5 blocked until those facts are recorded.
