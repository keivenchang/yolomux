# 2026-08-13 v0.7.5 cleanup follow-ups

- Completed all 13 behavior-preserving follow-ups from the 0.7.4 cleanup: one framed-panel construction owner with a fail-closed registry guard, one panel-body reconciliation transaction, narrower registered frontend partials, and complete lifecycle-scope ownership.
- Consolidated fixture HTTP lifecycle, prompt-corpus loading, local-service fixture records/traffic accounting, file-index test cleanup, typed/total gate steps, recursive semantic architecture discovery, and browser-capability preflight diagnostics.
- Routed app-owned controls through the existing builders/dispatcher and rebuilt-panel normalization. Migrated 43 implementation-only bound sentinels across 20 families to the shared scoped `bindOnce` owner; the final negative search is zero and `makeButton` is the sole raw DOM button constructor.
- Classified all 19 exact custom radius literals (14 at 5px and five at 7px) as component-specific. No false shared token was introduced. A fail-closed inventory plus real-browser dark/light computed-radius and bounding-box checks preserve geometry.
- Independent review found and the implementation fixed three defects before landing: rebuilt panels losing `.btn-base`, a popover child receiving two listener sets, and a disposer retiring timers without retiring listeners.
- Verification on the integrated primary tree: 288 focused owner tests; all 19 Node shards with the composed contracts; `layout_restore` 88/88; radius lint 4/4; real-browser radius geometry 1/1; `python3 tools/check.py --lane static`; generated bundle rebuild/check; direct architecture-budget gate; and `git diff --check`.
- Existing parents checked: `createFramedPanel`, `reconcilePanelBody`, `createLifecycleScope`, `makeButton`, `createToolbarButton`, `bindActionDispatcher`, `bindOnce`/`bindScopedOnce`, `FixtureHttpServer`, `PromptCorpus`, `LocalServiceTrafficRegistry`, `FileIndexTestScope`, and `StepId`. The Queue 3 implementation is +5,500/-4,720 non-generated lines, net +780, across the three integrated groups, including their tests and architecture-manifest ownership updates.
