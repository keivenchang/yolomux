# 2026-08-16 macOS path aliases, xterm teardown, and Quick Open exclusions

Landed `origin/fix/unify-quick-open-exclusions` at merge commit `a544089e2` after first making its Quick Open help truthful in `325bfb094`. The original branch changed macOS Finder repo-cache identity, narrowed an xterm renderer teardown catch, and rewrote Quick Open help, but it contained no matcher change for the new bare-directory claim.

The bare-name reproduction failed first: with `index_exclude_dir_names: []` and `index_exclude_paths: ["node_modules"]`, the shared policy produced `skip_dirs == set()` and indexed both `/repo/node_modules/root.js` and `/repo/a/node_modules/nested.js`. `ExclusionPolicy.from_settings()` now promotes a bare `index_exclude_paths` value into the existing `skip_dir_names` owner while leaving `glob:`, `regex:`, absolute, home-relative, and slash-containing rules in `exclude_rules`. The exact regression now excludes both directory depths and admits `/repo/src/app.js`; the Finder/Differ/watchd exclusion selection passed 29 tests.

The in-flight v0.7.8 work was committed at `4c51744bf` before merge analysis. Re-running `git merge-tree --write-tree 325bfb094 origin/fix/unify-quick-open-exclusions` returned 1 and found real conflicts in `tests/fixtures/architecture_budgets/v1.json` and `tests/test_browser_ring_landing_verify.py`; the raw result is `/tmp/yolomux-078-merge-tree-325bfb094-59676f11b.txt`. The browser test keeps both required owners: `child_process_artifact_environment()` for host-local artifacts and `inherited_python_path()` for interpreter-attached imports. The architecture manifest was regenerated from the final merged tree with `python3 tools/architecture_budgets.py --write-current`, and its 26-test matrix passed.

Generated JavaScript and locales were rebuilt with `python3 tools/static_build.py`; `python3 tools/static_build.py --check` exited 0. All 19 Node shards passed. The xterm regression proves a disposed renderer with no `_renderer` falls back without throwing while a `TypeError` from a live renderer is rethrown. The focused Python merge matrix passed 269 tests with one skip, and the Quick Open exclusion matrix passed 29 tests.

Real macOS acceptance remains blocked on authentication, not hardware availability. `https://ereview.com:8882` is reachable and serves a byte-for-byte match of commit `59676f11b`: its `static/yolomux.js` SHA-256 is `65183927112204af739dd7df27dd533c40e1e8a3cb87e62aab651355bca14028`, with both `cache_path` and `terminalRenderCellDimensions` present. Its `/api/ping` returns typed 401 `authentication_required`; a local signed cookie and one bounded attempt with each documented default account were rejected, so no further password attempts were made against the limiter. Bundle identity and Linux Node coverage support the implementation, but neither proves the required authenticated Finder `/tmp` versus `/private/tmp` journey or terminal mid-render teardown on the Mac.

The first unchanged post-merge `python3 tools/check.py` run passed all nine functional lanes: browser in 438.93 seconds, non-browser in 311.58 seconds, e2e in 172.50 seconds, and timing-sensitive serial in 7.20 seconds. The command remained non-green because the pre-run status writer had inserted trailing whitespace and certification refused the loaded host: postflight I/O full stall measured 0.088552 against 0.051 and some stall measured 0.089649 against 0.056; certification-phase samples reached 0.133893. Evidence is retained under `/tmp/yolomux-certification/cert-1786880465880210795-3576131` and `/tmp/yolomux-check-runs/check-1786880465879967236-3576131.json`.

Current integrated evidence is green at `1406566f9`. The final full gate passed all nine functional lanes, including browser in 439.64 seconds, non-browser in 316.16 seconds, e2e in 172.11 seconds, and timing-sensitive serial in 7.09 seconds; its report is `/tmp/yolomux-check-runs/check-1786882610409235988-596763.json`. A clean-checkout `python3 tools/check.py --certification-only` then certified all seven exclusive units on the same SHA in 92.30 seconds, with evidence in `/tmp/yolomux-certification/cert-1786883696483278863-1231173`.
