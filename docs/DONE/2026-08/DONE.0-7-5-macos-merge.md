# 2026-08-14 v0.7.5 macOS runtime merge

- Squashed the two reviewed `origin/fix/macos-v0.7.3` commits into signed side commit `e89c85043`, then merged it in signed release commit `82247555f` with the reviewed Darwin process-identity fence already applied to the resolved tree.
- Resolved the sole `yolomux_lib/local_services/registry.py` conflict by retaining the split `StartupFailureState`, `ChildOwnershipState`, and `HealthProbeCache` owners while routing Darwin and Linux process starts through the shared native identity helper. The auto-merged CPU, RPC, launch, terminal-path, and generated owners were reviewed by intent.
- Independent review found that three registry paths rebuilt native `darwin:` identities as `proc:`. The correction routes service groups, web groups, and stale-orphan preflight through `process_table_start_identity()` with only a legacy numeric fallback; the named re-review found no remaining code defects.
- Rebuilt the generated bundle and passed the focused 452-test integration set, all Node layout shards, the 120-test registry/preflight/host-identity suite, the exact Linux browser journey, and a 603.13-second Linux soak with stable six-service ownership and zero browser/server/integrity failures.
- Real-hardware macOS acceptance remains explicitly blocked, not substituted: this Linux host has no supported command-capable Mac shell, tmux pane, or forwarding path. The approved next external check is the Mac-local `~/Documents/bin/yolomux.dev8881` checkout on port 8881.
- Release gate evidence: rewritten product commit `5c899077d` has the exact tree tested as source candidate `1ab141ea9`, which passed all eight functional lanes in 436.98 seconds with the four process-global/timing nodes isolated in the final serial lane. Product-identical source parent `199d36cd2` passed all six certification units on a qualified host; later exact-candidate certification attempts were refused before units only by measured host disk/I/O saturation.
