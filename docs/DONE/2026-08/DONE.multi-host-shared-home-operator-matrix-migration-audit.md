# 2026-08-01 Multi-host shared-home operator matrix and migration audit (superseded implementation snapshot)

This was a pre-landing audit. Its statements about unpartitioned production chat, unbuilt host-local alert/tmux state, and unresolved YO!chat roaming are superseded by the verified implementation record above; it remains only as the audit trail for that earlier state.

- Protected a shared physical Git worktree with one `infra.worktree_writer` declaration in per-worktree Git metadata. Servers detect and warn but remain read-only consumers; the gate, direct pytest, static builds, and the explicit command wrapper refuse a fresh writer. Token-checked release, five-second heartbeats, immediate same-host dead/recycled/previous-boot reclaim, and 30-second foreign expiry prevent both permanent stale blocks and late-owner deletion of a successor. Python bytecode, pytest, pip/npm, and coverage caches now route below a host-local worktree artifact root; uploads and boot logs retain their existing `/tmp` owners; checked-in generated static output remains single-writer; external package/build/profile output is documented-only because YOLOmux cannot intercept those tools. The alert inventory now applies the settled stable-host-ID contract directly, retains hostname for display, and leaves YO!chat roaming unresolved.
- Completed the documentation slice of `DOIT.multi-host-state-isolation.md` without claiming the broader migration is complete. README now gives a two-machine operator the safe launch shape, environment variables, current support matrix, SQLite/NFS constraints, the settled host-local alert/error/notification contract, the separate open YO!chat conversation question, transcript-read boundary, absent cross-host snapshot model, one-writer worktree rule, and conservative legacy adoption/rollback procedure. DEVELOPMENT names each landed source owner and each unbuilt or unwired boundary.
- The source audit corrected the planning summary in three material areas: production `TmuxWebtermApp` still opens the unpartitioned YO!chat database despite a partitioned helper; the shared transcript-root reader has no production caller/configuration; and host diagnostics is not exposed to operators and still projects the partitioned chat helper rather than the production legacy path. The current integration branch has since wired settings/state/auth/rules writers through `shared_config_lock` and repaired late-override/model-pricing diagnostics, but the actual exporter-local/NFS-client lock acceptance remains unrun. The owner/service-root contract and all six tmux/alert/watch/activity contracts remain strict XFAIL, so the docs require separate local roots rather than promising shared-state isolation.
- Verification: a disposable snapshot of current `phase2-infrastructure` ran the focused isolated-container multihost/config batch with 121 passed and the expected seven strict XFAILs. SQLite citations were checked against the official WAL and network-filesystem pages, Markdown links/path claims were source-audited, and `git diff --check` passed. Net non-generated documentation change: +111 lines (127 insertions, 16 deletions).
