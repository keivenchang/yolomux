# 2026-07-02 Work Queue Drain

### Internationalization and shared locale ownership

- Completed and removed `DOIT.i18n-refactor.md`. All 19 source catalogs now share one Python-owned locale registry, exact key/placeholder/protected-token/Chinese-brand semantics, locale-correct CLDR plural categories, and full translations for the app, Help, YO!stats, auxiliary shells, panels, previews, notifications, accessibility text, and structured error paths. Live locale changes preserve mounted Finder, editor, terminal, pane, and scroll state.
- Collapsed 85 ordinary duplicate concepts plus the duplicate path-count plural family into neutral `common.*`, `brand.*`, and `state.*` parents across every catalog and consumer. The validator rejects new unowned ordinary or plural duplicates; the remaining context-sensitive equal strings use narrow reasoned allowlists. Preferences labels come from backend catalog descriptors, stream labels use the canonical message descriptor with legacy compatibility, and PR lifecycle/CI/review rendering uses one classifier and badge/link renderer.
- Rebuilt generated catalogs/bundle and verified zero semantic, literal-key, removed-key, or dynamic-prefix errors. Focused verification passed 138 Python tests and the relevant Node suites; the final canonical eight-lane gate passed compile, static, syntax, Node layout, non-browser pytest, browser pytest, E2E, and whitespace in 119.56 seconds.

### Tabber activation and YO!stats history performance

- Completed and removed `DOIT.zippy_tabs_and_yostats_history.md`. Focus/layout changes that previously rebuilt the same Tabber tree roughly 30 times now perform zero full renders, zero Dockview `fromJSON()` reloads, and at most one lightweight mounted-row state sync per click. The 12-session, 24-window, 41-row focused Chrome fixture measured p95 42.2 ms and max 45.2 ms while preserving panel identity, active/current state, and view state.
- Replaced the 24-hour no-data path's 86,400-step nested bucket scan with one shared sorted interval merge/complement sweep. A 5,280-record mixed-resolution stress fixture with all three client overlays now renders below the focused 300 ms budget in its isolated Node shard; Chrome measured a 32.5 ms synchronous range change and a 64 ms longest task instead of the prior roughly 6.3-second 24-hour graph block.
- Added one retained-chart history readiness owner with initial/older/retry/error states, 120 ms older-load anti-flicker, stale-generation rejection, interval- and resolution-aware coverage, missing-tranche requests, and paint-before-clear behavior. The server contract uses inclusive `history_start`, exclusive `history_end`, safe/latest cursors, one aggregation encoder, and shared dynamic JSON gzip; the representative 5,280-record payload fell from 5.68 MB raw to 60 KB on the wire while exact metric totals remained stable at both 6,000- and 500-point budgets. The older snapshot/readiness requirements were consolidated into this one implemented queue, so no parallel YO!stats queue remains.

### Full refactor ownership audit

- Completed and removed `DOIT.refactor_full_audit.md`. CSS now has one mechanically enforced owner per selector/property/color concept, compact YO!agent rows share one component parent, duplicated frontend/backend primitives route through shared helpers, server and Codex child PATH construction share one dependency-light owner, and share authorization/plaintext policy comes only from the HTTP route registry.
- Repaired the central slow-test ranking and added a collection invariant. Two same-code default xdist runs at 43.91/45.46 seconds versus work-stealing runs at 34.89/42.39 seconds reduced median non-boot browser wall time from 44.69 to 38.64 seconds, user CPU from 102.02 to 97.16 seconds, and system CPU from 28.79 to 23.99 seconds; the measured smaller ranking stayed because the expanded ranking was slower.
- Final verification rebuilt generated assets, passed focused Node/backend/browser regressions, and passed all eight canonical `python3 tools/check.py` lanes in 49.45 seconds. The strict YO!stats benchmark moved into its own single-owner Node shard so unrelated accumulated garbage collection cannot create a false timing failure, and the delayed Finder-root browser fixture now waits for the complete root transaction rather than only the early path-field paint.
