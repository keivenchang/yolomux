# YO!stats legacy migration crash-loop recovery (2026-07-17)

- Completed and removed `DOIT.polling-to-sse.md`. Healthy authenticated SSE now suppresses normal browser discovery reads; a reconnect, missed revision, or visibility restoration uses a bounded keyed repair instead of `refreshAll()`. The active Log reader preserves its scroll position while burst invalidations coalesce to one in-flight fetch plus one follow-up. A live 7772 restart reconnected a 5m/1s history view with zero broad refreshes; a temporary compatible follower took Quick Open ownership after the owner stopped while Finder retained a ready index and Quick Open retained results, then 7772 was restored as the sole preferred leader. The final niced eight-lane gate passed in 76.29 seconds and local unauthenticated `/api/ping` returned 401.

### Compact pane minimize spacing

- Completed `DOIT.tab-minimize-spacing.md`. Fixed-width pane tabs now use tab-specific 1px end padding and title/control gap, reclaiming 7px of title width without changing the shared 8px by 1.4px minus glyph or generic toolbar controls. The minimize target remains 18px on desktop and 36px for touch-compact layouts.
- Verification: focused Selenium covers classic and Dockview tabs at 172px and 128px, title/control geometry, and center clicks for desktop/touch; static build, all Node layout shards, and the canonical eight-lane gate passed.

### Web status compute offload

- Completed `DOIT.offload_web_compute.md`. `statusd` is the shared owner of lightweight session discovery, pane classification, public agent-status assembly, immutable generations, and pre-encoded auto-approve bytes; `approvald` remains the target-keyed action-worker owner.
- `/api/auto-approve`, SSE transition detection, YO!stats Agent Status, and Tabber activity consume statusd output. Web status paths authenticate, validate, route local RPC, and forward bytes or a generation; they do not rebuild the roster. Statusd retains stale complete bytes on a failed rebuild and otherwise returns structured unavailable responses without a web fallback.
- Verification: the statusd protocol/session/app/local-RPC tests cover private/wrong-version rejection, lightweight discovery, stale retention, and two app clients sharing one daemon/build/body. The active `7771` restart served its expected auth-gated `/api/ping`; a controlled 78-sample live status-demand capture held web CPU to 2% maximum and statusd to 1% maximum. The canonical gate passed after implementation.

- Completed and removed `DOIT.yostats-statsd-migration-crashloop.md`. The outage came from a fictional migration gate: every shipped legacy database used `schema_meta.schema_version` 2..4 with SQLite headers 0/0, while the new reader demanded header schema 4. Real v2/v3/v4 stores now migrate; unsupported stores are atomically preserved as `unsupported-*` and activate empty v5 history instead of crash-looping. The cumulative `agent_token_state` checkpoint remains retirement evidence rather than fabricated atom history.
- Shared local-service startup now owns per-service stderr capture, bounded redacted exit reasons, a three-exit terminal latch, and explicit Retry clearing. HTTP 503s and deduplicated logs expose the daemon reason. YO!stats error readiness uses the live range, suppresses contradictory waiting text, and Retry resets statsd then repairs snapshot and stream without reload. The shared filesystem reindex batch skips and logs credential-blocked watch paths once while preserving safe siblings.
- Writer build 3 fences this migration/startup generation so older same-protocol worktrees fail closed instead of repeatedly replacing the shared daemon. Local-service socket, record, stderr, and registry lock parents now remain consistent through long Unix-socket fallback without chmodding `/tmp`; runtime singleton locks remain untouched. Existing parents checked: `LocalServiceRegistry`, `LocalServiceClient`, `StatsCurrentClient`, `StatsHttpForwarder`, `createBrowserClient`, `jsDebugHistoryReadiness`, and `reindex_roots_for_paths`. Searches covered retired migration constants, duplicate service parents, daemon revision fencing, and all current-stats request/retry owners; the only remaining `services/services` text is the negative regression assertion.
- The first live verification was a false green: HTTP 200 still contained 180 empty buckets because writer build 3 fenced the preferred `7770` sampler and lower-priority `7771` stayed a follower. Background-owner capability handoff now promotes the compatible generation through the existing release/takeover parent and repeats that check on heartbeat, covering the startup race where `7771` briefly owns and then sees `7770` reappear.
- Verification: 285 focused Python tests and 52 Node stats tests passed after the ownership fix. A normal-preference guarded `boot.sh 7771` restart elected `7771` at inherited priority 100 with `stats_writer_build=3`; the live runtime held its lease with all six workers alive, the slower token collector recovered to four successes, and the final 30-minute snapshot contained 159 nonempty buckets and 39 named server/token/cost series. Selenium rendered that exact live payload through both the current component and full YO!stats panel: 36 nonempty component paths, 65 full-panel SVG series elements, zero full-chart no-data rectangles, and visible CPU, agent-status, and token lines. A separate capture loaded the served `https://localhost:7771` bundle and produced 15 chart SVGs, zero no-data rectangles, and visible CPU, Agent status, and Agent tokens output. The canonical eight-lane gate passed in 88.18 seconds. Non-generated diff: +881/-84 (net +797).
- Completed and removed `DOIT.disclosure-triangle-and-blink-sync.md`. The adjacent `0:codex` and `1:claude` RUN rows now use one shared sub-window glyph geometry token family, so equivalent play triangles keep matching pseudo-element dimensions at desktop, narrow, and mobile widths. The mutation observer remains available for always-pulsing RUN glyphs after the optional broad status pulse is disabled, but ordinary disabled broad-pulse dots retain their fresh render-time delay rather than being shifted to an older root phase. Focused browser regressions passed for the original five replacement working dots and adjacent RUN rows; Node layout, static-build check, and the canonical gate passed. Browser captures were visually inspected, including `/tmp/yolomux-subwindow-run-sync.png`.

- Completed and removed `DOIT.mermaid-fix.md`. Mermaid Markdown preview now requests native SVG labels (`htmlLabels: false`) from its one shared strict configuration owner. This removes incompatible `foreignObject` HTML labels before the strict standalone-SVG sanitizer can collapse their line breaks while retaining stale Mermaid geometry; the sanitizer remains strict and blocks `foreignObject`. Browser coverage verifies the original network topology with home/internet clients, relay, Mac, Linux, and forward listener: native labels, no `foreignObject`, readable non-overlapping geometry, intrinsic sizing, fit, zoom, and pan. Focused Mermaid/browser checks, Node layout, static-build check, and the canonical gate passed; port 7771 restarted and served the rebuilt bundle, with `/api/ping` returning 401. `/tmp/yolomux-mermaid-network-topology-after.png` was visually inspected.
