# 2026-07-17 Mobile keyboard corrections

- Completed and removed `DOIT.mini-keyboard.md`. The touch palette is again two compact pages rather than one scrolling 27-key surface: primary and More share one responsive 288x204 footprint, both retain More and the same bottom-right Ctrl-C definition, PgUp/PgDn are wider without growing the palette, and the full-width drag header has visible contrast and a larger grip. The stale launcher was a state synchronization defect: only the first matching DOM copy was updated; the shared surface synchronizer now applies launcher/palette XOR state to every rendered copy for the session. Focused Node and three touch-browser regressions passed, including duplicate-surface state and rendered geometry; the canonical eight-lane gate passed in 562.88s.
