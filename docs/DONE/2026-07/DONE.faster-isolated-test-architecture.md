# 2026-07-18 Faster isolated test architecture

- Completed and removed `DOIT.test_architecture_speedup.md`. Canonical pytest phases now have one explicit catalog and a collection regression proving every node belongs once; the runner can write opt-in `/tmp` JSON with lane/step timing, worker budgets, child CPU/RSS, commands, return codes, and bounded pytest slow calls. Boot, behavior, and serial goldens remain separately measurable, while the default keeps boot fail-fast before browser work.
- Shared immutable source/AST inventory avoids redundant structural parsing; private tmux E2E waits now capture immediately, poll at 50ms briefly, then cap at the historical 400ms. The real Claude/Codex auto-approve E2E lane passed 20/20 repeats in 20.77-21.58s. The tmux cancellation regression retains one real timeout but avoids a duplicate timeout tail through the same state owner; it passed 20/20 in 4.97-5.60s versus the earlier 8.53s call.
- The Selenium audit retained real-browser ownership for geometry, DOM events, CodeMirror/xterm, replay/WebSocket, and pixel paths, while removing one generated-output duplicate already covered more strongly by Node. Session-scoped Chrome reuse is available only as `YOLOMUX_SESSION_SCOPED_BROWSER=1`; its Linux probe did not clear the acceptance threshold, so default module scope remains. Docker, browser-pool, browser-context, and golden-overlap paths were rejected: no runnable Docker daemon/image or approved hermetic pixel baseline exists, and no measured browser startup tail justified extra lifecycle complexity.
- Final warm full gates passed in 77.62, 76.89, and 75.10 seconds (76.89s median), with 257.79-266.23s child user CPU, 47.68-62.21s system CPU, and 456596-508396 KiB peak RSS; no Chrome, ChromeDriver, or fixture tmux processes remained. Raw reports and repeat logs are under `/tmp` only.
