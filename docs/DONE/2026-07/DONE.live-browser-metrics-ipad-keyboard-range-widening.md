# 2026-07-17 Live browser metrics, iPad keyboard, and range widening

- Completed and removed `DOIT.tls_setup_trusted_https.md`. `self_signed_san()` now discovers the primary outbound address plus non-loopback addresses from portable `ip`/`ifconfig` fallbacks, and `tools/setup-tls.sh` reuses that same SAN owner plus `common.STATE_DIR` to issue an idempotent local-CA leaf. Startup prints the SAN and per-client CA action. The real script reused and trusted the local CA, installed a leaf containing both current routable LAN addresses, and trusted curl without `-k` returned the expected 401 over localhost, `10.110.42.35`, and `10.110.42.242`. The unrelated 414 JSON-fragment request-line observation remains an evidence-gated follow-up in `docs/TODO.md`.
- Completed and removed `DOIT.tab_popover_width_span_panes.md`. `positionPaneTabPopover()` remains the one session/file geometry owner, but desktop session popovers measure intrinsic content, apply width before measuring replay height, retain the owning-pane anchor, and cap width to the pane plus a 15%/64px allowance under the current follow-up spec; file previews keep their measured CSS path. The share matrix compares host/viewer inline width in addition to the full rectangle. The original live proof reproduced the superseded 577px-over-220px behavior; the follow-up browser contract now limits that fixture to the bounded allowance.
- YO!stats recovery now keeps one capable background owner even when the preferred process is an older writer, lets compatible followers use a newer running statsd without opening its fenced database, and materializes browser latency/API/SSE/bandwidth/disconnect as privacy-safe all-client averages instead of request-private overlays. Focused tests prove same-protocol newer-daemon compatibility, shared snapshots/deltas, and per-client-weighted folds; live Selenium verification and the canonical gate are recorded in the handoff.
- Completed and removed `DOIT.stats-client-metrics.md`. The browser observation writer fence now comes only from the immutable server bootstrap, stale writers remain terminal at HTTP 426, and current API/SSE/heartbeat originals feed private latency, traffic, and bandwidth series. Guarded 8881 live QA proved one normally authenticated client persisted 56 fresh originals, served positive private data in exact 5m/1s and 24h/300s snapshots, rendered the established three cards, and did not expose any private series to a different client. The HTTP `stats-*` ID is intentionally privacy-hashed again to the SQLite `browser:*` source; current heartbeat and upload cadence is 10 seconds. The cold 24h private overlay warmed in about 19 seconds.
- Completed and removed `DOIT.ipad-keyboard-and-scroll-regressions.md`. Late-mounted launcher/palette copies now re-enter the one per-session XOR synchronizer; every corner opens the fixed palette over the remembered launcher position while staying pane-clamped; claimed terminal pans suppress iPadOS's following 350ms mouse/click chain without changing tap focus; Cmd follows Alt and Enter follows Right Arrow in the fixed grid. Exact Chrome regressions cover late duplicates, four-corner geometry, synthesized mouse focus suppression, and key placement; a trusted WebKit touch smoke proved the launcher click and XOR state.
- Completed and removed `DOIT.range-widen-missing-history.md`. The retained UI already fetched full exact generations for 15m→4h and 5m→1h; the new matrix found the remaining 1h→24h boundary defect, where client compaction deleted the first overlapping 300-second bucket by start time and painted 287/288 cells. Expiry now uses bucket end, so all three widen transitions issue one generation-zero full-window request, paint every bucket, and clear loading. Focused verification passed five exact browser cases and all 16 Node shards; the canonical eight-lane gate passed in 562.76 seconds. Port 8881 runs one listener from this worktree and `/api/ping` returns the expected 401.
