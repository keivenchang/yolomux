# 2026-07-08 Work Queue Drain

### YO!agent roster calm then send

- Completed and removed `DOIT.84.md`. YO!agent now recognizes the exact roster-monitor request before any model call and persists a generic `wait_roster_then_send` job: it watches only the condition-clause roster, requires every real Claude/Codex target to remain idle or done, prompt-accepting, and draft-free for ten seconds, then sends the exact redacted command once to the separately named destination.
- The existing client-event watcher remains the sole poller. A shared state-file mutation atomically claims `firing` across servers; fire-time revalidation returns a busy destination to waiting, while an ambiguous claimed send persists as failed/unknown rather than risking a duplicate. The shared localized row reports roster, quiet window, blockers, destination, and lifecycle state without exposing protocol labels.
- Verification covered deterministic parsing/model bypass, calm/blocker and quiet-reset matrices, restart/cancel/timeout/risk/redaction behavior, local and two-server claim races, shared SSE rendering, and a private four-session mock tmux run that delivered `/dyn-tps-report 1 2 3 4 EOD` to session `1` exactly once. The canonical `python3 tools/check.py` gate passed in 68.86 seconds.
- Follow-up: flexible equivalent roster wording now uses one shared built-in model-intent registry rather than a roster-specific prompt/parser branch. Each declaration supplies candidate terms, a provider-lowered JSON Schema, defaults, and a named server handler; Codex and Claude run isolated read-only/no-tools native-structured-output planners. The server rejects malformed or unknown-session data, keeps the ten-second quiet default out of model control, reuses the normal persisted-job validator, and forces confirmation for every accepted model-derived job. Offline registry/schema/transport regressions plus real Codex and Claude acceptance each produced the exact four-session plan and pending-confirmation job payload.

### Shared agent-window status ownership

- Completed and removed `DOIT.audit_shared_agent_status.md`. Regular Tabs, Tabber, and YO!info now resolve visible Claude/Codex state from the same canonical `/api/auto-approve` row. Tabber/transcript data supplies only explicitly allowed identity, path, git, and process enrichment; it cannot restore a stale green/red/yellow state when the canonical row is absent.
- YO!info no longer renders a copied status payload. Its rows resolve the shared current window selector at render time, its cache key includes the canonical visual signature, and auto-approve status renders refresh the visible Info panel so acknowledgement flags and green/red/yellow transitions converge with Tabs and Tabber.
- Shared cross-server attention state now decodes through one typed record and is snapshotted once per status-payload build. The fresh-follower test proves a server that first observes idle recovers the owner’s completion timestamp, acknowledgement key, and acknowledgement state; the multi-agent regression proves unchanged idle rows use one shared-file read.
- Added contradictory-payload coverage for Regular Tab, Tabber, and YO!info yellow/acknowledged states, focused live-browser status checks, 20 attention-sync tests, 364 app tests, and the full canonical eight-lane gate. The final gate passed in 46.45 seconds.
