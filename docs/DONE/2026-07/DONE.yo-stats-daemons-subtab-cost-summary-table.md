# 2026-07-20 YO!stats Daemons subtab and Cost summary table

- Completed and removed `DOIT.yostats-daemons-tab-and-cost-table.md`. The third YO!stats subtab (right of API/SSE) now reads "Daemons" instead of "Services": only the `debug.tab.services` label value changed across all 20 locales (en "Daemons"; de/nl/pt-BR use qualified forms `System-Daemons`/`Systeemdaemons`/`Daemons do sistema` because the i18n semantic check rejects a value identical to English), while the internal subtab key `system` and `data-js-debug-subtab="system"` are untouched. The in-panel "Local services" heading was intentionally left as-is. The Cost summary card (`debugGraphCostSummaryHtml`) now renders a ruled table sharing the YO!cost report cost-table style (`js-debug-system-table js-debug-cost-table` inside `js-debug-cost-table-wrap`, `data-js-debug-cost-table="summary"`) with Usage/Tokens/Price columns, Input/Cache/Output body rows through one shared `summaryRowHtml`, and a Total foot row; row-label cells keep the per-usage explain-attrs owner, prices stay concise via default-omit `debugGraphCostPricePairHtml`, and the "At API list prices" basis is stated once in the heading. Reused the report table CSS (no new cost-table style) and removed the five dead compact-card selectors (net −31 CSS lines).
- Verification: `node tests/layout_url.test.js` all 16 shards green (new node source-contract test pins the summary-table DOM, shared helpers, basis-once, and absence of the retired `<dl>`/`js-debug-cost-compact`/token-count in both DOM and CSS; new i18n catalog test pins non-empty `debug.tab.services` in every shipped locale with en=="Daemons"); the existing subtab Selenium `test_current_stats_system_tab_order_visible_polling_refresh_scroll_and_narrow_layout` updated (not replaced) and the yocost report basis-once/pricing Selenium both pass; canonical `python3 tools/check.py` PASSED all 8 lanes in 82.13s. Guarded `./boot.sh 7772` restart from this checkout returned `/api/ping -> 401`, and live smoke confirmed the served locale label "Daemons" and the served bundle's summary table with zero old-compact markers.
