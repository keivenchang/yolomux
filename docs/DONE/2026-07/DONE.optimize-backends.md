# DOIT.optimize-backends.md (2026-07-16)

- Completed and removed `DOIT.optimize-backends.md`: statsd and web-server CPU work, measured first and after every change with equal-duration captures in `/tmp/yolomux-backend-bench/` (same state directory, no cache deletion, no Defender/indexing changes, unchanged sampler cadence).
- Measured outcome: statsd hidden/idle fell from ~30% of a core sustained to ~0.3% (0.40 CPU-s/120s); the heaviest visible state (5m/1s with a held SSE stream, browser-faithful HTTPS workload) fell from ~26% to ~14.2% (17.1 CPU-s/120s); the five-minute no-change reconciliation no longer performs its 18.6s full rebuild (`build.full` flat across 6+ minutes). Two comparisons preserved per the queue: the deterministic public-plus-four-private fixture `test_public_plus_four_private_fixture_has_exact_encode_work_counts` (exact slice/alias/entry counts, no hidden multipliers) and the live browser-faithful workload captures (CPU/RSS, `browser_workload.py` + `benchmark.sh`).
- statsd: no-change prune schedules no build and deletions dirty only cutoff cells; AUTO aliases its concrete twin (one slice, one body); private overlays and PUBLIC encoding are demand-gated with a 120s grace (idle publishes retain previous entries as instantly-servable stale bodies); only demanded `(range, resolution)` views encode at live cadence with a 60s sweep for the rest; delta entries are generated per demanded view only and carry changed buckets/gaps/tombstones; unchanged frozen Bucket wire dicts are memoized by object identity byte-for-byte; labelled full-build reasons and `last_encode`/`encode_totals`/`encodes_skipped_idle` accounting; `gc.freeze()` + threshold tuning removed ~a fifth of active CPU. Rings were evaluated and CLOSED by the queue's own re-measure gate: the per-tick SQLite read they target is ~8% of the remaining active cost.
- Web/session-files: durable cache identity split into a stable logical view key + replaceable source generation (one file per view, replaced in place; older generations serve as stale last-known-good; old key-per-generation files ignored, never deleted, aged out by the existing prune; key version 3 -> 4); index updates append one JSONL journal line (merge-on-read, fold-on-rewrite); `shared_git_identity` is the one owner for both the cache-key and snapshot paths with a watcher-backed repository-state record (worktree + .git HEAD/index/refs/packed-refs/MERGE_HEAD events, warm request = ZERO git commands, 60s safety bound, fail-open when unwatched); transcript discovery uses one per-directory mtime-keyed catalog (recursive glob/rglob retired; resumed old-name rollouts keep their mtime contract; immutable session_meta identity-cached); untracked line counts identity-cached; Tabber warmer parks on demand expiry and unparks by event; bounded cumulative work counters flow through the existing performance owner.
- Existing parents checked: `_fold_or_reuse_bucket`, `_publication_resolutions`, `PublishedCache`, `SessionFilesService` (extended, not duplicated), `TabberActivityWarmerRecord`, `client_events.has_demand`, `TtlCache`, `atomic_write_text`/`file_lock`, `common.git`. Searches: `rg 'glob("\*\*|rglob\(' yolomux_lib` (zero request-path hits afterwards), `rg git_snapshot_identity` (two callers -> one owner), `rg TABBER_ACTIVITY_IDLE_REFRESH` (dead code deleted). Retired paths verified absent by grep and by regressions that monkeypatch `Path.glob` to raise.
- Verification: stats suites 130 passed (service/materializer/client/storage incl. the randomized full-vs-incremental differential with delta byte-exactness); web suites 490+ passed (app, session_files, sessions, background_owner, common); regression battery is operation-count based (zero warm re-lists, zero warm git spawns, exactly 2 identity runs for a six-view burst, one durable file per view, one journal line per write). Net non-generated change (this queue's edits, approximate — the tree is co-owned with a concurrent v2 landing): ~+950/-120 across yolomux_lib, tests, and docs.
- Delivery (2026-07-16): guarded `boot.sh 8881` restart (load ramp refused twice while gate load lingered, then restarted at load1 3.97), one listener, `/api/ping` 401 auth-gated, statsd writer compatible (schema gen 5 / protocol 23, warm 26/26). check.py: 3051 tests passed; the only 3 failures are the concurrent stats-panel UI rewrite in files this queue never touched. Delivery benchmark repeat: 25.5% of a core with four reconnected real browser clients; the isolated accepted 5m/1s workload stands at 14.2%; per-tick incremental 0.079s.
