# 2026-07-20 YO!cost always-visible column legend

- Completed and removed `DOIT.yocost-column-legend-inline.md`. The YO!cost report now renders one always-visible column legend (color swatch + bold label + terse one-line gloss) for Input/Cached/Output/Other/Total once, above the usage tables, so the column meanings are glanceable without hovering or expanding. The full per-column definitions stay as the header hover tooltips — one shared column owner backs both the tooltip and the gloss (no forked wording). "Cached" stays ONE combined column, self-labeled in the gloss as "cache reads + writes, combined" (not split, so no added width). The legend is a tight container-query grid (five intrinsic columns wide, a bounded 3+2 layout below ~34rem) that never forces horizontal overflow. (This queue was drained concurrently with the standing release; the two implementations were reconciled to one coherent `column-legend` naming — the dead `usage-legend` CSS block was removed and the browser test's selector unified.)

- Follow-up: the visible Model Usages, By Agent, and source-attribution tables now separate `Cache read` from one compact `Cache write` column. The storage/API schema and Cost calculation retain exact 5-minute and 1-hour cache-write dimensions because provider rates differ. OpenAI/Codex has no emitted cache-write counter, so its visible write cell is `—` with a lower-bound tooltip rather than a fabricated `$0`; browser coverage proves the six-column responsive table and the unreported-cell treatment.
- Verification: node panel source-contract (one legend / shared owner / all five keys / Cached-combined gloss / tight CSS) plus the extended browser geometry test (legend count 1, five entries, swatches, header tooltip titles, no overflow, retained scroll); the canonical `python3 tools/check.py` PASSED all 8 lanes in 83.25s (full browser suite); GUI.md documents the legend contract; guarded `./boot.sh 7772` restart returned `/api/ping -> 401` and live smoke confirmed the served legend + Cached gloss.
