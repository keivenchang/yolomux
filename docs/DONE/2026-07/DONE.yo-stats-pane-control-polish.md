# 2026-07-12 YO!stats and pane-control polish

- Completed and removed `DOIT.status-hover-explanations.md`. The topbar `IDX|STATS|SESS` owner-status hover title now defines Index, Stats, and Session-file ownership, explains leader/follower state, reports live owner/current-server state, and documents right-click takeover. The latency meter remains noninteractive but now explains browser↔YOLOmux round-trip latency in milliseconds.
- Completed and removed `DOIT.moreinfo-below-total.md`, `DOIT.cost-pricing-sources-section.md`, and `DOIT.cost-usage-chart-compact.md`. Cost Summary remains a condensed non-chart card with `More Info` below the Total row; the details modal now ends with one deduplicated safe `Pricing sources` section, and `Model Usages` uses one shared header grid with aligned token-over-cost cells, accessible row summaries, and plain `0`/`$0` zero-token cells.
- Completed and removed `DOIT.hide-minimize-on-pinned.md`. A shared `minimizeBlockedByPinned()` predicate now gates both behavior and chrome, hiding dead minimize controls for fully blocked pinned panes while preserving valid mixed pinned+unpinned Generic-pane minimize. Generic, Side Pane, and narrow active-tab cases are covered.
- Verification: `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, focused browser coverage for owner-status and cost-summary behavior, and the full canonical `python3 tools/check.py` gate passed in 383.39 seconds. Port 8881 restarted from this checkout with `./boot.sh 8881`; post-restart checks found exactly one `yolomux.py` listener on 8881 and `/api/ping` returned 401 in 0.009712 seconds.

### Mobile terminal smart-key palette

- Completed and removed `DOIT.mobile-keyboard-fixes.md`. The coarse-pointer terminal launcher now opens on the next intentional tap after a drag, clamps itself through render/window/pane resize, and stays visible inside its owning pane. The palette can also be dragged by its background/padding without stealing key-button taps; current follow-up work uses the launcher coordinates as the single position model so the `×` and palette stay attached.
- The smart-key palette now exposes Alt, Shift, and Cmd/Meta modifiers. Ctrl, Alt, Shift, and Cmd share one modifier owner: single-tap arms one key, double-tap locks with a distinct visual state, locked modifiers persist across multiple terminal inputs, and tapping a locked modifier clears it. Shift maps printable keys and Shift+Tab; Alt and Cmd/Meta use the terminal ESC-prefix convention.
- Follow-up left-column work moved Esc, Ctrl, Shift, Alt, Cmd, and direct `^C` into one vertical utility column beside the D-pad; the top row keeps Tab and tmux prefix. The launcher remains the close control while open, and there is still no separate palette close button or visible drag handle.
- Verification: `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, focused emulated-iPad Selenium coverage for launcher drag/open/resize, palette drag/key hit-testing, sticky Ctrl double-tap, and the full canonical `python3 tools/check.py` gate passed in 410.75 seconds. The initial guarded `./boot.sh 8881` restart refused to launch under high load, so stale duplicate check/pytest processes in this worktree were terminated and port 8881 was restarted once via the equivalent `launchctl` command with `YOLOMUX_BACKGROUND_OWNER_PRIMARY_PORT=8881` and `--dev`; post-restart checks found one listener PID 9348 and HTTPS `/api/ping` returned 401 in 0.004071 seconds.

### YO!stats explanatory hover details

- Completed and removed `DOIT.yostats-hover-explanations.md`. YO!stats chart titles, displayed-summary chips, legend labels, metadata fragments, and Model-token dimension choices now expose localized plain-language explanations through one shared native-hover/accessibility attribute helper. The data-point `js-debug-hover-tooltip` remains a separate max/time graph readout.
- Chart groups each have localized description keys across all source locales. Cryptic API/SSE/Bandwidth abbreviations, RSS/server-sequence/uptime/traffic/removal metadata, Attention/Transition/Working/Idle status terms, displayed-sum summaries, model-token dimensions, fixed series, dynamic agent/model token series, and GPU device series all route through shared description lookup or explicit series `descKey`s.
- Touch and accessibility are covered by `aria-label` fallbacks on the explanatory elements without adding passive touch-hover handlers. Browser coverage verifies chart-title descriptions survive dark/light themes and Generic/Vertical Side Pane roles; Node coverage verifies locale completeness, rendered desc-key hooks, accessible attribute ownership, and stale bare-span assumptions in legends.
- Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/layout_url.test.js`, focused Selenium coverage `tests/test_browser_layout.py::test_yostats_chart_title_descriptions_cover_theme_and_pane_role`, and the full canonical `python3 tools/check.py` gate passed in 379.31 seconds under a 900-second guard.

### YO!stats Cost bounds and tmux attribution

- Completed and removed `DOIT.cost-bounds-tmux-subagents.md`. Cost summaries now expose `lower_micro_usd` and `upper_micro_usd` alongside the backward-compatible known `total_micro_usd`; exact all-priced intervals render as a single estimate, while incomplete or unknown-model intervals render a lower–upper range instead of collapsing missing pricing to `$0`.
- Unknown/unpriced model atoms remain unpriced but use the pricing catalog’s comparable billable-class rate band for a finite upper estimate. The lower contribution stays zero unless an exact catalog row exists. The packaged catalog helper chooses the cheapest and most expensive active rates for the same direction/modality/cache-role/unit class, with profile/service-tier exact match first and class-wide fallback.
- The Cost `More Info` report now includes `Cost by tmux window`, grouping each interactive tmux session/window once using the same Agent-token row identity (`tmux_key`/label/session/window). Parent and subagent cost rows fold under the interactive window, while the event dedupe identity stays `(event_id, direction, modality, cache_role, unit)` so attribution changes cannot double-count.
- Audit coverage now proves Claude and Codex transcript-family event IDs are stable and distinct across parent/subagent files, parent+child transcript usage is self-only and summed once, out-of-window Codex children are a documented under-count, and component retention eviction marks summaries `truncated/lower_bound` with public `complete=false`.
- Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, focused Python cost/session/app tests, `node tests/layout_url.test.js`, focused Selenium Cost modal coverage, and the full canonical `python3 tools/check.py` gate passed in 344.01 seconds. Port 8881 was restarted from this checkout with `YOLOMUX_BACKGROUND_OWNER_PRIMARY_PORT=8881`; post-restart checks found one listener PID 49535 and HTTPS `/api/ping` returned 401 in 0.011996 seconds.

### YO!stats CPU sampling and durable-history coverage

- Completed `DOIT.fix-cpu-1-second-problem.md`. The elected stats sampler now advances on absolute one-second deadlines, records late/missed-cycle diagnostics rather than overlapping or fabricating catch-up samples, and retains real owner-failure backoff. Token recovery runs outside the SQLite-owning listener, macOS host CPU/memory measurements use native APIs, and GPU refresh is cached so expensive probes do not gate every CPU sample.
- Live samples arrive over the existing stats event channel instead of waiting for the former 30-second browser poll. Durable history maintains coarse rollups for broad ranges and interval/resolution-aware coverage for selected ranges, so a partial live tail cannot hide earlier persisted CPU records.
- CPU charts preserve observed zero values and visibly break at truly unsampled intervals. They do not carry CPU forward or reuse the red Agent-status outage overlay; while an older prefix loads, current points remain visible under the shared in-chart loading state.
- Verification: focused statsd/browser reproduction coverage passed 6/6; `tools/static_build.py --check` and `git diff --check` passed. A monitored 8881 run during `agent-token-persist` stayed healthy with no sampler failure; over the latest 70 durable one-second rows it observed 39 one-second and 30 two-second intervals, represented as honest CPU gaps. An initial concurrent gate hit an unrelated chat-store timing ceiling (the exact test passed alone in 2.11s); the final canonical `python3 tools/check.py` rerun then passed all eight lanes in 425.00s. Port 8881 was restarted as its sole listener and `/api/ping` returned 401.

### Tab-scoped pinned behavior

- Completed `DOIT.pinned-is-tab-scoped-not-pane-minimize.md`. Pinning now protects only tab-level user movement and LRU/cap eviction: pinned tabs cannot be user-dragged across panes or evicted, but they no longer hide, refuse, or alter pane minimize.
- Generic minimize transfers every source tab (pinned included) through the existing capacity owner; one-column and Vertical Side Pane minimize retain their normal pane-action behavior. Full target capacity still protects pinned target residents through the shared capacity rules.
- Verification: focused Node layout and three Dockview browser cases passed; `python3 tools/static_build.py --check` passed; final `python3 tools/check.py` passed all eight lanes in 419.84s. Restarted only 8881; its single fresh listener returned expected `/api/ping` 401.

### Finder Sync session cursor memory

- Completed `DOIT.finder-sync-remember-cursor-per-session.md`. Finder Sync now stores each `(session, root)` target's expanded/collapsed state, selected paths, keyboard lead cursor, and selection anchor together. Returning to that session restores the visible cursor through the existing active-descendant/scroll helper without taking terminal or browser focus; collapsed or missing rows fall back to a visible ancestor.
- Same-root sessions remain independent, and restore uses the cache-first sync path without a blocking filesystem request. The behavior is covered by the committed Node/browser regressions in `d311d528` and `cffa2070`; this completion adds the explicit GUI contract and archive record.

### YO!stats history latency and timeout honesty

- Completed `DOIT.fix-yostats-history-slow-sporadic-abort.md`. Stats history reads remain independent of slow sampler/control work, use bounded indexed range reads and byte-bounded browser uploads, and no longer replay a timed-out current RPC through the legacy transport.
- Browser requests use an 8-second short-range timeout scaled to a 30-second 24-hour cap. A genuine timeout reports its named duration and enters the existing exponential retry/backoff path; empty complete history and real server errors remain distinct states.
- Measured production/copy-DB reads were well below the timeout budget (24-hour full history median about 351ms); existing statsd, Node, and browser regressions cover slow-owner responsiveness, coverage, and timeout/readiness behavior.

### Cost Summary detail report tables

- Completed `DOIT.cost-details-agent-model-tables.md`. Cost `More Info` uses a responsive wide report with semantic By Agent and Model Usages tables: each class pairs token quantity with cost, each row has accessible detail, and each table ends in a reconciling grand total.
- Agent/source attribution carries only safe canonical transcript metadata; absolute `.jsonl`/`.ndjson` sources open through the shared Preview action, while missing or unsafe paths remain plain text. The GUI contract and focused Node/browser tests cover modal containment, exact arithmetic, table columns, and Preview navigation.

### Finder Sync cache-first switching

- Completed `DOIT.finder-sync-switch-instant-cache.md`. A warm Finder Sync target reconciles its remembered tree synchronously from the bounded shared listing LRU, preserving mounted rows and session-specific disclosure state. The next frame revalidates visible directories in the background; a cold path uses bounded parallel workers.
- `tests/layout_async.test.js` covers zero blocking warm requests, changed/unchanged revalidation behavior, mounted-node preservation, bounded cold batches, and LRU eviction.

### Mobile terminal keyboard bottom modifiers and Backspace

- Completed and removed `DOIT.keyboard-alt-cmd-bottom-left-add-backspace.md`. The popup keyboard's shared side stack is now Esc, Ctrl, Shift, Ctrl-C, Alt, Cmd, placing Alt/Cmd at the bottom. Its compact primary row exposes the existing one-shot Backspace definition beside Tab and Ctrl-B without duplicating the `\x7f` transport path.
- The flexible primary grid uses three `minmax(0, 1fr)` tracks. Node contracts and the real coarse-pointer Selenium case pin the order, payload, compact/overflow behavior, and 320x240 containment in both themes.
- Verification: generated assets and `static_build.py --check` passed; Node layout reported 48/0; focused Selenium reported 2/0; the bounded browser matrix reported 447 passed and 5 skipped. The other canonical lanes passed before the first browser launcher orphaned; that exact lane was stopped and rerun under a 600-second watchdog. Port 8881 restarted as one listener, `/api/ping` returned 401, and the served JS/CSS contain the tested arrays and relative grid.

### First-class YO!cost tab

- Completed and removed `DOIT.yocost-tab.md`. Cost Summary stays a compact default-off card immediately after Model tokens; its explicit More Info action opens or activates a normal `YO!cost` virtual tab instead of a modal. The tab restores/shares like other YO!* tabs and owns an internal scroll region with Agent/Model charts first, one Range/Resolution owner shared with YO!stats, freshness/Refresh, and sortable responsive accounting/attribution tables.
- Known priced cost remains distinct from unpriced/lower-bound usage. Tables retain all agent, model/effort, component, source, and tmux-window values with per-class token/cost breakdowns, safe transcript Preview links, authoritative pricing links, and reconciling totals. Large selected ranges use one cached aggregate; zoom disables the shared slider until Reset Zoom without a hardcoded viewport width.
- Verification: `static_build.py --check`, the YO!cost performance suite (2/0), focused YO!cost Selenium (1/0), statsd (75/0), and the bounded browser matrix (447 passed, 5 skipped) passed. All other canonical lanes passed in the integration gate. Port 8881 restarted as one listener with expected `/api/ping` 401, and its post-restart token payload has identical Agent and nested Model totals.

### YO!stats history hardening and token-chart recovery

- Completed and removed `DOIT.yostats-harden-validate.md`. The blank Agent/Model token charts were not missing data: a valid live history response contained nonzero synchronized token records, but its available prefix began a few seconds after the selected boundary. The client rejected `complete=false` before applying any records, discarded the payload, and left only the right-edge SSE sliver. A usable terminal-partial response now paints its records and treats the explicitly unavailable prefix as satisfied for that domain; missing or malformed coverage remains a bounded error.
- Mixed raw/rollup data now uses exactly one finest source per instant and one domain-wide display width. The stats API/RPC/SSE contract, deterministic 503/413/timeout/restart/idempotency/lower-bound tests, and real-browser 1h/4h/24h range matrix pin the corrected coverage and recovery behavior. System exposes sampler deadlines, history latency, and cache rate; Logs names retry, coverage, timeout, owner-change, and rollup paths.
- Dense history no longer re-coalesces the growing cost-component list for every source bucket. The realistic 2,700-slot budget fixture improved 1h from 0.885s to 0.496s and 24h from 2.346s to 1.443s while preserving exact Agent=Model and cost totals, <=360 points, and compressed-payload bounds. The final canonical eight-lane gate passed in 388.03s under a 600-second watchdog. Port 8881 restarted as the sole listener (PID 26137), statsd restarted as its sole current-code writer (PID 30756), and live browser QA reached `ready` with no overlay and identical Agent/Model displayed totals (43.8k after restart).
