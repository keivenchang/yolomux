# 2026-07-20 YO!stats Cost summary min-width and Model output tokens chart

- Completed and removed `DOIT.yostats-cost-summary-table-min-width.md`. The Cost summary table no longer stretches to fill the card: `.js-debug-cost-summary .js-debug-cost-table` was added to the existing report-table content-sizing selector (`inline-size: max-content; min-inline-size: auto; max-inline-size: none`), so it shrinks to its content like the By Agent / Model Usages tables while keeping the `.js-debug-cost-table-wrap` scroll affordance. Source contract + browser geometry regressions assert the table is narrower than its wrap and shares the one sizing owner. Live 7772 serves the rule.
- Completed and removed `DOIT.yostats-model-output-tokens-and-full-chart-names.md`. The Model tokens chart is renamed to "Model output tokens/min" (all locales) and plots output only — the "Tokens:" dimension selector and its machinery (array, persistence, helpers, event wiring, selector markup) were removed as a net deletion, leaving `dynamicTokenDimension: 'model'` only as the model-series filter; the served bundle has zero `data-js-debug-model-token-dimension-select`. The Charts popup checkboxes now render the full localized chart titles instead of the compact `toggleLabelEn` labels. GUI.md updated in the chart-list/heading-row/axis passages (including removing the stale inline `Tokens:` dimension-select clause). Node panel/UI suites and the moved full-name test pins pass.
- Verification for both: the canonical `python3 tools/check.py` PASSED all 8 lanes in 83.59s (full browser suite); guarded `./boot.sh 7772` restart returned `/api/ping -> 401` and live smoke confirmed the served title and CSS rule.
