# 2026-07-17 Touch interaction and YO!cost attribution

- Completed and removed `DOIT.touch-drag-zoom-stops.md`. One shared horizontal-touch claimant preserves native vertical chart scrolling, commits a threshold-clearing selection on pointer cancel, and prevents passive YO!stats/YO!cost repaint owners from replacing active gesture DOM. Trusted CDP touch coverage forces a repaint during a wobbling drag and proves the deferred zoom settles afterward.
- Completed and removed `DOIT.touch-terminal-scroll.md`. Vertical terminal pans use fractional rendered-row distance through the existing tmux scroll transport without focusing xterm; alternate-screen apps receive cursor-mode-aware arrows or one PgUp/PgDn flick. Horizontal/multi-touch cancels, taps retain focus, and end/cancel has no inertia. Pure Node and trusted CDP coverage exercise every route.
- Correction (2026-07-19): the prior generic alternate-screen arrow claim was wrong for mouse-reporting Claude panes. Claude is `alternate_on` and consumes wheel reports for transcript scrolling; arrows cycle its input history. Mouse-reporting panes now use the wheel route, with arrow synthesis kept only for non-reporting alternate-screen apps.
- Completed and removed `DOIT.yocost-by-agent.md`. Cost agent attribution now carries a stable opaque key and bounded privacy-safe label through Python, wire validation, frontend aggregation, chart legends, and By Agent tables. Separate Codex/Claude windows no longer collapse; background labels are compact; full bounded labels remain accessible; content-sized tables preserve trailing cells at 260/360/720/1200px. The exact browser matrix exposed and fixed one stale JavaScript mandatory-field list.
- Focused verification passed 20 current-panel tests, 145 materializer/protocol tests, the exact HTTP/client/RPC/browser matrix, and the relevant touch/YO!cost Selenium cases. That batch's canonical eight-lane gate passed in 616.06s; the later live metrics/iPad/range completion above records the final guarded restart and current gate.
