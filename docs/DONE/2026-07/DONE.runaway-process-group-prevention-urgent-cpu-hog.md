# 2026-07-19 Runaway process-group prevention (urgent-cpu-hog)

- Completed and removed `DOIT.urgent-cpu-hog.md` (incident: a failed capture left the 8881 dev server plus two orphaned workers at ~300% CPU; recurring class). Four deliverables, one shared identity source:
- **Process-group ledger** extending the existing ownership parents — `local_services/registry.py` service records (v2) carry `pgid`/`launcher_pid`/`launcher_port`/persisted jobd `worker_pids`; the port lease records `pgid`; new `bounded_process_table` / `service_record_identity_matches` (exact-socket command check — PID reuse and Defender/Spotlight-shaped processes structurally rejected) / `tracked_local_service_groups` / `tracked_port_process_group` (lease PID cross-checked against exact `--port N` argv); System exposes it via `runtime_local_services().ledger`.
- **Bounded overload watchdog** (`local_services/watchdog.py`): armed only during startup (`cli`, env-tunable) and captures; fires only on sustained breach (250% aggregate for 30s, or >32 tracked children); graceful SIGTERM to leaders → bounded grace → SIGKILL only still-live tracked PIDs; conservative shared-service veto when another web port is live; bounded redacted evidence under `/tmp`.
- **Fail-closed launch/capture**: boot.sh runs `local_services/preflight.py` between teardown and launch — refuses a wedged live owner, targeted-reaps a dead owner's identity-verified orphans (ppid 1 + recorded pgid); the capture tool gained signal handlers, a SIGALRM wall-clock deadline, Selenium timeouts, benchmark process-group isolation, bounded `driver.quit()` with chromedriver-tree fallback, and a ledger-unchanged proof; `--print-runtime-report` no longer constructs a second `TmuxWebtermApp` (new `runtime_report` control action on the live server; bounded ledger-records fallback).
- Verification: 42 focused tests across launch/watchdog/capture/report suites; canonical gate passed all eight lanes (512.56s); guarded 8881 restart through the live preflight; 60-second authenticated browser capture (web 87.4% CPU, statsd 15.8%, armed watchdog correctly silent = no false kill). Two real mid-capture failures (driver exception, benchmark crash) each ended with zero orphans and an untouched server — live fail-closed drills. Forced-runaway containment proven at unit level against injected tables (operator constraint: no live core-burning). A post-fix full-gate rerun failed only on 26 tmux `TimeoutExpired` cases in months-old tests while Defender ran at 243% — a representative case passes in 0.70s isolated (environmental). Follow-up candidates recorded: unconditional `check.py` self-nice; Spotlight exclusions for project roots (script staged at `/tmp/spotlight-exclude-dev.sh`, needs operator sudo).
