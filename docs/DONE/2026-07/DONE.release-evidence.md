# DOIT — release evidence

Phase: 5. These items are last and require user-authorized access to a real authenticated desktop/live deployment. Fixture coverage cannot close either checkbox.

Session rule: one checkbox per fresh session with the human available. Record exact runtime identity, journey/workload, observed transitions, and artifacts here; never convert missing live evidence into a fixture substitute.

## Reported-symptom traceability

Assertion-efficacy audit: 7 PROVEN, 4 PLAUSIBLE, 5 ADJACENT. These labels describe whether the cited regression would fail when the reported symptom returns; branch availability remains a separate release-coverage requirement.

| Reported symptom | Fix commit | Regression that should fail | Coverage status |
|---|---|---|---|
| Green RUN while idle or blocked | `498f7dc0`, `d9def621`; `5732721b` is required but not on `main` | `tests/tabber.test.js` real idle capture cases; the regression added by `5732721b` is not on `main` | Partial — the ball-detector fix that keeps idle panes out of RUN remains on `groupB-ui`, so current main is not release-covered for the repeatedly reported idle-RUN symptom. |
| No green ball while actively working | `498f7dc0` | `tests/tabber.test.js` Working-to-idle and idle-to-Working revision cases | Covered |
| Preferences toggles dead | `154142d3` (not on `main`) | `tests/preferences_panel_render.test.js` (not on `main`) | PLAUSIBLE, not current-main coverage — the test executes the renderer and asserts that all eleven checkboxes exist, including read-only state; the symptom was dead controls, and the test never changes one or verifies persistence/effective state through the backend. |
| Preferences sections vanish after clearing search | `154142d3` (not on `main`) | `tests/preferences_panel_render.test.js` section-id case (not on `main`) | PLAUSIBLE, not current-main coverage — the test renders an already-empty query and asserts every section id; the symptom was a filtered-to-cleared transition that left stale DOM, and that transition is not exercised. |
| Runtime line does not wrap | `a2df16d6` | `tests/test_browser_boot.py::test_fresh_browser_workload_measures_runtime_wrap_drag_and_terminal_input` is absent from `main` | Partial — the source-side change landed, but the cited browser regression is no longer present on current main. |
| Daemons shows 55 unknown cells | `f9285ef3`, `97039c25` | `tests/debug_daemon_diagnostics.test.js` reads all eleven `SubsystemSpec` entries and verifies every rendered state, reason, and typed absence reason | ADJACENT / UNPROVEN — the test gives the renderer manufactured typed `not_measured` values and proves it does not display those values as unknown; the symptom was the real backend/API supplying unexplained nulls that rendered as 55 unknown cells. |
| Empty token charts | `17a396ba`, `10ac02da` | `tests/test_stats_current_transcripts.py::test_fixture_transcript_token_series_reaches_browser_snapshot_wire` builds a fixture-owned Codex transcript through `StatsCurrentService` to the browser snapshot wire and asserts `model_tokens_per_minute:input:gpt-known == 1200` | ADJACENT / UNPROVEN — the test proves non-empty token series reach the browser snapshot wire; the symptom was an empty rendered chart, and browser consumption and paint are not asserted. |
| Cost summary shows $0.00 | `818c6db0`, `efa8285d` | `test_seed_claude_opus_five_projects_a_rate_but_unknown_models_stay_unpriced` | ADJACENT / UNPROVEN — the test proves seeded price projection and unknown-model semantics; the symptom was a rendered `$0.00` summary after live attribution, which the test never builds or paints. |
| YO!chat sends but does not answer | `159c81bb`, `405c5441` | `tests/test_browser_layout.py::test_yochat_plain_message_surfaces_that_yoagent_was_not_requested` | PLAUSIBLE — the browser test proves a plain message shows the `/yo` guidance notice and intentionally makes zero YO!agent requests; the symptom was no answer, and an actual `/yo` request receiving an answer is not exercised. |
| Finder expansion reverts or reloads blank | `ec2a7c0d`, `beaa17ef` | `test_finder_second_click_cancels_a_pending_expansion`; `test_finder_reexpand_renders_loaded_children_before_refresh_returns` | ADJACENT / UNPROVEN — the tests prove cached-child and cancellation behavior with `fetchDirectory` replaced by controlled promises; the symptom occurred on the production SSE/batch/direct-fetch path that these tests bypass. |
| Differ file open intermittently fails | `5df477dc` | `test_differ_open_keeps_git_diff_when_snapshot_file_has_vanished` | ADJACENT / UNPROVEN — the test proves the vanished-file helper branch after replacing `openFileInEditor` and `apiFetchJson`; the symptom was failure on the production exact-read and repeat-open path, which is not exercised. |
| Persistent-mux error flood | `dcbfd763`, `a0a8450c`, `d8d88c93` | `tests/test_local_services_rpc.py::test_persistent_mux_listener_names_transport_loss_in_diagnostics`; `tests/test_local_services_mux.py::test_persistent_mux_reconnect_storm_has_one_transport_record_per_dedupe_window` | Covered: the reconnect-storm repro fails before `dcbfd763` with 10 records and passes with one transport record per dedupe window. |
| `client_event_stream_os_error` flood | `6b588baf`, `031f2369` | `test_server_supervisor_process_log_names_exception_type_and_errno`; `test_client_event_stream_disconnect_storm_is_one_deduplicated_warning_not_errors` | Covered for diagnosed sendall disconnects; `next_event` cause remains held |
| Tab drag feels sluggish | `8f1777dc`, `1b608606` | `tests/test_browser_boot.py::test_fresh_browser_workload_measures_runtime_wrap_drag_and_terminal_input` is absent from `main` | PLAUSIBLE, not current-main coverage — the test bounds `tabDragStart` and `keydownToTermData` handler work; the symptom was perceived end-to-end latency, while event delivery and wall latency are outside those counters. |
| Tmux session list stays stale until 7770 restart | `56a6f3b4`, `c82a816e` | `tests/test_three_webserver_process_topology.py::test_follower_session_metadata_revalidates_after_daemon_roster_change_without_restart` | Covered |
| Mock-driven agent status detection | `92239c36`, `295bfc04` | `tests/test_mock_agents.py::test_live_mock_agents_drive_run_stop_and_yellow_tab_states` drives RUN, STOP, and yellow through live `claude.py --mock` and `codex.py --mock` | Covered |

- [ ] Real GUI release matrix. After every P0/P1 data path and architecture gate is green, run one authenticated real-backend desktop tour covering layout restore, menus, Tabs, Finder, Differ, Tabber, editor/preview modes, drag/move/split, popovers, Preferences, YO!metrics/Cost, chat/status recovery, and terminal connectivity across supported themes/viewport sizes. Require exact serving fingerprint/PID/cwd, hard-reloaded current bundle, visible geometry/state assertions, no browser console errors, and human confirmation for subjective visual parity.

- [ ] E1 rollout evidence. Capture matched before/after workloads and an authorized guarded 7772 restart with `YOLOMUX_START_LOAD_WAIT_SECONDS=30`; monitor the live deployment through child replacement, first events, namespace recovery, metrics cadence, agent/token/cost attribution, and at least 15 minutes of steady state. Acceptance: all canonical gates are green, one exact daemon/storaged pair serves the expected fingerprint/modules, no retired/runaway child or listener survives, production data remains intact, every unavailable interval has a reason, and the user approves the release evidence.
  - PARTIAL 2026-07-29: container-only E1 rerun logged at `/tmp/yolomux-e1-retry-stop-20260729-074809.log` after canonical green gate `/tmp/yolomux-gate-loadmeasure-064134.log`. A real provisioned admin account drove matched 60-second active before/after workloads around the guarded restart on private container port 7792; both boot readiness checks verified one daemon/storaged pair and a completed product. The 900-second idle-YO!metrics capture completed with 897 timer callbacks, 841 graph renders, zero duplicate seconds, and a maximum of one render per second. Before/after active CPU-seconds were daemon/storaged/web 1/4/4 and 1/10/4; peak RSS bytes were 102252544/59367424/93372416 and 101556224/95662080/97001472. The container proves no loss across this disposable instance, not the user's production data. Attribution remains incomplete: the preceding capture recorded `RuntimeError: agent roster unavailable` at `yolomux_lib/daemon/fs/transcript.py:499` (`/tmp/yolomux-e1-retry-20260729-073110.log`). User approval and the real GUI matrix remain required.
