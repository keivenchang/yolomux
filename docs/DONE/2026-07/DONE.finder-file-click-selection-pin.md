# 2026-07-19 Finder file-click selection pin

- Completed and removed `DOIT.finder-pin-selection.md`. `setFileExplorerSelectionPin()` is now the one owner for Finder/Differ tree-selection pin state. A normal Finder file click pins the current Sync target just like modifier and keyboard selection; a directory click still only expands/collapses. Only explicit session/file sync clears that pin, so passive hover, terminal focus, metadata/transcript refresh, and SSE cannot re-scope a selected file.
- Same-root sync refreshes now preserve selection, expansion, and scroll while root changes alone prune selection. Explicit session target changes retain the existing clean re-scope behavior. The browser regression extends `test_sync_mode_does_not_follow_hovered_tmux_session` with a real file click followed by an `applySessionMetadataPayload()` refresh while another pane is hovered; it proves root, selected path, and expansion stay unchanged. GUI.md records the file-click pin rule and coverage.
- Verification: `python3 -m pytest tests/test_browser_finder.py -q` (22 passed), `node tests/layout_url.test.js`, `python3 tools/static_build.py --check`, and the canonical gate. The gate exposed a pre-existing scheduler test that used strict float equality after repeated cadence arithmetic; its assertion now uses `pytest.approx`. Live: restarted 7771, opened its served UI in test-bypass mode, clicked a visible Finder file, hovered the other live terminal pane, waited 31 seconds, then applied the metadata-refresh path; the selected file, root, and expansion remained pinned. Restored normal authentication afterward (`/api/ping` 401).
