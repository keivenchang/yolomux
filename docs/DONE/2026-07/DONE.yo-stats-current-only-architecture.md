# 2026-07-16 YO!stats current-only architecture

- Completed and removed `DOIT.claude-token-coverage.md`. Claude token discovery now uses the same shared bounded filename/mtime catalog policy as Codex while keeping each top-level project transcript and its own subagents as an independent family; non-pane sessions receive a stable bounded project/session identity instead of being mislabeled as the idle tmux pane. Newest cold roots run first, durable path cursors survive a later pane-attribution change, and unchanged scans remain idempotent. Focused scanner/session verification passed 142 tests; the canonical gate passed every lane, including browser in 528.36s. After a guarded 8881 restart, the previously invisible real background transcript backfilled 5,170 Claude usage rows on its first cadence and stayed stable on the next interval; the exact 24h/300s response had 288 buckets, positive Claude Agent/Model series, and priced Claude YO!cost model/agent rows. Port 8881 is the sole listener, `/api/ping` returns 401, and one protocol-24 statsd owns the database.
- Completed and removed `DOIT.usage-fork-counter.md`. Codex subagent rollouts copied the parent's cumulative history under new child identities and fresh timestamps, inflating the reported screenshot window from an evidence-based hundreds of millions of tokens to 4.437 billion tokens / $2,676.14. The shared cumulative parser now suppresses the complete copied prelude through the explicit child handoff, and protocol-24 exact tombstones delete only rows matching identity, timestamp, quantity, provider, model, child thread, and source. Scan version 3 repairs every active and orphan fork through bounded ACK-safe receipts; rollback/restart are idempotent, old writers stop before mutation, and the completion marker tracks each Codex sessions root so a later `CODEX_HOME` cannot be skipped. Live exact proof found zero copied-prefix rows/tokens or mismatches; the retention-truncated original window had fallen to 599.3M retained tokens, five genuine Input/Cache/Output events advanced afterward, and the 24-hour snapshot contained synchronized nonempty Agent/Model series plus populated cost attribution. Focused verification passed 516 tests and 18 transcript tests. The hard-timeout canonical gate passed every lane; one xdist-only settings-cache timing failure passed 10/10 isolated and the complete non-browser lane passed on rerun. Port 8881 restarted as the sole listener/background owner with one protocol-24 statsd writer, a durable root-aware marker, and `/api/ping` 401.
- Completed and removed `DOIT.usage-collector-hardening.md`. The token collector had two independent whole-pipeline stop conditions: a browser demand change switched 10s/60s cadence inside one immutable coverage epoch, and one conflicting atom rolled back every clean atom plus coverage before the scanner receipt could advance. `FamilyScheduler._new_epoch` now owns every start/cadence/wall-clock/restart identity, cadence regimes form adjacent non-overlapping coverage, and early backlog wakes stay in the current regime. Strict `Store.append_batch` atomicity remains unchanged; its typed conflict returns only bounded identity/SHA-256 evidence, statsd owns a 32-identity quarantine, and runtime bisects only rejected atom groups so clean atoms and coverage land before the receipt commits. System compares durably committed transcript growth with accepted atoms and renders a warning only for a real advancing-but-stale condition. Live QA then found historical family backlog was still delaying active transcripts by 115–1,884 seconds, so the receipt-aware scanner now prioritizes previously caught-up live tails while preserving round-robin fairness within live and historical groups. After restarting 8881, every active monitored rollout persisted within the 60-second idle cadence, Agent/Model snapshots advanced through the same 60s/300s buckets, statsd had zero failed builds, and the server logged zero immutable/conflict/receipt/runtime failures. Focused integration passed 143 backend tests, 18 Node checks, and the real-browser warning/clear case; the canonical gate's browser lane passed in 606.26s, its sole first-run failure was a load-sensitive assertion in the new startup-discount test, that test passed three repeated deterministic runs after correction, and the complete non-browser lane then passed in 163.34s. Shared parents and negative searches covered `_new_epoch`, `Store.append_batch`, `StatsCurrentRuntime._append_batch`, `StatsCurrentTranscriptUsageScanner._ordered_sources`, conflict status, and unchecked queue items. The co-owned current-architecture worktree is +31,204/-28,657 non-generated lines versus HEAD (net +2,547).
- Completed and removed `DOIT.yocost.md`. A Codex fork had copied 1,499 parent records into its child transcript, and the old scanner stored 3,015 `unknown` usage atoms totaling 1,650,388,346 duplicate tokens. Explicit fork metadata now suppresses copied cumulative history until the first real child context and emits exact five-part tombstones through the ACK-safe scanner/collector/client/service/storage path. The live repair initially remained starved because every bounded pass restarted from the same sorted filename; one receipt-aware round-robin cursor now advances only after commit, rollback repeats the same source, and a budget-limited pass schedules another bounded pass only after append ACK and scanner commit. Live 8881 advanced the target to EOF, removed every duplicate atom without relabeling it, retained 1,563 `gpt-5.6-sol` atoms / 71,836,289 tokens, and served a 24h/300s snapshot with 288 buckets, exactly synchronized nonempty Agent/Model-output totals, positive marginal/API-list cost series, and no unknown cost-model row. statsd remained schema-ready, warm 26/26, queue-empty, failure-free, with one 8881 listener. Focused backend verification passed 582 tests; the canonical eight-lane gate passed in 581.86s. Shared parents checked: `codex_usage_atoms_from_record`, `StatsCurrentTranscriptUsageScanner`, `usage_scan_success`, `iter_append_batches`, `StatsCurrentRuntime._append_facts`, and `Store.append_batch`; negative searches covered fork metadata, scan identities, tombstone identities, append callers, and the retired sorted-zero scan path. The co-owned current-architecture worktree is +30,268/-28,669 non-generated lines versus HEAD (net +1,599).
- Restored and removed `DOIT.retain-UI-and-add-to-spec.md`. The current-only backend had incorrectly replaced the shipped YO!stats presentation with a generic renderer, mixing units, exposing wire labels, stacking no-data paint, and deleting the exact Graph/API-SSE/System/Logs controls and layouts. The canonical `cb88b486` renderer and tokenized CSS are restored as the one visual parent, while a bounded adapter feeds current exact snapshots/cost reports into those established semantic cards; browser observations now use only `/api/stats-observations`, coalesce for 10 seconds, and back off to five minutes instead of writing the retired history API. The image-paste slowdown was server starvation: `_encode_generation` sliced every Range/Resolution/private cache cell before discarding all but the publication cadence; it now resolves the requested publication layers first and slices only those cells. Parent/equivalent search: `git show cb88b486:static_src/js/yolomux/85_debug_panel.js`, the matching `30_preferences_changes.css`/`00_tokens_base.css`, and `rg` for every `data-js-debug-*`, retired history endpoint, and primitive mount. The goal-wide non-generated diff is +2,995/-28,045 lines (net -25,050). Verification after the final edits: generated assets rebuilt and clean, all 16 Node shards passed, focused stats-service tests passed 2/2, focused real-browser System/Logs tests passed 2/2, browser stats coverage passed 4/4, and the live authenticated 8881 check proved the exact four subtabs, range/resolution controls, twelve chart toggles, established card DOM with no primitive mount, functional API/SSE/System/Logs views, zero unexpected console errors, one server listener, settled statsd CPU, and a 256 KiB paste upload in 23-57 ms. The full canonical gate was not rerun after this visual restoration; the exact affected lanes and live regression were run instead.
- Completed and removed the four current-only YO!stats queues (storage, engine, UI, and cutover). Supervised statsd is the sole writer for schema-versioned `stats-v5.sqlite3` (schema 5, minimum writer build 2, service protocol 23), which stores original observations, usage atoms, coverage epochs, unavailable spans, migration reconciliation, and schema metadata. Every writer performs a read-only schema/minimum-writer preflight before mutation; a newer fence returns terminal `upgrade_required`, cannot be replaced by an old runner, and is physically isolated from the retired `stats-history.sqlite3` name. Atomic migration activates only after all-source reconciliation, removes retired storage/protocol state on success, and startup deletes any subsequently recreated retired files without importing them.
- statsd asynchronously builds the four immutable `1s/10s/60s/300s` memory layers and serves exact cached generations through current binary RPC. Web processes only authenticate and forward `/api/stats-capabilities`, `/api/stats-snapshot`, `/api/stats-delta`, `/api/stats-stream`, and `/api/stats-observations`; browser metrics remain bounded private overlays. The browser renders exact buckets/no-data, keys X-axis second precision directly from the echoed `resolution_seconds === 1`, uses resolution-driven presentation, keeps sparse families moving without invented observations, and contains no temporal aggregation or legacy fallback. YO!stats exposes current Graph/API-SSE/System/Logs, and YO!cost uses the same exact renderer for Estimated cost and Token usage.
- Live migration QA found overlapping legacy unavailable spans that made a valid 16h/300s snapshot fail protocol validation. The common storage/materializer/migration normalizer now performs a deterministic sorted linear sweep, append-time validation rejects coverage/span and span/span overlap, and an idempotent build-1-to-build-2 startup repair reduced the live database from 13,331 unavailable rows to 1,950 while advancing its source generation once; conflicting coverage fails closed. The first quadratic repair attempt pinned statsd at one core and was replaced before cutover by the current O(n log n) sort plus O(n) sweep. Focused current storage/materializer/migration/service coverage passes 138 tests, and the broader current stats suite passes 460 tests.
- Live dev8881 evidence on the current target Mac (14 cores, 48 GiB RAM): cold migration took 0.499s, first full warm took 11.862s, reconciliation bookkeeping took 0.048s and its background full rebuild took 6.687s, with no failures; settled statsd CPU averaged 34.7% of one core (about 2.5% of host capacity), normal physical footprint was 185-193 MiB, and the cache held 130 snapshots plus 85 deltas in about 10.87 MiB shared and 44.71 MiB private bytes. All 26 exact Range/Resolution cells validated; the largest decoded/gzip snapshot was 1,208,536/76,303 bytes, average/max snapshot latency was 10.706/185.971ms, delta latency was 0.366ms, Agent/Model output totals matched across 288 buckets, token and cost reports were nonempty, and an old client received `upgrade_required` without replacing statsd or changing the database inode. Desktop/iPad browser tests prove one path per series and at most 600 points. The canonical eight-lane gate passed in 474.76s; its first attempt had one untouched editor scroll-sync load flake that then passed 6/6 alone before the complete clean rerun.
- Completed and removed `DOIT.gui.md`. Touch charts now distinguish native vertical pan and small finger motion from deliberate horizontal zoom; resolution completion follows accepted exact data and a focused 300s selector cannot hold the loading paint; plot movement and X-axis seconds share one resolution-scaled cadence; API/SSE logs preserve the reader's position; and the mobile keyboard replaces its launcher with the full internally scrolling 27-key surface, full-width handle, and top-right X. Focused live/browser proof covered the reported iPad interactions and a 60s-to-300s switch now paints in roughly 60-70ms with one snapshot and one SSE stream.
