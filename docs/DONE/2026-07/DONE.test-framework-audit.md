# 2026-07-07 Test-framework audit

- Completed and removed `DOIT.test-framework-audit.md`. The Node gate now treats signals, missing shard summaries, unawaited async tests, unsupported selectors, and unregistered shards as failures. The old editor-preview monolith is three independently executable 66-test shards, UI color pins have one registry plus an anti-rehardcode guard, and source-only ownership policies live in named static lints instead of anonymous node regexes.
- Browser coverage now exercises the real topbar packer, measured animation salience, light/DPR profiles, real xterm output over isolated HTTP/WebSocket, fixture-builder parity, and a local ignored 20-page golden matrix with a one-command refresh. Shared browser helpers own frame/settle/rect/paint/probePaint/gradient/row operations; test pin expectations derive from component tokens where that is the actual contract.
- Removed pure change-detector and cross-layer CSS/theme twins while retaining behavior, DOM, ForTest, and static-lint owners. The share-theme suite reports named independent fresh-runtime sections while preserving its intentionally ordered long-lived API sequence; docs now identify the actual shard/browser test owner instead of the launcher.
- Verification: final `python3 tools/check.py` passed all eight lanes in 73.26 seconds after the final UI-pin recovery; focused golden comparison passed 20/20 and the node launcher passed all 12 shards. Net test/source edits in this audit include substantial cleanup alongside new false-green, visual, isolation, and lint coverage; local golden PNGs remain ignored.

- Completed the panel/control/state ownership refactor and removed `DOIT.ui_architecture_refactor.md`. Shared panel frames, buttons/actions, virtual tab descriptors, drag/drop, editor state, conversation composer, Finder hover, YO!stats descriptors, and YO!agent controller routing now have one named owner, guarded by `static_build.py --check` ownership and normalized-clone lints.
- `TmuxWebtermApp` no longer owns stats-history, session-file, activity/transcript, or client-watch cache/work/watcher fields directly. `StatsHistoryService`, `SessionFilesService`, `ActivityTranscriptService`, and `ClientWatchService` own them, with standalone ownership coverage and the existing app/background-owner characterization suite migrated to those explicit services.
- The Node launcher keeps nine distinct behavior shards, a terminal pass/fail summary, a nonzero failure exit, and a real-node watchdog against hung suite tails. Static ownership tests cover raw pane-control rejection, unreviewed production clones, and shard integrity. Generated assets were rebuilt and the final canonical gate plus live server restart are recorded by the landing change.
