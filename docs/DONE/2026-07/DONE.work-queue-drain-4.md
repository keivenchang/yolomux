# 2026-07-03 Work Queue Drain

### Unified tab interaction controller

- Completed and removed `DOIT.tab_interaction_controller.md`. Regular pane tabs, Dockview tabs, and Tabber rows now use one controller for cursor details, right-/Control-click and keyboard actions, and pure-touch long press. Mouse, trackpad, and Pencil keep desktop hover behavior even on tablets; touch uses a cancellable bottom sheet and restores any Dockview pointer-down activation before it appears.
- The action surface has one identity/status header, in-place Details/Back, directional Move & Resize commands, normal actions, and destructive actions last. A one-tab directional split preserves an explicit empty peer with targeted Add tab quick-open, while Expand pane is a temporary Fill workspace snapshot/restore rather than browser fullscreen or an implicit whole-workspace arrange.
- Verification passed generated-asset/i18n checks, 189 editor-preview assertions, 67 layout-restore assertions, 44 Tabber assertions, the focused tab interaction Chrome cases, and the complete browser lane (335 passed, 4 skipped).

### Standard CSS radius ownership

- Completed and removed `DOIT.1.md`. All 86 standard component radius declarations across the six component CSS partials now reuse the existing `--radius-sm`, `--radius-control`, `--radius-md`, `--radius-lg`, and `--radius-pill` owners, including compound segmented corners and logical YO!info corners. The rendered pixel values are unchanged; a future token edit now propagates to every matching control, card, tab, and pill.
- Added `lint_raw_standard_border_radii()` to the normal static-build gate. Its clean-tree and negative fixtures cover simple, compound, logical, custom one-off, and multiline declarations, and the existing Node source contracts now require token references instead of pinning copied pixel values. Updated the GUI and development docs with the enforceable design-token contract.
- Verification rebuilt the generated stylesheet, passed 45 static-build tests, 165 editor-preview assertions, the share-theme source guard, `python3 tools/static_build.py --check`, `git diff --check`, and all eight canonical `python3 tools/check.py` lanes in 53.43 seconds.

### Canonical CSS paint ownership and concurrent test isolation

- Completed and removed `DOIT.2.md`. Sixteen repeated numeric paint shades across 39 component declarations now have one `--paint-*` token owner. The migration preserves every channel and alpha value while covering black shadows/outlines, white overlays/checker marks, editor/control highlights, and fixed animation endpoints.
- The repeated-color gate now canonicalizes short/long/alpha hex, comma/space `rgb()` and `rgba()`, and decimal/percent alpha through the existing color parser. Nested `var()` fallbacks mask only themselves, so they cannot hide a neighboring raw literal; allowlist keys are canonical too. Negative fixtures cover equivalent spellings, same-line compound copies, mixed raw-plus-fallback declarations, nested fallbacks, and stale allowlists. Removed one redundant whole-bundle cooldown tuple pin that falsely classified the editor's gold paint as status CSS while retaining the scoped status-partial invariant.
- Fixed an intermittent xdist false failure in the session-files batch cache test. Its process-global discovery spy now attributes calls to the test thread before asserting the exact two batch discoveries, so unrelated daemon work cannot enter the count; the payload and cache-hit assertions remain unchanged. The focused test passed 20/20 in isolated processes before the ownership fix, and the final canonical eight-lane gate passed in 52.99 seconds.

### Attention lifecycle, wheel routing, compact CSS, and private-tmux tests

- Completed and removed `DOIT.audit-attention-wheel-css-test-harness.md`. Browser acknowledgement timestamps, timers, and pending requests now share one bounded per-transition record with session detach/rename cleanup and stale-callback rejection. YO!info agent-icon geometry consumes the existing variable-driven renderer, while app-menu and Finder quick-access strips consume one six-property compact-overflow owner enforced by the static declaration-set lint.
- Added full-bundle terminal wheel coverage through the real capture-phase listener: 105 px on an alternate-screen pane produces three single-line xterm events, five 7 px touchpad deltas accumulate into one additional line, and switching the same pane to normal mode produces one three-line tmux frame with no synthetic recursion. The GUI audit backlog now records this live path as covered.
- Extended the existing private-tmux browser harness with named commands, terminal dimensions, and shared run/capture/wait/cleanup operations. Mock and opt-in real-agent prompt tests no longer recreate socket/process/poll lifecycles, and real-agent cleanup no longer hides failures. The helper plus combined mock case passed in 3.40 seconds, and the final canonical eight-lane gate passed in 55.66 seconds.

### Displayed activity, YO!stats durability, dialog capacity, and browser waits

- Completed and removed `DOIT.refactor_display_stats_dialog_waits.md`. The browser title, favicon, and topbar now consume one per-window activity-count snapshot, so two working agents in one tmux session report two everywhere. Keyboard Shortcuts consumes the shared wide dialog capacity plus viewport clamp, with narrow/wide dark/light Chrome geometry coverage.
- YO!stats process-write and shared-merge lifecycle fields now live in two typed records. A failed client-history write requeues the detached process samples, makes the retry immediately due, and forces a persisted-state reload before reapplying them; the injected fail-after-merge regression persists two samples exactly once.
- Editor, Preferences, and YO!info scroll roundtrips now use the injected observable wait helper instead of fixed 120–140 ms sleeps or local frame-count loops. Focused source/backend/Node/Chrome coverage passed, generated assets are current, and the final canonical eight-lane gate passed in 52.32 seconds.

### Metadata ownership, CSS spacing, and static browser fixtures

- Completed and removed `DOIT.refactor_metadata_spacing_browser_fixtures.md`. Metadata warming now has one worker/stop-event record; losing background ownership stops the loop before another session network lookup, and a finishing stale worker cannot clear a replacement record. The deterministic demotion regression and all background-owner tests pass. Standard component gaps use `--space-1` through `--space-12`, enforced by `lint_raw_standard_spacing()` alongside padding and margin ownership.
- Added the shared `--space-1` through `--space-12` scale and migrated 207 scalar/compound `gap`, `row-gap`, and `column-gap` declarations without changing rendered values. `lint_raw_standard_gaps()` now rejects scalar, compound, row/column, and custom-property fallback copies in the normal static gate.
- Promoted `load_static_html_fixture()` as the single UTF-8 write/navigation owner and migrated all 74 adjacent browser-test copies across layout, editor, Finder, and share modules. An AST invariant rejects new copies while preserving standalone HTTP-fixture writes and intentional repeated viewport navigation. Per-item canonical gates passed in 55.23s, 53.01s, and 49.64s.

### Demand-aware browser and server work

- Completed and removed `DOIT.network_demand_gating.md`. One browser demand descriptor now maps visible panes, active subtabs, document visibility, and notification delivery to the canonical `core`, `status`, `files`, `activity`, `transcripts`, `yoagent`, and `attention` channels. Hidden pages close SSE and unregister pane watches when system notifications are off; with system notifications on they retain only `attention`. Visibility restoration reconnects once, refreshes current state, resumes latency sampling, and does not replay hidden deltas.
- The backend client-event broker now filters every subscriber independently while aggregate channel demand controls filesystem/transcript/activity, auto-approve/tmux, watched-PR, and YO!agent producers. Debug runtime data includes bounded sanitized per-client channel/delivery records plus published, delivered, and filtered SSE events/bytes by type. The two-client regression delivers one files event and one status event instead of broadcasting four deliveries, while live baseline payload examples show why this matters: irrelevant auto-approve snapshots were about 259 KB and filesystem snapshots about 156 KB per subscriber.
- Recurring `/api/share` backup requests now require an active share; latency and disconnected auto-status polling stop while hidden; file-index polling requires visible Finder/files search; clean hidden editors no longer retain five-second watches, while dirty hidden editors keep conflict protection. YO!stats remains visible-Debug-only on the browser and keeps its independent persistent server history. Terminal WebSocket suspension was intentionally moved to the product backlog because closing it without current-snapshot, input, resize, scrollback, and notification proofs is riskier than the completed HTTP/SSE savings.
- Focused verification passed 174 demand/layout assertions, the share and clean/dirty watch regressions, nine backend demand/accounting tests, and the repaired Node/browser lanes; the final canonical gate and live 8001 restart verification are recorded by the publishing commit.

### YO!stats automatic resolution and axis alignment

- Completed and removed `DOIT.yostats_resolution_axis.md`. Client graphs now choose a friendly automatic resolution bounded to about 120 displayed buckets: 1m/5m/15m use 1s/5s/10s, wider ranges progressively coarsen, and retained history or chart-specific limits remain authoritative. Sparse event-driven samples aggregate into continuous lines, while the shared communication-gap threshold preserves real outages instead of painting a stripe for every empty bucket.
- All chart geometry now derives the plot height from the shared SVG height and top inset, so the zero grid line, labels, data baseline, and visible bottom/X-axis border meet at the same coordinate. Server history requests remain at the finest retained tier so pending local request totals are not double-counted or discarded.
- Verification passed 175 focused Node assertions, three focused Chrome cases, all nine Node layout shards including the 24-hour outage benchmark, and all eight canonical `python3 tools/check.py` lanes in 51.01 seconds. Port 8001 was restarted from the correct worktree and its served bundle hash matches disk; live inspection confirmed `Resolution: 5s`, a continuous sparse-client line, and zero aligned to the bottom baseline.

### YO!stats all-client communication coverage

- Completed and removed `DOIT.yostats_communication_coverage.md`. Client no-data overlays and polyline segmentation now consume one 31-60 second communication-gap threshold derived from the 30-second upload cadence and the displayed bucket width. Empty one-second or ten-second event buckets no longer create red comb stripes or split a line, while real long gaps remain visible.
- All-client bad-connection overlays now intersect mapped disconnect state across every client represented in the selected domain. One stale or disconnected browser cannot paint over a live peer's aggregate bandwidth/API/latency data; legacy records without client maps retain their top-level fallback. The disconnected ranges are computed once and reused by all three client charts.
- Node passed 177 assertions plus the isolated 24-hour performance shard. Focused Chrome passed the sparse 5-second, sparse 10-second, stale-client/live-peer, and five-minute retained-outage cases; visual inspection confirmed the 10-second line is continuous and unshaded. Seven canonical lanes passed initially, and the browser lane passed cleanly after rerunning a shared ChromeDriver-worker disconnect. Port 8001 was restarted from the correct worktree, returns the expected 401 ping, serves the exact disk bundle hash, and logged no launch error.

### Global YO!chat

- Completed and removed `DOIT.chat.md`. YO!chat is one authenticated global room backed by WAL SQLite with server-owned identity/timestamps, private read cursors, signed keyset paging, bounded context/search, seven-day retention, a 100,000-row ceiling, idempotent optimistic sends, five-second typing leases, focus-aware notifications, shared red/yellow/green/gray status, and explicit YO!share denial. YO!agent and YO!chat consume one conversation message/composer owner, and the lazy Unicode Emoji 16.0/CLDR 47.0.0 picker preserves exact grapheme sequences across storage, API, cross-process fanout, rendering, paging, context, search, and notification snippets.
- The 100,000-row acceptance fixture keeps unread bootstrap empty and page/context/search/prune p95 below 250 ms; two-process tests prove concurrent writes and invalidation fanout. Focused chat acceptance passed 72 tests, generated assets are current, the Node layout suite passed all nine shards, and the canonical eight-lane `python3 tools/check.py` gate passed in 49.44 seconds.
- Final live acceptance found and fixed overlapping-request races that could leave bootstrap busy forever or discard an explicitly requested older page when a live delta completed first. Sync, older-page, context, and search generations are now independent while one loading-request record owns bootstrap/page busy state. Two authenticated headless Chrome contexts on live ports 8001 and 8011 then passed same-login typing, exact Unicode question delivery in both directions, inactive notification versus focused suppression, red status, gray acknowledgement removal, unread-only reload, explicit older paging, search/direct context, and an 8011 process restart with zero post-recovery severe browser errors; screenshots remain under `/tmp`. After removing the temporary 8011 process, the final canonical eight-lane gate passed in 52.43 seconds.
- Follow-up chat acceptance added schema-v2 server IP attribution, exact-case author labels, distinct shared-token author borders, relative-to-exact time switching, local typing expiry, hidden Cmd/Ctrl-F search, scroll-only keyset history, a localized timeline introduction, the existing-controller `/yo` bridge, a served emoji catalog, and a content-sized half-pane composer. Panel recreation now clears stale search/render state, `/yo` work uses the normal `YO!agent is typing…` presence instead of a synthetic thinking message, and one explicit follow-tail owner keeps bottom readers current while preserving manual scrollback. Global 12/24-hour formatting now repaints YO!stats and every mounted timestamp surface, while addressed working-transition popups disappear on popup click or direct target-tab navigation.
- YO!chat now follows YO!stats in File, corresponding Preferences sections consume the same shared order, and the search X has browser coverage proving that it hides both chrome and results. Exact committed messages are additionally saved in private UTC-dated JSONL journals with cross-process locking, startup backfill, retry deduplication, diagnostics, and the same configurable seven-day retention as SQLite.
