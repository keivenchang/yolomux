# 2026-07-13 Independent YO!stats cadences and mobile keyboard v3

### Jobd repeated-work elimination and measured browser load

- Completed `DOIT.jobd-cpu-and-measurement.md`. `PersistentJobBroker` reuses repository/ref/watcher-generation snapshots across volatile agent and transcript updates, and metadata warmups retain valid repository identities; only a genuine repository/ref generation change recomputes the Git snapshot.
- The canonical authenticated 7772 60-second browser workload captured `/tmp/yostats-active-browser-7772-20260719-final-jobd.json` and its contention sibling. `session_files_view` remained 6 accepted/6 completed with no repeated Git snapshot; intentional metadata work advanced 3→5 accepted and 2→5 completed. CPU-time deltas were web 4.991%, statsd 1.664%, and jobd/workers 0%.
- Verification: the two-port/generation regression owners passed, `static_build.py --check` and `git diff --check` passed, and the niced canonical eight-lane gate passed in 76.75s. The guarded 7772-only leader restart served auth-gated `/api/ping` 401.

- Completed and removed `DOIT.async-stats-cadence.md`. The elected app now owns one non-overlapping worker per metric family: CPU at 1 second, Agent Status and GPU at 10 seconds, system memory at 60 seconds, and tokens at 10 seconds while watched or 60 seconds while idle. Repeated token demand is clamped to the active cadence. Every family persists its own partial record before SSE and reports attempts/successes/failures/late/missed/runtime independently in System.
- `statsd` remains the sole SQLite mutator. Potentially long range aggregation and JSON encoding now run in the persistent `stats-reader` process through an independent SQLite WAL `mode=ro`/`query_only` connection, with its own socket, lifecycle, cache, failure, queue, resource, and System status. Hot writes, token maintenance, rollups, and retention advance in bounded turns rather than monopolizing the writer listener.
- Sparse memory and GPU gauges use presentation-only 60-second/10-second hold at fine display resolution. Hover retains the actual source time/count, held pixels do not inflate durable samples/totals/rates/cost, and expiry/outage stays an honest gap. CPU, Agent Status, and token series remain unprojected.
- Completed and removed `DOIT.keyboard-cc-overflow-altcmd-bottom-row.md` and `DOIT.keyboard-more-toggle-jump.md`. The coarse-pointer palette now has Esc/Ctrl/Shift at left, normal-width C-c/Alt/Cmd on the bottom, `⌘C`/`⌘V` D-pad labels, a shared-drag top grabber, and inactive-tab launcher paint. Primary/overflow layouts retain one 288×204 footprint and one fixed `⋯` viewport box, so repeated toggles do not jump.
- Live leader 8881 survived 70 seconds and 27 repeated 24-hour reads with CPU 70/70 and no raw one-second gaps or failure/late/missed cycles; GPU, Agent Status, and active tokens were exactly 7/7, memory 1/1, and every history request succeeded (maximum 1.024s). The blocked-reader regression freezes history while the writer commits the next CPU point under 0.9s, then proves all 21 totals are present. Live 834×1112 keyboard QA kept palette and `⋯` rectangles identical through six toggles.
- Verification: statsd/local RPC 102 passed; focused app scheduler/System/history 14 passed; token-demand cadence 2 passed; generated assets and all 15 Node shards passed; focused keyboard browser 3 passed; the canonical eight-lane gate passed in 386.49 seconds. Existing shared parents and negative searches are recorded in the completed queue notes. Whole-goal non-generated diff from `8b6c43b4`: +2,177/-552 (net +1,625).

### Range-scaled YO!stats delivery cadence

- Completed and removed `DOIT.stats-range-scaled-cadence.md`. One shared range/zoom predicate now controls delivery for both YO!stats and YO!cost: 5m/15m views demand the one-second `stats` SSE feed, while 30m+ and fixed historical zooms omit that channel and poll once per minute. Hidden pages retain neither path. Returning to a short live view reapplies demand and immediately backfills the SSE-less interval through the existing history merger.
- The backend keeps its existing aggregate-demand guard before `publish_client_event`; a regression proves a sole coarse subscriber produces zero stats serializations/events and that publication resumes exactly once when live stats demand exists. Freshness tests pin a rendered latest coarse cell, a ticking age label, and the 68001ms cadence-plus-timeout budget below the 70-second UI limit. Collection remains independently scheduled at CPU 1s, Agent/GPU 10s, memory 60s, and adaptive tokens; only browser delivery changed.
- Existing parents checked: `clientEventDemandDescriptor()`, `setDebugGraphRange()`/`clearDebugGraphZoom()`, `jsDebugStatsPollIntervalMs()`, and `TmuxWebtermApp.merge_stats_family_record()`. Search: `rg "channels.add\\('stats'|stats_sample|jsDebugStatsPoll|syncClientEventDemand|has_demand\\("`; negative search confirmed the retired visibility-only stats-demand expression and two-value poll expression are absent, and the new predicate/poll owner each has one definition.
- Verification: generated assets and `static_build.py --check`; Node core 76/0, tmux 76/0, settings 75/0; backend demand/publish 2/0; focused real-browser cadence 1/0. The eight-lane gate passed seven lanes; its concurrent non-browser lane had one unchanged dense-history budget outlier at 1.028s versus 1.000s, then that exact test passed three isolated repetitions and the complete non-browser lane passed on bounded rerun (2,234 passed, one skipped). Automated before/after publication counts are zero coarse events and one live event. Live 8881 restart/frame counting was intentionally deferred to the integrating parent because this queue ran in an isolated worktree with an explicit no-primary/no-restart instruction. Non-generated diff: +248/-5 (net +243).
