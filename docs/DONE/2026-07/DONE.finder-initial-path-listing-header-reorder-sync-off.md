# 2026-07-20 Finder initial-path listing, header reorder, Sync-off pinning, no reset-to-top

- Completed and removed `DOIT.finder-initial-path-and-sync-layout.md` (all four operator requirements implemented, not just queued). (1) Initial listing: `refreshFileExplorerPanelTree` (`86_changes_editor.js`) now paints the first (empty) tree directly under the live client-push transport instead of early-returning to `syncServerWatchRoots` — the Finder lists the current path (`~` → home) on open with no Sync press; an already-painted tree still defers to incremental push deltas (so requirement 4's no-full-rebuild is preserved). (2) Header reorder: the Finder toolbar now renders the path input + copy + Reload on line 1 and the Sync toggle + Session dropdown on line 2 (swapped from Session+Reload / Sync+path); the Reload button's CSS was re-scoped from `.file-explorer-primary-row .changes-refresh` to its own `.file-explorer-refresh-cluster` class since it changed rows. (3) Sync-OFF pinning: verified the existing `fileExplorerRootMode !== 'sync'` guards (7 sites) fully decouple the path from session/editor focus, hover, refresh, and Session-dropdown change; root changes only via the dropdown with Sync ON or an explicit typed path. (4) Incremental updates route through `refreshFileExplorerTreesInPlace` (preserve scroll/expansion), never a full rebuild that scrolls to top.
- Verification: NEW `test_finder_panel_paints_initial_path_under_push_without_sync` drives the exact gated path (empty tree + push active + Sync OFF, no entries) and was proven to FAIL against the pre-fix early-return and PASS with the fix; NEW `test_fixed_finder_session_dropdown_change_does_not_move_root` pins the Sync-OFF dropdown contract; NEW node source-contract in `file_surface_menu.test.js` pins the row order (path row first, Sync+Session second) and the reload re-scope; existing node contracts in `share_theme.test.js` / `file_surface_menu.test.js` and the Selenium `test_dockview_file_surface_header_uses_common_one_line_controls_and_finder_row_order` were updated to the new order. Full Finder Selenium suite (41) green, all 16 node shards green, and the canonical `python3 tools/check.py` PASSED all 8 lanes in 82s (full browser suite). GUI.md updated with the two-row header contract and the initial-listing / Sync-OFF-pinned / no-reset-to-top rules. Guarded `./boot.sh 7772` restart returned `/api/ping -> 401`.
