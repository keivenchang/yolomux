# 2026-07-19 YO!stats reliability and touch interaction follow-through

- Completed and removed `DOIT.yostats-426-shared-statsd.md`. Stats services use version-scoped socket identity with the existing schema-scoped database, so concurrent server generations cannot replace each other's daemon or database. Read-side 426 repairs its own service once and renders an updating/retrying state; an immutable stale writer page remains terminal. Focused service/client/http tests (118), panel/UI Node tests (29/33), static freshness, and the niced eight-lane gate passed. The operator confirmed real-iPhone YO!stats history on 7771 (its own worktree) loaded without HTTP 426 while 7772 served this branch.

- Completed and removed `DOIT.touch-scroll-keyboard-pop.md`. `enableTerminalScroll` is the sole touch tap-versus-pan owner: a claimed/cancelled pan stays focus-neutral while a true tap focuses the terminal. Trusted CDP normal/alternate-screen coverage passed, static freshness and the niced gate passed, and the guarded 7772 leader restart returned authenticated `/api/ping` 401. The operator confirmed that real-iPhone scrolling across Claude and Codex panes did not show the keyboard.

- Completed and removed `DOIT.stats-raf-idle-cpu.md`. The live chart ticker now schedules one due timeout rather than a per-frame rAF loop. Matched 60-second captures dropped callbacks from 34,480 (574.67/s) to 60 (1.0/s) and renderer CPU from 7.70 CPU-s (12.833% of one core) to 4.21 CPU-s (7.017%), a 99.83% callback and 45.32% CPU reduction. Focused capture tests (12), stats panel/browser regressions, static freshness, and the niced eight-lane gate passed; the operator confirmed a calm idle renderer on live 7772.

- Completed and removed `DOIT.tablet-pane-split.md`. On a wide iPad, long-pressing a tab exposes the existing directional action path with truthful Split/Move labels; Split right creates a local pane. In single-column widths, View > Layout remains visible but disables its shared commands with the localized width explanation. CDP wide/narrow tablet and desktop drag coverage, static freshness, and the niced gate passed; the operator confirmed that iPad splitting works.

- Completed and removed `DOIT.yostats-narrow-scroll-reset.md`. `refreshDebugGraphElement` preserves the narrow graph view's scroll position through live plot/axis replacement; returning to the front resumes live following. The range/resolution sweep includes the scrolled-back state. Focused Node/browser tests, static freshness, and the niced gate passed; the operator confirmed scroll-left holds on real iPhone live 7772.

- Completed and removed `DOIT.yostats-toolbar-one-line.md`. Graph controls are Charts, Size, Range, Resolution on one measured wide-pane line and exactly two narrow lines (Charts/Size then Range/Resolution). Geometry regression coverage, static freshness, and the niced gate passed; the operator confirmed both desktop-wide and iPhone layouts on live 7772.

- Completed and removed `DOIT.daemons-load-axis.md`. Daemons load uses the shared broken-linear spike-compression descriptor, retaining true percentage labels/tooltips and low-load readability beneath rare high spikes. Token parity, panel/browser geometry, static freshness, and the niced gate passed; the operator confirmed a real-spike chart looks correct on live 7772.

- Completed and removed `DOIT.codebase-refactor-audit.md`. The audit's hot-loop, shared-owner, Python/CSS cleanup, dead-code, and test-speed items were completed with focused regressions. The final Node-harness assessment retained the existing isolated parallel VM shards because a shared context would leak lexical/global state and a forced serial run was 12.20s versus 3.53s parallel; compilation is already cached per process. The final full Node launcher passed all 16 shards, static freshness/diff checks passed, and the niced canonical gate passed.

- Completed and removed `DOIT.hover-timing-uniform.md`. Every hover surface now uses the shared Preferences-backed 1300ms open / 120ms close-and-follow pair. Focused suites, static freshness, the niced gate, and the guarded 7772 restart passed; the operator confirmed the live timing.

- Completed and removed `DOIT.yocost-scroll-and-price-labels.md`. Keyed inner cost-table scroll positions survive periodic rebuilds, an active table scroll defers the existing refresh path, and each cost surface states API-list pricing basis once while rows retain concise amounts. Focused suites, static freshness, the niced gate, and the guarded 7772 restart passed; the operator confirmed the live table scroll and labels.
