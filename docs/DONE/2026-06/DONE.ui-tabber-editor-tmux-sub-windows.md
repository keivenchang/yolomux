# UI, Tabber, Editor, and Tmux sub-windows

## Active window parity and attention red-dot pulse
- Completed and removed `DOIT.popover_active_window_wrong.md`, `DOIT.popover_tabber_infoline_agree.md`, and `DOIT.ask_red_dot_glow_animation.md`. Popover active-window state now uses one shared active-index helper that prefers the optimistic override, then tmux `window_active`, with selected pane only as fallback; the focused tmux sub-window shows `active` while non-focused windows keep their own working/idle/attention state. Info Bar now joins the same per-window metadata source as popover/Tabber when touched path/git metadata exists, so active AI windows show the touched repo root and matching git facts (`0 dirty · N ahead`) instead of raw cwd subdirs; transcript-derived repo metadata is preserved when no shared per-window metadata exists. Red attention/attention dots now have the same-strength pulse selector as working dots plus a real 1px border channel, making the window-bar, popover, and Tabber dot visibly pulse in lockstep with the attention badge. Verification: runtime Chrome computed-style repro before/after, `python3 -m pytest tests/test_browser_layout.py::test_attention_agent_dots_share_ask_badge_pulse_computed_style -q`, `node tests/editor_preview.test.js`, `node tests/tabber.test.js`, `node tests/layout_url.test.js`, rebuilt static assets, and full `python3 tools/check.py` (`CHECK PASSED in 92.32s`).

## Tabber AI icon before window name
- Completed and removed `DOIT.tabber_ai_symbol_before_name.md`. Tabber window labels now render the Claude/Codex agent icon before the canonical `index:name` text while leaving the disclosure triangle in the tree-icon column and keeping the activity dot, PID, and right-side status after the name. Bash/non-AI rows do not gain a leading icon. Verification: `python3 tools/static_build.py`, `node tests/tabber.test.js` (`38 passed`), and full `python3 tools/check.py` (`CHECK PASSED in 97.02s`). The full gate also exposed a YO!agent preference-test isolation flake; `tests/test_yoagent_preferences.py` now uses the existing isolated YO!agent conversation fixture and the focused test passes.

## attention status recency and active-dot brightness pulse
- Completed and removed `DOIT.ask_status_time_ago.md` and `DOIT.circle_pulse_brightness_visible_on_active.md`. Tabber and popover attention rows now read `attention <time> ago` using the same compact recency formatter as idle rows, so the old `attention approval` / `attention needs input` subtype words are gone while the red attention pulse remains. Working rows still show `working for <duration>`.
- Status dots now pulse their own brightness through the shared `attention-ring-fade` animation in addition to the glow: dot users opt into `--attention-pulse-brightness-rest: 0.82` and `--attention-pulse-brightness-peak: 1.34`, while non-dot users keep neutral filter defaults. The active tmux-window button contrast halo remains in place, so the pulse is visible on bright active-accent backgrounds.
- Verification: `python3 tools/static_build.py`, `node tests/editor_preview.test.js` (`92 passed`), `node tests/tabber.test.js` (`38 passed`), full `python3 tools/check.py` (`CHECK PASSED in 101.10s`), 8001 restart/ping (`pid 1066151`, unauthenticated `/api/ping` returned 401), and live Chrome against the served 8001 app confirmed `attention <15 sec ago` / `attention <1 min ago` with no subtype words plus active-dot `attention-ring-fade`, brightness `0.82 -> 1.34`, and the active-control text-shadow halo.

## Shared working-dot glow waveform
- Completed and removed `DOIT.circle_pulse_glow_waveform_match_ask.md`. Working/cooldown/attention status dots now use the same parameterized `attention-ring-fade` glow waveform as attention badges: the dot stays fully opaque, the halo pulses weak-to-strong, and state color is supplied by `--attention-ring-rgb` (green working, yellow cooldown, red attention).
- Verification: `node tests/editor_preview.test.js` (`91 passed`), `python3 -m pytest tests/test_browser_dockview.py::test_dockview_window_bar_working_status_dot_uses_shared_pulse -q`, `python3 tools/static_build.py`, full `python3 tools/check.py` (`CHECK PASSED in 81.89s`), 8001 restart/ping (`pid 2254306`, unauthenticated `/api/ping` returned 401), and live 8001 computed-style verification reported `animationName: attention-ring-fade`, `animationDuration: 1.55s`, `opacity: 1`, and green `--attention-ring-rgb: 82 210 115`.

## Tabber PIDs and transcript copy rows
- Completed and removed `DOIT.tabber_agent_windows_missing_pid.md` and `DOIT.hover_transcript_id_and_location_copy.md`. Tabber window rows now render `index:agent (pid=N)` and `index:bash (pid=N)` from `record.pid` data instead of parsing the label string; session popover agent-window blocks now show per-AI-window Transcript ID and Transcript location rows with copy buttons.
- Backend `agent_window_status_payloads` now exposes `transcript`, `transcript_id`, and `agent_session_id` per Claude/Codex tmux sub-window, with the frontend falling back to the transcript basename when no explicit ID exists. The copy buttons reuse the existing delegated `[data-copy-path]` handler.
- Verification: `python3 tools/static_build.py`, `node tests/tabber.test.js` (`38 passed`), `node tests/editor_preview.test.js` (`91 passed`), `python3 -m pytest tests/test_app.py::test_auto_approve_payload_includes_agent_window_statuses -q`, full `python3 tools/check.py` (`CHECK PASSED in 85.55s`), 8001 restart/ping (`pid 3984578`, unauthenticated `/api/ping` returned 401), and live Selenium against `https://localhost:8001` confirmed Tabber PID labels plus Transcript ID/path copy clicks.

## Tabber status column clipping
- Completed and removed `DOIT.tabber_status_column_clips_working_ask.md`. Tabber long status rows (`working for…` and `attention…`) now get a `tabber-status-long` class so the status cell sizes to content and truncates at the end when needed; normal ago rows keep the compact fixed recency column. Date/status text now uses a row-scale font one step below the tree label instead of the tiny 3xs size.
- Verification: `python3 tools/static_build.py`, `node tests/tabber.test.js` (`38 passed`), full `python3 tools/check.py` (`CHECK PASSED in 83.12s`), 8001 restart/ping (`pid 390948`, unauthenticated `/api/ping` returned 401), and live Selenium rendered `working for 2m 4s`, `attention needs input`, and `<1 min ago` rows from the served bundle to confirm no leading-character clipping and larger date/status text.

## Window activity, Tabber chrome, recency, popovers, editor theme, and YO rotation
- Completed and removed `DOIT.agent_activity_circle_states.md`, `DOIT.recency_red_bold_under_1min.md`, `DOIT.tabber_session_row_reuse_tab_chrome.md`, `DOIT.popover_merge_window_rows_no_repeat.md`, `DOIT.editor_theme_follows_global_theme.md`, `DOIT.window_button_redundant_index_label.md`, `DOIT.active_window_circle_contrast.md`, and `DOIT.yo_rotates_on_any_window_active.md`.
- Tmux/Tabber activity now uses one per-sub-window state source: attention persists as a red pulsing dot and propagates to the tab, just-finished tmux sub-windows cool down yellow for 60 seconds, active tmux sub-window dots get a theme-aware contrast halo, and the YO/running count spins when any hidden or shown Claude/Codex tmux sub-window is working. Duplicate indexed labels now render as `n:name` instead of `n:name(n)`, while name-only labels keep `name(n)` for disambiguation.
- Recency styling makes <15s dates red/bold/pulsing and <1m dates red/bold in Ago and Date modes; Tabber session rows reuse the real pane-tab chrome with the YO button and attention badge; session popovers show each tmux sub-window once with metadata beneath; CodeMirror follows the Global theme when the editor theme inherits it.
- Verification: `python3 tools/static_build.py`, `node tests/editor_preview.test.js` (`91 passed`), `node tests/tabber.test.js` (`38 passed`), full `python3 tools/check.py` (`CHECK PASSED in 89.91s`), 8001 restart/ping (`pid 3295417`, unauthenticated `/api/ping` returned 401), and live Selenium against `https://localhost:8001` confirmed duplicate `bash` labels, active-dot contrast CSS, and hidden-window YO working aggregation.

## Current-window popovers, attention pulse text, sub-minute Ago labels, and theme contrast
- Completed and removed `DOIT.popover_per_window_fixes.md`, `DOIT.ask_status_text_red_pulse.md`, `DOIT.recency_under_15sec_ago_label.md`, and `DOIT.light_dark_font_contrast.md`. Current/focused agent windows no longer display false `idle` text in popovers, tmux-window header rows use the pane-tab shaded token, and all popover copy buttons route through the shared `data-copy-path` handler.
- attention/needs-input status text now uses the same red attention label, pulse class, and animation-delay clock as the attention badge and red dot; non-attention working/idle rows keep their prior styling. Ago text now shows `<15 sec ago` inside the same 15-second pulse window, `<1 min ago` from 15-60 seconds, and idle AI windows use the same `<time> ago` wording instead of `idle …`.
- Light-mode contrast now keeps blue/green/link/code text dark enough against light surfaces, active-accent text contrasts per accent, and Preferences labels read `Global appearance` with `System` / `Dark mode` / `Light Mode` plus `Theme color`.
- Verification: `python3 tools/static_build.py`, `python3 tools/static_build.py --check`, `node tests/editor_preview.test.js` (`92 passed`), `node tests/share_theme.test.js` (`1 passed`), `node tests/tabber.test.js` (`38 passed`), full `python3 tools/check.py` (`CHECK PASSED in 117.31s`), and final 8001 restart/ping (`pid 2654734`, unauthenticated `/api/ping` returned 401). User-side visual checks remain the final eyeball for the live pulse/copy/light-dark rendering.

---

Completed 2026-06-21. Extracted from the 2026-06-21 daily log.
