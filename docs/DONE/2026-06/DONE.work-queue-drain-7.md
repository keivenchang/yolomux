# 2026-06-28 Work Queue Drain

### Finder, Tabber, YO!stats, and status visuals

- Completed and removed the Tabber recency, sluggishness, sub-window glyph, YO!stats client-work layout, disclosure indicator, and Finder folder zip queues. Tabber recency now uses the requested child/window and touched-path timing rules, sluggishness work reduces hidden-pane and repeated render cost, sub-window glyphs are clean solid play/stop/pause shapes with no clipped glow box, YO!stats graph layout keeps the chart area from being stretched by the Client work box, disclosure controls use a scaled rotating chevron instead of solid play-like triangles, and Finder folder rows expose `Zip & download` for single admin-owned folder selections using the Preferences file transfer cap.
- The new folder zip endpoint is `GET /api/fs/zip`, reusing the filesystem route/auth group and `_validated_path()` path safety, skipping symlinks and special files, returning JSON 413 errors with measured size, and serving `<folder>.<YYYYMMDD-HHMMSS>.zip` names stamped in Pacific Time. The client uses `apiFetch()` so over-cap errors show in the status line and successful archives save with the server attachment filename.
- Verification for the final batch included `python3 tools/static_build.py --check` (passes with existing i18n untranslated-value warnings), `node tests/editor_preview.test.js`, `node tests/share_theme.test.js`, `node tests/tabber.test.js`, `node tests/layout_restore.test.js`, focused zip route tests in `tests/test_login_auth.py` and `tests/test_server_query.py`, focused browser coverage in `tests/test_browser_finder.py`, focused sub-window glyph coverage in `tests/test_browser_layout.py` and `tests/test_browser_dockview.py`, visual screenshot review at `/tmp/subwindow-glyph-visual.png`, and full `python3 tools/check.py`.
- Completed and removed the session cooldown aggregation and pause-bar polish queues. Session-level Tab and Tabber indicators now aggregate child window cooldown through the same `agentWindowActivityTone`/status-payload path used by sub-window glyphs, with attention and working states still outranking cooldown; pause glyph bars use the shared thinner `0.17` width and centered `0.19` offset while play/stop and Tab dot shapes remain unchanged. Verified by `node tests/editor_preview.test.js`, focused browser status tests, and full `python3 tools/check.py`.
- Completed and removed the Finder delete-count warning queue. Directory deletes now keep the normal first confirm, then fetch a recursive regular-file count from the new readonly-gated `GET /api/fs/count`; directories with more than 5 files show `You have <N> files in this directory, CONFIRM?` plus the path, count failures fall back to a generic second confirm, single files and small directories stay one-confirm flows, and readonly mode exits before prompts/count/delete work. Verified by `node tests/layout_async.test.js`, `python3 -m pytest tests/test_filesystem.py::test_count_directory_files_counts_recursive_regular_files tests/test_login_auth.py::test_fs_count_returns_recursive_file_count tests/test_server_query.py::test_http_route_registry_groups_dispatch_and_keeps_verbs_thin tests/test_server_query.py::test_do_get_fs_routes_reject_readonly_before_file_handlers -q`, `python3 tools/static_build.py --check`, and full `python3 tools/check.py`.
