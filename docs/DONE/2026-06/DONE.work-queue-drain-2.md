# 2026-06-30 Work Queue Drain

### Status, stats, share, and Help

- Completed and removed the incremental transcript/fast YO!stats, known-gate, YO!info sub-window row, Help/i18n, and live pulse-period queues. Claude transcript scans now use the same append/partial/truncate-aware byte-progress owner as Codex and resolve raw paths per cwd; cold stats responses defer expensive token work to the sampler; HTTP shares no longer TLS-wrap redirect sentinels; YO!info keeps the pid inline and recency right-aligned.
- Status pulse defaults are 2.55s with 20 approximately-125ms steps. A live Preference period change invalidates its duration-aware phase cache and restarts only current status animations. The preference uses a reusable example slot containing the same play/pause/stop glyph markup as real sub-windows.
- Help now describes the real play/pause/stop glyphs. Shortcut, Legend, YO!info, YO!agent, and debug strings route through locale keys with parity-safe catalog placeholders. Verified by focused source/browser tests, dev-server restart on 8001 with `/api/ping` 401, and a full `python3 tools/check.py` pass in 45.01 seconds.

### Info Bar and tabs

- Completed and committed the Info Bar/tab UI polish queue as `3e08ce38`: sub-window buttons lead the Info Bar with no duplicate close control, flush-left square controls, rounder top tabs, top-aligned detail-toggle glyph, repo-menu selection updates the Info Bar repo, and Dockview collapses every non-Finder inner head consistently. Verified with full `python3 tools/check.py`, a focused macOS Chrome Dockview geometry test, and a targeted reload of the local `:7777` page.
- Completed and removed `DOIT.tab_ball_dim_play_no_glow_multiclick.md`. Aggregate Tab status balls now retain opaque canonical colors with a static ambient halo; working play glyphs keep the lowest-cost static green halo; direct sub-window clicks focus an unfocused pane, select the freshly rendered target during pointer capture, and suppress duplicate clicks. Added Selenium/source regressions and GUI spec coverage. Verified with `python3 tools/check.py`, rebuilt static assets, restarted 8001, and confirmed `/api/ping` returns `401`.
- Completed and removed `DOIT.subwindow_switch_needs_two_clicks.md`. Focused tmux sub-window buttons now switch during pointer capture too, before a poll can replace the pressed window bar node. The Selenium regression forces that replacement between pointerdown and click, then proves the detached original produces exactly one `/api/tmux-window` request and activates the requested window. Verified with the full baseline `python3 tools/check.py`, `python3 tools/static_build.py`, `node tests/editor_preview.test.js`, and focused browser coverage.
- Completed and removed `DOIT.fix_metadata.md`. Hidden tab metadata now removes every `tab-symbol` chip from both regular `.pane-tab` and compact `.tmux-pane-tab-token` shells, so the hidden `MAIN` chip leaves no blank bar; the YO marker, session number, and description remain, and visible metadata still shows `MAIN`. Verified by the focused Selenium DOM test, generated-assets check, full `python3 tools/check.py` (88.70s), a port-scoped 8001 restart, and the no-store served CSS response.
- Completed `DOIT_yostats_fresh_reload_slowness.md`. YO!stats now polls every two seconds only until its first real server sample, then re-arms the existing 30-second steady cadence. A five-second abort deadline releases stalled fetches for the next fast retry. The empty metadata label uses the shared three-dot animation through a new translated `debug.waitingForServerStats` key in every locale. Verified by `node tests/editor_preview.test.js` (133 passed), focused Selenium debug-graph coverage (4 passed), `python3 -m pytest tests/test_app.py tests/test_server_query.py -q` (360 passed), static-build check, an 8001 restart, and `/api/ping` 401. The full gate still has an unrelated HTTP-share browser fixture failure when it TLS-wraps a later plain-HTTP request; the focused queue coverage passes.

### Status acknowledgments and YO!stats cold start

- Completed and removed the gray acknowledgment and sub-window latency queue. A stable session/window/agent transition key no longer changes when async tmux readback adds a pane target, and a retained clicked-window snapshot keeps the gray acknowledgment glyph visible after the backend row disappears. Live Chrome showed yellow `#ffd633`, then gray `#9aa5b1` immediately, with decreasing opacity until removal after one configured Status ball pulse period. Raw tmux signal windows now repaint the window bar without changing transcript cache semantics; a temporary transcript-less window appeared in 0.16 seconds. The active-pane `MAIN` badge now uses explicit shared foreground, background, and border colors.
- Corrected a final false-positive in the gray acknowledgment proof: the acknowledged span had a gray custom property but had dropped the original attention/cooldown class that draws the stop-square or pause-bar pseudo-elements, so the glyph was visually absent. Acknowledged markers now retain their original shape modifier while the acknowledged tone forces the shared gray fill, then fade to transparent over the configured Status ball pulse period before removal. The Preferences timing example uses the live renderer for colored aggregate Tab balls, colored play/pause/stop glyphs, and fading gray play/pause/stop glyphs; all nine share the configured duration and global phase. The browser matrix checks nonzero `::before`/`::after` geometry, rendered gray backgrounds, decreasing opacity for red/yellow pulsing/static trusted clicks, and synchronized computed animation state across the Preferences groups.
- Completed and removed the YO!stats cold-state queue. Cold start shows only `Waiting for server stats…`, and boot waits for the first visible stats sample before opening the global client-events and dev-reload EventSources, avoiding the reproduced HTTP/1.1 connection starvation. Live startup returned the sample at 469 ms before the EventSources opened at 664/675 ms. The event total intentionally counts all captured JavaScript events while graph buckets accept only API and SSE events.
- Verified the combined changes with focused Node and browser regressions, including trusted Chrome clicks across red/yellow and blinking/static sub-window states: each visible stop/pause shape turned gray immediately, faded during the configured period across window-bar rerenders, and then disappeared. Rebuilt static assets, `git diff --check`, and `python3 tools/check.py` all pass; the final eight-lane gate completed in 36.05 seconds. Restarted port 8001 and confirmed `/api/ping` returns 401. Authenticated live Chrome rendered all nine Preferences examples at the 2550 ms setting with phase `0.500`: colored balls/glyphs at opacity `1.000`, gray play/pause/stop at opacity `0.500`, and all expected green/yellow/red/gray fills and nonzero glyph geometry.

### YO!info session work descriptions

- Completed and removed `DOIT.yoinfo_session_work_description.md`. YO!info Tab group headers and leaf values now show the shared 200-character session work description through the compact Tab detail renderer, compute it once per session, suppress empty or already-included descriptions, and preserve the bare session identity for grouping, sorting, and search. Verified with the 135-assertion Node suite, focused Chrome regression and live 8001 visual geometry, generated-bundle check, and the canonical gate; every lane passed except the documented unrelated editor-preview baseline failure (256 browser tests passed, 4 skipped).
