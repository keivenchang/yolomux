# Tabber and shared status rendering

- Completed and removed `DOIT.attention_ack_cross_port_sync.md`. Attention ACKs were already written to the shared host-level `tmux-AI-status.json`, but a peer server that missed control-socket fanout and had no active client-events subscriber kept its stale in-memory ACK set; `/api/auto-approve` never merged the shared revision before serving a per-session or cached roster response. Explicit auto-status reads now merge the shared ACK revision and invalidate caches built before it. The pre-fix two-instance regression returned `prompt_attention_acknowledged: false`; after the fix, 15 focused ACK-sync tests, 300 expanded app tests, and the full `python3 tools/check.py` gate pass. A live isolated 8001-to-8003 test wrote five ACKs while 8003 was offline, then confirmed a cold 8003 saw all five on its first read in 123 ms; the normal 8001/8003 servers were restored afterward and the fixed backend was restarted on 8002.
- Completed and removed `DOIT.status_indicator_gui_spec.md`. Agent status indicators now follow the flat-size/vibrant-color spec: aggregate Tab/session balls keep the base 100% size, tmux sub-window play/stop/pause glyphs use one flat 80% scale in glowing, settled, and acknowledged states, settled red/yellow stay vibrant, ACK waits 700 ms before turning gray without resizing, and working/play keeps pulsing while the agent remains working. Verification: `node tests/editor_preview.test.js`, focused browser glyph/ACK tests, `python3 tools/static_build.py --check`, and full `python3 tools/check.py` (`CHECK PASSED in 91.30s`).
- Completed and removed `DOIT.refactor-agent-status.md`. Status-ball sizing and tone ownership now route through shared helpers/tokens instead of one-off topbar mappings: topbar balls use `--agent-status-ball-size`, YO!stats status series/legend/bucket values share named owners, and topbar tone classes use the same agent-window tone wrapper helper as the rest of the UI. Focused verification: `python3 tools/static_build.py`, `node tests/editor_preview.test.js`, `node tests/layout_restore.test.js`, and the full `python3 tools/check.py`.
- Completed and removed `DOIT.narrow_topbar_status_pill.md`. The top-right activity count pill is narrower through scoped padding/gap/separator changes only; shared status-dot base sizing remains unchanged for Tabber, popovers, sub-windows, and Dockview tabs. Focused verification: `python3 tools/static_build.py`, `node tests/layout_restore.test.js`, `node tests/editor_preview.test.js`, `node tests/tabber.test.js`, and the full `python3 tools/check.py`.
- Completed and removed `DOIT.tabber_respect_date_toggle.md`. Tabber agent sub-window rows now respect the Finder Date toggle through the shared date-mode owner: `none` hides the trailing date/status text, `date` shows an absolute timestamp, and `relative` shows relative recency; popover recency helpers remain independent because popovers do not have the Finder Date toggle. Focused verification: `python3 tools/static_build.py`, `node tests/tabber.test.js`, `node tests/editor_preview.test.js`, and the full `python3 tools/check.py`.
- Completed and removed `DOIT.subwindow_play_stop_pause_glyphs.md`. Tmux sub-window status indicators keep the Tab/Dockview aggregate ball untouched while the sub-window row draws play/stop/pause CSS glyphs. The reopened visibility pass made glyphs larger, gave them real drop-shadow contrast, and fixed active-button green-on-green by filling active glyph shapes with `--active-control-text` while preserving the tone color as the outline/halo owner. Focused verification: `python3 tools/static_build.py`, `node tests/editor_preview.test.js`, `node tests/tabber.test.js`, and `python3 -m pytest tests/test_browser_dockview.py -k 'status_dots_render_subwindow_state_glyphs_only' -q`.
- Completed and removed `DOIT.subwindow_glyphs_center.md`. The sub-window play/stop/pause glyphs now share one centered CSS implementation across tmux sub-window buttons and session-popover sub-window rows: play no longer uses the old side-biased transform, cooldown pause draws two symmetric pseudo-element bars instead of a one-bar-plus-box-shadow blob, and current popover rows reuse the same active contrast variables as active sub-window buttons. Dockview Tab aggregate status dots remain the plain shared `●`. Focused verification: `node tests/editor_preview.test.js`, `node tests/tabber.test.js`, `python3 -m pytest tests/test_browser_dockview.py -k 'status_dots_render_subwindow_state_glyphs_only' -q`, and `python3 tools/static_build.py --check`.
- Completed and removed `DOIT.subwindow_glyph_polish.md`. The sub-window glyph containers no longer clip their glow into rounded boxes, the pause state is two clean solid bars with a clear gap, play/stop stay solid tone shapes without heavy per-shape borders, disclosure chevrons are full row-font size, and normal Tab `●` dots remain unchanged. Focused verification: `node tests/editor_preview.test.js`, `node tests/layout_url.test.js`, focused Selenium coverage in `tests/test_browser_layout.py`, `tests/test_browser_finder.py`, and `tests/test_browser_dockview.py`, visual screenshot review at `/tmp/subwindow-glyph-visual.png`, and the full `python3 tools/check.py`.
- Completed and removed `DOIT.work_icon_sizes_and_red.md`. Sub-window glyphs now use the Tab status ball as the 100% reference with live/pulsing glyphs at 75% and stale/non-pulsing glyphs at 50%, while Dockview Tab aggregate `●` dots remain unchanged. Stop/attention glyphs use a scoped saturated `--danger-strong` red fill on tmux sub-window buttons, session-popover rows, and Tabber window rows, including active green buttons; play stays green and pause stays gold. Focused verification: `node tests/editor_preview.test.js`, `python3 -m pytest tests/test_browser_layout.py::test_subwindow_status_glyphs_are_solid_unclipped_shapes_without_tab_dot_override tests/test_browser_dockview.py::test_dockview_window_bar_status_dots_render_subwindow_state_glyphs_only -q`, `python3 tools/static_build.py --check`, and full `python3 tools/check.py`.
- Completed and removed `DOIT.subwindow_pause_more_yellow.md`. Sub-window cooldown/pause glyphs now use scoped `--subwindow-status-cooldown-fill: #ffd633` instead of global `--accent-gold`, so settled 80% pause bars read clearly yellow on normal, active-green, and light-theme buttons. The same scoped yellow applies to active/pulsing cooldown for consistency; play-green, stop-red, and global gold usages are unchanged. Focused verification: `node tests/editor_preview.test.js`, `python3 -m pytest tests/test_browser_layout.py::test_subwindow_status_glyphs_are_solid_unclipped_shapes_without_tab_dot_override tests/test_browser_dockview.py::test_dockview_window_bar_status_dots_render_subwindow_state_glyphs_only -q`, `python3 tools/static_build.py --check`, and full `python3 tools/check.py`.
- Completed and removed `DOIT.subwindow_glyph_pulse_no_rectangle.md`. Sub-window play/stop/pause glyphs now use a sub-window-only pseudo-element pulse path: the square status-dot element keeps `animation`/`box-shadow` off, working/play pulses continuously while the agent is working, stop/pause remain transition-limited, and cooldown pause bars have a widened gap plus scoped dark outline so active green buttons stay readable without merging the bars. Normal Tab aggregate `●` dots remain on the shared dot path. Focused verification: `node tests/editor_preview.test.js`, `python3 -m pytest tests/test_browser_layout.py::test_subwindow_status_glyphs_are_solid_unclipped_shapes_without_tab_dot_override tests/test_browser_dockview.py::test_dockview_window_bar_status_dots_render_subwindow_state_glyphs_only tests/test_browser_dockview.py::test_dockview_window_bar_working_agent_glyph_uses_static_symbol_and_static_ball -q`, `python3 tools/static_build.py --check`, and full `python3 tools/check.py`.
- Completed and removed `DOIT.tab_segmented_ball_missing_attention.md`. Session/Tab aggregate segmented balls now collect tones through the same visible status-item classifier used by sub-window glyphs, so working+cooldown+attention renders the `attention-cooldown-working` tri-color ball and acknowledged red sub-windows still contribute a red segment while dominant selection remains separate. Focused verification: `node tests/editor_preview.test.js`, `python3 -m pytest tests/test_browser_dockview.py::test_dockview_tab_agent_ball_segments_visible_window_states tests/test_browser_dockview.py::test_dockview_red_window_ball_click_switches_and_acknowledges -q`, `python3 tools/static_build.py --check`, and full `python3 tools/check.py`.
- Completed and removed `DOIT.info2-box-labeled-fields.md`. The stale Info2 queue mapped to the current YO!info tree implementation, where one-field-per-line labeled rows, full path/branch/PR/Linear text, Tab(tmux session), and tmux sub-window action buttons were already present. The remaining gap was that those labels and group dimension prefixes were hardcoded English; they now route through one localized `infoFieldLabel()` owner with new translated `info.field.*` catalog keys. Focused verification: `node tests/editor_preview.test.js` and `python3 tools/static_build.py --check`.

---

Completed 2026-06-28. Extracted from the 2026-06-28 daily log.
