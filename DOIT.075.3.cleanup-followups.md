# 0.7.4 cleanup follow-ups

These 13 items were appended after the original 26-item cleanup queue was established. They remain useful follow-up work, but they are outside the frozen P0 denominator in `DOIT.p0.0.7.4-cleanup.md`.

## Frontend follow-ups

- [ ] Add one declarative framed-panel Factory above the existing `panelFrameHtml`/`bindPanelShell` parents. Twelve article constructors and eleven frame consumers still repeat article ID/class/frame/bind setup across `80_info_panel.js:138-178`, `82_chat_panel.js:597-624,1276-1305`, `83_preferences_panel.js:780-801`, `85_debug_panel.js:9487-9505,9560-9586`, `99_terminal_boot.js:1055-1102`, and `78_panel_shell.js:2622-2640`. Extend the existing parent with a descriptor-driven `createFramedPanel` while leaving distinct editor/empty panels and feature body builders explicit. Migrate by panel family and freeze exact outer HTML, ARIA/datasets, tab/toast/body order, bind timing, relocalization, delegated actions, DOM identity, focus, observer, and replay behavior; then reject unregistered framed-article setup.
- [ ] Make panel-body replacement one explicit reconciliation transaction. Preferences (`83_preferences_panel.js:833-869`), YO!cost (`85_debug_panel.js:9509-9541`), and Debug (`85_debug_panel.js:9611-9631`) repeat capture, `innerHTML` replacement, scroll/selection restoration, and rebinding around existing helpers in `10_core_utils.js:3959-3993`. Extend that parent with named anchor adapters and an `afterReplace` hook, preserving panel-specific drag/focus/graph skip rules and exact synchronous-plus-animation-frame restore order. Characterize nested scroll, focused control and selection, Debug log caret/near-bottom state, one content update, and one listener invocation before retiring all three flows.
- [ ] Continue decomposing the frontend orchestration partials behind narrow registered facades without changing the concatenated runtime. `85_debug_panel.js` still spans observation/liveness, stats transport, projections, panel factories, and graph interaction; `99_terminal_boot.js` spans panel construction, owner resources, terminal startup, transcripts, client-event transport, and shortcuts. Move cohesive declaration groups verbatim into ordered registered partials, keep one global bundle and one state owner, and preserve declaration-before-use. Freeze public globals, normalized bundle AST, load order, duplicate declarations, Node summaries, and feature tests before each mechanical move; update tests that locate source regions only after equivalent semantic seams exist.
- [ ] Finish lifecycle ownership for remaining manual timers and streams. Route current-observation, pricing, graph ticker, transcript/client transport watchdog/reconnect/debounce resources through existing `createLifecycleScope` scopes, retaining `runtimeIntervals` only for named recurring loops. Fake-clock characterization must prove start-twice uniqueness, replace invalidation, exact `0`/`null` sentinel behavior, self-reschedule order, hide/deactivate/pagehide disposal, bfcache resume, stale-callback rejection, and unchanged reconnect diagnostics.
- [ ] Finish base-control and bind-once ownership. Extend `makeButton`, `createToolbarButton`, and the action dispatcher so app-owned actionable controls inherit `.btn-base`, then migrate component families while preserving geometry/paint and computed light/dark hover/focus/pressed/disabled styles. Inventory the 49 bound-sentinel references before introducing a WeakMap-backed `bindOnce(root, key, installer)` with an explicit disposer; keep DOM dataset sentinels only where replay serialization intentionally requires them. Prove double bind, dispose/remount, and rebuilt/replayed DOM each invoke exactly one listener, with no implementation-only sentinel leaked into shared DOM.
- [ ] Classify repeated exact 5px and 7px radii before extracting tokens. These values appear across sessions, topbar, Preferences, and terminal/tree CSS but must not be rounded to existing 4px/6px/8px tokens. Create a named token only for a demonstrated shared semantic tier, migrate every member together, and otherwise leave component-specific literals alone; require computed border-radius and screenshot geometry parity in both themes plus a scoped negative search.

## Test and gate follow-ups

- [ ] Consolidate fixture HTTP request/server lifecycle behind a typed `FixtureHttpServer` parent. `tests/test_login_auth.py:51-68,107-238,241-244` and `tests/test_login_throttle_integration.py:41-48,70-90` independently own loopback requests, ephemeral bind, serving thread, and shutdown/join behavior. Reuse the completed `FixtureRuntime`/`GateLiveServer` lifecycle contract where its semantics fit; otherwise add one narrow test helper with injected auth/policy setup, not a second production server abstraction. Freeze exact status, duplicate-header multiplicity, body bytes, port-0 binding, exception precedence, shutdown/close/join order, and zero surviving thread before migrating both modules.
- [ ] Replace the three prompt-corpus parsers with one readable typed `PromptCorpus` loader. Current copies in `tests/test_agent_tui.py:23-41`, `tests/test_auto_approve_detector.py:26-45`, and `tests/test_mock_agents.py:38-58` disagree about JSON, YAML empty-document, capture-relative paths, and raw-text handling; `tests/helpers/mock_agents.py` is an existing candidate parent but is too narrow and compressed to own all policies clearly. Pin every current corpus input/output first, express real format/path differences as named presets rather than booleans, migrate all consumers, and negative-search the retired loaders.
- [ ] Give fixture-owned local-service records and traffic accounting explicit builders/scopes. Consolidate repeated process/lease and host-identity constructors from `tests/test_local_services_launch.py`, `tests/test_local_services_watchdog.py`, `tests/test_gate_orphans.py`, `tests/test_gate_owner_election.py`, `tests/test_gate_cross_host_views.py`, `tests/test_gate_tmux_identity.py`, and `tests/test_gate_chat_host_local.py` into typed builders with visible defaults and exact mapping parity. Replace the module-only reset around the process-global RPC traffic ledger (`tests/test_local_services_rpc.py:18-23`, cumulative restart case at `:1107-1139`) with an injectable `LocalServiceTrafficRegistry` plus production-singleton facade and fixture-owned scope; preserve cumulative totals across a service restart while preventing another test/thread from resetting the active owner. Do not serialize, retry, or weaken assertions.
- [ ] Move file-index test cleanup into one idempotent lifecycle owner. `tests/conftest.py:149-179` currently knows six callback setters, `_reset_search_progress_coalescing`, memory-index cleanup, and `SearchIndexerClient.ensure_started` suppression. Introduce a public test scope owned beside `file_index` that clears callbacks, trailing timers/coalescing, and memory indexes at enter and exit, including when setup or the test body raises; keep detached-indexer suppression explicit at the fixture boundary. Characterize callback order, late timer rejection, zero post-teardown publication, and repeated cleanup before retiring direct private resets from `conftest.py`.
- [ ] Finish the declarative gate boundary by making step identity typed and total. `tools/test_plan.py` owns lane names and step IDs, while `tools/check.py:227-257` still owns a separate string-keyed `step_catalog`; `pytest-unit` and `pytest-socket` at `:245-246` also bypass phase-derived file ownership with hard-coded ignores. Extend the descriptor with a typed `StepId` or validate registry references in both directions, route focused aliases through the same phase catalog, and prove byte-identical argv plus identical collected node IDs, order, markers, and one execution owner. A missing/extra/duplicate step, prerequisite cycle, or phase drift must fail closed.
- [ ] Make architecture-budget discovery recursive and continue only semantic monolith decomposition. `_test_imports` in `tools/architecture_budgets.py:101-124` scans only top-level `tests/test_*.py`, and line targets at `:40-61` omit nested collecting modules and very large JS helpers. Add compliant and violating nested fixtures, recursively inventory collecting Python and registered JS test owners, and ratchet the reviewed manifest without imposing arbitrary rewrite quotas. Continue cohesive-family strangler moves from `test_app.py`, `test_browser_layout.py`, `tests/browser_helpers/editor_preview_suite.js`, and `tests/share_theme.test.js` only with exact `--collect-only` ID/order/marker/lane parity and unchanged assertions.
- [ ] Make browser-test capability an explicit gate preflight without changing dependencies in this cleanup. `pyproject.toml` declares pytest timeout/xdist but browser-gate availability depends on ambient Selenium. Decide in the owning packaging queue whether Selenium is an operator prerequisite or a separate test extra; here, add only a deterministic diagnostic that distinguishes unavailable dependency/browser/driver from product failure and never silently skips a requested browser lane. Adding or changing a dependency requires separate approval.
