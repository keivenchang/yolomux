# DOIT.9 — File diff editor (2026-05-31)

A real in-editor DIFF view: an extra **Diff** button in the editor toolbar when the open file is under a repo + has changes; clicking a modified file opens the editor DIRECTLY in diff mode; the diff renders SIDE-BY-SIDE when the pane is wide and INLINE (unified) when the pane is narrow. Builds on what shipped in DOIT.8 (`/api/fs/diff`, numstat counts, the `codeMirrorDiffLineExtension` gutter). Dev checkout only. Validate `python3 -m pytest tests` + `node tests/layout_url.test.js`; restart `:7778` via `systemd-run`; bump version on commit.

WHAT EXISTS TODAY (do NOT redo): `/api/fs/diff` -> `filesystem.diff_file` (`filesystem.py:285-314`) returns `{path, repo, relative_path, diff (unified text), untracked}` (tracked = `git diff HEAD`, untracked = `git diff --no-index /dev/null file`). `/api/session-files` carries per-file `added`/`removed` numstat (`session_files.py:137-195`). On the frontend, `parseUnifiedDiffLineClasses` (`yolomux.js:5834`) + `codeMirrorDiffLineExtension` (`:5857`, wired at `:6057`) COLOR the working doc's +/- lines — a ONE-document gutter (no deletions shown, not side-by-side). The `'diff'` mode below is the real TWO-document merge view; keep the gutter as the always-on cue in plain edit mode.

- [ ] [L] **Bundle `@codemirror/merge` (PREREQUISITE — gates everything below).** It is NOT in the vendored bundle (`grep MergeView static/codemirror.js` = 0 hits). Add `@codemirror/merge` to the CM ESM imports in `loadCodeMirrorApi` (`yolomux.js:5503-5607`), export `MergeView` (side-by-side, two editors) + `unifiedMergeView` (inline, one editor with deletions shown), then REBUILD `static/codemirror.js`. BLOCKER: there is NO `build.mjs`/`package.json` in the repo root, so the bundle-rebuild step must be located/defined first (DOIT.8 rebuilt the bundle for fenced languages, so a process exists somewhere — document it). This is the one real dependency; the rest is wiring.
- [ ] [M] **Backend: expose the HEAD/base content for a two-document diff.** A merge view needs BOTH documents; `/api/fs/diff` returns only the unified diff text today. Extend `diff_file()` (`filesystem.py:285`) to ALSO return `original` = `git show HEAD:<relative_path>` (empty string when `untracked`; for deleted files `original` = HEAD content and working = empty). Keep the existing `diff` field. The frontend takes the working doc from the open buffer / `/api/fs/read` and `original` from this field. Respect the existing `MAX_READ_BYTES` cap and skip binary gracefully.
- [ ] [L] **`'diff'` editor mode: SIDE-BY-SIDE (wide) / INLINE (narrow).** Add `'diff'` to `editorViewModes` (`yolomux.js:47`). In `renderFileEditorPanel`, when the item's mode is `'diff'`, build a merge view from {original, working}: editor-content width >= ~800px -> `MergeView({a:{doc:original}, b:{doc:working}})` (two columns); narrower -> `unifiedMergeView({original})` on the working editor (deletions inline). Keep the `b`/working side EDITABLE with revert-chunk controls; SAVE via `/api/fs/write` (`:395`) must still work from diff mode. Reuse the per-item view-mode machinery (`setFileEditorViewMode(path, mode, item)` `:6188`, `updateEditorModeControl` `:6198`, the mode-control toolbar `:11077`/`:11133`) and the `codeMirrorConfigSignature` destroy/rebuild path when entering/leaving diff mode.
- [ ] [M] **Responsive switch side-by-side <-> inline on resize.** `ResizeObserver` on the editor content (same pattern as the terminal-fit observer `:8660-8661`); cross a width threshold -> swap `MergeView` <-> `unifiedMergeView`. Debounce, and only REBUILD when actually crossing the threshold (not on every resize); preserve scroll + cursor. Apply the scroll-sync suppression lesson from DOIT.8 (the split-view peg-to-top fix) so the two MergeView panes don't echo-fight.
- [ ] [S] **"Diff" toolbar button, gated on repo + changes.** Add a Diff button to the editor toolbar cluster (by the mode-control `:11077`); SHOW it ONLY when `/api/fs/info` returns a non-empty `repo_root` (`filesystem.path_info` `:264`) AND the file has changes (status `M`/`A` from `/api/session-files`, or a non-empty `/api/fs/diff`). Clicking toggles the open file into/out of `'diff'` mode.
- [ ] [S] **Changed-file click -> open the editor DIRECTLY in diff mode.** The modified-files panel / `Changes` pane row click already opens the editable file + applies the diff gutter (`yolomux.js:5112` fetch, `:5857` extension, `:6057` wiring). Set the opened item's view mode to `'diff'` so it LANDS in the diff editor, not plain edit.
- [ ] [M] **Auto-update the diff when the AI rewrites the file (no flash, keep cursor).** On the watched-filesystem refresh that already updates the changes list (DOIT.8), re-fetch `original` + working and update the merge view INCREMENTALLY (CM `dispatch`, replace only changed ranges) instead of rebuilding — the same no-flash / cursor-preserving approach as the Finder-tree reconcile and the shipped diff-decoration refresh.

Batch LOE: ~L overall — the `@codemirror/merge` bundle rebuild is the only risk; everything else is M/S and reuses shipped infra (`/api/fs/diff`, numstat, view-mode machinery, ResizeObserver, the diff gutter).

## Launch auto-approve follow-up

- [ ] [L] **API-driven auto-approve for YOLOmux-LAUNCHED Claude/Codex sessions (researched 2026-05-31; verified vs claude 2.1.158 / codex 0.134.0).** Re-audited 2026-05-31 while closing DOIT.8: Claude hook mechanics are locally evidenced by existing PreToolUse JSON hooks, and Codex still only has local `prefix_rule(pattern=[...], decision="allow")` examples in `~/.codex/rules/default.rules`. I am not marking this done or half-implementing it because there is still no verified Codex schema/load path for translating YOLOmux `regex`/`glob`/`contains`/`block`/`ask`/`notify`/`off` safely. The safe next action remains a focused Codex execpolicy schema spike plus a Claude hook smoke test; launched-session API YOLO should not ship until both are proven.
