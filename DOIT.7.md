# DOIT.7 — codebase health, refactor, and small leftovers (2026-05-30)

Focused refactor batch plus the small residual follow-ups moved out of the completed near-term UI batch. Forward product features moved to `DOIT.8.md`. Dev checkout only. Validate `python3 -m pytest tests` + `node tests/layout_url.test.js`; restart `:7778` via `systemd-run`; bump version on commit.

Answers "split / merge / reorg / reuse". Headline: the Python lib is ALREADY well-layered and acyclic (no merges needed); the real monolith is the FRONTEND `static/yolomux.js` (13,120 lines / 914 fns, served as a plain `<script>`). Ordered safe-high-ROI first.

## Small Leftovers
- [ ] **Finish the remaining user-initiated xterm focus gaps when auto-focus is off.** Most user paths now call `focusTerminalFromUserAction`, but two paths still skip `term.focus()` with auto-focus disabled: `selectSession` picks via `focusPanel(session)` with no `userInitiated`, and the tmux window stepper still uses `focusTerminalWhenAutoFocus`. Route both through the explicit user-action focus path so typing reaches the terminal after those actions too.
- [ ] **Decide and implement the image-viewer toolbar policy.** Image viewers correctly hide text-only controls (`#`, wrap, find, save, edit/preview/split), but still show the editor theme button. Either keep it deliberately because it helps inspect transparent images against dark/light backgrounds, or replace it with image-specific controls such as fit-to-window, actual-size, zoom, and open-in-new.

## Frontend
- [ ] **Delete the DEAD standalone `#fileEditor` editor (~350-450 lines) — safe NOW, no behavior change.** Files always open as pane tabs, so the standalone singleton path (`renderEditorForActive` ~5707, only runs when a file is active with NO pane slot — never happens) is a parallel duplicate of `renderFileEditorPanel`. Remove `renderEditorForActive`, `ensureEditorVisible`/`hideEditor`, `ensureStandaloneCodeMirror` & siblings, `renderStandaloneSyntaxHighlight`, and the `#fileEditor*` markup in `web.py`. Independent of the textarea-engine decision.
- [ ] **Extract ONE `createHoverPopover` controller (4 copies -> 1) — also fixes the popover-flash CLASS.** `bindDelayedSessionPopover` (tab), `keepPanelPopoverOpen`/`closePanelPopoverSoon` (panel), `queueAppMenuHoverOpen`/`Close` (menu), and `bindFileImagePreview` (image) each hand-roll the same show/hide-timer debounce + "don't open while topbar/menu hovered" guard. One `createHoverPopover({anchor, popover, showDelay, hideDelay, canOpen, stillActive, onOpen, onClose})` collapses ~120-150 lines and centralizes the open/close logic that the popover-flash bug keeps re-breaking. Also extract `clampToViewport()` (3 copies: `positionPaneTabPopover` / `positionFileImagePreview` / `fitAppMenuPopover`).
- [ ] **Split `static/yolomux.js` into modules via esbuild (match the CM bundle precedent).** ~29 cohesive modules, or a coarse 8-10 first cut (state, util, layout, tabs/panels, finder, editor, terminal/session, prefs/changes/info, notifications/palette, boot). Build `src/*` -> `static/yolomux.js` with esbuild `--bundle --format=iife` (keeps the shared global-state model -> NOT a per-function import rewrite; one local file, offline, `web.py:159` unchanged). NOT ES-module + import-map (29 round-trips on the stdlib server), NOT ordered `<script>`s. Add a `build.mjs`/`package.json` that builds BOTH `static/yolomux.js` and `static/codemirror.js`. Already-shared, do NOT redo: `reconcileChildNodes`, `applyCssSettings`, `TAB_TYPES`.
- [ ] **Make per-type code read `TAB_TYPES`, not if/else ladders** — `terminalTabLabel` (~9262, 5-way `isInfoItem`/…), `resolveLayoutItem` (~1748). Add a `shortLabel` field to each `TAB_TYPES` entry.
- [ ] **CSS: modularize + tokenize + fix font-scaling.** Split `static/yolomux.css` (5,321) into ~7 partials (tokens / chrome / panels / terminal / panes-misc / file / responsive) delivered by build-time OR serve-time CONCAT into the SINGLE existing `<link>` (NOT `@import`, NOT N links — `static_asset_url` mtime cache-bust already supports a concatenated file). Tokenize magic values: `#f5c542` (31x, no token) -> `--accent-gold`; finish `--good`/`--nv-green` (literals that contradict existing tokens); add a `--lt-*` light-theme group (the markdown/diff blues/greens are 100% inline) and `--shadow-popover`/`--shadow-elevated` (collapses ~7 near-identical `0 18px 50px` shadows). DEAD CSS is small: only `@keyframes yolo-ring-fade` + `yolo-pill-fill-fade` (~14 lines, orphaned when the working-YO pulse was calmed) — mac controls already gone, the textarea overlay is LIVE. Also finish the remaining font-scaling cleanup: hardcoded control heights, bare `13px` fonts that ignore `--ui-font-size`, and stranded `--editor-font-size`/`--file-explorer-font-size: 13px` aliases.
- [ ] **(gated, product decision) Drop the textarea editor engine** -> removes ~600 more JS lines (`renderSyntaxHighlightInto`, `editorVisual*`, `simpleCodeSyntaxHtml`, `syncFileEditorSplitScroll`) + ~121 CSS lines + the `editor.engine` setting. Textarea is still the live CM-load-failure fallback; first confirm the code-Preview pane doesn't still need `simpleCodeSyntaxHtml`/`markdownSyntaxHtml`.

## Python Backend
- [ ] **`auto_approve_tmux.py` (1965) — biggest win.** (a) Move the inline `_self_test` (~732 lines = 37% of the file) into `tests/`. (b) Split the pure detector (`detect_prompt`, `yes_is_selected`, `approval_prompt_state`, `extract_command`, `prompt_hash` — no tmux, no argparse) into `yolomux_lib/prompt_detector.py`, leaving tmux helpers + target resolution + `main`/CLI thin. Lets `auto_approve_worker.py` do a normal `import` instead of the `importlib`-by-path `auto_approve_module()` hack (`common.py:541`).
- [ ] **De-wildcard `core.py` — the one real architectural smell.** `yolomux_lib/core.py` is a 12-line hub doing `from .X import *` for 10 modules with NO `__all__`; `web.py`/`server.py`/`app.py`/`cli.py` then `from .core import *`. The double-wildcard hides every dependency and leaks stdlib imports. Replace the 4 consumers' `from .core import *` with explicit named imports (pyflakes enumerates them), then delete `core.py` (or add `__all__` everywhere).
- [ ] **`server.py` (1215) — `Handler` god-class.** Extract by group: `AuthMixin` (cookie/basic/session ~36-217), `FsRoutesMixin` (`handle_fs_*`, thin adapters over `filesystem.py`), the Codex-summary SSE subsystem (~701-854), the WS/tmux PTY bridge (~963-1134, pair with `websocket.py`). `do_GET`/`do_POST` dispatch chains are fine as-is.
- [ ] **`common.py` (612): lift the ~300-line auth block into `auth.py`** (`AuthUser`/`AuthIdentity`, `parse_auth_yaml`, `read_auth_users`, cookie secret/value, credential check, ~104-416). Keep paths / tmux-primitives / git / session-name / text utils in `common`.
- [ ] **`metadata.py` (791): extract `github_client.py` + `linear_client.py` (+ the shared `http_json`)**, leaving `metadata.py` as orchestration.
- [ ] **Consolidate the 3 tmux helper copies.** `common.py` `tmux`/`run_cmd` is canonical; `auto_approve_tmux.py` (`tmux_run` + own capture/send) and the standalone `tmux_wall.py` (verbatim re-impl) duplicate it -> collapse onto `common.tmux`. Route `git_root_for_path` (`filesystem.py:283`, re-rolls the subprocess) through `common.git`. Tiny modules (`websocket`/`workdir`/`uploads`/`control`/`events`) are each single-purpose — LEAVE them, no merges.

Cross-all priority: (1) delete dead standalone editor [safe ~400 lines], (2) `createHoverPopover` [fixes flash class + dedup], (3) move `_self_test` -> tests + split detector, (4) de-wildcard `core.py`, (5) esbuild JS split, (6) CSS tokenize/modularize, (7) server.py / common / metadata splits.
