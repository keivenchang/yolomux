# DOIT.8 — forward product features (moved from DOIT.7, 2026-05-30)

Forward features moved out of the completed DOIT.7 codebase-health batch. Do after the near-term UI and refactor batches are stable. Dev checkout only. Validate `python3 -m pytest tests` + `node tests/layout_url.test.js`; restart `:7778` via `systemd-run`; bump version on commit.

NOTE: the summarizer (Tier 1) shares its per-session log scanner with the AFCA observability follow-up. Build that scanner once; whichever lands first owns it.

## Top priority (user-flagged 2026-05-31)
- [ ] **Editor split: a within-pane split AND a cross-pane split (left editor / right preview), with the two views SCROLL-SYNCED.** Today there is ONE within-pane mode-control (edit / preview / `split`, `editorViewModes` `yolomux.js:47`, `setFileEditorViewMode`, rendered in `renderFileEditorPanel` ~11094-11125) and `syncFileEditorSplitScroll(panel, source)` (`yolomux.js:11733`) already keeps editor<->preview aligned by `data-source-line` anchors — but ONLY within a single panel. ADD a CROSS-PANE split: open the SAME file as a CM editor in the LEFT pane and a live preview in the RIGHT pane (two layout items, one path), kept scroll-synced. Pane/slot infra exists (`basePaneKeys=['left','right']` :229, `applyLayoutSlots` :5008, `fileEditorItemFor(path)` :245). BLOCKERS (per audit): (a) view-mode is keyed per-PATH not per-pane (`fileEditorViewMode` Map :154) -> make it per-(slot,path) so left=edit / right=preview; (b) `syncFileEditorSplitScroll` queries within one `host` panel (:11733) -> generalize to sync two panels showing the same path; (c) one editor item per path today -> introduce a paired preview item (e.g. `file:<path>#preview`) or a per-pane mode. Validate: same file, left CM editor + right rendered preview, scroll either side and the other tracks by source line; the existing in-pane edit/preview/split still works.
- [ ] **"Projects" + a live "modified files" panel in the Finder, with an editable, auto-updating diff view (user: "I need this feature").** THREE linked pieces: (1) **Projects (new concept):** the Finder can open a "Project" = a repo root + a set of opened files; tie a Project to a session's cwd->repo (repo detection exists: `REPO_MARKERS`/`_directory_is_repo` `filesystem.py:49/82`, `git_root_for_path` :284; session->repo via `session_git_inventory` `metadata.py:313` -> `github_repo`). (2) **Modified-files sub-panel = bottom ~1/3 of the Finder, a FIXED region (NOT a draggable pane tab).** It is pinned to the Finder pane: non-movable on its own, but it travels WITH the Finder pane wherever the Finder is moved (up/down, left/right) or resized in the layout. It shows the active session's AI-modified files for that repo, each row annotated with the repo git-diff +added/-removed counts, clickable. INFRA (this is what makes it "move with the Finder" for free): `.file-explorer-pane` is a flex column (`yolomux.css:4394`) with `.file-explorer-tree-panel` (flex:1) — add a second flex child `.file-explorer-changes-panel` (`flex:0 0 ~33%`); as a child of the Finder pane it follows the Finder anywhere in the layout, never its own movable tab. DATA: REUSE the SHIPPED `/api/session-files?session=N` (TODO.md:247) — it already returns the AI-attributed changed files (Claude Edit/Write/MultiEdit + Codex apply_patch paths, resolved to git roots, merged with `git diff --name-status HEAD` + untracked, status A/M/D) but has NO per-file +/- yet, so ADD `git -C <root> diff --numstat` (+added/-removed per file) to it; add `git -C <root> diff -- <file>` behind a new `/api/fs/diff` route (next to `/api/fs/list|read|write` `server.py:193-202`) for the diff view. (The plain Finder today only has a dirty COUNT: `git status --short` -> `dirty_count` `metadata.py:113/127`.) (3) **Editable, auto-updating diff view:** clicking a changed file opens a MODIFIABLE diff (reuse the CM editor + `/api/fs/write` :395) that AUTO-UPDATES when the AI rewrites the file — WITHOUT flashing or losing the cursor: reuse the `reconcileChildNodes` key-diff (`yolomux.js:4178`) for the file list and CM's incremental `dispatch` (replace only changed ranges, keep selection) for the document, exactly like the Finder-tree reconcile. Detect "AI just modified file X" from the transcript (`tool_use` Write/Edit `file_path` — see Tier 1) and/or a filesystem watch on the repo. THIS IS the repo <-> AI-modified-files <-> diff link the user wants.

## Near-term UI + test coverage (user-requested 2026-05-31)
- [x] **Remove the orbiting "working" ball around the YO badge — user did NOT request it.** Done 2026-05-31: deleted the `.session-yolo-marker.active.working::after` satellite, `@keyframes yolo-marker-orbit`, and orbit CSS vars. The working cue is now only the existing calm `yolo-marker-rotate` on the YO badge itself. Updated `tests/test_browser_layout.py` to assert that the pseudo-element is gone.
- [x] **Preferences: split the single editor color scheme into TWO (a DARK scheme + a LIGHT scheme) so the editor ◐ toggle flips between the user's chosen pair.** Done 2026-05-31: Preferences now shows separate dark/light editor scheme selects (`appearance.editor_dark_color_scheme`, `appearance.editor_light_color_scheme`), the editor ◐ toggle reads that pair instead of hard-coded defaults, reset returns to VS Code Dark+ / GitHub Light, and tests cover the new preferences UI plus toggle defaults.
- [x] **Add the MISSING detector/state tests surfaced by the 2026-05-31 audit (fixes shipped, but the exact failing scenarios are uncovered).** Done 2026-05-31: added the running-header-plus-live-approval precedence fixture in `tests/test_auto_approve_detector.py`, added an integration test in `tests/test_app.py` proving `prompt_and_screen_status()` falls back to transcript activity when the visible pane is idle, and updated the browser-layout assertion when the YO orbiting pseudo-element was removed.
- [x] **FIX the transcript-fallback FALSE POSITIVE: a finished Claude turn reads as "working" forever (image 003, reproduced 2026-05-31).** Done 2026-05-31: `transcript_activity_state_from_text()` now treats terminal Claude `stop_reason` values as completed turns, user/tool/system turn-boundaries clear streaming when no tool is pending, and `transcript_activity_state()` only returns working for recent transcripts (mtime or newest timestamp within the recency window). Covered by `tests/test_transcripts.py` stale/recent/completed-turn cases plus the `prompt_and_screen_status()` integration test in `tests/test_app.py`.
- [ ] **YO detection misses Claude's NEW bottom "work queue" UI (image 004, reproduced 2026-05-31) — a working spinner reads as idle.** With the new multi-task work queue the pane shows `● Honking… (1m 12s · ↓ 5.8k tokens)` (working) but BELOW it Claude renders the queue block (`● main … ↑/↓ to select · Enter to view`, `○ Explore  <task>  47s` rows) plus a `● xhigh /effort` status line. Reproduced: `visible_agent_working`=False, `agent_screen_state.key`="idle" (working line found at index 6, but `_working_line_has_later_prompt` counts the queue rows + the effort line as "later activity"). TWO distinct misses: (1) the new **work-queue block** (`●`/`○` task rows often ending in a `\d+s` age, plus the `↑/↓ to select · Enter to view` / `↑ to manage` hints) is not recognized as chrome; (2) the effort line is `● xhigh /effort` here but `_EFFORT_STATUS_LINE_RE` (`prompt_detector.py:362`) only skips the `⏺` bullet, so a `●`-prefixed effort line slips through. FIX in the skip set of `_working_line_has_later_prompt` / `_is_prompt_trailing_ui_line`: (a) broaden the effort regex to any leading bullet/symbol (`^\s*\S?\s*\S+\s+/effort\b`); (b) recognize the work-queue region — rows matching `^\s*[○●◦]\s+\S` (queue items) and the `↑/↓ to select · Enter to view` / `↑ to manage` hint lines. No-regress fixture (`tests/test_auto_approve_detector.py`): a spinner followed by the work-queue block reads `working`. NOTE: this is the 4th TUI-chrome break (70/72 context line, 78 boxed input, 003 transcript, 004 work queue) — which is exactly why the API/JSONL path (Tier-1 file-edit detection + the API-auto-approve item) is the DURABLE fix; the snapshot detector will keep needing patches like this.

## YOLOmux color schemes + Preferences (user-requested 2026-05-31)
- [ ] **Preferences pane must NOT auto-scroll when ANOTHER pane is focused.** `focusPanel(session)` (`yolomux.js:8354`) unconditionally calls `panel.scrollIntoView({block:'nearest', inline:'nearest'})` at :8357, and the prefs branch also runs `focusPreferencesSearchSoon(panel)` — so incidental focus changes / routine re-renders move the Prefs scroll position. FIX: (a) gate the `scrollIntoView` so it runs only for the USER-focused target (thread `userInitiated`/a `scroll` flag through and skip otherwise); (b) when the Prefs pane re-renders on a routine refresh, PRESERVE its `scrollTop`, and only auto-focus the search field when `autoFocusEnabled` AND the prefs pane is the one actually being focused. Confirm against the live app which path actually moves it (incidental `scrollIntoView` vs a prefs re-render resetting `scrollTop` vs `focusPreferencesSearchSoon`) and gate that one. Validate: scroll down in Preferences, click a terminal pane -> Preferences keeps its scroll position.
- [ ] **No registry change for headings (per-level dropped 2026-05-31 — keep it simple): one `heading` color per scheme.** Use the EXISTING single `syntax.heading` field (`yolomux.js:53-59`), which already drives both the CM editor heading color (`tags(t.heading, t.heading1, t.heading2)`, `yolomux.js:5513`) and the preview `--markdown-heading` for all of h1-h6 (`yolomux.css:4235`). No `heading1..4` fields, no per-level CSS, no highlight-extension change. BOLD/ITALIC already work: the extension applies `fontWeight:'700'` to `t.strong` and `fontStyle:'italic'` to `t.emphasis` (`yolomux.js:5527-5528`), so "bold = red" is just setting the scheme's `strong` color to red.
- [ ] **YOLOmux Dark (id `dark`, already labeled "YOLOmux Dark") — recolor to spec.** normal text `fg` = WHITE `#ffffff`; inline `code` + a bare ```fenced``` block (no language) = GRAY (`inlineCode` `#9aa5b1`, subtle `inlineCodeBg`); bold `**…**` = RED + bold (`strong` `#ff5c5c`); italic `_…_` = italic, normal color (`emphasis` `#ffffff`); `heading` (one color, all of #/##/###/####) = NVIDIA green `#76b900`; keep the dark `bg`. (Hex values are a starting proposal — tune to taste.)
- [ ] **YOLOmux Light (NEW id `yolomux-light`, label "YOLOmux Light").** Add a new `dark:false` record: `bg` = gentle BEIGE `#f4efe1`; `fg` dark `#2b2b2b`; inline `code` + bare fenced = GRAY (`inlineCode` `#6b7280`, light `inlineCodeBg`); bold = RED + bold (`strong` `#c0392b`); italic stays italic; `heading` (one color, all levels) = very dark green `#14532d`; pick readable `selection`/`activeLine`/`gutterBg`/`line`/`previewBg` on beige. Register the id in the light `SETTING_CHOICES` (`settings.py`) + `EDITOR_SCHEME_IDS`. (Proposal — tune.)
- [ ] **Make YOLOmux Dark / YOLOmux Light the DEFAULT and the FIRST option in both Preferences dropdowns.** DEFAULT: `settings.py` DEFAULT_SETTINGS `editor_dark_color_scheme` -> `"dark"` (was `vscode-dark-plus`), `editor_light_color_scheme` -> `"yolomux-light"` (was `github-light`); match `defaultEditorScheme`/`defaultLightEditorScheme` in `yolomux.js`. TOP OF LIST: `preferenceSelectOptionsHtml` (`yolomux.js:10039`) renders choices in array order (loose options first, then `<optgroup>`s) — order the dark choices with `dark` FIRST and the light choices with `yolomux-light` FIRST (or lead with a "YOLOmux" optgroup). Validate: fresh settings default to YOLOmux Dark/Light; both dropdowns list them at the top.
- [ ] **(feasible) Per-language syntax coloring INSIDE ```bash/sh/python/js/rs``` fenced blocks.** EDITOR: `codeMirrorLanguageExtension` calls plain `api.markdown()` (`yolomux.js:5483-5498`) — pass `markdown({codeLanguages})` mapping fence info-strings to the LanguageSupport already bundled (python/javascript/rust/json/html/css/yaml/shell/toml are loaded, `yolomux.js:5398-5409`), so a ```python block inside a `.md` highlights as Python while a bare ``` block stays monospace/gray (matches "code = gray": language blocks get language colors, plain blocks stay gray). PREVIEW: `renderMarkdownPreviewInto` already runs `hljs` over `pre code` (`yolomux.js:11297`), covering bash/python/js/rust. ~50 lines in the editor; the preview already does it.

## Tier 1 — GLOBAL stateful summarizer (marquee feature; researched in depth)
- [ ] **Stateful incremental summarizer (M2), per-session + global roll-up.** Background loop maintains a rolling per-session summary and rolls them into one GLOBAL "what needs attention / who's blocked / what each is doing" overview; surface it (dedicated summary tab / read-only wall / top-bar line). Build on the existing AI-summary plumbing: `codex_summary_prompt` (`yolomux_lib/transcripts.py` ~134) + `app.py` ~429, `/api/summary-stream`, `SUMMARY_CODEX_MODEL`/EFFORT/SERVICE_TIER (`common.py`). Today's path (`run_codex_summary`, `server.py` ~682) is STATELESS one-shot (`codex exec --json --ephemeral -`, re-sends the whole window every tick).
  - **Use M2 (incremental), not M1/M3.** Research finding: M2 wins (~6.5x fewer tokens, fastest); M1 (re-send window) is the slow/pricey baseline; M3 (`codex exec resume`) is worst (replays the whole conversation). Persist `{rolling_summary, last_processed_offset}` per session; each tick feed only `[prior summary] + [new transcript lines since last_offset]` -> small input, fast/cheap output. Global view = cheap roll-up of the per-session summaries.
  - **Provider selection (cheapest/fastest).** Support BOTH Claude and Codex (one may be unavailable). Claude -> Haiku (`claude-haiku-4-5`); Codex -> gpt-5.x low/fast. A setting picks which to use when both are present; auto-pick the only one available; if neither, the summarizer feature is simply unavailable.
  - **Live "Currently modifying <file> in order to <goal>" line (FEASIBLE — the file half is deterministic, reuses existing plumbing).** "<file>" comes straight from the transcript: `tool_use` records already parse name+input (`extract_content_blocks` `transcripts.py:364`; Codex `function_call`/`apply_patch` :229/242) — take the most recent Write/Edit/MultiEdit (or apply_patch) and read `input.file_path` (just add `file_path` to the parsed block, ~5 lines; today the input JSON is parsed but truncated, so the path is present but not surfaced). "<goal>" comes from the most recent USER message (`compact_transcript_items(text, N)` filtered to role=="user") — cheap, deterministic; optionally sharpened by the rolling per-session summary. Session->transcript->cwd->repo is already wired (`AgentInfo.transcript`/`.cwd`, `session_git_inventory` `metadata.py:313`). So a `/api/live-activity?session=` field (or a field on the existing auto-status payload) returns `{file, goal, repo, branch, active}` with NO new model call for the deterministic half. This DOUBLES as the repo<->AI-modified-files link for the Projects/modified-files panel (the recent `tool_use` file_paths per session ARE the AI-modified set).
  - **Detect which repo(s) the AI is working on.** Per session: `AgentInfo.cwd` -> `git_root_for_path`/`session_git_inventory` already gives repo + branch + `github_repo`. For agents that `cd` across repos mid-session, also harvest the distinct directories from recent `tool_use` `file_path`s / Bash cwds in the transcript tail — the union = "which repo(s) this AI is touching", which feeds the global roll-up's per-repo grouping and the Projects concept.

## Tier 2 — YOLO maturity (next layer after the rule-engine fixes)
- [ ] **Per-session YOLO policy modes.** Initial: `off`, `prompt-only`, `safe`, `edit`, `full`; make the active mode visible on the per-session `YO` control. (TODO P1.)
- [ ] **Concrete risk labels** everywhere decisions surface: `read`, `edit`, `network`, `process`, `delete`, `credential`, `unknown`.
- [ ] **Approval queue view** for pending high-risk actions — start read-only if live interception is hard.
- [ ] **Rule scoping dimensions:** global vs per-repo vs per-session, per-agent (`claude`/`codex`), and per prompt-type (`bash`/`file`/`tool`). (Schema A is shipped; this adds the overlay/precedence layer — see TODO "Option C: profiles + scope overrides".)

## Tier 3 — longer roadmap (pull individually from TODO.md when reached)
- [ ] Launch & Resume (P4); Worktrees (P5).
- [ ] Search/Cost/History (P6); Mobile & Network use (P7).
- [ ] Host & process vitals (P8); Multi-machine connector (P9).

## Future Refactor / Build-System Work
- [ ] **API-driven auto-approve for YOLOmux-LAUNCHED Claude/Codex sessions (researched 2026-05-31; verified vs claude 2.1.158 / codex 0.134.0).** The decision brain already exists and is GUI-independent: `yolo_rules.evaluate(cmd, prompt_type, agent, session) -> {action: approve|decline|block|ask|notify|off}` (`yolo_rules.py:529`) + `hard_floor_decision`. Today only the TRIGGER + keystroke are scraped (`auto_approve_worker.py` -> `tmux_capture_pane`/`tmux_send_*`) — which is why every Claude TUI restyle breaks detection (images 70/72/78/003/004). For LAUNCHED sessions, replace BOTH scrape steps with the agent's native permission API and call the SAME `evaluate()`. CLAUDE: launch `claude --settings <per-session.json>` (`--settings` takes a file path or inline JSON — verified; scopes config to THIS session without touching `~/.claude/settings.json`) registering a PreToolUse `command` hook -> a ~30-line `claude_permission_hook.py` shim that reads stdin (`tool_name`, `tool_input.command`, `session_id`, `cwd`), calls `yolo_rules.evaluate(...)`, and prints `{"hookSpecificOutput":{"permissionDecision":"allow|deny|ask",…}}` (approve->allow, decline/block->deny, ask/notify/off->ask); an `allow` suppresses the prompt; a settings `deny` is an extra hard floor (use the `command` hook type — only that is verified, NOT an HTTP hook). CODEX: native policy — generate an execpolicy `.rules` file from the YAML (Codex loads "user or project execpolicy `.rules` files" per `--dangerously-bypass-hook-trust`; VERIFY the exact `.rules` schema + load path first), or coarse `-c approval_policy=… -c sandbox_mode=…` / `--sandbox`, or full `codex exec --json`. WIRING: one YOLO-on branch in `agent_command(agent, dangerously_yolo)` (`workdir.py:38-43`) -> `tmux new-session` (`app.py:741`); gate on the per-session `auto_approve_enabled` list in `state.json` (`app.py:52-66`) — if off, return `ask`. HYBRID: launched sessions use the API; externally-attached sessions keep the (hardened) scraper; both call the same `evaluate()`, so the audit trail is identical and the scraper drops from primary to fallback.
- [ ] **Split `static/yolomux.js` into modules through a build step.** Use a single bundled output so the Python stdlib server still serves one script, likely with esbuild or the same local bundling pattern used for CodeMirror. Do not convert the page to many ordered `<script>` tags.
- [ ] **Modularize `static/yolomux.css` through a single-output concat/build step.** Keep one served CSS asset, but split source partials into tokens/chrome/panels/terminal/file/responsive when the build step exists.
- [ ] **Finish full font-scaling cleanup.** Replace remaining hardcoded compact control heights and bare fixed font sizes with shared `--ui-font-size` / editor / Finder variables after the CSS source split exists.
- [ ] **DROP the textarea editor engine — go CodeMirror-EXCLUSIVE (user-confirmed 2026-05-31).** CM is already the default (`defaultEditorEngine='codemirror'` `yolomux.js:49`); textarea is only the CM-load-FAILURE fallback + the legacy highlight overlay. REMOVE (per audit): the `editor.engine` setting (`settings.py:62-64` + choice validation :148; the Preferences row ~9840) and the engine-selection logic (`editorEngineModes`/`readConfiguredEditorEngine`/`codeMirrorRequested` :47-48/699-712 -> always CM); the textarea + highlight DOM (`file-editor-textarea-panel`/`file-editor-highlight-panel` :10701-10702) and listeners (:10756-10770); the textarea fallback path (:10103-10112, :10986); and the textarea-only highlight engine `renderSyntaxHighlightInto`/`renderSyntaxHighlight`/`syncSyntaxHighlightScroll` + visual-row measurement (:11606-11651, :11249-11304). KEEP (NOT textarea-coupled — confirm before deleting): `simpleCodeSyntaxHtml`/`markdownSyntaxHtml`/`editorVisualHighlightHtml` are ALSO used by the code/markdown PREVIEW pane (`renderEditorPreviewPane`/`renderEditorCodePreviewInto` :11404) and the `data-source-line` anchors that drive split-scroll — keep the preview path, drop only the textarea overlay. RISK: if CM fails to load there is then NO editor — show a clear error + a read-only raw view instead of silent breakage. Validate: open a file -> CM only; preview + split-scroll still work; `editor.engine` is gone from Preferences and settings.
- [ ] **Split root-script tmux helpers without auth/config import side effects.** `common.tmux` is the canonical app wrapper, but root scripts currently avoid importing `common.py` because it initializes auth/config state. A future small `process_utils` / `tmux_utils` module can let `auto_approve_tmux.py`, `tmux_wall.py`, and `common.py` share tmux/process helpers cleanly.

Note: many smaller Preferences / File Explorer / Transcript items remain in TODO.md; promote them into a DOIT only when they're the next focused batch.
