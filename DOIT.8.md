# DOIT.8 — forward product features (moved from DOIT.7, 2026-05-30)

Forward features moved out of the completed DOIT.7 codebase-health batch. Do after the near-term UI and refactor batches are stable. Dev checkout only. Validate `python3 -m pytest tests` + `node tests/layout_url.test.js`; restart `:7778` via `systemd-run`; bump version on commit.

NOTE: the summarizer (Tier 1) shares its per-session log scanner with the AFCA observability follow-up. Build that scanner once; whichever lands first owns it.

## Top priority (user-flagged 2026-05-31)
- [x] [S] **BUG (priority, image 006): split-view preview scroll PEGS BACK TO TOP when the source lines are long so the editor fits but the rendered preview is much taller.** Done 2026-05-31: `syncFileEditorSplitScroll` now uses delayed scroll-sync suppression plus source-pane scrollability guards, so a non-scrollable editor cannot echo-scroll the preview back to line 1.
- [x] [L] **Editor split: a within-pane split AND a cross-pane split (left editor / right preview), with the two views SCROLL-SYNCED.** Done 2026-05-31: added a paired `file-preview:` layout item, per-item editor view mode, a side-preview toolbar action, and generalized same-path scroll sync across editor/preview panels while preserving the existing within-pane edit/preview/split modes.
- [x] [XL] **"Projects" + a live "modified files" panel in the Finder, with an editable, auto-updating diff view (user: "I need this feature").** Done 2026-05-31: the Finder now embeds a fixed modified-files panel grouped by repo root, `/api/session-files` includes git numstat `added`/`removed` counts, `/api/fs/diff` returns tracked/untracked diffs, changed-file clicks open the editable file and apply CodeMirror diff-line decorations, and watched filesystem refreshes update the list/diff without turning the list into a movable tab.

## Near-term UI + test coverage (user-requested 2026-05-31)
- [x] **Remove the orbiting "working" ball around the YO badge — user did NOT request it.** Done 2026-05-31: deleted the `.session-yolo-marker.active.working::after` satellite, `@keyframes yolo-marker-orbit`, and orbit CSS vars. The working cue is now only the existing calm `yolo-marker-rotate` on the YO badge itself. Updated `tests/test_browser_layout.py` to assert that the pseudo-element is gone.
- [x] **Preferences: split the single editor color scheme into TWO (a DARK scheme + a LIGHT scheme) so the editor ◐ toggle flips between the user's chosen pair.** Done 2026-05-31: Preferences now shows separate dark/light editor scheme selects (`appearance.editor_dark_color_scheme`, `appearance.editor_light_color_scheme`), the editor ◐ toggle reads that pair instead of hard-coded defaults, reset returns to VS Code Dark+ / GitHub Light, and tests cover the new preferences UI plus toggle defaults.
- [x] **Add the MISSING detector/state tests surfaced by the 2026-05-31 audit (fixes shipped, but the exact failing scenarios are uncovered).** Done 2026-05-31: added the running-header-plus-live-approval precedence fixture in `tests/test_auto_approve_detector.py`, added an integration test in `tests/test_app.py` proving `prompt_and_screen_status()` falls back to transcript activity when the visible pane is idle, and updated the browser-layout assertion when the YO orbiting pseudo-element was removed.
- [x] **FIX the transcript-fallback FALSE POSITIVE: a finished Claude turn reads as "working" forever (image 003, reproduced 2026-05-31).** Done 2026-05-31: `transcript_activity_state_from_text()` now treats terminal Claude `stop_reason` values as completed turns, user/tool/system turn-boundaries clear streaming when no tool is pending, and `transcript_activity_state()` only returns working for recent transcripts (mtime or newest timestamp within the recency window). Covered by `tests/test_transcripts.py` stale/recent/completed-turn cases plus the `prompt_and_screen_status()` integration test in `tests/test_app.py`.
- [x] [S] **YO detection misses Claude's NEW bottom "work queue" UI (image 004, reproduced 2026-05-31) — a working spinner reads as idle.** Done 2026-05-31: broadened effort-line chrome matching and skipped Claude work-queue rows/hints in `_is_prompt_trailing_ui_line`; added a detector regression fixture for spinner + work queue + `● xhigh /effort`.

## YOLOmux color schemes + Preferences (user-requested 2026-05-31)
- [x] [S] **Preferences pane must NOT auto-scroll when ANOTHER pane is focused.** Done 2026-05-31: `focusPanel` only scrolls panes on explicit user focus, Preferences search auto-focus is gated by user focus + auto-focus, and routine Preferences re-renders preserve body scroll position.
- [x] [S] **No registry change for headings (per-level dropped 2026-05-31 — keep it simple): one `heading` color per scheme.** Done 2026-05-31: kept the existing single `syntax.heading` field and changed only scheme values.
- [x] [S] **YOLOmux Dark (id `dark`, already labeled "YOLOmux Dark") — recolor to spec.** Done 2026-05-31: dark scheme now uses white normal/italic text, NVIDIA-green headings, gray inline/bare code, red strong text, and the existing dark background family.
- [x] [M] **YOLOmux Light (NEW id `yolomux-light`, label "YOLOmux Light").** Done 2026-05-31: added the beige YOLOmux Light scheme, registered it in JS and settings validation, and tuned line/gutter/preview/selection colors for the light editor.
- [x] [S] **Make YOLOmux Dark / YOLOmux Light the DEFAULT and the FIRST option in both Preferences dropdowns.** Done 2026-05-31: defaults are `dark` / `yolomux-light`, matching frontend constants and backend settings defaults, and the Preferences dropdown ordering shows YOLOmux schemes first.
- [x] [M] **(feasible) Per-language syntax coloring INSIDE ```bash/sh/python/js/rs``` fenced blocks.** Done 2026-05-31: CodeMirror Markdown now receives bundled `LanguageDescription` entries for JS/TS/Python/Rust/JSON/HTML/CSS/XML/YAML/Shell/TOML fences, and the vendored CodeMirror bundle was rebuilt.

## Short-term, promoted from TODO (2026-05-31)
- [x] [M] **Editor power keys now that CodeMirror is the default (TODO:268-277).** Done 2026-05-31: the CodeMirror bundle now exports go-to-line, replace, indent/outdent, and comment-toggle commands; editor keymaps wire replace, go-to-line, line-comment, `Tab`, and `Shift+Tab`, and the status bar shows `line:col` plus selection counts.
- [x] [S] **Finder quick-access jump buttons `/` · `~` · `/tmp` (TODO:364).** Done 2026-05-31: Finder/File Explorer has compact quick-root buttons from `file_explorer.quick_access_paths` (default `~`, `/`, `/tmp`) that call the existing validated `openFileExplorerAt()` path.
- [x] [M] **Git-aware Finder path bar + hover (TODO:354).** Done 2026-05-31: `/api/fs/info` now returns repo branch/dirty/ahead/behind data; the Finder path bar shows a cached repo summary and repo rows get hover titles without spawning git on every hover.
- [x] [M] **Clickable PR/CI/issue links + a repo-metadata refresh button (TODO:249-250).** Done 2026-05-31: PR/Linear links remain clickable, CI checks now carry URLs through the GitHub client and render as links, and `YO'sup (info)` has a repo-metadata refresh button.
- [x] [S] **(minor YOLO correctness) Non-bash prompts auto-approve unconditionally (TODO:160).** Done 2026-05-31: file/tool prompts now run through `yolo_rules.evaluate()` and the hard floor instead of bypassing policy; regression coverage proves rules and destructive-command blocking apply.
- [x] [S] **Finder row selection + editor Find highlight are too FAINT (low-contrast) — moved from TODO Bug Fixes.** Done 2026-05-31: Finder selection/current-file contrast is stronger, CodeMirror search matches use more visible theme-aware fills, and the current match has a distinct stronger outline/background.

## Editor toolbar + split polish (user-requested 2026-05-31)
- [x] [M] **Make the editor toolbar MORE COMPACT (image 007).** Done 2026-05-31: the toolbar is now a nowrap icon row with transparent buttons at rest, smaller 20px controls/icons, thin group separators, and context-hidden controls that do not reserve space. Hover/active still gets a visible background/border.
- [x] [S] **"Split (to a new tab)" / side preview should open in the NEXT BIGGEST EXISTING pane — not split the editor 50/50 (it looks too big).** Done 2026-05-31: `largestPaneSlotForFileEditor()` now accepts excluded slots, and `openFileCrossPaneSplit()` moves the preview into the largest existing non-Finder pane other than the editor pane before falling back to a new split. `node tests/layout_url.test.js` covers the existing-pane choice.
- [x] [S] **Use a DISTINCT icon for the "Split to next-biggest pane" (side-preview) button.** Done 2026-05-31: the cross-pane side-preview button now uses `.file-editor-icon-side-split`, while the in-pane split-view mode keeps `.file-editor-icon-split`. `node tests/layout_url.test.js` asserts the distinct icon CSS exists.
- [x] [M] **Editor auto-save + external-change / conflict handling (user-requested).** Done 2026-05-31: Preferences now exposes `editor.autosave` and `editor.autosave_delay_seconds`; dirty CodeMirror edits schedule a debounced save with optimistic `expected_mtime`; stale saves route through the same conflict dialog used by manual save; dirty close flushes when autosave is on and shows Save / Discard / Cancel when it is off. Focused checks: `tests/test_settings.py`, `tests/test_filesystem.py`, `node tests/layout_url.test.js`.
  - External change + clean editor: auto-loads when autosave is on; otherwise keeps the editor clean and shows the Reload button/status.
  - No external change + dirty editor: auto-saves after `editor.autosave_delay_seconds` through the same optimistic `expected_mtime` guard as manual save.
  - Close editor: autosave on flushes; autosave off asks Save / Discard / Cancel.
  - Save conflict: Overwrite / Keep disk version / Compare; autosave never silently overwrites.
  - Validate: edit + wait -> auto-saves; external change while clean -> reloads only with autosave on or shows Reload with autosave off; external change while dirty + save -> conflict prompt; close dirty with autosave off -> 3-way prompt; close with autosave on -> saves.
- [x] [S] **Editor external-change: prompt "Content changed on disk, reload?" when the user EDITS a changed-on-disk buffer (imitate Cursor).** Done 2026-05-31: clean externally changed text files auto-reload only when autosave is enabled; otherwise the Reload button/status appears. The first edit after an external-change event opens a Reload / Keep editing dialog, and keeping the stale buffer preserves the later save-conflict dialog path. Covered by source assertions and `node tests/layout_url.test.js`.
- [x] [S] **tmux menu: list Claude / Codex (/ Term) as IMMEDIATE items, not nested under a "New tmux session" submenu (image 011).** Done 2026-05-31: `+ Claude` / `+ Codex` / `+ Term` are direct `tmux` menu entries with no "Create tmux session" detail, and the `YOLO` submenu is in the trailing group after Rename / Kill / Resume. `node tests/layout_url.test.js` verifies the order and absence of the old submenu.

## YO'sup info + detection + editor polish (user-requested 2026-05-31)
- [x] [S] **YO'sup session line: lead with "Codex <model>" / "Claude <model>", drop "is idle in <repo>".** Done 2026-05-31: added `AgentInfo.model`, populated it from Claude metadata / agent command flags when available, and changed local summaries to lead with `Codex <model> session N is active/idle` while keeping repo/branch in detail lines.
- [x] [M] **Make the per-session summary DESCRIPTIVE with status.** Done 2026-05-31: session summaries now combine goal/work text with PR/CI, dirty-file, ahead/behind, and review/commit status signals instead of only echoing a branch/PR title.
- [x] [M] **Modified-files count = diff between merge-base(main) and LOCAL (HEAD + working), NOT AI-attributed only.** Done 2026-05-31: `/api/session-files` now chooses `origin/HEAD`, `origin/main`, `origin/master`, local `main`, or local `master` as the base, diffs from `merge-base(base, HEAD)` through committed + working changes, keeps untracked files, and also scans the active session repo even when no AI tool attribution is present.
- [x] [S] **Tab header should show the owning SESSION + file (e.g. "1 TODO.md", "session-2 README.md").** Done 2026-05-31: changed-file opens record the owning session, file tabs prepend a compact owner badge, and tabs show `multi` when the same path is associated with multiple sessions.
- [x] [S] **BUG: modified-files panel separator disappears when shrunk + "slowly goes up" on hover.** Done 2026-05-31: the modified-files resizer now has reserved size/z-index tokens, the panel uses the same min-size token, and drag math clamps to that measured min size so the separator stays visible at small heights.
- [x] [S] **YOLOmux Light editor background too beige — make it "very white beige".** Done 2026-05-31: YOLOmux Light now uses a near-white beige editor background (`#fbf9f4`) with a lifted active line and matching panel/gutter tones.
- [x] [S] **Make the "Split to next-biggest pane" button smaller.** Done 2026-05-31: the side-preview split button now uses the same compact 20px toolbar button sizing/padding as the other editor icon buttons.
- [x] [S] **Modified-files panel must NOT reset scroll on auto-refresh (you were at the bottom and it jumped).** Done 2026-05-31: Changes pane and Finder-embedded modified-files refreshes now use `replaceHtmlPreservingScroll()`, and the existing payload-signature guard still skips silent refresh rendering when data is unchanged. `node tests/layout_url.test.js` covers preserved `scrollTop`/`scrollLeft`.
- [x] [S] **Modified-files rows must be TRULY compact: small tall/skinny A/M/D badges, non-bold file names.** Done 2026-05-31: changed-file status chips are 13px wide with condensed text, compact rows have tighter padding/line-height, and file names are normal-medium instead of bold. Covered by CSS assertions in `node tests/layout_url.test.js`.
- [x] [S] **Preview mode = only files that actually RENDER (md + html); render HTML for real, with NO JS.** Done 2026-05-31: Preview/Split stays gated to Markdown/HTML only, and HTML preview now renders in `<iframe sandbox srcdoc="...">` with no `allow-scripts`, `allow-same-origin`, forms, or popups. `node tests/layout_url.test.js` verifies `.html` uses the sandboxed iframe while `.py` stays edit-only.
- [x] [S] **Modified-files rows: show the agent ICON (not the "claude"/"codex" word) + a skinnier date font.** Done 2026-05-31: changed-file rows now render `agentIcon(item.agent)`, wrap dates in `.changes-file-date`, and order metadata as agent icon, skinny date, then +/- counts. `node tests/layout_url.test.js` verifies the icon/date slots and no lowercase agent word text.
- [x] [S] **Drag drop-target box: on a LIGHT/bright pane, invert the dashed YELLOW indicator to BLUE (for clarity).** Done 2026-05-31: drop indicators now use shared `--drop-outline*` tokens, default to gold, and switch to high-contrast blue under `body.editor-theme-light`. Covered by CSS assertions in `node tests/layout_url.test.js`.
- [ ] [S] **External change + CLEAN buffer -> ALWAYS auto-reload (NOT gated on autosave).** The shipped external-change item auto-reloads a clean, externally-changed file ONLY when `editor.autosave` is on — the user wants: if the file changed on disk and there are NO unsaved edits, JUST reload it, regardless of the autosave setting. FIX: drop the autosave gate for the CLEAN case — clean + external change -> reload immediately (no button, no prompt). The Reload button + "Content changed on disk, reload?" edit-prompt + save-conflict dialog stay for the DIRTY case only. Validate: with autosave OFF, change a clean file on disk -> it reloads automatically.
- [ ] [S] **BUG: modified-files panel SCROLLBAR disappears when scrolled to the lower part.** Distinct from the resizer-separator fix and the no-jump-on-refresh fix — here the actual scrollbar vanishes once the list is scrolled down. Likely the `.file-explorer-changes-panel` (overflow:auto) scrollbar is hidden/overlaid (a custom `::-webkit-scrollbar { display:none }`, the resizer/`min-height` clipping the bottom, or `replaceHtmlPreservingScroll` momentarily collapsing height). Find why the scrollbar isn't shown at lower scroll positions and keep it persistently visible + draggable. Validate: scroll the modified-files list to the bottom -> the scrollbar stays visible and usable throughout.
- [ ] [S] **Compact modified-files row layout: `[M|A]  <file>  <AI-icon>   ⟶ [right] +40 -7  <date>`.** Refine the just-shipped row order. LEFT cluster = status chip (`M`/`A`) + filename + the AI ICON (icon right AFTER the file). RIGHT-aligned cluster = the `+40 -7` diff counts THEN the date (date LAST). The shipped row groups agent-icon + date + counts together in the meta (`changeFileRowHtml`); MOVE the AI icon next to the filename on the left, and right-align `[+/- counts][date]` (counts before date). Validate: each compact row reads `M|A  name  <AIicon>   …   +40 -7  <date>` with the right cluster flush-right.
- [ ] [S] **Finder header buttons: 1px gaps + make `~` visible (the `.*` is ALREADY a toggle — keep it).** The `.*` button (`fileExplorerHiddenToggle`, `toggleHiddenFiles()` `60_popovers_tabs.js:1`) already toggles show-hidden/dotfiles in the current view with `aria-pressed`/`.active` — keep it, just ensure its on/off state is visually obvious. SPACING: set ~1px gap between the header buttons — `.*` (`.file-explorer-hidden-toggle`), `Root` (`.file-explorer-root-mode-toggle`), and the quick-access `~`/`/`/`tmp` (`.file-explorer-quick-access` gap is `3px`, `yolomux.css:4069`) — across the whole header button row. PADDING: the `~` quick-access button (`.file-explorer-quick-access-button` `padding: 0 6px`, `:4077`) renders a lone `~` that looks cramped/near-invisible — give it the same visible padding/min-width as the others. Validate: `.* Root ~ / tmp` sit ~1px apart, `~` is a clearly visible button, `.*` shows a clear toggle state.
- [ ] [S] **Pane frame `×` / `_` should act on ALL tabs in the pane (close-all / minimize-all), not just the active tab.** Proper behavior: the pane-HEAD `×` closes EVERY tab in that pane (the whole pane), and `_` minimizes them ALL together — treat the pane as a unit, distinct from the per-TAB close button on each tab (`data-pane-tab-close`, `80_panes_preferences.js:420`, which closes one). The slot helpers already target the whole pane — `removePaneFromLayout(item)` (`70_layout_actions.js:37`) drops the entire slot (`paneTabs(slot)` = all tabs) and `minimizePaneFromLayout` moves all pane tabs — and the frame buttons bind to them (`99_terminal_boot.js:313/318`). VERIFY this is the observed behavior; if the pane `×`/`_` only affects the active tab, route them to the whole-pane path. Also decide: pane `×` should genuinely CLOSE the tabs (remove from layout / close editors), not merely "hide from layout" while they linger. Validate: a pane with 3 tabs -> pane `×` closes all 3 (pane gone); pane `_` minimizes all 3 together.
- [ ] [S] **Pane header buttons (image 012): match the `_` color, vertically center the `...`, and stop clipping the button tops.** (1) COLOR: the `...` actions button is BLUE (`.tabs .pane-actions` color `#9fd8ff`, bg `rgba(64,125,180,0.22)`, border `rgba(159,216,255,0.42)`, `yolomux.css:2907`) while `_`/`+`/`×` are neutral (`.tabs .pc-window-control` color `#dfe6ef` + `--pc-control-bg`, `:2988`) — make the `_` minimize match its neighbors (same color/bg/border as `+` expand and the rest of the cluster) so the frame controls are consistent. (2) CENTER: the `...` glyph sits LOW because the literal "..." periods rest on the BASELINE (`.pane-actions-dots`, `:2921`, despite the flex centering) — vertically center it (nudge up / adjust line-height, or render a vertically-centered dot / `⋮` glyph) so the dots are mid-button. (3) TOP CLIP: `.panel-head` (`:2251`) has `padding: 1px 5px 0 6px` + `overflow: hidden` (`:2257`/`:2260`), so the top ~1px of the bordered buttons is cut off — bump the top padding to >=2px (or vertically center the controls in the head / relax the overflow) so no button edge is clipped. Validate: `_` matches the `+`/cluster color; the `...` dots are vertically centered; no button's top pixel is cut off.

## Command palette, quick-open & Tabs search (moved from TODO 2026-05-31)
- [x] [S] **Shared FUZZY subsequence matcher** (none today — `commandPaletteMatches` `yolomux.js:2317` is plain substring). Done 2026-05-31: added shared `fuzzySubsequenceScore()` / `fuzzySearchScore()` utilities, ranking contiguous runs and word starts, and wired them into command palette, Tabs search, and file quick-open. Covered by `node tests/layout_url.test.js`.
- [x] [S] **Platform app-modifier+B = Cursor-style toggle of the LEFT Finder + Modified-files panel.** Done 2026-05-31: the shortcut now snapshots the prior Finder layout before hiding it and restores that exact pane position/split size on the next toggle, so the embedded modified-files panel travels with Finder. Covered by `node tests/layout_url.test.js`.
- [x] [S] **Shift+platform app-modifier+P = COMMAND palette** (tabs + menu options), CENTERED (image 010). Done 2026-05-31: Shift+platform app-modifier+P opens the existing centered command palette, platform app-modifier+K remains supported, and plain platform app-modifier+P is reserved for file quick-open. Covered by source assertions in `node tests/layout_url.test.js`.
- [x] [M] **Tabs ▾ dropdown SEARCH bar** (placeholder "search") at the very top, FUZZY subsequence match. Done 2026-05-31: Tabs menu starts with a sticky search input and filters active/minimized/inactive rich rows using the shared fuzzy matcher across raw session id, label, PR/branch/description/detail, and file path fields. Covered by `node tests/layout_url.test.js`.
- [x] [M] **Platform app-modifier+P = Cursor-style FILE quick-open**, CENTERED modal (image 009). Done 2026-05-31: the command-palette shell now has a file mode, `/api/fs/search` returns bounded recursive file candidates under the active cwd/repo, recent open files rank first, and Enter opens the selected path in the editor. Covered by `tests/test_filesystem.py` and `node tests/layout_url.test.js`. Later suffixes (`:line`, `@symbol`) remain future work.
- [x] [M] **Command palette + quick-open — copy Cursor's behaviors (base shipped; this is the parity layer).** Done 2026-05-31: result labels highlight fuzzy-matched characters, arrow navigation wraps, Enter opens, Esc closes, app-modifier+Enter opens a file into an adjacent/existing pane, quick-open supports `>` command mode and `:NNN` line suffixes, file results show file-type icons plus dimmed paths, command rows show keybindings where known, and invoked commands get an MRU score boost. `@symbol` is explicitly surfaced as unavailable/future. Covered by `node tests/layout_url.test.js`.
- [x] [S] **[audit follow-up] Add a "generated — do not edit" header to the built `static/yolomux.js` + `static/yolomux.css`.** Done 2026-05-31: `tools/static_build.py` prepends a generated/do-not-edit header to both served assets, and `tests/test_static_build.py` plus `node tests/layout_url.test.js` cover the generated output.
- [x] [M] **Clean up ALL keyboard shortcuts: ONE app-modifier per platform (Cmd on Mac, Ctrl on PC) — never "(Ctrl OR Cmd)", never the Win/Super key — so Ctrl stays free for tmux (be faithful to Cursor).** Done 2026-05-31: added `appModifier()` / `appShortcutText()`, gated global app shortcuts through the single platform modifier plus `globalShortcutTargetAllowsAppAction`, updated menu/help/editor labels to render `⌘` on Mac and `Ctrl` on PC, and added platform override regression coverage. `node tests/layout_url.test.js` asserts Mac Ctrl is not claimed, PC Ctrl is used, generated UI has no combined shortcut labels, and the old `event.ctrlKey || event.metaKey` global binding is gone.
- [ ] [S] **Cmd+P file search MUST fuzzy-match the FULL PATH as a subsequence, JUST LIKE Cursor (e.g. "hokread" finds `/home/keivenc/.../README.md`).** The base shipped, but matching is INCONSISTENT end-to-end: the BACKEND `search_files` (`filesystem.py:178`) pre-filters with `_fuzzy_subsequence_match(token, "{rel} {name}")` where `rel` is relative to the SEARCH ROOT, while the CLIENT scores `fuzzySearchScore(query, [basename, ABSOLUTE path, detail, relpath])` (`20_layout_state.js:1149`). So a path-SPANNING query (e.g. "hokread" needing the `home/keivenc` dirs) is DROPPED by the backend rel-path filter before the client can match the absolute path -> the file never appears. FIX: (1) run the subsequence over the SAME full path on both sides — the full displayed/absolute path (or the search-root-prefixed path) — so ONE query spans parent dirs + filename; (2) make the search ROOT broad enough that the directory letters the user types are in scope (Cursor searches the whole workspace, not just the repo basename level); (3) rank basename + contiguous-run matches above scattered deep-path hits, and keep results bounded/fast. Validate: typing "hokread" (dir letters then filename letters, in order) returns the deep `README.md`; basename hits still rank first.

## Tier 1 — GLOBAL stateful summarizer (marquee feature; researched in depth)
- [x] [XL] **Stateful incremental summarizer (M2), per-session + global roll-up.** Done 2026-05-31: added a cached `/api/activity-summary` roll-up keyed by transcript/git/changed-file signatures, surfaced it as the global `YO'sup (info)` panel, and added matching local summaries to per-session tab popups. The first implementation is deterministic and cheap: it uses agent kind/status, recent user goal, repo/branch metadata, CI status, and `/api/session-files` changed-file totals instead of launching a model on every poll. A future LLM-sharpened layer can sit behind the same cache if needed.
  - **Use M2 (incremental), not M1/M3.** Done: summaries are recomputed only when the per-session signature changes, and unchanged sessions reuse cached text.
  - **Live "Currently modifying <file> in order to <goal>" line.** Done in deterministic form: file counts and file rows come from the same transcript/git attribution as the Finder modified-files panel; the goal comes from the most recent user transcript item.
  - **Detect which repo(s) the AI is working on.** Done for the current session repo and changed-file repos; the roll-up lists the repo, branch, CI state, and changed files per agent.

## Future Refactor / Build-System Work
- [x] [L] **Split `static/yolomux.js` into modules through a build step.** Done 2026-05-31: added `tools/static_build.py`, split the browser app into ordered `static_src/js/yolomux/*.js` partials, kept the served single-file output at `static/yolomux.js`, and added `tests/test_static_build.py` so stale generated JS fails tests.
- [x] [M] **Modularize `static/yolomux.css` through a single-output concat/build step.** Done 2026-05-31: split CSS into ordered `static_src/css/yolomux/*.css` partials for tokens, topbar/menus, popovers, Preferences/Changes, panes/tabs, Finder, and editor panels; `tools/static_build.py` still emits one `static/yolomux.css` for the stdlib server.
- [x] [M] **Finish full font-scaling cleanup.** Done 2026-05-31: moved compact UI/editor font sizing to shared variables (`--ui-font-size-*`, `--editor-ui-font-size*`, dense control sizes), removed remaining bare fixed font declarations from source CSS except the single centralized default `--ui-font-size: 13px`, and rebuilt the served CSS from source.
- [x] [M] **DROP the textarea editor engine — go CodeMirror-EXCLUSIVE (user-confirmed 2026-05-31).** Done 2026-05-31: removed the `editor.engine` setting and Preferences row, removed URL/config engine selection, removed the textarea/highlight DOM and listeners from file editor panels, and deleted the textarea-only highlight/scroll measurement path. CodeMirror is now the only editable engine. If CM fails to load, the panel shows a read-only raw text view with an explicit error. The preview/highlight helpers used by rendered preview and source-line scroll anchors remain.
- [x] [M] **Split root-script tmux helpers without auth/config import side effects.** Done 2026-05-31: added `yolomux_lib.tmux_utils` for side-effect-light process/tmux helpers, re-exported the shared helpers through `common.py` for existing app code, and switched `auto_approve_tmux.py`, `tmux_wall.py`, `app.py`, and `server.py` to use the shared helper where it removes duplication or avoids auth/config initialization.

Note: many smaller Preferences / File Explorer / Transcript items remain in TODO.md; promote them into a DOIT only when they're the next focused batch.
