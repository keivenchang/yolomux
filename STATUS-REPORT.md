# Progress

Updated: 2026-08-18 05:30 PM PT
Worktree: `/home/keivenc/dev/yolomux.dev7771`

**Goal:** v0.7.9 technical debt: close nine explicit release outcomes across decisions, implementation, verification, landing, and deployment. The checklist counts equal-weight release outcomes, not effort; supporting queue checkboxes show subtask volume. INTEGRATION AND DEPENDENCY PLAN for the four uncommitted patches, in order, one integration owner: (1) statsd first - it touches only `stats_current/materializer.py` and its test, is disjoint from every other group, and can land alone. (2) tmux and filesystem must SERIALIZE, not run in parallel: both edit `tests/test_app.py`. (3) descriptor residuals and the delete-lane split share `yolomux_lib/filesystem/paths.py` and belong to ONE parent owner; they must not be split across two integrators. (4) HTTP framing last of the source changes, because the global architecture budget it touches is regenerated by the coordinator AFTER source integration, never merged from an agent branch. (5) Then one clean exact-SHA certification gate on the integrated tree, followed by authorized runtime acceptance. Generated assets are always rebuilt centrally after integration, never merged from a worktree.

**Goal totals:** 2/9 done (22%); 7 TODO.

## Goal checklist

- [x] 1. Decide the two standing p0 queues: DONE 2026-08-18. Both downgraded with recorded reasons and the residual scope split, so `DOIT.p0.*` no longer exists. Both files were kept TRACKED via `git mv` rather than replaced, so no requirement was deleted; every original checkbox survives and proven ones are checked with evidence. Filesystem core already landed and its own five-file focused command passes unmodified at 155 passed, reproduced first-hand, so it became `DOIT.p2.e2.filesystem-descriptor-authorization.md` at 7/13 with the residual Darwin `descriptor_path()`, the `search.py:852-885` unauthorized absolute reopen, and stale specs retained plus an explicit risk rationale. The tmux fixture-leak mechanism was disproven by its own 2026-08-04 tripwire and its seven-day criterion needs a `tools/tmux_state.sh` that does not exist, so it became `DOIT.p2.e5.tmux-session-destruction.md`; the one real gap split to `DOIT.p1.e1.tmux-exact-session-target.md`, which then found a live data-loss defect.
- [ ] 2. Land Finder working-tree diffs for v0.7.9: implementation landed as `c59a8dd64` and archived in `docs/DONE/2026-08/DONE.finder-working-tree-diff.md`, so the dirty-18-file wording is stale. REMAINING: clean-tree exact-SHA certification and final release/live acceptance. All nine functional lanes pass at `7cb75e3a5`, but certification returned RC=4 `dirty_start_checkout`.
- [ ] 3. Attribute and fix the 414 Request-URI Too Long responses. ATTRIBUTION DONE and FIX IMPLEMENTED, blocked only on integration authority. Cause is not a long URL: a terminal response to a POST with an unread `Content-Length` body never closes, so the leftover body is parsed as the next request line. Producer is a browser tab on the pre-`4392504a7` bundle POSTing the deleted `/api/stats-history` route with a 98,258-byte body; `jsDebugStatsSampleQuery()` is disproven at 273 bytes worst case. The captured line had `request_line_complete=false`, so no complete request line is claimed. Fixed at one owner, `Handler.send_response` (`server.py:1721`), the single point every response already passes through. Independently re-verified: 8 passed with the fix, 7 failed with `yolomux_lib/` stashed, the survivor being the negative control by design. Also fixes a sub-65537-byte unread body that previously HUNG the connection while logging nothing. `HTTP_REQUEST_LINE_CAPTURE_LIMIT` untouched. REMAINING: integrate into the primary checkout, canonical gate, restarted authenticated live reproduction.
- [ ] 4. Close the two acceptance-only queues, now split because they share nothing. NFS CONFIGURATION LOCK: DONE and archived to `docs/DONE/2026-08/DONE.nfs-configuration-lock-acceptance.md`. It was never actually blocked - this host exports `/home/keivenc` to `10.77.0.2`, `keivenc-linux2` mounts it over NFSv4.2 with `local_lock=none`, and both hosts were already at the identical HEAD `7cb75e3a5`. All five scenarios passed first run with no product defect: bidirectional exclusion with waiters acquiring 7.4 ms and 21.9 ms after release, crash release on exact-PID SIGKILL without deleting the lock file, typed stale-revision conflict preserving all three keys across 3400 monitor reads, and same-key replace yielding exactly one winner. BACKGROUND OWNER: BLOCKED ON A USER DECISION. The original criteria presume a shared root, but the measured topology is a durable 7770 root plus private ephemeral `/tmp` roots for 7771-7773 where `DisabledBackgroundOwner` bypasses same-root election by design, so cross-port takeover cannot occur. Declaring those criteria not-applicable would materially weaken the acceptance and was deliberately NOT done unilaterally.
- [ ] 5. Split the filesystem delete lane so an unbounded recursive delete cannot strand the interactive lane. IMPLEMENTED, blocked on integration authority. Both failures were reproduced red first: a one-entry unlink sat `queued` behind held interactive work, and one non-recursive delete of a flat 20,000-entry directory performed 20,001 destructive syscalls. Bounded unlink now uses the mutation lane; a non-recursive directory delete returns `pending: "subtree"` WITHOUT ever reaching `os.scandir`, and recursive work escalates to `bulk` under the same `operation_id` with the deadline not extended. Route, jobd arm, authorization owner, and terminal result shape all remain singular. Focused five-file command 170 passed; lane isolation and bound proof 4 passed / 556 deselected, verified first-hand. TWO REAL DEFECTS FOUND OUTSIDE SCOPE AND RECORDED: recursive delete keeps deleting after the completion reports `deadline_expired`, and partial failure propagates through the facade so the reindex never fires and the search index still advertises deleted files. REMAINING: canonical gate and restarted live evidence.
- [ ] 6. Bound statsd CPU and memory, statsd first. CPU DEFECT FOUND AND FIXED, blocked on integration authority; the memory scope is untouched. Hot loop is `_append_uncovered_gap` (`materializer.py:1685-1695`) at 39% of profile, linearly rescanning every span for every coverage epoch. The proven cost is ALGORITHMIC O(epochs x spans) per build at 6 sources x 633 epochs x 589 spans, i.e. superlinear in the retained window; the exact base doubling regression measures about 4.29x for 2x history. An earlier empirical exponent of N^1.66 is withdrawn as unstable curve-fitting. Buckets were already incremental; the gap model was not, and `_coverage_gaps` takes no dirty parameter so every build re-derived the full retained model. Fixed with a `bisect` seek over a per-source starts index, exploiting the already-guaranteed start-ordered non-overlapping invariant; frozen-fixture output byte-identical across 3625 gaps. A/B on MATCHED UNPROFILED REPLICATES ONLY, whole-service, readiness-gated, calibrated to the measured 4.00 appends/s: base 55.9% and 55.7% versus patched 27.4% and 26.9%, about 2.05x. A third base replicate at 55.3% is EXCLUDED because that run had an append timeout, and a third patched replicate at 28.4% is EXCLUDED from the matched comparison because it was the py-spy profiled run. TIMING-GATE BLOCKER RESOLVED. The original regression was a load-sensitive timing gate that failed intermittently on the patched tree (61 passed / 1 failed at 2.80x against a strict <2.8; five isolated reruns 4 pass / 1 fail at 2.88x), and an earlier report of 8 passed was a single run of a gate flaky by construction. It has been REPLACED with a deterministic access-count and forbidden-iteration proof. Re-measured independently by the main context at host loadavg 34: full module 64 passed rc=0; the exact new tests 3 passed / 61 deselected rc=0; and a forced-red against the old full-scan implementation gives 3 failed rc=1, proving the new gate can actually fail. Load-independence is structural rather than statistical, so no synthetic load was used to demonstrate it. Withdrawn permanently: the earlier 332-pass claim and the N^1.66 exponent. THE DECODE-AMPLIFICATION HYPOTHESIS IS DISPROVEN for CPU at about 2.7% of profile. STILL OPEN: the whole memory scope - startup still schedules a full build, observations and usage still use `fetchall()`, RSS about 950 MiB with 1113 MiB high-water. MEMORY LEAK STATUS IS UNRESOLVED: no runaway growth was established within 60s, but every retained 60s run rose 7.4-17.4 MiB, so an earlier claim that RSS is flat in steady state and therefore not a leak is withdrawn. Also open: the double-normalization residual. LIVE 7772 IS STILL RUNNING THE OLD CODE: a fresh audit measures 85.0% user-heavy CPU with zero physical reads, unchanged, because the patch is uncommitted and 7772 was never restarted. PATCH FROZEN: `/tmp/statsd-cpu-ac62/statsd-coverage-gap-fix.patch`, SHA-256 `a13d47c3c7dd2e4cc43466c3ad4293fdba7fcd995d135d9c1e18ff9ba88940ce`, 2 files, independently re-hashed and confirmed to apply cleanly to `7cb75e3a5`; `git diff --check` exit 0. The deterministic gate is `_SeekOnlyGaps`, an explicit-span sequence whose `__iter__` raises outright and whose `__getitem__` is counted; the access count at 256/1024/4096/16384 retained spans must equal the count at 64 for a candidate overlapping two spans, under an absolute bound of 8, and no clock is read anywhere in it. METHOD NOTE: the owner's forced-red injected the OLD full-scan body into the CURRENT function signature, so the red proves the algorithmic property. The main context's own forced-red instead stashed `yolomux_lib/`, which reverts the signature too, so that variant is weaker evidence and is not the basis of the claim.
- [x] 7. Deploy the shipped statsd and daemon-lifetime fixes to production 7770: DONE 2026-08-18. IMPORTANT SCOPE NOTE - this deployed the ALREADY-SHIPPED release-theme work, NOT the new statsd coverage-gap hot-loop patch from item 6, which remains uncommitted and is not running anywhere. `main` fast-forwarded to `7cb75e3a5` at 15:34:37 and the process started at 15:34:47, after the move, so the running code is the deployed code; the prior instance had been serving `a2c781223` and was missing all eight release-theme commits including bounded daemon lifecycles. Verified: listener in `/home/keivenc/dev/yolomux`, HTTPS `/healthz` 200, unauthenticated `/api/ping` 401, served bundle SHA-256 matching disk, and real external client traffic.
- [ ] 8. Land origin/fix/macos-boot-tmux-env: LANDING DONE. `6bf61d5e6` merged as `1691743b9`; both are ancestors of HEAD and `git log HEAD..origin/fix/macos-boot-tmux-env` is empty. Queue at 8/12. REMAINING IS ACCEPTANCE ONLY: real-macOS verification is BLOCKED with no Darwin host, and the changed launcher is Darwin-gated so its production path has never executed here; plus clean-tree exact-SHA certification. The Linux-live half is proven separately - a stale plan file was silently accepted pre-merge (rc=0) and is refused after (rc=2).
- [ ] 9. Integrate `dev/0.7.8-7773` Finder Git diff viewer into current main without regressing the already-landed Finder working-tree/Differ split. Branch-only scope, not a blind 92-file merge: `dev/0.7.8-7773` is 4 ahead and 5 behind with conflicts overlapping `c59a8dd64`, and its worktree holds 20 dirty files of live work that must not be disturbed. Use a separate integration worktree and port only unique diff-viewer behaviour.

**Supporting queue totals:** 33/78 done; 45 pending.

## Active queues

| queue | done | pending | complete |
| --- | ---: | ---: | ---: |
| `queues/backlog/DOIT.p1.e1.tmux-exact-session-target.md` | 7 | 2 | 78% |
| `queues/backlog/DOIT.p1.e2.background-owner-live-fleet-acceptance.md` | 0 | 5 | 0% |
| `queues/backlog/DOIT.p1.e2.merge-macos-boot-tmux-env.md` | 8 | 4 | 67% |
| `queues/backlog/DOIT.p1.e2.request-uri-too-long-attribution.md` | 4 | 1 | 80% |
| `queues/backlog/DOIT.p1.e3.filesystem-delete-lane-split.md` | 5 | 5 | 50% |
| `queues/backlog/DOIT.p1.e5.memory-hog.md` | 2 | 9 | 18% |
| `queues/backlog/DOIT.p2.e2.filesystem-descriptor-authorization.md` | 7 | 6 | 54% |
| `queues/backlog/DOIT.p2.e5.tmux-session-destruction.md` | 0 | 13 | 0% |

## All queue checkboxes

### `queues/backlog/DOIT.p1.e1.tmux-exact-session-target.md` (7/9)
- [x] Capture red first: a regression proving a prefix target resolves onto the wrong session and that an exact target refuses. DONE: 16 new assertions failed before…
- [x] Change `tmux_session_target()` to the exact `=<session>:` form, routed through the existing shared owner with no second guard and no new route. DONE: `tmux_uti…
- [x] Migrate every assertion of the old prefix form repo-wide in the same change. DONE by repo-wide scan for non-`=` `-t` literals: `test_gate_tmux.py` (the known b…
- [x] Cover the matrix: no socket and no opt-in fails closed; default socket with explicit opt-in permits only an exact target; private socket works; `kill-server` r…
- [x] A prefix target is refused with a typed error and an exact target succeeds, proven by a regression that fails before the change. DONE, see above.
- [x] `python3 -m pytest -q tests/test_tmux_runtime.py tests/test_gate_tmux.py` exits 0. DONE: 31 passed, 1 skipped, 5 xfailed. Also measured: `test_app.py` 552 pass…
- [x] Default-socket session count is recorded before and after the work and is unchanged. DONE: before/after session lists are byte-identical by `diff`, 30 sessions…
- [ ] The change is integrated into the primary checkout and the canonical gate is green there. NOT DONE: the patch is uncommitted in an isolated worktree and integr…
- [ ] The second producer is reconciled or explicitly split out. NOT DONE: `tmux_exact_target_from_sessions` (`tmux_utils.py:259`) still emits the bare `f"{target}:"…

### `queues/backlog/DOIT.p1.e2.background-owner-live-fleet-acceptance.md` (0/5)
- [ ] With explicit permission to restart or drive 7770-7773, record each port's PID, CWD, HEAD, state root, owner generation, service roster, and served bundle befo…
- [ ] Verify startup ownership order, 7771 takeover after restart, multi-port Tabber/Finder cache writes, shared-root Quick Open/search indexing, follower worker-thr…
- [ ] Restore the requested fleet topology and retain only bounded evidence under `/tmp`; unrelated ports and state must remain unchanged.
- [ ] One current same-SHA run proves exactly one background owner, deterministic takeover, shared cache convergence, no follower worker, and responsive authenticate…
- [ ] The DONE record links the already-complete isolated 8004-8007 implementation evidence and separately records the real-fleet acceptance result.

### `queues/backlog/DOIT.p1.e2.merge-macos-boot-tmux-env.md` (8/12)
- [x] Re-run `merge-tree` against the real HEAD immediately before merging and record the result; the clean result above is a snapshot. DONE: `git merge-tree --write…
- [x] Merge, then regenerate `tests/fixtures/architecture_budgets/v1.json` from the merged tree rather than taking either side. DONE: merged as `1691743b9`; ratchet…
- [ ] Prove the defect is actually fixed: launch from inside a tmux session that has a stale `YOLOMUX_ROW_PLAN_FILE` exported and confirm the new server does not inh…
- [x] Confirm both launch paths still work after the collapse — `boot.sh` direct start and the supported multi-row launcher — since the branch merges two behaviours…
- [ ] Verify on real macOS hardware, since that is the platform named. If no host is available, say so and mark that item blocked rather than passing it on Linux evi…
- [x] Check no product root is written under `$HOME` after the change, using the same check as the 0.7.8 root-leak work. DONE: `~/state`, `~/runtime`, and `~/.yolomu…
- [x] `git log HEAD..origin/fix/macos-boot-tmux-env` is empty. DONE: verified empty after merge `1691743b9`; both `6bf61d5e6` and the merge commit are ancestors of H…
- [ ] A launch from a tmux session carrying a stale plan-file variable does not inherit it, proven by driving it, not by test alone. PARTIAL: driven and confirmed at…
- [x] Both direct and multi-row launch paths verified working post-collapse. DONE: see the plan item above; direct start proven by live restarts reaching `/healthz -…
- [x] Architecture-budgets ratchet regenerated post-merge. DONE: regenerated from the merged tree; `python3 tools/architecture_budgets.py` exits 0 and the `static so…
- [x] macOS acceptance, or explicitly recorded as blocked with the reason. DONE by explicit block: recorded as BLOCKED with the reason above — no Darwin host availab…
- [ ] Canonical gate green. PARTIAL: all nine functional lanes pass at `7cb75e3a5` (`git diff --check`, node syntax, py_compile, node layout, static source checks, p…

### `queues/backlog/DOIT.p1.e2.request-uri-too-long-attribution.md` (4/5)
- [x] Reproduce or capture one live 414 with timestamp, peer, user agent, method, raw bounded request-line bytes, route/parser state, and browser/network initiator w…
- [x] Prove or disprove each producer from current evidence. `jsDebugStatsSampleQuery()` is seven scalar parameters and about 160 characters, so do not attribute the…
- [x] Add one exact regression at the proven owner and preserve normal HTTP parser/authentication limits; do not widen request-line limits or change parsing from fra…
- [x] The DONE record contains one joined failing request and names the exact producer and framing defect, or records a bounded no-product-defect conclusion after ev…
- [ ] Any fix has red/green parser/producer tests, the canonical gate, and a restarted authenticated reproduction with no credential retention. PARTIAL. Red/green ve…

### `queues/backlog/DOIT.p1.e3.filesystem-delete-lane-split.md` (5/10)
- [x] Reproduce a one-entry unlink delayed behind held interactive/bulk work and a recursive delete whose work exceeds the mutation-lane bound. DONE red-first: `file…
- [x] Split the existing delete operation into an authorized bounded unlink case and a subtree case without adding a second path policy or route. DONE: `io_ops.delet…
- [x] Route bounded unlink through the mutation lane and recursive deletion through a bulk receipt, preserving symlink safety, exact target authority, conflict behav…
- [ ] Add cross-class isolation, recursive receipt, cancellation, partial-failure, file/symlink/directory, and namespace-replacement regressions. PARTIAL: isolation…
- [x] The red/green tests prove a held bulk/interactive lane cannot delay bounded unlink and prove recursive deletion never occupies the mutation lane. DONE, verifie…
- [ ] File, symlink, empty directory, nonempty directory, missing target, blocked path, namespace replacement, cancellation, and partial failure each have one typed…
- [x] The route, job descriptor, authorization owner, and operation result remain singular; focused tests and an unmodified `python3 tools/check.py` exit 0. DONE for…
- [ ] Restarted runtime evidence records bounded unlink responsiveness and honest recursive pending/terminal behavior on one unchanged HEAD. NOT DONE: no live-server…
- [ ] Cooperative cancellation: `_delete_directory_contents` has no cancellation or deadline check, so when an escalated operation's deadline expires the completion…
- [ ] Partial-failure reporting: `_delete_directory_contents` raises on the first failing entry and the exception propagates through the facade, so `invalidate_path_…

### `queues/backlog/DOIT.p1.e5.memory-hog.md` (2/11)
- [x] DONE: replaced the timing pass/fail with a DETERMINISTIC OPERATION-COUNT BOUND at `_append_uncovered_gap`. For example, an indexable explicit-gap test double t…
- [x] DONE: full module re-run independently by the main context at host loadavg 34 - 64 passed, rc=0. Exact new tests 3 passed / 61 deselected, rc=0. Forced-red aga…
- [ ] Refresh the patch backup and its hash after the test change.
- [ ] **Freeze production-scale reproductions and derive explicit budgets.** Create content-addressed fixtures and isolated subprocess harnesses for (a) the measured…
- [ ] **Make the bounded aggregate ring the YO!stats cold serving owner.** On daemon start, validate and load the fixed aggregate-ring publication, publish a bounded…
- [ ] **Bound every exact YO!stats rebuild by rows, decoded bytes, memory, and lifetime.** If an exact raw-history rebuild remains necessary, fold it in bounded chun…
- [ ] **Separate shallow native freshness from large indexed-root reconciliation.** Keep immediate native notification only for visible/open Finder directories and e…
- [ ] **Expose honest per-daemon memory and registration readiness.** Add one side-effect-free measured projection for peak and steady PSS/USS, anonymous/file PSS, s…
- [ ] **Add daemon-specific regression and resource gates.** Extend the isolated gate to fail on statsd cold peak/steady PSS/USS, watchd native watch-descriptor coun…
- [ ] **Certify the all-daemon release on one frozen identity.** After the implementation groups are complete, create the separately authorized freeze commit and run…
- [ ] **Document the bounded ownership and archive the completed queue.** Update `README.md`, `docs/DEVELOPMENT.md`, the relevant YO!stats/watchd/search specificatio…

### `queues/backlog/DOIT.p2.e2.filesystem-descriptor-authorization.md` (7/13)
- [x] Build deterministic failing probes for each known race: replace an authorized file before read consumption, replace a listed regular child with a blocked symli…
- [x] Add one shared descriptor-bound authorization owner in the filesystem package that opens without following unintended links, validates policy and identity from…
- [x] Migrate single-file reads, raw/media reads, path info, directory listing, symlink text/metadata, recursive count/zip, and search/index metadata to consume the…
- [x] Migrate `diff_file()` and other Git-backed file consumers so the authorized descriptor remains live until Git or the replacement bounded consumer has finished.…
- [x] Route create, write, rename, and delete through an authorized parent descriptor plus exactly one validated basename. Preserve configured-root refusal, expected…
- [x] Add an exact regression for every reproduced race plus a property-derived matrix across regular files, symlinks, hardlinks, missing targets, parent replacement…
- [ ] Update `docs/specs/GUI.md`, `docs/DEVELOPMENT.md`, and operator-facing error text for the descriptor-bound contract; run focused filesystem, route, search/inde…
- [ ] Before editing, the DONE note names the implementation worktree and current `origin/main` HEAD; every deterministic barrier test is captured red before the fix…
- [x] The focused command `python3 -m pytest -q tests/test_filesystem.py tests/test_filesystem_access_policy.py tests/test_filesystem_authorize_repoint.py tests/test…
- [ ] For every race, the post-fix result contains zero `BLOCKED_SENTINEL_DO_NOT_EXPOSE` bytes and zero blocked metadata fields; it either returns data from the orig…
- [ ] The property matrix covers regular files, symlinks, hardlinks, missing targets, allowed-root boundaries, blocked-secret paths, unsupported descriptor semantics…
- [ ] A consumer inventory under `/tmp` names every filesystem route, daemon producer, search/index annotator, Git consumer, and namespace mutation, and its negative…
- [ ] `python3 tools/static_build.py --check` when frontend assets are touched and an unmodified `python3 tools/check.py` both exit 0; after restart, the DONE note r…

### `queues/backlog/DOIT.p2.e5.tmux-session-destruction.md` (0/13)
- [ ] Resolve the leaking invoker by executable, PID/start identity, parent, argv, exact CWD, start source, socket environment, and PT minute; immediately stop or re…
- [ ] Inventory `~/dev/yolomux` and `~/dev/yolomux.dev7771` through `~/dev/yolomux.dev7776`; for each stale checkout, record pre-HEAD/status and either safely fast-f…
- [ ] Keep one shared destructive-authority owner: refuse ambient/default-socket `kill-server` unconditionally; permit exact-target `kill-session -t =<session>` on a…
- [ ] Add failing-first unit and API regressions for no socket/no opt-in, default socket with opt-in, private socket, `kill-server` in every mode, malformed/prefix t…
- [ ] Extend the shared `tmux_state.sh` snapshot owner to emit one retained JSONL event for a vanished session that cannot join an allowlisted teardown by exact sess…
- [ ] Run the focused tmux suites and an unmodified canonical gate while capturing default-socket session/history state, then begin the seven-day retained tripwire o…
- [ ] The DONE note identifies the leaking invoker with executable, PID/start identity, parent, argv, exact CWD, start source, resolved socket, and PT minute; that r…
- [ ] The seven permanent checkouts each have a recorded pre/post HEAD and status plus one explicit disposition: `updated`, `retired-from-invocation`, or `blocked-by…
- [ ] `python3 -m pytest -q tests/test_tmux_runtime.py tests/test_gate_tmux.py` exits 0 and proves: no opt-in fails closed; explicit default-server opt-in permits on…
- [ ] The API fixture starts two sessions on one private socket, deletes one through `POST /api/kill-session`, receives 200, proves the exact named session is gone,…
- [ ] A deliberate unexplained scratch-session disappearance creates exactly one retained tripwire row with session, socket, prior/current snapshot, PT minute, and `…
- [ ] An unmodified `python3 tools/check.py` exits 0 and produces zero `yt-*` sessions or history entries on the default socket before, during, and after the gate.
- [ ] Seven complete consecutive PT calendar days, each with at least one retained snapshot per minute except explicitly recorded monitor downtime, contain zero unex…

## Pending goal items

- 2. Land Finder working-tree diffs for v0.7.9: implementation landed as `c59a8dd64` and archived in `docs/DONE/2026-08/DONE.finder-working-tree-diff.md`, so the dirty-18-file wording is stale. REMAINING: clean-tree exact-SHA certification and final release/live acceptance. All nine functional lanes pass at `7cb75e3a5`, but certification returned RC=4 `dirty_start_checkout`.
- 3. Attribute and fix the 414 Request-URI Too Long responses. ATTRIBUTION DONE and FIX IMPLEMENTED, blocked only on integration authority. Cause is not a long URL: a terminal response to a POST with an unread `Content-Length` body never closes, so the leftover body is parsed as the next request line. Producer is a browser tab on the pre-`4392504a7` bundle POSTing the deleted `/api/stats-history` route with a 98,258-byte body; `jsDebugStatsSampleQuery()` is disproven at 273 bytes worst case. The captured line had `request_line_complete=false`, so no complete request line is claimed. Fixed at one owner, `Handler.send_response` (`server.py:1721`), the single point every response already passes through. Independently re-verified: 8 passed with the fix, 7 failed with `yolomux_lib/` stashed, the survivor being the negative control by design. Also fixes a sub-65537-byte unread body that previously HUNG the connection while logging nothing. `HTTP_REQUEST_LINE_CAPTURE_LIMIT` untouched. REMAINING: integrate into the primary checkout, canonical gate, restarted authenticated live reproduction.
- 4. Close the two acceptance-only queues, now split because they share nothing. NFS CONFIGURATION LOCK: DONE and archived to `docs/DONE/2026-08/DONE.nfs-configuration-lock-acceptance.md`. It was never actually blocked - this host exports `/home/keivenc` to `10.77.0.2`, `keivenc-linux2` mounts it over NFSv4.2 with `local_lock=none`, and both hosts were already at the identical HEAD `7cb75e3a5`. All five scenarios passed first run with no product defect: bidirectional exclusion with waiters acquiring 7.4 ms and 21.9 ms after release, crash release on exact-PID SIGKILL without deleting the lock file, typed stale-revision conflict preserving all three keys across 3400 monitor reads, and same-key replace yielding exactly one winner. BACKGROUND OWNER: BLOCKED ON A USER DECISION. The original criteria presume a shared root, but the measured topology is a durable 7770 root plus private ephemeral `/tmp` roots for 7771-7773 where `DisabledBackgroundOwner` bypasses same-root election by design, so cross-port takeover cannot occur. Declaring those criteria not-applicable would materially weaken the acceptance and was deliberately NOT done unilaterally.
- 5. Split the filesystem delete lane so an unbounded recursive delete cannot strand the interactive lane. IMPLEMENTED, blocked on integration authority. Both failures were reproduced red first: a one-entry unlink sat `queued` behind held interactive work, and one non-recursive delete of a flat 20,000-entry directory performed 20,001 destructive syscalls. Bounded unlink now uses the mutation lane; a non-recursive directory delete returns `pending: "subtree"` WITHOUT ever reaching `os.scandir`, and recursive work escalates to `bulk` under the same `operation_id` with the deadline not extended. Route, jobd arm, authorization owner, and terminal result shape all remain singular. Focused five-file command 170 passed; lane isolation and bound proof 4 passed / 556 deselected, verified first-hand. TWO REAL DEFECTS FOUND OUTSIDE SCOPE AND RECORDED: recursive delete keeps deleting after the completion reports `deadline_expired`, and partial failure propagates through the facade so the reindex never fires and the search index still advertises deleted files. REMAINING: canonical gate and restarted live evidence.
- 6. Bound statsd CPU and memory, statsd first. CPU DEFECT FOUND AND FIXED, blocked on integration authority; the memory scope is untouched. Hot loop is `_append_uncovered_gap` (`materializer.py:1685-1695`) at 39% of profile, linearly rescanning every span for every coverage epoch. The proven cost is ALGORITHMIC O(epochs x spans) per build at 6 sources x 633 epochs x 589 spans, i.e. superlinear in the retained window; the exact base doubling regression measures about 4.29x for 2x history. An earlier empirical exponent of N^1.66 is withdrawn as unstable curve-fitting. Buckets were already incremental; the gap model was not, and `_coverage_gaps` takes no dirty parameter so every build re-derived the full retained model. Fixed with a `bisect` seek over a per-source starts index, exploiting the already-guaranteed start-ordered non-overlapping invariant; frozen-fixture output byte-identical across 3625 gaps. A/B on MATCHED UNPROFILED REPLICATES ONLY, whole-service, readiness-gated, calibrated to the measured 4.00 appends/s: base 55.9% and 55.7% versus patched 27.4% and 26.9%, about 2.05x. A third base replicate at 55.3% is EXCLUDED because that run had an append timeout, and a third patched replicate at 28.4% is EXCLUDED from the matched comparison because it was the py-spy profiled run. TIMING-GATE BLOCKER RESOLVED. The original regression was a load-sensitive timing gate that failed intermittently on the patched tree (61 passed / 1 failed at 2.80x against a strict <2.8; five isolated reruns 4 pass / 1 fail at 2.88x), and an earlier report of 8 passed was a single run of a gate flaky by construction. It has been REPLACED with a deterministic access-count and forbidden-iteration proof. Re-measured independently by the main context at host loadavg 34: full module 64 passed rc=0; the exact new tests 3 passed / 61 deselected rc=0; and a forced-red against the old full-scan implementation gives 3 failed rc=1, proving the new gate can actually fail. Load-independence is structural rather than statistical, so no synthetic load was used to demonstrate it. Withdrawn permanently: the earlier 332-pass claim and the N^1.66 exponent. THE DECODE-AMPLIFICATION HYPOTHESIS IS DISPROVEN for CPU at about 2.7% of profile. STILL OPEN: the whole memory scope - startup still schedules a full build, observations and usage still use `fetchall()`, RSS about 950 MiB with 1113 MiB high-water. MEMORY LEAK STATUS IS UNRESOLVED: no runaway growth was established within 60s, but every retained 60s run rose 7.4-17.4 MiB, so an earlier claim that RSS is flat in steady state and therefore not a leak is withdrawn. Also open: the double-normalization residual. LIVE 7772 IS STILL RUNNING THE OLD CODE: a fresh audit measures 85.0% user-heavy CPU with zero physical reads, unchanged, because the patch is uncommitted and 7772 was never restarted. PATCH FROZEN: `/tmp/statsd-cpu-ac62/statsd-coverage-gap-fix.patch`, SHA-256 `a13d47c3c7dd2e4cc43466c3ad4293fdba7fcd995d135d9c1e18ff9ba88940ce`, 2 files, independently re-hashed and confirmed to apply cleanly to `7cb75e3a5`; `git diff --check` exit 0. The deterministic gate is `_SeekOnlyGaps`, an explicit-span sequence whose `__iter__` raises outright and whose `__getitem__` is counted; the access count at 256/1024/4096/16384 retained spans must equal the count at 64 for a candidate overlapping two spans, under an absolute bound of 8, and no clock is read anywhere in it. METHOD NOTE: the owner's forced-red injected the OLD full-scan body into the CURRENT function signature, so the red proves the algorithmic property. The main context's own forced-red instead stashed `yolomux_lib/`, which reverts the signature too, so that variant is weaker evidence and is not the basis of the claim.
- 8. Land origin/fix/macos-boot-tmux-env: LANDING DONE. `6bf61d5e6` merged as `1691743b9`; both are ancestors of HEAD and `git log HEAD..origin/fix/macos-boot-tmux-env` is empty. Queue at 8/12. REMAINING IS ACCEPTANCE ONLY: real-macOS verification is BLOCKED with no Darwin host, and the changed launcher is Darwin-gated so its production path has never executed here; plus clean-tree exact-SHA certification. The Linux-live half is proven separately - a stale plan file was silently accepted pre-merge (rc=0) and is refused after (rc=2).
- 9. Integrate `dev/0.7.8-7773` Finder Git diff viewer into current main without regressing the already-landed Finder working-tree/Differ split. Branch-only scope, not a blind 92-file merge: `dev/0.7.8-7773` is 4 ahead and 5 behind with conflicts overlapping `c59a8dd64`, and its worktree holds 20 dirty files of live work that must not be disturbed. Use a separate integration worktree and port only unique diff-viewer behaviour.

## Hourly history (last 24 hours)

| PT hour | done | pending | delta done | note |
| --- | ---: | ---: | ---: | --- |
| 2026-08-18 14:00 PT | 0 | 8 | - | Audited the new v0.7.9 ledger at HEAD 532db2064: eight named queues are 0/71; Finder implementation remains in an 18-file tracked dirty batch with an untracked DONE draft and pending full landing gate; reset history so completed v0.7.8 work does not appear as a v0.7.9 regression. |
| 2026-08-18 15:00 PT | 0 | 9 | +0 | Queued dev/0.7.8-7773 integration after measuring HEAD 2d6ef5722 at 4 commits ahead and 5 behind current 7cb75e3a5; the 92-file branch (+10492/-407) has current merge-tree conflicts with the already-landed Finder work. |
| 2026-08-18 16:00 PT | 0 | 9 | +0 |  |
| 2026-08-18 17:00 PT | 2 | 7 | +2 | Consolidated the fleet results. Five conflict-group owners reported: NFS two-host acceptance passed all five scenarios and is archived; tmux exact-target found and fixed a live data-loss defect (a kill aimed at an absent session `1` destroyed session `12`); the 414 was attributed to an unread POST body reframed as the next request line and fixed at one owner; the delete lane was split with a bound proof of zero scandir; and the statsd hot loop was named as `_append_uncovered_gap` with a measured ~2x CPU reduction. Four patches remain uncommitted in isolated worktrees pending commit authorization. |

<!-- progress-report-goal: {"goal":"v0.7.9 technical debt: close nine explicit release outcomes across decisions, implementation, verification, landing, and deployment. The checklist counts equal-weight release outcomes, not effort; supporting queue checkboxes show subtask volume. INTEGRATION AND DEPENDENCY PLAN for the four uncommitted patches, in order, one integration owner: (1) statsd first - it touches only `stats_current/materializer.py` and its test, is disjoint from every other group, and can land alone. (2) tmux and filesystem must SERIALIZE, not run in parallel: both edit `tests/test_app.py`. (3) descriptor residuals and the delete-lane split share `yolomux_lib/filesystem/paths.py` and belong to ONE parent owner; they must not be split across two integrators. (4) HTTP framing last of the source changes, because the global architecture budget it touches is regenerated by the coordinator AFTER source integration, never merged from an agent branch. (5) Then one clean exact-SHA certification gate on the integrated tree, followed by authorized runtime acceptance. Generated assets are always rebuilt centrally after integration, never merged from a worktree.","items":[{"id":1,"text":"Decide the two standing p0 queues: DONE 2026-08-18. Both downgraded with recorded reasons and the residual scope split, so `DOIT.p0.*` no longer exists. Both files were kept TRACKED via `git mv` rather than replaced, so no requirement was deleted; every original checkbox survives and proven ones are checked with evidence. Filesystem core already landed and its own five-file focused command passes unmodified at 155 passed, reproduced first-hand, so it became `DOIT.p2.e2.filesystem-descriptor-authorization.md` at 7/13 with the residual Darwin `descriptor_path()`, the `search.py:852-885` unauthorized absolute reopen, and stale specs retained plus an explicit risk rationale. The tmux fixture-leak mechanism was disproven by its own 2026-08-04 tripwire and its seven-day criterion needs a `tools/tmux_state.sh` that does not exist, so it became `DOIT.p2.e5.tmux-session-destruction.md`; the one real gap split to `DOIT.p1.e1.tmux-exact-session-target.md`, which then found a live data-loss defect.","done":true},{"id":2,"text":"Land Finder working-tree diffs for v0.7.9: implementation landed as `c59a8dd64` and archived in `docs/DONE/2026-08/DONE.finder-working-tree-diff.md`, so the dirty-18-file wording is stale. REMAINING: clean-tree exact-SHA certification and final release/live acceptance. All nine functional lanes pass at `7cb75e3a5`, but certification returned RC=4 `dirty_start_checkout`.","done":false},{"id":3,"text":"Attribute and fix the 414 Request-URI Too Long responses. ATTRIBUTION DONE and FIX IMPLEMENTED, blocked only on integration authority. Cause is not a long URL: a terminal response to a POST with an unread `Content-Length` body never closes, so the leftover body is parsed as the next request line. Producer is a browser tab on the pre-`4392504a7` bundle POSTing the deleted `/api/stats-history` route with a 98,258-byte body; `jsDebugStatsSampleQuery()` is disproven at 273 bytes worst case. The captured line had `request_line_complete=false`, so no complete request line is claimed. Fixed at one owner, `Handler.send_response` (`server.py:1721`), the single point every response already passes through. Independently re-verified: 8 passed with the fix, 7 failed with `yolomux_lib/` stashed, the survivor being the negative control by design. Also fixes a sub-65537-byte unread body that previously HUNG the connection while logging nothing. `HTTP_REQUEST_LINE_CAPTURE_LIMIT` untouched. REMAINING: integrate into the primary checkout, canonical gate, restarted authenticated live reproduction.","done":false},{"id":4,"text":"Close the two acceptance-only queues, now split because they share nothing. NFS CONFIGURATION LOCK: DONE and archived to `docs/DONE/2026-08/DONE.nfs-configuration-lock-acceptance.md`. It was never actually blocked - this host exports `/home/keivenc` to `10.77.0.2`, `keivenc-linux2` mounts it over NFSv4.2 with `local_lock=none`, and both hosts were already at the identical HEAD `7cb75e3a5`. All five scenarios passed first run with no product defect: bidirectional exclusion with waiters acquiring 7.4 ms and 21.9 ms after release, crash release on exact-PID SIGKILL without deleting the lock file, typed stale-revision conflict preserving all three keys across 3400 monitor reads, and same-key replace yielding exactly one winner. BACKGROUND OWNER: BLOCKED ON A USER DECISION. The original criteria presume a shared root, but the measured topology is a durable 7770 root plus private ephemeral `/tmp` roots for 7771-7773 where `DisabledBackgroundOwner` bypasses same-root election by design, so cross-port takeover cannot occur. Declaring those criteria not-applicable would materially weaken the acceptance and was deliberately NOT done unilaterally.","done":false},{"id":5,"text":"Split the filesystem delete lane so an unbounded recursive delete cannot strand the interactive lane. IMPLEMENTED, blocked on integration authority. Both failures were reproduced red first: a one-entry unlink sat `queued` behind held interactive work, and one non-recursive delete of a flat 20,000-entry directory performed 20,001 destructive syscalls. Bounded unlink now uses the mutation lane; a non-recursive directory delete returns `pending: \"subtree\"` WITHOUT ever reaching `os.scandir`, and recursive work escalates to `bulk` under the same `operation_id` with the deadline not extended. Route, jobd arm, authorization owner, and terminal result shape all remain singular. Focused five-file command 170 passed; lane isolation and bound proof 4 passed / 556 deselected, verified first-hand. TWO REAL DEFECTS FOUND OUTSIDE SCOPE AND RECORDED: recursive delete keeps deleting after the completion reports `deadline_expired`, and partial failure propagates through the facade so the reindex never fires and the search index still advertises deleted files. REMAINING: canonical gate and restarted live evidence.","done":false},{"id":6,"text":"Bound statsd CPU and memory, statsd first. CPU DEFECT FOUND AND FIXED, blocked on integration authority; the memory scope is untouched. Hot loop is `_append_uncovered_gap` (`materializer.py:1685-1695`) at 39% of profile, linearly rescanning every span for every coverage epoch. The proven cost is ALGORITHMIC O(epochs x spans) per build at 6 sources x 633 epochs x 589 spans, i.e. superlinear in the retained window; the exact base doubling regression measures about 4.29x for 2x history. An earlier empirical exponent of N^1.66 is withdrawn as unstable curve-fitting. Buckets were already incremental; the gap model was not, and `_coverage_gaps` takes no dirty parameter so every build re-derived the full retained model. Fixed with a `bisect` seek over a per-source starts index, exploiting the already-guaranteed start-ordered non-overlapping invariant; frozen-fixture output byte-identical across 3625 gaps. A/B on MATCHED UNPROFILED REPLICATES ONLY, whole-service, readiness-gated, calibrated to the measured 4.00 appends/s: base 55.9% and 55.7% versus patched 27.4% and 26.9%, about 2.05x. A third base replicate at 55.3% is EXCLUDED because that run had an append timeout, and a third patched replicate at 28.4% is EXCLUDED from the matched comparison because it was the py-spy profiled run. TIMING-GATE BLOCKER RESOLVED. The original regression was a load-sensitive timing gate that failed intermittently on the patched tree (61 passed / 1 failed at 2.80x against a strict <2.8; five isolated reruns 4 pass / 1 fail at 2.88x), and an earlier report of 8 passed was a single run of a gate flaky by construction. It has been REPLACED with a deterministic access-count and forbidden-iteration proof. Re-measured independently by the main context at host loadavg 34: full module 64 passed rc=0; the exact new tests 3 passed / 61 deselected rc=0; and a forced-red against the old full-scan implementation gives 3 failed rc=1, proving the new gate can actually fail. Load-independence is structural rather than statistical, so no synthetic load was used to demonstrate it. Withdrawn permanently: the earlier 332-pass claim and the N^1.66 exponent. THE DECODE-AMPLIFICATION HYPOTHESIS IS DISPROVEN for CPU at about 2.7% of profile. STILL OPEN: the whole memory scope - startup still schedules a full build, observations and usage still use `fetchall()`, RSS about 950 MiB with 1113 MiB high-water. MEMORY LEAK STATUS IS UNRESOLVED: no runaway growth was established within 60s, but every retained 60s run rose 7.4-17.4 MiB, so an earlier claim that RSS is flat in steady state and therefore not a leak is withdrawn. Also open: the double-normalization residual. LIVE 7772 IS STILL RUNNING THE OLD CODE: a fresh audit measures 85.0% user-heavy CPU with zero physical reads, unchanged, because the patch is uncommitted and 7772 was never restarted. PATCH FROZEN: `/tmp/statsd-cpu-ac62/statsd-coverage-gap-fix.patch`, SHA-256 `a13d47c3c7dd2e4cc43466c3ad4293fdba7fcd995d135d9c1e18ff9ba88940ce`, 2 files, independently re-hashed and confirmed to apply cleanly to `7cb75e3a5`; `git diff --check` exit 0. The deterministic gate is `_SeekOnlyGaps`, an explicit-span sequence whose `__iter__` raises outright and whose `__getitem__` is counted; the access count at 256/1024/4096/16384 retained spans must equal the count at 64 for a candidate overlapping two spans, under an absolute bound of 8, and no clock is read anywhere in it. METHOD NOTE: the owner's forced-red injected the OLD full-scan body into the CURRENT function signature, so the red proves the algorithmic property. The main context's own forced-red instead stashed `yolomux_lib/`, which reverts the signature too, so that variant is weaker evidence and is not the basis of the claim.","done":false},{"id":7,"text":"Deploy the shipped statsd and daemon-lifetime fixes to production 7770: DONE 2026-08-18. IMPORTANT SCOPE NOTE - this deployed the ALREADY-SHIPPED release-theme work, NOT the new statsd coverage-gap hot-loop patch from item 6, which remains uncommitted and is not running anywhere. `main` fast-forwarded to `7cb75e3a5` at 15:34:37 and the process started at 15:34:47, after the move, so the running code is the deployed code; the prior instance had been serving `a2c781223` and was missing all eight release-theme commits including bounded daemon lifecycles. Verified: listener in `/home/keivenc/dev/yolomux`, HTTPS `/healthz` 200, unauthenticated `/api/ping` 401, served bundle SHA-256 matching disk, and real external client traffic.","done":true},{"id":8,"text":"Land origin/fix/macos-boot-tmux-env: LANDING DONE. `6bf61d5e6` merged as `1691743b9`; both are ancestors of HEAD and `git log HEAD..origin/fix/macos-boot-tmux-env` is empty. Queue at 8/12. REMAINING IS ACCEPTANCE ONLY: real-macOS verification is BLOCKED with no Darwin host, and the changed launcher is Darwin-gated so its production path has never executed here; plus clean-tree exact-SHA certification. The Linux-live half is proven separately - a stale plan file was silently accepted pre-merge (rc=0) and is refused after (rc=2).","done":false},{"id":9,"text":"Integrate `dev/0.7.8-7773` Finder Git diff viewer into current main without regressing the already-landed Finder working-tree/Differ split. Branch-only scope, not a blind 92-file merge: `dev/0.7.8-7773` is 4 ahead and 5 behind with conflicts overlapping `c59a8dd64`, and its worktree holds 20 dirty files of live work that must not be disturbed. Use a separate integration worktree and port only unique diff-viewer behaviour.","done":false}],"queues":["queues/backlog/DOIT.p1.e1.tmux-exact-session-target.md","queues/backlog/DOIT.p1.e2.background-owner-live-fleet-acceptance.md","queues/backlog/DOIT.p1.e2.merge-macos-boot-tmux-env.md","queues/backlog/DOIT.p1.e2.request-uri-too-long-attribution.md","queues/backlog/DOIT.p1.e3.filesystem-delete-lane-split.md","queues/backlog/DOIT.p1.e5.memory-hog.md","queues/backlog/DOIT.p2.e2.filesystem-descriptor-authorization.md","queues/backlog/DOIT.p2.e5.tmux-session-destruction.md"]} -->
<!-- progress-report-history: [{"hour":"2026-08-18 14:00 PT","done":0,"pending":8,"note":"Audited the new v0.7.9 ledger at HEAD 532db2064: eight named queues are 0/71; Finder implementation remains in an 18-file tracked dirty batch with an untracked DONE draft and pending full landing gate; reset history so completed v0.7.8 work does not appear as a v0.7.9 regression."},{"hour":"2026-08-18 15:00 PT","done":0,"pending":9,"note":"Queued dev/0.7.8-7773 integration after measuring HEAD 2d6ef5722 at 4 commits ahead and 5 behind current 7cb75e3a5; the 92-file branch (+10492/-407) has current merge-tree conflicts with the already-landed Finder work."},{"hour":"2026-08-18 16:00 PT","done":0,"pending":9},{"hour":"2026-08-18 17:00 PT","done":2,"pending":7,"note":"Consolidated the fleet results. Five conflict-group owners reported: NFS two-host acceptance passed all five scenarios and is archived; tmux exact-target found and fixed a live data-loss defect (a kill aimed at an absent session `1` destroyed session `12`); the 414 was attributed to an unread POST body reframed as the next request line and fixed at one owner; the delete lane was split with a bound proof of zero scandir; and the statsd hot loop was named as `_append_uncovered_gap` with a measured ~2x CPU reduction. Four patches remain uncommitted in isolated worktrees pending commit authorization."}] -->
